/* Generated by Once compiler */
#include "echo_server.h"

/* Function definitions */
void* once_serverEndpoint(void* x) {
    (void)x;
    return (void*)4;
}
void* once_replySlot(void* x) {
    (void)x;
    return (void*)0;
}
void* once_serverLoop(void* x) {
    return ({ typeof(once_seL4_Recv((OncePair){ .fst = once_serverEndpoint(x), .snd = once_replySlot(x) })) recvResult = once_seL4_Recv((OncePair){ .fst = once_serverEndpoint(x), .snd = once_replySlot(x) }); ({ typeof(once_seL4_Yield(((void*)0))) y = once_seL4_Yield(((void*)0)); once_serverLoop(y); }); });
}
void* once_main(void* x) {
    return once_serverLoop(x);
}
