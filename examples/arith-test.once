-- arith-test.once: Test arithmetic primitives
--
-- Build:
--   cd compiler && stack exec -- once build --exe \
--     --strata ../Strata ../examples/arith-test.once -o ../.build/arith-test
--   gcc -o ../.build/arith-test ../.build/arith-test.c
--   ../.build/arith-test

import I.Linux.Syscalls as S
import I.Linux.Arith as A

-- Verify imports work - exit cleanly
main : IO Unit
main = exit0@S
