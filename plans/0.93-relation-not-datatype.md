---
parent: 0-ocp3
status: S0 PASSED 2026-09-17 — the stop gate cleared, both questions YES,
        verified by the typechecker (Once/Spike/RelSpike.agda, 0 errors,
        0 postulates, 0 pragmas). S1-S6 remain.
supersedes: an earlier 0.93 draft (never written to disk) proposing to lower
        labels to ADDRESSES in the machine model. That was backwards — see §2.
---

# Plan 0.93 — The machine relation is a FUNCTION ON TYPES, not a datatype

## 0. The one measurement

`block-runs` is FALSE and load-bearing. Four designs to replace it have been
refuted or shown impossible, each for a different reason, each only visible
after the previous died:

    block-runs      state → syntactic, hypothesis determines nothing   FALSE   D213
    CodeResolves    state → the body's TEXT                            FALSE   D216
    CodeWF          state → the label alone                            sound, WRONG HALF
    BlockAt-in-witness                                                 NO-GO   (supplies
                    layout where a BEHAVIOURAL fact is needed; and
                    `block-runs` is the ONLY producer of a `CalleeRun`
                    in the development — `grep -rn 'callee-run'` returns
                    one hit, the constructor declaration, never applied)

Progress is ONE number: the count of postulates in the machine-correctness
layer. It goes DOWN only when the thing they stand in for becomes provable,
never by moving an assumption somewhere else.

## 1. THE ONE FIXED POINT

**Correctness is the effectful-SigOp trace.** `Behavior = ℕ → List SigOpEvent`,
indexed by EVENT COUNT (D058's content, which survives regardless of what else
in the log is revised). The compiled binary's sequence of effectful SigOp events
must equal the source's. That is the claim; it cannot move.

Everything else in the decision log is a prior decision, and prior decisions get
reverted — D057 was corrected by D058, and D216 was corrected by D216 the same
day it was written. **Do not treat the log as axioms.** Treat it as evidence
about what was tried and what happened.

## 2. THE DEFECT

    data ValidAtWF : AllocMode → AllocState {FS} →
         {A : IRTy} → ⟦ A ⟧ → ValueLocation FS → LocState FS → Set where

`ValidAtWF` is a **`data`** — an inductive family INDEXED by the type. A data
constructor can only ASSERT facts; it cannot RECURSE ON THE INDEX. So the
closure constructor is structurally incapable of saying anything beyond
syntactic facts about cells, and therefore **cannot pin the function a closure
denotes**.

That is not a quirk. It is machine-checked (D217,
`Once/Probe/ClosureAmbiguous.agda`): two `ValidAtWF` witnesses at the SAME cell,
state and label, for `const fits-int (+ 0) ∘ terminal` and
`const fits-int (+ 1) ∘ terminal`, both typecheck. **One cell, two denotations.**

Everything else follows from that one choice:

* `BodyCorrect` tried to put behaviour on a constructor — a genuine cycle,
  because `data` cannot recurse on its index. Removed (D170), correctly.
* `program-bound` / `ir-size` tried to well-found it by SIZE — false, because a
  closure can name a body bigger than the code that built it. Deleted (D214).
* `block-runs` axiomatised the resulting hole — false (D213).
* `CodeResolves`, `CodeWF`, `BlockAt`-in-witness all tried to recover a
  BEHAVIOURAL fact from a relation that structurally cannot carry one.

**The missing thing was never a premise. It was that the relation should have
been a function on types.**

### Why the earlier 0.93 draft was wrong

It proposed splitting `AbstractTrace` into unlinked/linked forms and lowering
labels to ADDRESSES so `do-call` could jump instead of scan. Rejected: the
general layer is ALREADY label-addressed — `SV-Code : LabelId → StoredValue`,
`find-thunk` returns a FETCH INDEX into the abstract trace (pinned by
`find-thunk-sound`), and every address is minted arch-side (`AddrMap.cmap`,
`lea rax (rip+label n)`). Lowering belongs in the arch backend and the
assembler, and it is already there. Labels stay labels all the way up.

## 3. THE TEMPLATE — and it is already in this repo

`Once/Adequacy/MeaningRelation.agda` does the right thing one layer up:

```agda
RelV : ∀ (A : Type) → ⟦ A ⟧ᴰ → ⟦ A ⟧ᴰ → Set
RelT : ∀ (A : Type) → T ⟦ A ⟧ᴰ → T ⟦ A ⟧ᴰ → Set   -- forward-declared, mutual

RelT A t₁ t₂        = ∀ n → (projTrace t₁ n ≡ projTrace t₂ n)
                          × RelV A (valueT t₁ n) (valueT t₂ n)
RelV Unit    _ _    = ⊤
RelV Int     x y    = x ≡ y
RelV (A * B) (a₁ , b₁) (a₂ , b₂) = RelV A a₁ a₂ × RelV B b₁ b₂
RelV (A ⇒ B) f g    = ∀ {a b} → RelV A a b → RelT B (f a) (g b)
RelV (μ-type F) x y = x ≡ y
RelV (ν-type F) x y = x ∼ᵈ y
```

Four properties to copy exactly:

1. **Recursion on the TYPE**, mutual value/computation, forward-declared.
2. **The arrow is a Π over related inputs** — "related arguments map to related
   computations", never an equality of functions.
3. **The computation relation is indexed by the EVENT BUDGET** (`∀ n`), which is
   the observable's own index. No steps, no size, no fuel.
4. **At `μ` and `ν` the recursion STOPS** — `≡` and `∼ᵈ`. That is why the whole
   definition typechecks with NO `TERMINATING` pragma. The machine instance must
   stop at the same two places.

## 4. THE TARGET

The same shape, one instance lower: semantics versus MACHINE.

```agda
RelV : ∀ (A : IRTy) → ⟦ A ⟧ᴰ → StoredValue FS → LocState FS → Set
RelT : ∀ (B : IRTy) → T ⟦ B ⟧ᴰ → FlatState → Set

RelT B comp fs      = ∀ n → (events of the run from fs ≡ projTrace comp n)
                          × RelV B (valueT comp n) (the result) (the settle state)
RelV (A ⇛ B) f sv s = ∃[ ℓ ] ( sv holds code ℓ
                             × ∀ a av s′ → RelV A a av s′
                                         → RelT B (f a) (entering block ℓ with av from s′) )
```

and the theorem is ONE induction on the IR:

    ir-correct : ∀ {A B} (ir : IR A B) {x av s}
               → RelV A x av s
               → RelT B (evalᴰ ir x) (run of `emitted ir` from s)

* **`apply`** discharges FROM the arrow clause. No lookup, no `find-thunk`, no
  `CalleeRuns`, no `block-runs`. The relation IS the fact.
* **`curry`** ESTABLISHES the arrow clause from its IH on `body` — a genuine
  subterm, so the recursion is honest.

**Labels stay labels.** The arrow clause says *entering block ℓ behaves*. WHERE
ℓ sits in the linked image is a separate, purely SYNTACTIC layout lemma — S2's
`BlocksAt` / `entry-blocks` shape, state-free, discharged once by induction over
`blocks-layout`. That obligation survives this plan and is the only syntactic
one left.

## 5. TOP-DOWN ONLY

Same rule as 0.91, and it earned its place there (D214):

> Change a TYPE, never blank a BODY. A hole suppresses the module interface, so
> consumers fail to LOAD rather than to typecheck — one import error, no
> work-list. A changed statement fails at every CALL SITE, and that list is the
> work-list, free.

And the rule this plan exists to enforce:

> **No short-cut abstractions.** A postulate is admissible only where the thing
> it stands for is genuinely outside the system (the assembler, the loader). A
> postulate standing in for something the system SHOULD be able to say is a
> defect in the abstraction, and it will not be dischargeable later — the four
> refuted attempts in §0 are what that looks like.

## 6. STEPS

### S0 — SPIKE, and it is the gate for the whole plan  ✅ PASSED (2026-09-17)

**`formal/Once/Spike/RelSpike.agda`, 857 lines. `EXIT=0`, 0 errors, 0 unsolved
metas, `.agdai` deleted and `Checking Once.Spike.RelSpike` confirmed. ZERO
`TERMINATING` pragmas, ZERO postulates, ZERO holes.**

* **Q1 — YES.** `RelV`/`RelT` recurse on the TYPE, mutually, with no pragma. The
  template's call graph has two strict decreases and the machine instance adds
  only INERT arguments, so the graph is identical.
* **Q2 — YES**, and definitionally: `evalᴰ fmt apply p = proj₁ p (proj₂ p)`
  (DenotTrace.agda:143) and `⟦ A ⇒[ mk-kind Many π ] B ⟧ᴰ = ⟦A⟧ᴰ → T ⟦B⟧ᴰ`, so
  **apply's goal and the arrow clause's conclusion are THE SAME TERM.**
  `spike-curry` establishes the clause from its IH on `body`, a real subterm.

FIVE CORRECTIONS TO THIS PLAN, each forced by the source:

* **(A) S0 is FOUR types, not three.** `apply : IR ((A ⇛ B) * A) B`, so `_*_` is
  needed to STATE either theorem. §6 said three.
* **(B) The arrow clause must carry `find-thunk prog lbl ≡ just j`.** §4 put the
  resolution in `BlocksAt`; that cannot reach `apply`, because
  `blocks n l apply ≡ []` (pinned in the spike as `apply-no-blocks … = refl`) so
  apply's own premise is `All _ []`. The resolution travels WITH THE VALUE;
  `curry` discharges it from ITS non-empty `BlocksAt`. Apply still does no
  lookup — that half of §4 is right, and S4 still makes it true of a linked image.
* **(C) Every location the relation names is a HEAP location**, and there is NO
  `next-slot alloc ≤ n` premise. `do-call` enters only on `AtDynamic` and halts
  on `AtStack` (D184). Dropping a premise STRENGTHENS the statement.
* **(D) `RelT` carries a resume pc and a resume return-stack** — syntactic
  continuation parameters fixed BEFORE the `∀ bud`, not a second observable index.
* **(E) `RelV` takes the allocator** — §9 Q1 answered NO: the frontier does not
  fall out of the state, because the closure's cells are existential inside `RelV`.

FINDING THAT MAY COST A STEP IN S1: `do-thunk` uses `grow-frame`
(Flat.agda:692-723), which sets `current-frame`/`frame-slots` and does NOT reset
`next-slot`, while the body is emitted at frontier 0. Any obligation keeping
`next-slot alloc ≤ n` makes curry's IH application unprovable. Invisible until
now because `block-runs` ASSUMED the callee's run. The spike takes the heap-only
route; S1 owes a heap-only `mem-pres` (~20 lines). Check `CataNextSlot.agda:86-98`
before letting `do-thunk` reset `next-slot`.

**ORIGINAL S0 TEXT (kept for the record):**
Define `RelV`/`RelT` for a THREE-CASE fragment only — `Unit`, `Int`, `_⇛_` —
in a new module, with no consumers. Then prove exactly two clauses end to end:
`curry` (establishes the arrow) and `apply` (consumes it).
*Gate:* (a) does Agda accept the type recursion with NO pragma, as the template
does? (b) does `apply` discharge from the arrow clause with no axiom? If either
fails, STOP and report — the plan is wrong and the rest is wasted.
*Cost:* a day. It decides everything; do not skip it.

### S1 — The full relation
Extend to every `IRTy`: `Void`, `Float`, `Str`, `Buffer`, `*`, `+`, `μ`, `ν`.
Stop the recursion at `μ`/`ν` exactly as the template does; the machine analogue
of `≡`/`∼ᵈ` is the existing layout relation (`valid-μ-wf`) and the suspension
relation. Still no consumers.
*Creates:* the honest statement of "this machine value represents that semantic
value", for the first time.

### S2 — State the theorem, change nothing else
Write `ir-correct`'s signature. Do NOT port any clause yet. Let the typechecker
state what each case owes.

### S3 — Port the clauses, `curry`/`apply` FIRST
Fourteen cases. Do the pair that motivated the plan first, then the leaves, then
the composites, then `Ana`/`Out`. Each clause is a commit; each commit gates.
*Expect:* the composite cases (`∘`, `⟨,⟩`) to be the bulk, and the D204/D206/
D208/D210 memory-preservation content to reappear as obligations of `RelT`
rather than as fields bolted onto a record.

### S4 — The layout lemma
`BlocksAt` / `entry-blocks`: every block the emitter produced is placed at its
label in the linked image. State-free, syntactic, safe. This is the induction
D188 called provable and nobody wrote, and it is the ONE syntactic obligation
the new architecture keeps.

### S5 — Retire the old layer
Delete `ValidAtWF`, `MachineRefinesObsF`, `ValueRealized`, `BlockRuns`,
`CalleeRuns`, `CoalgRuns`, `block-runs`. Nothing should reference them.
*Gate:* `./run-ast-dumps.sh` and diff — nothing newly dead that should be live,
nothing live that should be dead.

### S6 — Re-point the arch correspondences
The three `ArchCorrectness` modules consume the apex through `flat-from-obs`.
Re-point them at `ir-correct`. The arch-side content (addresses, `AddrMap`,
`lea rip+label`) is UNTOUCHED — this plan never reaches it.

## 7. GATES

* Every step: root typecheck green, `cabal test` 746, exit tests 65/0/0 on
  x86-64, x86-32 and riscv64. This plan changes NO behaviour; a test movement
  means something is wrong.
* Every new or surviving residual gets an emptiness probe BEFORE acceptance
  (MERGE.md; D213/D216/D217 showed old residuals owe this too).
* No step lands with a construct unreachable from `Once.Certified` — AST dump
  and diff, as D212 and the S2 audit did.
* S0 is a STOP gate. If the spike fails, the plan is wrong.

## 8. WHAT SURVIVES, WHAT IS REWRITTEN

**Survives untouched:** the emitter (`ir-to-trace'`), the flat machine, the
denotation (`evalᴰ`, the `T` monad), all three arch correspondences and
everything arch-side, the frontier / slot-budget / allocation discipline,
`MeaningRelation` itself, and S1/S2 of plan 0.91 (the `program-bound` deletion
and the `BlocksAt` premise — the latter becomes S4's statement).

**Rewritten:** `ValidAtWF` → `RelV`; `MachineRefinesObsF` / `ValueRealized` →
`RelT`; the fourteen `IRObsCorrect` clause modules become the cases of one
induction.

**Deleted:** `BlockRuns`, `CalleeRuns`, `CoalgRuns`, `block-runs`.

## 9. OPEN QUESTIONS — settle with the typechecker, not by argument

1. Does the machine `RelT` need the allocator and the frontier invariant as
   extra indices, or do they fall out of the state? S0 answers this.
2. At `ν`, what is the machine analogue of `∼ᵈ`? The template uses bisimilarity;
   the machine side has suspensions and a re-suspension pass (D199). If this
   needs an index, it must be the EVENT BUDGET and nothing else (§1).
3. Do `RelV`'s arrow clause and `RelT` need to quantify over the PROGRAM? The
   arrow clause mentions "entering block ℓ", which is program-relative. Probably
   yes, and that is fine — it is not the D170 cycle, because the recursion is on
   the type and terminates.
4. Can the D204/D206/D208/D210 preservation content be DERIVED in the new shape
   rather than carried as fields? Four fields were added one at a time because a
   record forced them; a relation may not need them.

## 10. THE PARALLEL TRACK

Independent of this plan, and worth doing first because it is cheap: shrink
`block-runs` so it is UNPROVED rather than FALSE — move its
`find-thunk prog ℓ ≡ just j` conjunct out of the conclusion and into a premise.
That deletes exactly the conjunct D213's probe lands its `()` on. It does not
fix anything, but it stops the apex resting on a refutable statement while this
plan is built.


## 11. `Str` / `Buffer` — OUT OF SCOPE, and why that is safe (2026-09-18)

**Decision (project owner): leave them out completely. Pretend they do not exist.
Proof gaps related only to them may be postulated.** S3 does NOT need a
representation decision first; an earlier note in this plan said it did, and that
note is withdrawn.

Why it is safe, checked rather than assumed:

    strLit s = SigOp (str-lit-info s) ∘ terminal : IR Γ Str   Surface/Elaborate.agda:70-71
    data FitsInReg : Type → Set where
      fits-int   : FitsInReg Int
      fits-float : FitsInReg Float                            Type.agda:383-385
    obs-correct-sigop {A} {B} si with fits-in-reg? B | readable? A
    ... | nothing | _ = obs-correct-sigop-rest si             SigOp.agda:293-295

`FitsInReg` has no `Str` constructor, so `fits-in-reg? Str` is `nothing` and every
string routes to `obs-correct-sigop-rest` — an existing NAMED POSTULATE. There is
no PROVED path that reaches `RelV` at `Str`, so nothing in the tree is vacuously
true today.

**THE INVARIANT THIS RESTS ON, and it must be restated whenever it is relied on:**

> `RelV Str = RelV Buffer = ⊥` is sound ONLY while no theorem is instantiated at a
> string-containing type. It is NOT conservative. `RelV` occurs NEGATIVELY in the
> arrow clause, so a discharge at `Str ⇛ B` would be VACUOUSLY TRUE rather than
> hard — D217's defect, re-entering at the one constructor this plan exists to
> fix. The same leak reaches `μ` through `K Str`
> (`μ-type (K Str ⊕ (K Str ⊗ Id))` is a string list).

So: if a future clause proves something at `Str`, IT MEANS NOTHING. That is not a
reason to avoid the decision forever — it is the reason the decision, when it
comes, is a REPRESENTATION decision (what can a `StoredValue` hold?) and not a
proof task.

### Made explicit at the declaration sites (2026-09-18)

`Str`/`Buffer` now carry a note where they are DECLARED — `Once/IRTy.agda` and
`Once/Type.agda` — recording that they are declared but not represented, that
filling them in is LOCAL (the relation recurses on the type, so each constructor
is one independent clause), and the caution that `⊥` is not conservative.
Both modules re-typecheck.

### AND A CORRECTION TO §11's FIRST DRAFT

It said strings route to "an existing named postulate", which reads as though
`obs-correct-sigop-rest` were about strings. It is not. The DISPATCH is properly
enumerated — five rows, no variable catch-all (SigOp.agda:293-299) — but FOUR
distinct reasons route to that ONE postulate:

    fits-in-reg? B ≡ nothing     the result is not register-resident
    readable? A ≡ nothing        the input cannot be materialised
    Emits _                      effectful SigOp
    Halts _                      halting SigOp

and `FitsInReg` has only `fits-int`/`fits-float`, so the first row is "B is not
Int or Float" — covering `Unit`, `Void`, pairs, sums, closures, `μ`, `ν` AND
strings. Strings are a small corner of a large residual.

**OWED (not scheduled): split `obs-correct-sigop-rest` by reason.** The four are
different obligations with different futures — a representation gap, a
readability gap, and two effect-shape gaps — and one name for all four means the
ledger cannot say which part is which. This is the `enumerate over catch-all
postulate` rule applied to the DESTINATION rather than the dispatch: enumerating
the rows is not enough if they all land on one axiom.

## 12. S3 PROGRESS AND THE UNTANGLING ORDER (2026-09-18)

### Landed: 8 of 13 clauses

    id · terminal · initial · fst · snd · const · In · composition

`RelSpike.agda` is 3094 lines with ZERO pragmas, ZERO postulates, ZERO holes.
Every clause landed on the first typecheck, applied one at a time.

Two pieces of evidence that the relation sits at the right level, both
unplanned:

* `fst`/`snd` needed FOUR machine read-back lemmas and all four already existed
  (`SMPrimitives.agda:3775, 3801, 3849, 4628`). The relation consumes the
  machine's existing API unchanged — it did not force a new interface, which is
  exactly what `ValidAtWF` did wrong (it needed `validityWF-*` transport
  families that existed only to serve it).
* Composition's trace had to compose under a THREADED budget, and D203's
  `take-++-split` / `minus-take` / `take-++-threaded` transferred directly.

### The spec change, taken FIRST and top-down

`in-ν` had NO native `evalᴰ` clause: it fell to the catch-all
`inject (eval fmt ir (forget a))`, and the `forget` is lossy at `ν-type F` —
`forgetν` reads each child at budget ZERO and drops its events. So a ν built by
`in-ν` over an EMITTING child was SPECIFIED as silent, while the machine leaves
the child's suspension pointer untouched. **The spec was wrong, not the
compiler.**

Fixed by adding the introduction form the value domain was missing:

    in-νᵈ : ∀ {F} → ⟦ F ⟧SF (νᵈ F) → νᵈ F
    forceᵈ (in-νᵈ layer) = λ _ → ([] , layer)

`injectν` could NOT be reused: it maps itself over the children, and its
children come from the PURE model `νS`, so their events are already gone. That
is right for `inject` (lifting a trace-free value) and wrong for `in-ν`, whose
children are `νᵈ` values that force themselves. One `mapInjectν` call is the
entire difference.

Symmetric with `Ana` (D179) — both BUILD a suspension and emit NOTHING, the
events come at `Out` — and SIMPLER: no recursive call, so no guardedness
obligation, and `forceᵈ (in-νᵈ l) ≡ ([] , l)` gives `Out ∘ in-ν ≡ id`
definitionally. The coercion chain is `anaFᵈ`'s with the recursion removed.

NOTE FOR THE GATE: this changes the SPECIFICATION, so the usual rule inverts. A
test movement here is EVIDENCE THE FIX IS REAL, not a regression.

### The untangling order (agreed 2026-09-18)

Three blockers each serve more than one clause; that is the whole ordering
argument.

1. **`LabelsAt`** — unblocks `case`, `Ana`'s sum arm, and `Cata`'s gate.
   `SpanAt` is fetch-agreement and `BlocksAt` covers `find-thunk`; NOTHING
   covers `find-label`. Cheaper than it looks: `fl-go-++-miss`
   (Flat.agda:212-237) already exists, as does find-label soundness
   (Flat.agda:452-464, LabelScope.agda:2411-2426).
2. **`RelK ↔ RelV`** (obligation ν-c) — one lemma, two consumers: `Out` and
   `Ana`'s `wf-K` arm.
3. **`CurryBuild` = `TwoCellBuild10`** — one port serving `curry` AND `Ana`,
   because `Ana`'s ten rows are `curry`'s character for character (D189).

Then: `ApplySetup` → `apply`; `out-μ-lambek` → `out-μ`; `SumBuild` →
`inl`/`inr`; `pair`; `Cata` behind its own gate.

### Two things deliberately NOT done

* **`pair` is late, not blocked.** The S3 assembler claimed `RelT` needs
  stack/frame conjuncts. The decision log says otherwise: D210 shows
  `frame-pres` existed because the obligation was a RECORD, so `pair` could see
  only what `ValueRealized` chose to expose. `RelT` exposes the run itself, so
  the fact should be DERIVABLE once and generically — D208's own conclusion.
  D206 warns against exactly the move the assembler made ("added a supporting
  fact by reasoning from the MACHINE and guessing the statement… wrong twice, in
  the same place"). That the proposed repair would RE-BREAK the proved
  `spike-curry` is evidence the repair is wrong, not that `RelT` is. Doing
  `pair` after six more clauses means `RelT`'s shape will have been exercised
  properly first.
* **`Cata` behind a gate.** Three of four blockers confirmed: the obligation
  cannot be STATED generically (`cata-strategy` is a `with`-block routing four
  arms to four emitters); there is no loop-state relation (`RelV` is a value at
  rest, `RelT` a run that settles — the fold's partial state is a THIRD shape);
  and there is no denotational pin. Recommended gate: `strat-const` only,
  whose sole new requirement is `LabelsAt`.
