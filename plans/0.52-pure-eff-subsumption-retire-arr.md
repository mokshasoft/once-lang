# Plan 0.52 — retire `arr`; pure→eff is monotone subsumption, not a coercion

status: **M1 DONE + landed** (surface `arr` retired; `t-subsume` replaces it;
`subsume-residual` discharged postulate-free; MAlonzo re-extracted). M2/M3 open.
Enables OCP-0007; aligns with QTT for a dependent-types future. Decision: D068.

### State (2026-07-14 audit)
- **M1 — surface subsumption: COMPLETE.** `t-arr-app-check`/`t-arr-app-infer`/
  `m-arr`/`bbc-arr`/`ahv-arr` all deleted (0 declarations). `t-subsume` is live and
  emitted by the elaborator (compose/case/apply/pair/curry/cata/named/t-embed);
  `realize (t-subsume d) = arr' (realize d)` (Realize.agda:156). No arr-related
  postulate remains. Landed across commits `fbddbfe9`…`d2f50ad1` (+ OCP-0008
  classifier flatten `28fdd470`).
- **M2 — ungraded IR objects: NOT started.** The only `arr` left is internal
  `IR.arr`/`arr'` (Once/IR.agda:146), the erased no-op M1 keeps. Removing it needs
  the architectural decision below (separate IR-object layer vs grade-irrelevant
  field) — settle first.
- **M3 — capability lattice (OCP-0007): NOT started.**

## One-line

The grade (purity, later capabilities) is a **checked, runtime-erased typing
refinement**. The pure→eff direction is **subsumption** (`pure ⊑ eff`, a check),
never a **coercion term**. `arr` — the last pure→eff coercion — is retired.

## Why (the principle, see D068)

- `evalᴰ apply` is kind-polymorphic; `evalᴰ arr f = returnT f` (identity). The
  grade is a **phantom IR index**: present in the *type*, ignored by codegen
  (the IR is grade-erased, D046; `realize-morph` ignores `π`, D066).
- D065 already dropped **bare** `arr` (type error) and made bare morphisms
  grade-free. The only `arr` left is **applied `arr f`** (the closure lift,
  `t-arr-app-check`): a pure function *value* used at an eff arrow.
- That last `arr f` is a **no-op coercion** (`⟦arr f⟧ = ⟦f⟧`). OCP-0007's rule is
  "annotation is a **check, never a coercion**"; the QTT discipline (the
  `Zero/One/Many` quantity is already QTT's `{0,1,ω}` semiring) is that grade
  annotations are **tracked, checked, erased** — adjusted by subsumption, not
  coercion terms. `arr` is the pure/eff analogue of an explicit `0→ω` coercion,
  exactly what QTT avoids.

## Monotone, not free (the D066 guardrail)

Subsumption is **one-directional**: `pure ⊑ eff` (use a pure thing where eff is
allowed) is sound; `eff ⊑ pure` is **UNSOUND** (D066 — the optimizer drops pure
SigOps, so tagging an effectful SigOp pure drops effects). So this is *monotone*
subsumption up the grade lattice — which is precisely OCP-0007's **attenuation**
(authority only ever relaxes downward; you never fabricate it). The grade stays
load-bearing in the surface type for the effect/capability analysis; only the
*adjustment mechanism* changes (check, not term).

## Scope / milestones

NOTE (2026-06-30, on starting): the IR exponential **object IS the surface
(graded) `Type`** (`IR (A ⇒[k] B) …` uses `Once.Type._⇒[_]_`). So removing
`IR.arr` requires *ungraded* IR objects — that is genuinely M2, not M1. M1
therefore KEEPS `IR.arr` as the now-implicit subsumption coercion (`arr'`).

- **M1 — surface subsumption; retire surface `arr` + `t-arr-app-check` + `m-arr`.**
  - Add `t-subsume : ctx ⊢ᶜ e ∶ A⇒[pure]B → ctx ⊢ᶜ e ∶ A⇒[eff]B` (subject `e`,
    not `RApp (RVar "arr") arg`); `realize (t-subsume d) = arr' (realize d)`
    (identity denotation, since `⟦arr' f⟧ = ⟦f⟧`). It is `t-arr-app-check` with
    the `arr` wrapper dropped from the subject.
  - `checkElabV` at `A ⇒[eff] B`: when a value/closure only checks at
    `A ⇒[pure] B` (e.g. `RLam`), accept via `t-subsume` (the `pure ⊑ eff` check).
  - Retire surface `arr`: the `arr` builtin name (Classify), `ahv-arr` dispatch,
    `bbc-arr`, `t-arr-app-check`/`t-arr-app-infer`, and `m-arr` (already declared
    redundant by D065 — bare morphisms are grade-poly). KEEP `IR.arr`/`arr'`
    (used by `t-subsume`'s `se`).
  - Soundness: `t-subsume` denotation-preserving — **trivial** (`⟦arr' f⟧=⟦f⟧`).
  - Cascade: Elaborate, Soundness, Completeness, Realize, RealizeAgrees, Canon*,
    MorphComplete, Classify. Re-extract MAlonzo.
- **M2 — ungraded IR objects (big proof win).** Separate the IR exponential object
  from the surface graded `Type` (or make the grade an irrelevant field), so
  pure/eff arrows are the SAME IR object. Then `IR.arr` ≡ `IR.id` ⇒ removable,
  `t-subsume`'s `arr'` ⇒ identity (true no-op subsumption), and every
  `mk-kind Many/One/Zero × pure/eff` case-split in the agree/codegen proofs (apply,
  the RVar mirror) collapses. Requires optimizer rework (Normal/IRReducible/
  PairCaseNormal) and VERIFY optimizer purity rides on SigOp contracts (D061), not
  arrow-object grades. This is the architectural decision (separate IR-object layer
  vs grade-irrelevant field) with dependent-types implications — settle before M2.
- **M3 — generalize to the lattice (OCP-0007).** Once `pure ⊑ eff` subsumption is
  in, swap the two-point `{pure,eff}` for the capability lattice; subsumption
  becomes attenuation (`S ⊑ S'`), inferred bottom-up by union, checked top-down.

## Consequences

- **Proofs:** deletes `arr` codegen + `arr'`/`ahv-arr` lemma + `m-arr` + the `arr`
  bbc machinery; M2 deletes pervasive kind-splits. Correctness spec stays grade-free
  (it already is); grade soundness is a separate, smaller static-analysis property.
- **Programs:** drop `arr f` (rare); strictly more programs accept (subsumption).
  No expressiveness change (`arr` is denotationally identity).
- **Spec:** already grade-blind; no observable change.
- **Re-extract MAlonzo** (touches the live elaborator).

## Relationship to the RVar work (Plan 0.51 residual)

The check-mode RVar bbc value cases (`check-agreeV-todo` = {RVar}) carry the
`mk-kind` kind-splits this plan's M2 removes. Either (a) finish RVar first against
the graded objects and let M2 simplify it later, or (b) do M1/M2 first so RVar is
written once against erased objects. `bbc-other`'s poly path is blocked on
`bbc-other-poly-witness` regardless (orthogonal — poly-instantiation completeness).

## See also

D068 (this decision), D065 (bare `arr` dropped), D066 (grade-indexed `⊢ᵐ`, eff→pure
unsound), D046 (grade-erased IR), D032 (`arr` lifts), D061 (SigOp contract from
interpretation), OCP-0007 (capability-graded effects), Plan 0.39 (optimizer drops
pure SigOps), QTT (McBride/Atkey — quantity semiring, erasure, dependent on-ramp).
