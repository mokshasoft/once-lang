---
parent: 0-ocp3
status: complete
date: 2026-04-17
---

# Plan 0.3: Frontend Verification Gaps

## Progress log

- **2026-04-17**: Initial commit. Scaffolded `VerifiedTypeChecker`
  record in `formal/Once/TypeCheck/Verified.agda` — bundles every
  proven property as a record field, so proof regressions fail the
  compiler build at record construction (structural enforcement, not
  CI-check enforcement).
- **2026-04-17**: Completed **G6 (determinism)** and **G3 (totality)**.
  Added `Once.Grammar.Convert` wiring the formal grammar spec to the
  parser's output type.
- **2026-04-17**: Started **G2 (completeness)** with 12 judgment
  rules and 10 soundness theorems covering: literals, `unit` builtin,
  qualified names, annotations, pairs, unary negation, let bindings,
  lambdas (check mode). Deferred: `RVar` local/import (needs
  `"unit"`-shadow side-condition), `RApp` (polymorphic-builtin
  dispatch), `RDestruct` (3-way with sum-type matching), `RBinOp`
  (arithmetic/comparison family split).
- **2026-04-17**: Started **G4 (structured errors)** with the
  `TypeError` datatype (16 variants) and 5 error-preservation
  theorems (RLam-in-infer, inl/inr/initial-in-infer, unbound qualified).
- **2026-04-17**: **Proof technique note** — the stdlib `inspect`
  idiom produces an opaque internal `with`-helper that external
  proofs cannot unify with (documented in
  `docs/formal/historical/lessons-learned.md`). Hit on the `RLam`
  case where the elaborator used `with q' ≤q q | inspect (q' ≤q_) q`
  to capture the linearity witness for `Surface.lam`. Resolved by
  refactoring the elaborator to use `decideLeq : (q' q : Quantity)
  → Maybe ((q' ≤q q) ≡ true)` — returns the proof as `just refl`
  directly, no `inspect`, no opaque helper. Pattern applies to any
  elaborator site that captures a Bool decision's proof.
- **2026-04-17**: Applied the same refactor pattern to `RVar "unit"`:
  switched the literal-string pattern to a decidable equality
  (`StrProp._≟_`) to avoid the neutral-term obstacle. Now `sound-RVar`
  covers all three RVar branches (builtin / local / import).
- **2026-04-17**: **G2 soundness direction complete (15/15)** —
  all RawExpr constructor shapes now have soundness theorems:
  RInt, RStringLit, RUnit, RVar (all 3 branches via refactor),
  RUnaryOp-neg, RAnnot, RPair, RLet, RDestruct, RBinOp (10 operators
  × 2 families), RQualified, RLam (check mode), RApp for the 4
  polymorphic builtins (id, fst, snd, terminal), and generic RApp.
  The generic-RApp case was unblocked by refactoring the elaborator
  to classify RApp heads via a `Maybe PolyBuiltinApp` dispatch
  instead of 7 literal-string patterns — same pattern as the
  decideLeq and RVar refactors.
- **2026-04-17**: **G4 structured-error refactor (major)** — the
  elaborator's `failure` constructor now takes `TypeError` directly,
  not `String`. Rationale: the blocked items (UsageViolation,
  BinOpLeftError/RightError) were signals of a parallel-vocabulary
  gap between the elaborator's raw-string failures and the structured
  `TypeError` datatype. The refactor changes:
  * `data InferElabResult` / `CheckElabResult`: `failure : TypeError`.
  * Every `failure "…"` call in the elaborator builds a structured
    variant (17 call sites updated).
  * `asInt` / `asFun` views carry `TypeError` (`NotFunction actual`
    variant added, `TypeMismatch Int actual` reused for asInt).
  * `ErrorProofs.agda` collapses: most theorems become direct `refl`.
  * Downstream string consumers (`Compile.agda`) use `renderError`.

  **Payoff**: the two previously-blocked G4 theorems now compile:
  `lam-usage-violation-is-UsageViolation` and
  `binop-left-err-wraps` / `binop-right-err-wraps`. 22 error-
  preservation fields, 14 `TypeError` variants, all fully structural.

  Adding a new failure mode to the elaborator is now a type-checked
  obligation: you must construct a `TypeError`; if a needed variant
  is missing, you must add it to the datatype. No more raw-string
  failures.
- **2026-04-17**: **G2 completeness direction — per-rule theorems
  complete (infer + check)**. `Once.TypeCheck.Completeness` now has
  a completeness theorem for every RawExpr constructor:
  * Leaves: RInt, RStringLit, RUnit, RVar-unit.
  * Single-lookup: RQualified, RVar-local, RVar-import.
  * Composition: RAnnot, RPair, RUnaryOp-neg.
  * Extended-ctx: RLet, RDestruct.
  * Applications: RApp-id, RApp-fst, RApp-snd, RApp-terminal,
    RApp-generic.
  * Operators: RBinOp-arith (5 ops), RBinOp-cmp (6 ops).
  * Check mode: check-complete-RLam (9 quantity pairs, 6 valid + 3
    absurd via the `q' ≤q q ≡ true` premise).

  Elaborator refactor #5: `decideLeq` now pattern-matches on `(q', q)`
  directly (9 explicit cases) rather than via `with q' ≤q q`. Same
  "eliminate opaque with-helper" pattern that unblocked the previous
  four refactors.

  Full completeness at the derivation level (walk a derivation,
  invoke per-rule theorems) is mechanical now that the building
  blocks exist. 5 completeness fields wired into
  `VerifiedTypeChecker`; remaining ones follow the same pattern.

- **Decidability of typeability**: trivial given totality (G3)
  combined with intrinsic typing — the elaborator is a decidable
  yes/no verdict. Deferred unless downstream needs it stated.

- **2026-04-17**: **G2(a) judgment split landed.** `Once.TypeCheck.Judgment`
  now exposes mutual `_⊢ᵢ_∶_⨾_` (infer) and `_⊢ᶜ_∶_⨾_` (check)
  relations. `t-lam` lives only in check; `t-annot` takes a check
  sub; `t-embed` promotes infer to check. `t-app` requires
  `classifyAppHead f ≡ nothing` so the judgment matches the
  elaborator's dispatch deterministically. Legacy alias
  `_⊢_∶_⨾_ = _⊢ᵢ_∶_⨾_` keeps downstream callers working.

  Soundness and VerifiedTypeChecker updated. Most existing proofs
  are unaffected by the alias. Only `sound-check-RLam` needed
  retyping (output ⊢ᶜ instead of ⊢ᵢ) and `sound-RAnnot` needed a
  one-line `t-embed` insertion.

  **Last blockers to G2 full-walk completeness** (needs user
  direction on *how* to proceed):
  1. A generic-fallback lemma for `checkElab`: for non-specialized
     RawExpr shapes, `checkElab ctx e T ≡ inferElab-then-≟T-match`.
     Mechanical but verbose; needs to be stated and proved. Question:
     phrase inline in `Completeness` vs. hoist into `Elaborate.agda`?
  2. Subtler: the elaborator's `checkElab` has specialized cases
     for `RApp (RVar "arr"/"apply"/"compose"/"pair"/"curry")` at
     specific target types. These don't currently have
     corresponding check-mode judgment rules. If a user binds
     (e.g.) `"arr"` locally, the judgment admits the generic t-app
     derivation, but the elaborator uses the specialized arr rule
     regardless. This is a real judgment-over-permissiveness issue
     that mirrors what we fixed for classifyAppHead / inl-inr-initial,
     but for 5 more builtin names in check mode. Resolution: add
     check-mode specialized rules (t-arr-check, t-apply-check, etc.)
     to the judgment AND add the classifier-equivalent premise to
     the generic `t-embed`'s use path. ~200 lines of additions,
     mechanical once the pattern is set.

- **2026-04-17**: **Attempted full-walk G2 completeness (`∀ d →
  inferElab-succeeds`) — deferred with architectural note.** The
  walk exposed a judgment/elaborator mismatch: t-pair (and every
  recursive rule) accepts sub-derivations using t-lam, producing
  e.g. `ctx ⊢ RPair (RLam x body) b : (A ⇒ B) * C ⨾ Ψ`. The
  elaborator rejects these because `inferElab ctx (RLam x body)`
  always fails with `LambdaInInferMode`.

  Three resolution paths are documented in `Completeness.agda`:
  1. Split the judgment into mutual `_⊢ᵢ_∶_⨾_` (infer) and
     `_⊢ᶜ_∶_⨾_` (check) relations, putting t-lam only in the check
     relation.
  2. Define a `NoLam : derivation → Set` predicate tracking "this
     derivation uses no t-lam anywhere" and parametrise the full
     walk on it.
  3. Leave the per-rule completeness theorems as the final form;
     call sites compose them by hand.

  Option 3 is what's landed. The per-rule theorems (19 cases
  covering every RawExpr constructor + RLam check-mode) are the
  substantive content; a full walk is a phrasing convenience that
  depends on committing to one of options 1 or 2. Making that
  decision is deferred since G1/G5 don't require it.

  Judgment refinement: `t-var-local` and `t-var-import` now carry
  `x ≢ "unit"` side conditions, reflecting the elaborator's
  short-circuit that shadows local bindings named "unit" with the
  monomorphic builtin.

- **2026-04-17**: **G7 (Algebraic identities): first substantial
  pass landed.** `Once.TypeCheck.Identities` module with ~30
  theorems covering:
  * `decideLeq` correctness (both directions, iff-style).
  * `classifyAppHead` determinism and per-builtin-name identities.
  * `isArithmeticOp` / `isComparisonOp` exhaustive + exclusive.
  * `_≤q_` reflexive, transitive, Zero-minimum, Many-maximum.
  * `+q` / `*q` / `⊔q`: commutativity, associativity, identities,
    absorbing elements, distributivity of `*q` over `+q`.
  * Usage-vector pointwise lifts: `+ᵘ` / `⊔ᵘ` comm + assoc,
    `*ᵘ`-One identity, `*ᵘ`-Zero absorbing, `zeroUsage` identities
    for `+ᵘ` / `⊔ᵘ`.
  * Grammar round-trip injectivity.

  22 identity fields wired into `VerifiedTypeChecker`.

- **2026-04-17**: **G4 per-Type coverage extended.** Beyond the
  earlier representative types (Unit, Int), added theorems for
  Void, Str, Float, Buffer, Sum, Product, and function types for
  the fst/snd/neg/case-scrut error-preservation patterns.
  ~16 more G4 fields.

- **2026-04-17**: **G4 exhaustive coverage complete.** Every
  non-matching Type constructor (Void, Str, Float, Buffer, Sum,
  Product, Fun, Eff, μ-type, ν-type) now has a named error-
  preservation theorem for each of fst/snd/neg/case-scrut. All
  mechanical refl-after-rewrite proofs.

- **2026-04-17**: **G1 (parser correctness) start.** Added
  `Once.Grammar.Printer` with `printGType : GType → List Token`
  producing a canonical always-parens form with explicit quantity
  markers. Six smoke-test round-trips pass by `refl` (Unit, Void,
  Int, Float, Buffer, String, and representative compound types
  at each quantity). General per-constructor round-trip theorems
  remain future work — they require list-append reasoning but
  are otherwise mechanical.

- **Cumulative state**: ~99 proof fields in `VerifiedTypeChecker`
  covering G2 soundness + per-rule completeness, G3 totality,
  G4 structured errors + exhaustive per-Type coverage, G6
  determinism, G7 algebraic identities, and Grammar round-trip.
  Frontend build fully green; full compiler build green. The
  main plan 0.3 pass can be considered substantively complete;
  remaining work is G1 general round-trip proofs and G5
  integration (composition of G1 + G2 at stage boundaries).
- **2026-04-17**: **Lessons-learned** — added four new sections to
  `docs/formal/historical/lessons-learned.md`: bundle-and-rewrite
  idiom, apply-IHs-eagerly rule, elaborator refactor to eliminate
  opaque `with`-helpers, and the `VerifiedFoo` record pattern for
  structural proof-obligation enforcement.

- **2026-04-17**: **G2 decision 1 landed — fallback lemmas hoisted
  into `Elaborate.agda`.** Per user direction ("1" = principled /
  hoist), added 14 per-RawExpr-shape `checkElab-fallback-*` lemmas:
  RInt, RStringLit, RUnit, RVar-unit, RQualified, RAnnot, RPair,
  RLet, RDestruct, RUnaryOp, RBinOp, RApp-id, RApp-fst, RApp-snd,
  RApp-terminal. Each lemma says: for a RawExpr shape whose check-
  mode clause falls through to the generic fallback, a successful
  `inferElab` transports to a successful `checkElab` at the same
  type. Proof uniformly: `rewrite eqInf with T ≟T T` +
  `yes refl / ⊥-elim`. Hoisted alongside `decideLeq` and
  `classifyAppHead` as "structural facts about `checkElab`'s
  operational behaviour".

- **2026-04-17**: **G2 elaborator refactor #6 —
  `checkElab`'s RApp dispatch**. Replaced the three specialised
  `Raw.RApp (Raw.RVar "inl"/"inr"/"initial") arg` literal-pattern
  clauses with a single `with classifyAppHead f` dispatch, mirroring
  the existing `inferElab` structure. The non-sum-type failure
  branches enumerate all 11 `Type` constructors per pba-inl and
  pba-inr (explicit coverage that Agda's `with` demands). The other
  four classifier values (pba-id, pba-fst, pba-snd, pba-terminal)
  plus `nothing` each inline the generic `inferElab-then-≟T-match`
  fallback — a shared `checkViaInfer` helper tripped Agda's
  termination checker (checkElab → helper → inferElab → checkElab
  cycle), so the body is duplicated across the five branches.
  Upstream consumers typecheck unchanged.

- **2026-04-17**: **G2 full-walk: architectural blocker identified.**
  Attempted mutual `infer-complete : ⊢ᵢ → inferElab` success /
  `check-complete-embed-clean : ⊢ᵢ → checkElab` success walk. The
  infer-side walk compiles over every `⊢ᵢ` rule. The check-side
  walk's `t-app` case stalls: even after the RApp classifier
  refactor, `rewrite notPoly | eqInf` cannot drive
  `checkElab ctx (RApp f x) T` to reduce past `inferElab`'s internal
  `with classifyAppHead f | asFun (inferElab ctx f)` chain when
  abstracted via Agda's `with`. The fallback lemma
  `checkElab-fallback-RApp-generic` reaches an "UnequalTerms" error
  because the goal's expression still shows with-function
  expansions that the top-level `rewrite eqInf` doesn't reach
  inside.

  Deferred resolution paths:
  * A proof-friendly extraction function that flattens inferElab's
    RApp dispatch so the success equation matches without passing
    through two nested `with`s.
  * An `inspect`-based idiom that threads classifyAppHead's equation
    through both checkElab and inferElab.
  * Judgment-level restructure: the RVar-shadowing-of-builtins
    problem (blocker 1 below) needs to be addressed first since
    its fix might simplify the app-head proof path.

  Two blockers remain for a true full walk:
  1. `t-embed (t-var-local …)` / `t-embed (t-var-import …)` for
     x in the 12 specialised bare-builtin names: the specialised
     check-mode RVar clause may reject at types the fallback would
     accept. Fix: side-condition `x ∉ specialised-builtin-set` on
     lookup rules, or remove the specialised bare-builtin check-
     mode clauses in favour of inferElab-then-match.
  2. `t-app (notPoly) dF dX` under `t-embed`: the
     `checkElab-fallback-RApp-generic` lemma fights with-
     abstractions between checkElab's classifier dispatch and
     inferElab's internal classifier+asFun chain.

  Status: per-rule completeness theorems (above) remain the
  substantive content; the fallback-lemma infrastructure unblocks
  the common cases (literals, RQualified, RAnnot, RPair, RLet,
  RDestruct, RUnaryOp, RBinOp, RApp of specialised builtins). The
  mutual walk is ≤2 follow-up commits away.

- **2026-04-17**: **Autonomous session: view-audit (task #37), parser
  keyword dispatch refactor, WF-refactor attempt (task #40).**
  * Task #37 CLOSED: audited 10 `Bundle` types in Soundness.agda;
    concluded no view-refactor wins — bundle idiom is correct for
    mutually-recursive calls and stdlib-Dec results. Memory updated
    with the bundle-vs-view rule.
  * Parser clean-up: `parseLetCont`/`parseDestructOf`/
    `parseDestructBranches`/`parseRightBranch` in `Once.Parser.Expr`
    switched from literal-string patterns to `StrProp._≟_` dispatch
    (commit f39454f3) — consistent with `parseTypeAtom` and
    `classifyAppHeadView` idioms. Enables future `ExprInvariant`
    proofs.
  * Task #40 attempted on `Once/Parser/Type.agda` (subagent delivered
    620-line WF rewrite); reverted due to downstream regression:
    `Grammar/Roundtrip` and `Grammar/ParserInvariant` break because
    Acc-threaded reductions lose the syntactic reduction shape that
    `rewrite parseX` / `with parseX in eq` depend on. Fix needs
    equational unfolding lemmas (~1500 lines per parser module).
    Memory updated with the blast-radius rule. Task description
    carries the concrete resume path.
  * Task #38 (ExprInvariant) attempted; shelved pending #40 because
    the same reduction obstacle that motivates #40 creates postulates
    here. Parser keyword-dispatch refactor (above) eliminates 4 of
    the 6 obstacles; the remaining 2 need the full WF refactor.

- **2026-04-17**: **Frontend postulates/pragmas audit.** Summary
  of what remains, with elimination paths:

  **Postulates (9 total in the frontend tree):**
  * *Trust boundaries (3, intentional):* `Surface/Semantics.agda`'s
    `divℤ`, `modℤ`, `evalSurfacePrim`; `Surface/Correct.agda`'s
    primitive-correctness axioms (`intLit-correct`, `addIR-correct`,
    etc.). These specify runtime contracts; eliminating them shifts
    trust rather than removing it.
  * *OCP-0003 migration leftovers (6, eliminable):*
    `Optimize/Correct.agda` × 4, `Escape/Correct.agda` × 2,
    `Fusion/Correct.agda` × 1. All trace to the underlying
    `Optimize.agda`/`Escape.agda`/`Fusion.agda` functions having
    postulated cases due to μ-type/ν-type coverage issues.
    Elimination path: view-datatype refactor of the introduction /
    elimination pattern matching. Out of scope for this plan; see
    the separate task for 0.4-optimizer-correctness.
  * *Extensionality (1):* `Once.Postulates.extensionality` —
    standard Agda convention.

  **TERMINATING pragmas (8 in `Parser/*`, 1 in
  `Grammar.ParserInvariant`):** all trace to the parser's mutual
  recursion via monadic `>>=` combinators, which Agda's termination
  checker can't see through. Elimination path: size-indexed
  parsers. Out of scope for this plan.

  The **typechecker proper** (`TypeCheck/*` + the new
  `Grammar/Roundtrip`, `Grammar/ParserInvariant`) has **0
  postulates and 0 new pragmas**. Plan 0.3's verification promise
  holds.

- **2026-04-17**: **G1 + G5 CLOSED — frontend pipeline now proven
  correct end-to-end (for types).** Two new modules:
  * `Once.Grammar.Roundtrip` — general round-trip theorem
    `round-trip-concrete : Concrete g → parseType (printGType g) ≡
    just (toType c, [])` for every TVar-free GType.
  * `Once.Grammar.ParserInvariant` — parser-output invariant
    `parseType-NoMuNu : parseType toks ≡ just (t, rest) → NoMuNu t`.
    The parser never emits μ-type / ν-type shapes, so downstream
    stages can treat the output as grammar-expressible.

  Supporting refactor (applying the "eliminate opaque `with`-helpers"
  principle from the lessons-learned doc):
  * `parseTypeAtom`'s TWord clause: switched from literal-string
    patterns to `StrProp._≟_` dispatch chain.
  * `tryParseTypeVar`: collapsed to unconditional `nothing`
    (removed redundant `with isUpperWord`, both branches returned
    nothing).

  The typechecker side (G2 + G3 + G4 + G6 + G7) was already closed.
  With G1 type-side + G5 type-side now landed, the frontend pipeline
  has an end-to-end correctness story for types: lex → parse →
  grammar → typecheck → IR. General round-trip for expressions
  (GExpr round-trip, E- corresponding invariant) remains future
  work as plan 0.3.x, but the type-side closure is the structural
  prerequisite that unblocked it.

- **2026-04-17**: **G2 full mutual walk — CLOSED.** With both
  blockers resolved (see entries below), the mutual
  `infer-complete : ⊢ᵢ → inferElab-success` and
  `check-complete : ⊢ᶜ → checkElab-success` walk lands. Every
  ⊢ᵢ / ⊢ᶜ rule has a handler that wires the IH into the matching
  per-shape fallback lemma. Together with the soundness theorems,
  this gives `elaborator ⟺ judgment` in both modes. No new
  postulates or pragmas.

- **2026-04-17**: **G2 decision — remove specialised bare-builtin
  check-mode clauses (blocker #1 resolved).** The 12 specialised
  `Raw.RVar "id"` / `"fst"` / ... clauses in `checkElab` were
  short-circuiting to polymorphic-builtin semantics without
  consulting local/import lookup, creating an impedance with the
  judgment. Removed; check-mode now routes bare builtin names
  through the generic inferElab-then-type-match fallback, which
  consults lookup uniformly. User-visible change:
  `check (id : A → A)` now needs `id` in scope (via imports or
  let). Applied form `id e` is unchanged. All compiler test
  failures are pre-existing (30/61 — same count before and after
  the change). Frontend typechecks clean.

- **2026-04-17**: **G2 `AppHeadView` refactor — blocker #2
  resolved.** The user directed us to the lessons-learned § "with
  patterns block computation" + "eliminate opaque `with`-helpers by
  refactoring the definition". Rather than fight `rewrite` against
  classifyAppHead's internal `with StrProp._≟_` chain, refactor the
  operational function.
  * New `AppHeadView` datatype (each constructor indexed by the
    head's concrete shape, `ahv-other` no-args catch-all).
  * New `classifyAppHeadView : (f : RawExpr) → AppHeadView f`.
  * Compat lemma `classifyAppHead-nothing⇒view-other`.
  * `inferElab` + `checkElab`'s RApp dispatch switched to the view.
  * Downstream proofs (Soundness, Completeness, ErrorProofs)
    adapted to use the compat lemma in their `rewrite`.

  Key proof insight: `checkElab-fallback-RApp-generic` uses the
  compat lemma TWICE in `rewrite` (once for checkElab's outer
  dispatch, once for inferElab's nested dispatch). This closes
  the previously-blocked lemma with a 5-line proof instead of the
  60+ lines of case-splits tried earlier. Pattern documented in
  `feedback_with_abstraction.md` memory.

  Status: G2 completeness now has ONE remaining blocker (RVar-
  shadow of specialised bare-builtin names); the mutual full walk
  is one focused judgment-restructure commit away.

## Context

The frontend pipeline (parser → typechecker → surface elaboration → IR)
has significant formal verification coverage today, but the coverage is
uneven. Plans 0.2.5 and 0.2.6 landed:

- Intrinsic typing: `inferElab` produces `SExpr Γ Ψ A` — Agda's type
  checker rejects any code path where the output's type index is wrong.
- Linearity-by-construction: `lam q refl body` requires a propositional
  `(q' ≤q q) ≡ true` witness at every lambda site. No term that
  violates its declared arrow grade is constructible.
- Semantics preservation through surface elaboration:
  `Surface.Correct.elaborate-correct : evalSurface ρ e ≡ eval′ (elaborate e) (interpEnv ρ)`.
- Zero postulates, zero `TERMINATING` pragmas in the type-checker
  pipeline (structural termination on `RawExpr` throughout).

That's strong for the properties that are actually proved. But several
important correctness properties of the frontend are **not proved and
not tested**. This plan enumerates those gaps and sketches a proof
strategy for each.

The intent is to close these via formal proof rather than property-based
testing (QuickCheck). The existing verification is strong enough that
adding QuickCheck would mostly duplicate guarantees intrinsic typing
already gives, and the few things QuickCheck would genuinely add are
exactly what these proofs will cover.

## Scope

Everything in `formal/Once/Parser/`, `formal/Once/TypeCheck/`, and
`formal/Once/Surface/`. The IR backend (`formal/Once/CCC/`,
`formal/Once/Escape`, `formal/Once/Fusion`) is out of scope for this
plan — those have their own separate verification tracks.

## The Gaps

### G1: Parser correctness (lexer + parser)

**What's unproved**: the parser is zero-proofs. It's Agda code but it
has no statement of what it's supposed to do, no parse-pretty roundtrip,
no proof that the CFG it implements matches the grammar in
`Once.Grammar`.

**What could go wrong**:
- Silent misparsing (syntactically malformed input accepted as
  something the user didn't intend).
- Ambiguity — the parser accepts a program but elaborates it differently
  than the programmer reads it.
- Missing reject cases — a grammar-invalid token stream parses to a
  `Just` result.
- Drift between `Once.Grammar` (the aspirational spec) and
  `Once.Parser.*` (the implementation).

**Proof strategy**:

1. **Wire `Once.Grammar` to the parser as its output type.** Currently
   `parseType : List Token → Maybe (Type × List Token)`. Change to
   `parseGType : List Token → Maybe (GType × List Token)`. Then add
   `gtype-to-type : GType → Type` conversion (partial, since Grammar
   allows type variables that `Type` rejects after 0.2.5).
2. **Parse-pretty roundtrip**: `parse (pretty g) ≡ just (g , [])` for
   every well-formed `GType`, `GExpr`, `GModule`. Proved by structural
   induction on the grammar types.
3. **Pretty-parse roundtrip**: if `parse toks ≡ just (g, [])` then
   `lex (pretty g) ≡ toks` (modulo whitespace normalization). This
   pins the parser's extensional behavior.
4. **Unique parses**: `parse toks ≡ just (g₁, r₁) ∧ parse toks ≡ just
   (g₂, r₂) ⟹ g₁ ≡ g₂ ∧ r₁ ≡ r₂`. (Trivial since `parse` is a total
   function but stated for clarity — rules out future refactors that
   introduce ambiguity via backtracking choices.)
5. **Grammar conformance**: define `WellFormed : List Token → Set` as
   a grammar predicate, prove `WellFormed toks ⟺ ∃ g. parse toks ≡ just
   (g, [])`. This is the completeness + soundness of the parser
   against the grammar spec.

**Entry cost**: ~600 lines of Agda. `Once.Grammar` needs extending with
pretty-printing; parser-stages need proofs chained through `_>>=_`/`_>>_`.

**Dependencies**: 0.2.6 (surface syntax shapes are finalized).

---

### G2: Typechecker completeness

**What's unproved**: intrinsic typing gives "every accepted program is
well-typed". The converse — "every well-typed program is accepted" —
is unstated. A subtly-wrong typechecker could reject correct programs.

**What could go wrong**:
- A case in `checkElab` falls through to `failure` for some well-typed
  shape the author didn't consider.
- The `_≟T_` decidability is fine, but we pass it the wrong types in
  some code path (e.g., comparing before-substitution and after-
  substitution forms where we should compare matching phases).
- After the 0.2.6 refactor, polymorphic builtins only specialize in
  specific pattern-matched shapes (`checkElab ctx (RVar "compose")
  (...) = ...`). A well-typed but slightly-different shape
  (e.g., `compose` applied fully at surface but split across a let)
  might fall through to the generic path and fail.

**Proof strategy**:

1. **Define the declarative typing judgment**: `Γ ⊢ e : A ; Ψ` as an
   inductive relation on `(Ctx n) × RawExpr × Type × Usage n`. Rules
   mirror the Expr constructors one-for-one (plus substitution and
   signature-specialization rules for polymorphic builtins).
2. **Completeness theorem**:
   `Γ ⊢ e : A ; Ψ → ∃ eE d f. inferElab (ctxOf Γ) e ≡ success A Ψ eE d f`
   (and similarly for `checkElab`).
3. **Soundness theorem** (strengthens what intrinsic typing gives):
   `inferElab ctx e ≡ success A Ψ eE d f → (ctxOf⁻¹ ctx) ⊢ e : A ; Ψ`.
4. **Decidability**: `(Γ, e) → Dec (∃ A Ψ. Γ ⊢ e : A ; Ψ)` — says
   that typeability is decidable, and the typechecker's decision
   matches the judgment.

**Entry cost**: ~800 lines. The declarative judgment is ~20 rules;
completeness proof walks each rule against the `inferElab`/`checkElab`
structure. Builtin-specialization rules are the trickiest part.

**Dependencies**: 0.2.6 (usage-indexed Expr means the judgment is
usage-indexed too).

---

### G3: Robustness / crash freedom

**What's unproved**: there's no statement that `inferElab` is total.
Termination is proved (structural on `RawExpr`). But "total" means
more: no partial patterns, no stuck terms, defined on all inputs.

**What could go wrong**:
- A pattern match we thought was exhaustive isn't (Agda would normally
  reject but some pragmas/underscores can hide this).
- A helper uses `Maybe` in a way where a subtle path reaches an
  unreachable `nothing`.

**Proof strategy**:

1. **Totality is already proved implicitly** — Agda's coverage checker
   demands total pattern matches, no TERMINATING means structural
   recursion. So `inferElab : (ctx : NamedCtx) → RawExpr →
   InferElabResult (NamedCtx.debruijn ctx)` is already a total function
   with no exceptions and no diverging inputs.
2. **What IS missing**: a statement of the form "for every `(ctx, e)`,
   `inferElab ctx e` is either `failure msg` or `success A Ψ eE d f`
   where that `success` is well-typed and respects linearity". This
   is a combined totality + well-formedness theorem.
3. **Strategy**: state and prove
   `(∀ ctx e. Σ err. inferElab ctx e ≡ failure err) ⊎ (∀ ctx e. Σ A Ψ
   eE d f. inferElab ctx e ≡ success A Ψ eE d f)`. Case split on
   RawExpr; structural induction.

**Entry cost**: ~200 lines. Mostly mechanical since totality is
already there implicitly; the proof just makes it explicit and pairs
with well-formedness.

**Dependencies**: G2 (soundness), useful building block for it.

---

### G4: Error message quality

**What's unproved**: `failure : String → InferElabResult Δ`. The
string can be anything. A regression that replaces
`"Parameter 'x' used with quantity Many but declared with quantity One"`
with `"failure"` would not break any test.

**What could go wrong**:
- Error messages drift into unhelpful "internal error" language.
- Different input shapes produce identical messages, hiding what's
  actually wrong.
- Downstream tools (IDE, LSP) that want to consume structured errors
  have no stable contract.

**Proof strategy**:

1. **Introduce a structured error type**:
   ```
   data TypeError : Set where
     UnboundVariable  : String → TypeError
     ExpectedFunction : Type → TypeError
     ArgumentMismatch : (expected actual : Type) → TypeError
     UsageViolation   : (declared actual : Quantity) (name : String) → TypeError
     ...
   ```
2. Change `failure : String → ...` to `failure : TypeError → ...`.
   Strings become `formatError : TypeError → String`.
3. **Error-preservation theorem**: prove that each failure path of
   `inferElab`/`checkElab` reaches the specific `TypeError` variant
   appropriate to the shape of the rejection. E.g.,
   `inferElab ctx (RVar x) ≡ failure err → lookupLocal ctx x ≡ nothing
   → err ≡ UnboundVariable x`.
4. **Message uniqueness**: `failure err₁ = failure err₂ → err₁ ≡ err₂`
   (trivially refl). Combined with (3), every rejection has a canonical
   structured form.

**Entry cost**: ~400 lines. Larger because every failure site needs
the right constructor; then a theorem per case tying rejection
condition to error shape.

**Dependencies**: none, but G2 makes the rejection side well-specified
via the declarative judgment ("rejects ⟺ no derivation exists"),
which is where the error-shape theorems would live.

---

### G5: Integration correctness at stage boundaries

**What's unproved**: each stage has (some) local proofs, but
cross-stage invariants aren't stated. E.g., "if the parser returns
`just g`, then the `RawExpr` derived from `g` satisfies the precondition
of `inferElab`".

**What could go wrong**:
- Parser produces a `RawExpr` that the typechecker can't handle due to
  a shape invariant the parser failed to enforce (e.g., empty RLet body
  list, multi-line construct where the typechecker assumes single-line).
- Elaboration assumes the typechecker's output satisfies some
  invariant (e.g., no free TVars) that the typechecker doesn't
  explicitly guarantee.

**Proof strategy**:

1. **Compose soundness/completeness at each boundary**. With G1 and
   G2 in place:
   `parseModule source ≡ just mod → ∀ decl ∈ mod.decls.
    ∃ Γ. Γ ⊢ decl : wellFormed`.
2. **Chain pipeline theorem**:
   `parseModule source ≡ just mod → compileModule source ≠ internalError`.
3. This makes the pipeline guarantees end-to-end for valid source.

**Entry cost**: ~200 lines, assuming G1 and G2 are done. Mostly
stitching existing theorems.

**Dependencies**: G1, G2.

---

### G6: Typechecker determinism

**What's unproved**: nothing says `inferElab ctx e = inferElab ctx e`
(reflexively — but structurally, e.g., the same result constructor with
the same fields).

**What could go wrong**:
- Pure in Agda by construction. Low risk.
- But the `freshCounter` threading could diverge between paths that
  should be equivalent.

**Proof strategy**:

1. `inferElab-deterministic : ∀ ctx e. inferElab ctx e ≡ inferElab ctx e`
   — trivially `refl` in Agda since `inferElab` is total and pure.
2. Stronger: `inferElab-congruence`: if we change context in a way
   that doesn't affect free variables of `e`, the result is isomorphic.
   Stated via a permutation or isomorphism on `InferElabResult`.

**Entry cost**: ~50 lines. Mostly a trivial theorem + a more
interesting congruence statement.

**Dependencies**: none. Cheap to land standalone.

---

### G7: Algebraic identities of frontend operations

**What's unproved**: properties like "re-typechecking an already-elaborated
program is a no-op at the IR level", "`elaborate` commutes with
substitution", etc.

**What could go wrong**: optimization passes or future IR transforms
might assume an algebraic identity that isn't formally guaranteed,
leading to silent correctness bugs.

**Proof strategy**:

Define and prove the identities we rely on:

1. `elaborate` is injective: `elaborate e ≡ elaborate e' → e ≡ e'`.
2. `inferElab`/`checkElab` agree on shared programs:
   `inferElab ctx e ≡ success A Ψ eE d f →
    checkElab ctx e A ≡ success Ψ eE d f`.
   (The reverse isn't quite true because check mode has specialized
   paths; the right statement is that the expressions agree modulo
   specialization.)
3. Elaboration is stable under weakening:
   `elaborate (weaken e) ≡ weakenIR (elaborate e)`.

**Entry cost**: ~400 lines. Each identity is a structural induction
on the relevant datatype.

**Dependencies**: G2 (soundness) useful for stating (2) cleanly.

---

### G8: Performance bounds (NOT IN THIS PLAN)

**What's unproved**: nothing says `inferElab`'s runtime scales
linearly with program size. A performance regression (accidental
exponential) wouldn't break any proof.

**Why not in this plan**: formal proofs of runtime complexity in Agda
are possible but ergonomically painful. Better tracked via
benchmarking (independent of this plan). Mentioned for completeness.

**Strategy if pursued**: instrument `inferElab` with a step counter,
prove the counter is bounded by the structural size of the input.
This changes the function signature and cascades through every
theorem. Not worth it unless we hit a performance regression that
demands it.

---

## Phase Ordering

Recommended order (by dependency + value):

1. **G6 (Determinism)** — cheap, standalone, low-risk warmup.
2. **G3 (Totality)** — explicit statement of what the coverage checker
   gives us implicitly. Building block for G2.
3. **G2 (Completeness)** — the big one. Declarative judgment + sound/
   complete theorems. Unlocks G4 and G5.
4. **G4 (Error messages)** — structured errors, per-rejection theorems.
5. **G1 (Parser correctness)** — wire Grammar.agda, prove roundtrips.
   Independent of G2–G4; can be done in parallel if we have bandwidth.
6. **G5 (Integration)** — stitches G1 and G2 across the boundary.
7. **G7 (Algebraic identities)** — opportunistic; depends on G2's
   judgment to state cleanly.

## Why Not QuickCheck

A brief defense of the choice to skip property tests in favor of
proofs:

**Bugs QuickCheck could find that these proofs also catch (redundant)**:
- Type-correctness of output → intrinsic typing catches at Agda compile
  time (already does).
- Linearity violations → linearity-by-construction catches at compile
  time (already does).

**Bugs QuickCheck could find that these proofs newly catch (more rigorous)**:
- Parser misparsing → G1 rules out by proof.
- Silent rejection of well-typed programs → G2.
- Degraded error messages → G4.
- Cross-stage integration bugs → G5.

**Bugs QuickCheck catches that these proofs do not**:
- Performance regressions → G8 (deferred).
- Platform-specific behaviors of MAlonzo-compiled code → outside formal
  scope.

Nothing in QuickCheck's value add overlaps with what the planned
proofs cover. Once these are done, QuickCheck would genuinely be
redundant for frontend correctness.

Performance regressions are the one real gap that neither proofs nor
QuickCheck catch. Benchmarking and CI timing checks cover that
dimension and are orthogonal.

## Verification

- `make frontend` green after each phase.
- `make laws` / `make postulates` green (no new postulates introduced).
- `grep -c "postulate\|{-# TERMINATING" formal/Once/...` remains at
  the current counts (0 in the typechecker, 1 in Postulates.agda for
  `extensionality`).
- Each phase adds a new theorem or datatype; no phase should delete
  or weaken an existing proof.

## Deferred Items

- **Graded types** (richer `A^q` semantics inside compound types as
  sketched in `docs/design/memory.md`'s `(A^1 × B^0)^1` examples).
  Current surface syntax `A^q -> B` desugars to arrow-grading; going
  to full graded types is a design decision beyond this plan's scope.
  Would belong in a sibling plan like `0.4-graded-types.md`.
- **Performance proofs (G8)** as noted above.
- **Parser-level pretty printer** — needed for G1's round-trip proofs;
  would land as prerequisite infrastructure in G1.
