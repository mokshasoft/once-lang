---
parent: 0-ocp3
status: IN PROGRESS — 17 → 7 rows, measured 2026-09-22. The COUNT is flat
        across the SigOp work and that is honest: `obs-correct-sigop-rest`'s
        effectful content became a proof, and what it left behind is the
        narrow `decode-boxed`. What shrank is the CONTENT assumed, not the
        tally — see the note below, and D114's ledger classes.
        REMAINING (grep the `postulate` blocks under IRObsCorrect/):
          Interface  cata-correct           (D221 REFUTED it, then fixed the
                                             machine; now open, not false)
          SigOp      obs-correct-sigop-rest
          Simple     obs-correct-In, -Para, -Hylo, -Fuse
        (`-in-ν` left the list with `Ana`/`Out`; `-case` is DISCHARGED.)
        `obs-correct-case` CLOSED 2026-09-22, and two of its three recorded
        obstacles were defects in the STATEMENT, not difficulty:
          * it took no IHs, so no per-constructor clause could ever prove it;
          * its two jumps had no stated destination — `c-branch-tag-zero`
            resolves by scanning the WHOLE program and nothing said the scan
            lands in the fragment. `LabelsAt` (`SpanAt`'s dual: the program
            RESOLVES what the fragment's text resolves) fixed that, and
            `Comp`/`Pair`/the entry split it.
        The third — "the branch correspondence has no model anywhere" — needed
        no model: `flat-read-tag` reads the cell `Input1` points at and
        `valid-inl-wf`/`valid-inr-wf` already carry that equation.
        Five modules, ~1500 lines, zero postulates: `CaseShape` (layout +
        premise splits), `CaseRun` (fetches + shared prologue), one per ARM,
        and the dispatch. Split because one module cost 4.8 GB to typecheck;
        hoisting the `where`-bound arithmetic out first bought only 0.1 GB, so
        the telescope was NOT the cost — the two `MachineRefinesObsF` records
        were.
        THE THREE LEFT (Para/Hylo/Fuse parked as optimizations, 2026-09-22):
          * `obs-correct-In` — a SPEC decision, and D074's justification does
            NOT transfer. `in-unit` was justified by "a Unit value is
            materialisable from any register content" (`readTyped Unit _ _ =
            just tt`). For the dual, `readTyped (μ-type F) = nothing` — the
            catch-all — so a μ value is not materialisable at all. A
            residence-free `ResultPlace` therefore needs its own
            justification, not a mirror of D074's. Blast radius: `ResultPlace`
            + `InputAt` are matched at ~60 sites.
          * `obs-correct-sigop-rest` — EFFECTFUL CASE DISCHARGED 2026-09-22.
            Its `Emits` row was UNREACHABLE: the dispatch tested
            `fits-in-reg? B` first, and `Emits` carries `B ≡ Unit` with
            `FitsInReg Unit` empty, so every effectful SigOp fell through the
            first row — the one meaning "a Pure SigOp whose result does not fit
            a register". Effect-first dispatch makes it real; `B ≡ Unit` then
            settles the value half (`unit-result`, no residence) and
            `exec-sigop-halts-of (Emits _) ≡ false` keeps the state live, so
            all the content is the trace. `emits-arg-agree` splits on the
            DOMAIN's base type: `Unit` by η, `Int`/`Float` register-resident by
            `decode-arg`'s real clauses, and a BOXED base argument by
            `decode-boxed` — D114's existing `decode-unread` hole restated
            where it is consumed, not a new axiom. `obs-correct-sigop-rest` now
            covers only `Halts` and the two Pure fall-throughs — and the
            `Halts` row was REFUTABLE — and is now discharged too, see
            below.

          * `obs-correct-sigop-rest` @ Halts — **DISCHARGED 2026-09-22 by
            plan 0.97.** The row is now `halts-obs-correct-sigop`, a proof.
            What follows is the record of why it could not be proved before,
            because the diagnosis is the whole content of the fix.

            THE FIX, in one line: the Spec now records stoppedness (`T` gained
            a `stT` field, `_>>=T_` short-circuits on it, `Halts` returns
            stopped), and `ValueRealized` states `live`/`at-end`/`place` under
            "the Spec has not stopped" with a new `stops` for when it has. The
            probe below NO LONGER TYPECHECKS — it fails exactly at `VR.live`,
            which is no longer an unconditional `halted ≡ false`. It has been
            deleted. `obs-correct-sigop-rest` now covers only the two Pure
            fall-throughs.

            THE ORIGINAL DIAGNOSIS (kept — it is the record):

            NOT merely unproved. Its
            CONCLUSION is unsatisfiable, confirmed by a probe (untracked,
            `Once.Probe.HaltsRefute`, typechecks):
              `exec-sigop-halts-of (Halts _) ≡ true`, so executing the
              instruction sets the halt flag; but `ValueRealized.live` demands
              `halted (floc settle) ≡ false`, and `at-end` demands
              `fpc settle ≡ 1 + base`, which only executing that instruction
              reaches. Zero steps leaves the pc at `base`; one step sets the
              flag; two steps need the flag clear to take the second. All
              three close, so NO witness exists.
            `Halts` is not hypothetical: `Elaborate.agda:1953`/`:2006` build
            `haltsV refl` from an `se-halts` declaration, which is how `exit`
            is declared — so this row is on the apex path for every program
            that exits, i.e. every program.
            THE ROOT IS THE SPEC, not the codegen and not really the
            obligation. `T X = ℕ → List SigOpEvent × X` (TraceMonad:46) has no
            halt channel; `_>>=T_` (:58) unconditionally runs the continuation
            on `proj₂` of the first result; and `emit-eff` (TraceDenote:63) /
            `emit-D` (ValueDomain:323) treat `Halts` IDENTICALLY to `Emits` —
            one event, value `tt`, computation continues. So the Spec
            literally says a program CONTINUES after `exit`. The machine
            correctly stops, and `ValueRealized.live` sides with the Spec,
            which is why no witness exists.
            Two consequences, and the first bites immediately:
              (a) EVERY exiting program — i.e. every program — fails `live`,
                  because its settle state is after the halting instruction.
                  Note the EVENTS already agree for `main = exit 0`; it is the
                  liveness field alone that cannot be met.
              (b) for code AFTER a halt the Spec and the machine genuinely
                  DIVERGE: the Spec emits the later events, the machine emits
                  none. Latent today, real as soon as such a program is
                  written.
            Both come from the same absence, so one fix serves: put
            termination in the Spec — `T` carrying a stopped/`Maybe` result so
            `_>>=T_` short-circuits, `Halts` returning stopped rather than
            `tt` — and then `live` weakens to "live, or stopped AND the Spec
            stopped too", at which point `comp-obs-correct` matches
            structurally: if `f` stops, `g` does not run on either side.
            (Caveat on the strength of the claim: the probe refutes the
            CONCLUSION. For the postulate to be outright inconsistent its
            premises must be inhabitable at some instantiation — at the apex
            they are supplied by `entry-witness`, but that composite
            instantiation was not separately checked.)
            The ORIGINAL note, kept because the reasoning is the record:
            effectful SigOp trace IS the correctness claim), and it splits.
            `machine-event` decodes `Input1` DIRECTLY, and `decode-arg` on a
            non-literal falls to `decode-unread`, which is D114's existing
            named hole. So the Emits case is provable exactly for a
            REGISTER-RESIDENT argument, and the pointer case routes to a hole
            that already exists rather than needing a new one. `Emits` does not
            halt (`exec-sigop-halts-of (Emits _) = false`), so the value half
            is `pure-obs-correct-sigop`'s verbatim; only `traces-agree`
            differs, and it reduces to `decode-arg (baseA si) (prim-sv fit x)
            ≡ x` modulo the `coh`/`forget` transport.
          * `cata-correct` — surveyed 2026-09-22, and it does NOT depend on
            the `In` decision. It is the loop induction: `enough-fuel`/
            `run-halts` (totality), `traces-agree` by `μS-ind` over the events
            fold plus per-`instr-sigop` `respects-semM`, and a looping value
            correspondence. Plan 0.36/0.78 territory — the largest single row
            left, and not a sitting.
        FOUNDATION (2026-09-21): `Once.CCC.Codegen.LabelResolve`, the converse
        of `find-label-sound`, plus `found-in-window`. Zero postulates.
        ALSO LANDED (the `thunk` namespace, for 0.93 S4 / 0.91 S5):
          `ThunkScope` POSTULATE-FREE; `Once.CCC.Codegen.LabelsUnique`
          CONSTRUCTS `EmittedWF.labels-unique` for the real program; and
          `FlatFromObs.entry-no-thunks` is therefore a PROOF. That file is
          down to `rewrite-preserves-of`.
        Opened 2026-09-06 from D151, which found that the live
        machine-correctness path bottoms out in 17 postulates and that the
        nine `*WF` handler modules cannot discharge them.
---

# Plan 0.88 — Discharge `obs-correct-*`, the 17 live postulates

## 0. The one measurement that defines this plan

The import closure of `Once.Certified` is 335 modules. Every `*WF` handler is
OUTSIDE it. The live path is

    correct -> correctd -> correctR-sound -> correct-gm -> codegen-asm-correct
            -> ArchCorrect.ir-flat-correct -> ir-flat-correct-of
            -> ir-obs-correct -> obs-correct-{pair,inl,curry,apply,...} POSTULATE

So progress is measured in ONE number: how many of the 17 postulates in
`Once.CCC.Codegen.IRObsCorrectFlat` are gone. Green WF modules are not
progress; they have been green a long time and the number has not moved.

## 1. FIRST: fix the interface (D152)

`IRObsCorrectF` as it stands is NOT the principled obligation. It is stated
only at frontier 0 (`next-slot alloc == 0`, and `MachineRefinesObsF` speaks
about `ir-to-trace ir = ir-to-trace' 0 0 ir`), while the emitter emits every
sub-IR at a NONZERO frontier. The composition case cannot be proven against
it, which is why `comp-step` is a postulate.

So stage A is not "discharge obs-correct-fst". It is:

  A0. Re-index the obligation the way the emitter is indexed --

        IRObsCorrect ir = forall n l -> ... -> next-slot alloc == n -> ... ->
          MachineRefinesObs (trace-of (ir-to-trace' n l ir)) ir x s alloc

      with `n = l = 0` as the entry instance `ir-flat-correct-of` consumes.
  A1. PROVE `comp-step` against it. If it will not go through, the re-indexing
      is still wrong and nothing below is worth starting.

Only then do the per-constructor postulates mean anything: discharging one
against a non-composing interface buys nothing.

## 2. The interface as it currently stands (for reference)

    IRObsCorrectF ir =
      ir-size ir < program-bound ->
      forall mIn x input-loc s alloc -> next-slot alloc == 0 ->
      ValidAtWF mIn alloc x input-loc s -> BeforeFrontier alloc input-loc ->
      halted s == false -> InputAt x input-loc s ->
      MachineRefinesObsF ir x s alloc

Fixed from above by `ir-flat-correct`. Note `next-slot alloc == 0`: the live
interface ALREADY treats the frontier as construction-time, threaded from
zero -- independent confirmation of D150.

`ir-obs-correct` is the dispatcher and already recurses structurally, so a
handler gets its sub-IR results by calling `ir-obs-correct` on them. There is
no dispatcher to build and no `RecDispatcherWF` parameter to thread.

## 3. Vocabulary: what stays, what retires

    LIVE, keep     ValidAtWF, ResultPlace (at-loc/at-reg/unit-result),
                   InputAt (in-at-loc/in-at-reg/in-unit), ShapeAt,
                   MachineRefinesObsF, ir-obs-correct
    ISLAND, retire IRResultAWF, RecDispatcherWF, AllocBump, IRStackBudget,
                   IRHeapBudget, InputPlace, trace-is-ir-to-trace

`InputPlace` and `InputAt` are the same idea twice. The live one wins.

## 4. Stages

| # | Content |
|---|---------|
| A | Re-index per section 1 (A0), then PROVE `comp-step` (A1). The composition case is the test of whether the interface composes at all. |
| A2 | Only now: discharge one small constructor (`obs-correct-fst`) to calibrate per-postulate cost. |
| B | Salvage: for each island handler decide what transfers. Trace shapes, slot arithmetic and validity transports are reusable; the `IRResultAWF` scaffolding is not. |
| C | Grind the rest in dependency order (leaves first: fst, snd, terminal, initial; then pair, inl, inr; then curry, apply; then case; then the recursion schemes). |
| D | Delete the island once nothing in it is wanted, and with it `trace-is-ir-to-trace`, whose eleven gaps (D150) are island-local. |

## 5. What NOT to do

Do not repair, extend or re-green a `*WF` module before its content is known
to transfer. Nine modules are green and discharge nothing; a tenth green
module is not progress. The failure mode of bottom-up construction is not a
red build -- it is a green one that nothing reaches.

---

## The deleted `*WF` cluster is the value-half TEMPLATE (added 2026-09-11)

D176 deleted the ten structured-machine `*WF` modules. The verdict stands — the
module-level claim `trace-is-ir-to-trace` was assumed at nine sites and refuted
at two, and post-D159 the structured machine cannot execute a linked image at
all. But the deletion removed 386 PROVED INTERNAL LEMMAS along with 11 holes:

    ApplyWF             1488 lines   98 lemmas   2 holes
    PairWF              1357         116         4
    SumRecWF            1089          54         3
    ComposeWF            887          35         1
    SimpleWF             502          35         0
    CurryWF              300          16         1
    SumInlAllocWF        247          15         0
    SumInrAllocWF        233          15         0
    LambekValidity       194           2         0
    RecSchemePostulates  107           0         0

Those lemmas are about `exec-trace`/`LocState`, so they are NOT reusable as
they stand. They ARE the proof STRUCTURE for the flat discharges, and should be
consulted before writing each one rather than rediscovered.

    RECOVER:  git show 997e647b^:formal/Once/CCC/Machine/IR/<Module>.agda

`PairWF` is the worked example. `obs-correct-pair`'s value half needs exactly
what it proved — how the input survives `f` so that `g` can have it:

    backup-after-setup / -after-f / -at-fst-store   the input survives in
                                                    `backup-slot` across f's run
    restore-input-input1, restore-input-*-aux       `restore-input backup`
                                                    returns it to Input1 for g
    frame-after-f-eq, frame-after-fst-store-eq      the frame is unchanged
                                                    across a sub-IR execution
    store-fst-preserves, mem-preserved-through-setup  scratch writes do not
                                                    disturb before-frontier data
    bf-lift-to-scratch                              BeforeFrontier lifts across
                                                    the scratch reservation

The allocating TAIL of `pair` (`instr-alloc-heap 2` + two `store-indirect`s +
three `load-from-slot`s) is `inl`'s shape and is already covered by D174's
thirteen heap lemmas. What `PairWF` supplies is the part D174 does not: the
backup/restore argument around the spliced sub-IR runs.
