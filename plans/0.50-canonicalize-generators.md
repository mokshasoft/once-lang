---
parent: 0.50-named-defs-are-morphisms
status: BLOCKED on plan 0.81 (2026-09-02) — stage A + the resolver flip are
        landed and green through `CanonPreserveMutual`.
        `resolver-reflects-typing` is FALSE under D136; the fix is
        `plans/0.81-resolution-under-specification.md`, approved. Resume at
        step 2 once 0.81 lands.
---

## State 2026-09-02 (stage A landed) — resume at step 2

**STAGE A IS DONE AND THE APEX IS GREEN.** The whole verified pipeline
typechecks on the canonical-generator representation: `CanonicalName`,
`Judgment`, `Classify`, `Elaborate`, `ElaborateProofs`, `Completeness`,
`Soundness`, `ErrorProofs`, `Identities`, `Verified`, `Meaning`,
`MeaningBridge`, `Realize`, the Canon family, `RealizeAgrees`,
`Once.Certified`.

### What stage A settled (read this before touching anything)

  * `gen` is a **PATTERN SYNONYM**, not a function — it has to work on BOTH
    sides (rule indices in types, elaborator left-hand sides), and a function
    is rejected in a pattern. The namespace is spelled literally in it for the
    same reason. This bit once; it is the first thing to know.
  * **Route every `RResolved` dispatch through a VIEW PARAMETER, never a
    concrete clause.** Concrete `RResolved (gen "g")` clauses stop
    `checkElabV`/`inferElabV` reducing for an abstract `cn`, which the proofs
    depend on. The dispatch must also TAKE the infer result, not recompute it.
  * **A `with` cannot pin `classifyGen cn`.** It reaches the goal only through
    unfolding `inferElabV`, so there is nothing to generalise. Use a J-style
    bridge instead: `f ctx cn .(classifyGen cn) refl = refl`. Four of these
    exist now — `inferElabV-RResolved-J`, `checkElabV-RResolved-J`
    (`Completeness`), `agree-RResolved-view`, `check-agree-RResolved-view`
    (`RealizeAgrees`) — and any new consumer of the dispatch needs one.
  * **`t-var-resolved` carries `NotGenerator cn`** (`Once.CanonicalName`:
    `All (λ g → cn ≢ gen g) genNames`). It also concludes at `RResolved`, so
    without it the rule overlapped the eight generator rules. Per D134 it is a
    PROPERTY, not `classifyGen cn ≡ gv-other`; the elaborator discharges it
    because `GenView`'s `gv-other` now CARRIES the witness. That last part is
    the load-bearing trick: an uninformative `gv-other : ∀ {cn} → GenView cn`
    cannot discharge anything.
  * The seven `checkElab-fallback-RVar-*` lemmas are now PREMISE-FREE, and
    `¬ (x ≡ "unit")` is gone from every rule, lemma and record field that
    carried it. A bare `unit` is an ordinary variable that can be unbound.
  * `classifyAppHead` now looks at `RResolved` heads, so `classify-canon` /
    `classify-decanon` need `caHead-RApp-resolved-arg-irr` where they used to
    answer `refl`.

## BLOCKER 2026-09-02 — `resolver-reflects-typing` is FALSE under D136

Found while finishing step 2. It needs a decision before the plan can land.

`Once.Adequacy.CanonReflectMutual` proves the REVERSE of preservation: a
well-typed RESOLVED module gives a well-typed UNRESOLVED one. Its var case is

    reflect-var-ᵢ false bound x sub eb (t-var-resolved _ imp conc) =
      t-var-import (not-local …) imp conc

i.e. it turns `⊢ᵢ RResolved (canonical [x])` back into `⊢ᵢ RVar x`. After the
flip a bare `fst` resolves to `RResolved (gen "fst")`, and the reverse step
would need `⊢ᵢ RResolved (gen "fst") → ⊢ᵢ RVar "fst"` — **there is no such
rule, by construction.** Stage A deleted every bare-`RVar` generator rule;
that was the point. So reflection cannot be repaired by a side condition on the
source the way preservation was: the source genuinely writes `fst`.

**Why the reverse direction exists at all.** `Compile.agda:786` says it
outright: `accept-sound` gives `ModuleTyped mR` over the RESOLVED module, and
`resolver-reflects-typing` recovers a typing for the un-resolved `mU` *only* so
that the spec's `tp` / `_⊢R_` stay parse-based and therefore non-vacuous.

### RESOLVED 2026-09-02 — see `plans/0.81-resolution-under-specification.md`

Option (a) below is APPROVED and is now plan 0.81. Plan 0.50 resumes at step 2
once 0.81 lands; nothing in 0.50 needs redesigning, and 0.81 DELETES every side
condition step 2 added (`HeadUnclaimed`, `CabOK`, `DohOK`, and the extractors),
because they exist only to make the forward typing transport true.

### The two ways out

**(a) Move resolution into `⊢R` — the principled one. APPROVED (plan 0.81).** `src ⊢R tp` becomes
"the text parses AND resolves to tp", and `Typed` is the resolved module. Then
`accept-sound` already gives what the spec needs, and the ENTIRE canon family
deletes: `CanonReflect*`, `CanonPreserve*`, `CanonPrincipal`, `CanonComposeMid`,
`ResolverLits`' typing half, plus the `resolver-preserves-typing-imports`
postulate — roughly 2k lines and one residual. It is also the honest statement:
the user writes text, not an unresolved AST, and resolution is part of the
front end. This CHANGES `Once.Spec`, which MERGE.md freezes, so it is a
decision, not a refactor.

**(b) Keep the reverse direction by giving the judgment bare-`RVar` generator
rules again.** That undoes D136 and re-creates the exact collision the plan
exists to remove. Not recommended.

Everything up to this point is committed and green; the flip is landed through
`CanonPreserveMutual`. The remaining red is exactly the reflect family
(`CanonReflectMutual`, `CanonPrincipal`, `ResolverLits`), i.e. precisely what
(a) deletes.

### Remaining work, in order

2. **NEXT: the resolver flip** — `Once/Parser/Module/Resolve.agda`.
   Today `canonExpr bound um am (RVar x) = canonVar (elemStr x bound ∨
   isBuiltinName x) …`, and `b ≡ true` KEEPS the bare name. That means a
   generator still arrives as `RVar "fst"`, which stage A made an ordinary
   (unbound) variable — **so the language is currently unreachable from source
   and this step is not optional.** The rule (D136):

       elemStr x bound ≡ true                          → RVar x  (binders shadow)
       elemStr x bound ≡ false, isBuiltinName x ≡ true → RResolved (gen x)
       otherwise                                        → today's behaviour

   The decision is now TWO booleans, so `canon-RVar-keep` / `-resolve` split
   into three bridge lemmas and every `b`-split in the canon proofs becomes a
   two-boolean split. Blast radius: 59 `canonVar` sites —
   `CanonReflectMutual` 20, `CanonPrincipal` 17, `ResolverLits` 14,
   `Resolve` 5, `CanonPreserve` 2, `CanonComposeMid` 1.

   `name@this` needs NO parser change: `atomExprVarWF` already parses
   `TAt ∷ TWord alias` into `RQualified name alias`. Only `canonExpr`'s
   `RQualified` clause has to read alias `"this"` as
   `RResolved (canonical [name])`, and `"this"` must be reserved as an alias.

   No own-module definition set and no `ResolveEnv` threading is needed —
   D136's reversed resolution rule removed that requirement.

   *(Superseded sizing, kept because the variance argument is still the right
   way to think about `canonExpr`'s parameters if a scope set is ever needed:
   do NOT add a parameter to `canonExpr` — 219 occurrences across the canon
   family — change `bound`'s TYPE instead, so threading sites are untouched
   and only the ~20 that EXTEND it move.)*

3. Proof cascade from step 2, following the red top-down.
4. Stage C: discharge `named-morph-strong` / `-resolved` using `gen≢bare`.
5. Stage D: delete the dead bare-`RVar` generator path and
   `classifyBareBuiltin`. Note `check-agreeV-RVar-poly-todo` is STILL LIVE —
   the bare-`RVar` infer-failure path goes to the poly fallback.
6. Gate: re-extract MAlonzo, `cabal build`, `cabal test`, exit tests ×3, and
   **flip `TypeCheckSpec`'s "user-defined 'fst' is shadowed by builtin"** — it
   pins the OLD behaviour and must now assert the program is ACCEPTED.

**The design finding that made the spec flip cheap**, and which the original
staging did not anticipate: a migration PREDICATE (`IsGen ctx "fst" f`) would
make each rule's head ABSTRACT and destroy the syntax-directedness the
elaborator proofs depend on. `RResolved (gen "fst")` is a CONCRETE PATTERN, so
proofs keep reducing — `Realize` needed nothing but an arity fix. Prefer the
direct flip over twin rules for the same reason: twin rules double the
constructor count and every total function over `⊢ᶜ` pays for it.

# Plan 0.50 — Canonicalize the generators (finish the CanonicalName migration)

## CORROBORATED 2026-09-01 — and it now blocks plan 0.80 too

Hit from the other end while removing algorithmic premises from the typing
rules (plan 0.80 / D134). `t-app`/`t-effApp`/`t-arg-driven-app-check` carry
`classifyAppHead f ≡ nothing`; that premise was classified as DISAMBIGUATION
and slated for removal. Removing it typechecks `Judgment` and all 59 call
sites — and then `check-complete` fails, on exactly this shadowing case:

    fst : Int -> Int
    fst x = x
    test = fst 5        -- elaborator REJECTS ("fst requires a pair argument")

`classifyAppHead` is ctx-free and bare-string, so it calls that head the
BUILTIN whatever the user wrote; without the premise `t-app` derives the
program with the user's `fst`, and the judgment accepts what the elaborator
rejects.

**So `classifyAppHead f ≡ nothing` is load-bearing TODAY only because this
plan is unstarted.** It is not intrinsically semantics — it is a bare-string
COLLISION GUARD. Once generators are `Generators.fst` and a user's is
`User.Module.fst`, there is no collision, the guard has nothing to guard, and
plan 0.80's `classifyAppHead` item becomes the free removal it was thought to
be. Add that to this plan's payoff list.

**Two documentation facts found at the same time:**

  * **D001 has NOT been superseded in the decision log.** It still reads
    "Generators as Reserved Words / Accepted — Users cannot define variables
    named `fst` … The parser can assume these names always refer to
    generators." This plan's 2026-06-26 decision reverses that, and there is
    NO `D0xx` entry recording it. Per MERGE.md step 2 the reversal owes one
    before it lands.
  * **A test PINS the bug.** `compiler/test/TypeCheckSpec.hs`'s
    "user-defined 'fst' is shadowed by builtin" asserts the program above is
    REJECTED. That expectation must flip when this plan lands — it currently
    encodes D001, not the 2026-06-26 decision.

## The finding

The CanonicalName migration is NOT done: the CCC generators (id, fst, snd, inl,
inr, terminal, initial, compose, pair, case, curry, apply, cata, ana, arr, In,
Out) are the holdout — `classifyBareBuiltin : String → …` dispatches on the BARE
string `"fst"`. So `User.Defined.Module.fst` and the generator `fst` share one
identity space. Consequences (all the same root cause):
- **Shadowing bug** (confirmed): `fst = id; … fst 7` → "fst requires a pair
  argument" — the builtin wins, the user's def is silently unreachable.
- `checkElabV (RVar x)` dispatches `classifyBareBuiltin x` BEFORE scope lookup
  (Elaborate:1581), so a builtin-named import elaborates as the builtin.
- `named-morph-strong` is un-dischargeable: it's only true for `bbc-other` names,
  and the judgment doesn't track that.

## Decision (2026-06-26): reserved compiler namespace

Generators get a canonical path the compiler owns, e.g. `canonical ["Generators", g]`.
Then `User.Module.fst ≠ Generators.fst` BY CanonicalName — collision, ambiguity,
and the discharge gap all dissolve. (Not "reserve builtin names" — that was a
band-aid guard. Canonicalizing is the principled completion.)

## The change (top-down: change the classification spec, follow the red)

1. **`Generators` namespace + `classifyByCanonical : CanonicalName → BareBuiltinClass`**
   matching `canonical ["Generators", g]`; replaces bare-string `classifyBareBuiltin`.
2. **Resolver** (`Once/Parser/Module/Resolve.agda`, `canonVar`/`isBuiltinName`):
   a bare generator reference NOT shadowed by a user def → `RResolved (canonical
   ["Generators", g])`; a user-defined name → its own module path. Normal scoping
   then lets a user define `fst` (their `User.Module.fst`) and reference it — no
   reservation needed, no shadowing.
3. **Elaborator** (`checkElabV`/`inferElabV`): dispatch on the resolved
   `CanonicalName` (RResolved), `classifyByCanonical cn` — `Generators.*` → builtin
   IR; else the import/morphism path. Replaces the bare-string `RVar` dispatch (1581).
4. **Discharge `named-morph-strong` (+ `-resolved`)**: a user `RResolved cn` has
   `cn ≠ Generators.*` ⇒ provably not a builtin ⇒ the morphism path ⇒ the postulate
   discharges by construction. The implicit `bbc-other` assumption becomes
   type-enforced (the [[feedback_runtime_failure_find_hiding_postulate]] principle).

## Why the apex is GREEN today even though this isn't done

Two reasons, only one a postulate (the diagnostic, 2026-06-26):
1. **The spec agrees with the impl on bare strings.** The judgments identify a
   generator by bare-string `RVar` + scope-absence (`m-id`'s `lookupLocal ≡
   nothing`, `lookupImport ≡ nothing`) — the SAME rule as `classifyBareBuiltin`.
   Spec ↔ impl match ⇒ green. The bare-string-ness is in the SPEC, not hidden;
   the top was simply never written to demand canonical generators.
2. **The `StrongElab` postulates hide the COLLISION cases.** `named-morph-strong`
   / `const-morph-strong` / `cata-morph-strong` assert elaboration-completeness
   without proving it against the real elaborator; `named-morph-strong` papers
   over the `bbc-other` shadowing. Discharging them surfaces the gap (needs
   `bbc-other`). Canonicalizing makes them dischargeable (canonical names can't
   collide), so they stop hiding anything.

## Staging — each stage GREEN + committable (interrupt-safe)

A. **Additive**: define `Generators` namespace + `classifyByCanonical : CanonicalName
   → …`, and ADD an `RResolved (canonical ["Generators", g])` path to
   `inferElabV`/`checkElabV` (emit the builtin IR) ALONGSIDE the existing bare-`RVar`
   path. Both live; green. (Wire it immediately into the dispatch so it's not an island.)
B. **Flip the resolver**: `canonVar` emits `RResolved (canonical ["Generators", g])`
   for an un-shadowed generator; a user-defined name → its own path. The new
   elaborator path (A) handles it; the bare-`RVar` generator path goes dead but
   still typechecks. Green.
C. **Discharge `named-morph-strong`/`-resolved`**: a user `RResolved cn` now has
   `cn ≠ Generators.*` ⇒ provably `bbc-other` ⇒ morphism. Postulates → proofs. Green.
D. **Cleanup**: delete the dead bare-`RVar` generator path + `classifyBareBuiltin`.
   Green. (Re-extract + binary-check: `fst = id` now resolves to the user's def;
   generator programs still build.)

Top-down within each stage (flip representation, follow the red). Substantial but
NOT all-or-nothing once staged — commit at A, B, C, D. Best with fresh context for
the delicate `inferElabV`/`checkElabV` `RResolved` dispatch work.

## Bonus

Fixes the shadowing bug for real (user can define `fst`), and removes the
`bbc-other` standing debt that `named-morph-strong`/`-resolved` represent.
