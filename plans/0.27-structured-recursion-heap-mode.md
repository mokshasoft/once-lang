---
parent: 0.14-heap-only-allocation
blocks: 0.24-effect-trace-semantics
status: design
date: 2026-05-30
---

# Plan 0.27: Structured recursion in heap mode — real codegen + proofs for μ/ν schemes, trace-ready

## Do we need to define `List` (or tree/list primitives) first? — No.

`Functor` is already a **reified polynomial functor** (`Once.Type:242`):

```agda
data Functor : Set where
  K   : Type → Functor              -- constant
  Id  : Functor                     -- recursive position
  _⊕_ : Functor → Functor → Functor -- sum
  _⊗_ : Functor → Functor → Functor -- product
```

`μ-type F` / `ν-type F` are `Type`s for any `F` (`Once.Type:260`), and
the concrete inductive types are **derived instances**, already
typechecking (`Once.Type:317–325`):

```agda
NatF      = K Unit ⊕ Id                 -- Nat  = μ NatF
ListF A   = K Unit ⊕ (K A ⊗ Id)         -- List = μ (ListF A)
TreeF A   = K A ⊕ (Id ⊗ Id)             -- Tree = μ (TreeF A)
```

Every recursion-scheme IR constructor is **generic over `F`**
(`In`/`out-μ`/`Cata`/`Para`/`Out`/`in-ν`/`Ana`/`Hylo`/`Fuse`, all
`∀ {F} → WellFormedF F → …`, `Once.CCC.IR.agda:170–246`). So structured
recursion is **already typed and generic**; what is missing is the
*heap representation and execution* of a functor node `⟦ F ⟧T X`. We do
**not** add `List`/`Tree` as primitives — we implement the generic node
machinery once and pick the smallest concrete `F` as the proving
vehicle:

- **`NatF = K Unit ⊕ Id`** — the minimal μ (one nullary + one unary
  constructor). Proving vehicle; `NatCataProof.agda` already targets it.
- **`ListF A`** — the first "interesting" `F` (a `K A ⊗ Id` payload):
  exercises a constant field *and* a recursive field in one node. The
  end-to-end test program.

> Per `feedback_top_down_spec`: the spec is "correct over all of CCC +
> structured recursion." Picking `Nat`/`List` is a choice of *proving
> witness*, not a narrowing of the theorem — every lemma is stated
> `∀ {F} → WellFormedF F → …`.

## The gap (why this plan exists)

Structured recursion is **not actually compiled or verified** — the
abstract trace is a stub:

- `ir-to-trace'` emits **`[]`** for `In`/`Cata`/`Para`/`Ana`/`Hylo`/
  `Fuse` (`IRToTrace.agda:410–421`). The abstract machine never builds a
  μ-node nor folds one.
- `rec-scheme-semantic` (`RecSchemePostulates.agda:76`) is the postulate
  papering over it. Its own comment (lines 58–72) is explicit: *"the
  abstract machine model doesn't capture recursive execution. The
  abstract traces are stubs that don't actually compute recursion
  schemes."* The named exits are (A) extend the machine with recursive
  trace execution, or (B) direct structural proof.
- The **runtime** does work (Dispatcher → worklist loop code; `_start`
  bump allocator, per `project-heap-mode-layer2`), but the **verified**
  side is the stub + postulate. So the compiler is **not verified for
  the recursion schemes** — the headline target.
- **Codegen reality (confirmed 2026-05-31):** the actual X86-64 emitter
  is also a placeholder — `compile-ir' (Cata _ _) = ud2 ∷ []` (illegal
  instruction → SIGILL at runtime), same for `Para`/`Ana`/`Hylo`/`Fuse`
  (`X86-64/CodeGen/Compile.agda`). And the **surface language has no
  `Cata` syntax** — `SurfaceIR` is categorical combinators + `Let`/`SigOp`
  only (the `In/Cata/Out/Ana` comment is aspirational). So a Cata program
  is reachable only by hand-building IR, and it cannot run as real x86
  until the recursion codegen below exists.

Consequences this plan must fix:

1. **Correctness.** "Verified over CCC + structured recursion" is vacuous
   while `Cata`/`Ana` are `[]`-stubs covered by a blanket postulate.
2. **Trace (Plan 0.24).** A SigOp inside a fold's algebra emits **no
   event** at the abstract level, because the fold isn't executed. Bridge
   1 for the recursion schemes is therefore *impossible* until execution
   is real. 0.27 is the prerequisite 0.24 declares.

## The principle (fight the definition, not the proof)

The defect is two *definitions*: the `[]`-stub abstract trace and the
blanket `rec-scheme-semantic` postulate. Fix the definitions; don't
prove around the stub.

> **Heap-mode μ-node = tagged heap record.** `⟦ F ⟧T X` lays out
> generically by recursion on `F`: `K t` → the slots of `t`; `Id` → one
> pointer slot (to a child μ-node); `_⊕_` → a tag slot + the selected
> branch's payload; `_⊗_` → concatenation of the two layouts. `In`
> allocates such a record on the heap (reusing the **existing heap
> product/sum producers** — a node is a heap pair of a tag-sum and a
> payload); `out-μ` reads it back (Lambek inverse).
>
> **`Cata`/`Para` execution = structural fold over the finite μ-value**,
> proven by induction on the μ-value — extending the existing
> `MuValidity` / `NatCataProof` / `RecTrace` machinery from "value" to
> "value + emitted events." `Ana`/`Hylo` = productive unfold, fuel-
> bounded. The worklist loop stays as the *concrete realization*,
> bridged by the existing operation-aligned `Corresponds`.

This is option **B** (direct structural proof) for the *spec/abstract*
side, with the runtime worklist loop as the concrete realization — the
existing Star-based architecture (`RecCoreWF`, `MuValidity`) is already
built for exactly this induction.

## Trace-readiness — the "right shape" requirement (load-bearing for 0.24)

Everything new in this plan must be shaped so Plan 0.24 can thread events
through it **without re-proving**:

1. **Execution returns events.** The abstract execution of a recursion
   scheme returns `(LocState , AllocState , List SigOpEvent)` (or carries
   an `evs` field in its `IRResult`), not just `(state , alloc)`. Today
   the algebra is SigOp-free so `evs ≡ []`, but the **channel exists**
   and is threaded through every sub-fold (`evs = evs-children ++
   evs-node`). This mirrors `obs`'s `List SigOpEvent × …` result shape.

2. **Visitation order is fixed and matches `obs`.** The fold visits
   children then applies the algebra (for `Cata`) in a defined
   left-to-right order; `obs`'s effectful-`Cata` case (Plan 0.24 Phase D)
   is written to the *same* order. The bridge is then one structural
   induction, not an order-reconciliation.

3. **The marker slots in without changing state semantics.** Plan
   0.24's `instr-trace-mark` is emitted at the algebra-SigOp call site
   *inside* the fold; because the fold's correctness is proven over the
   μ-value with an `evs` accumulator already threaded, adding the marker
   appends to `evs` and is identity on the state the induction reasons
   about.

4. **`WellFormedF F` carries enough to drive layout.** The node-layout
   recursion on `F` must be available in the proof (not just at runtime),
   so the per-node `ValidAtWF` obligation is discharged generically by
   induction on `F`.

State these as explicit fields/lemmas now; leave the event content empty
until 0.24 fills it. This is the concrete meaning of "add everything
necessary to get the right shape in the proofs."

## Dependencies / sub-structure

A heap μ-node is built from the **heap product and heap sum** producers:
a `Cons` cell is a heap pair `(head , tail-pointer)`; the constructor
tag is a heap sum. So `In`'s codegen composes `PairAllocWF` +
`SumInl/InrAllocWF`. Per `project-heap-mode-layer2`, those still carry
~20 `SMP.!!` semantic obligations (`*-valid-final/cont`, `*-rax-eq`,
`trace-twf`, `mem-preserved-before`, `alloc-correct`). 0.27 must either
discharge the ones `In` depends on or reuse them as black boxes with a
clear note. **Folding a sum-functor μ (e.g. `NatF`) inherently requires
tag-dispatch** — the same mechanism `case` needs — so this plan also
makes `instr-case-on-tag` dispatch (the deferred Plan 0.13.1 Phase 1.5),
since `Cata` over `K _ ⊕ _` cannot fold without it.

## Phases

### Phase A — Generic functor-node heap layout
Define `node-layout : Functor → …` (slots, which are pointers) by
recursion on `F`. Define the abstract heap representation of a `μ F`
value as a pointer to such a node. POC on `NatF`. **Gate:** layout
computes for `NatF`/`ListF`; `WellFormedF` exposes the recursion needed
by proofs.

### Phase B — `In` / `out-μ` heap codegen + Lambek validity
**Key correction (verified 2026-05-30):** the existing `run-In`
(`SumRecWF.agda:1012`) is **stack-era** — it stores its input at a stack
slot (`result-loc = AtStack …`, a 4-instr `rec-scheme-trace-4`) and
papers over validity with the blanket `In-trace-valid` postulate
(`LambekValidity.agda:166`). In clean **heap** mode `In`/`out-μ` are
**pure pointer-identity**: the F-layer node (already heap-allocated by
the heap pair/sum producers, at an `AtDynamic` loc) *is* the μ-value —
same pointer. So `In Heap`/`out-μ Heap` emit `mov-to-output` (pass the
pointer), result-loc = input-loc; **no stack slot, no copy, no
allocation in `In` itself** (the node was allocated by the layer
producers). This aligns `In` with `out-μ` (which already emits
`mov-to-output`); the current `In → []` in `IRToTrace.agda:410` is the
stub to fix.

Validity is then **real, not postulated**: the forward correspondence
`layer→μlayer` (POC-1, **landed** — see below) + `valid-μ-wf` discharges
`In`; the symmetric reverse (`μlayer→layer`) + `valid-μ-wf` inversion
discharges `out-μ`. The `BeforeFrontier` for the node comes from the
heap producer's allocation evidence (`heap-before`), so no
`sucLoc`-monotonicity lemma is needed on the heap path. **Gate:** a
concrete `Nat` value builds on the heap and `out-μ ∘ In ≡ id` (Lambek
round-trip); `In-trace-valid`/`out-μ-trace-valid` promoted from
postulate to proof for single-slot-`K` functors; `make malonzo` green.

> **POC-1 forward — LANDED (commit a61b51d9).** `layer→μlayer` in
> `LambekValidity.agda` proves `ValidAtWF {⟦F⟧T(μ G)} → μLayerValid` by
> structural induction on `WellFormedF F`: `μlayer-inl/inr/prod` share
> the exact `sucLoc` offsets + pointer chains of `valid-inl/inr/pair-wf`;
> `BeforeFrontier` threads via the parent constructors' premises; the
> `Id` case wraps via `valid-μ-wf` (with `wf-irrel`/`isbase-irrel`
> singletons). Compiles clean. This is the kernel that makes `In`
> representational-identity validity a theorem.

> **`In` heap-identity — LANDED (commit 9a5dc1a2).** `run-In` rewritten
> heap-identity (`mov-to-output`, `bump-0`, result at `input-loc`),
> `IRToTrace` `In → mov-to-output ∷ []`, validity via `In-valid-bf` —
> the `In-trace-valid` postulate is no longer used by `In`.

### Phase B′ — Option-3 refactor: carry validity, don't recompute (the reverse)

> **Core LANDED (commit 66376507).** `valid-μ-wf`/`valid-ν-wf` now carry
> the layer's `ValidAtWF` (positivity + termination pass); the ~10
> `validityWF-*` μ/ν cases recurse on the layer (the `μValid-*` lemmas
> are no longer called); `In-valid-bf` is trivial (round-trip via
> `sem-Out-In` + `coerce-round-trip`); `In`/`run-In` mode-rigid (result
> mode = layer mode). ClosureWellFormed / LambekValidity / SumRecWF /
> RecTrace / Dispatcher all typecheck clean.
>
> **All four Lambek ops discharged (2026-05-31, commits 176bfbe7 +
> 2a4df93c):** `In`/`out-μ`/`in-ν`/`Out` now have REAL validity —
> `In-valid-bf`/`in-ν-valid` (wrap, via the Lambek round-trip
> `sem-Out-In`/`sem-CoOut-CoIn` + `coerce-round-trip`) and
> `out-μ-valid`/`Out-valid` (unwrap, invert `valid-μ-wf`/`valid-ν-wf` +
> `WellFormedF-irrelevant`). `run-in-ν` migrated off its stack-era form to
> heap-identity (like `run-In`). All producers mode-rigid (result mode =
> input mode). **8 now-dead postulates removed** (commit 2efxxx): the 4
> representational `*-to-*-valid` + 4 `*-trace-valid`. Green through
> `IRTraceCorrect` + `CompileCorrect` + `EndToEnd`.
>
> **RecTrace bridges (temporary, Phase-C targets) — STILL OPEN:**
> RecTrace's Cata-validity machinery (`process-layer`,
> `cata-dispatched-new`, ~34 `μValid`/`μLayerValid` uses) is still
> `μValid`-based, connected by two NAMED postulates: `μValid→μValidAtWF`
> (construct, inside `process-layer`'s Id handling — needs the *reverse*
> direction, hence the rework) and `extract-μLayerValid` (forward — now
> dischargeable via `out-μ-valid` + a re-added forward kernel, but the
> real fix is the rework). The Phase-C `process-layer` rework threads
> `ValidAtWF {⟦G⟧T(μ G)}` instead of `μLayerValid`, so Id-children are
> `ValidAtWF {μ}` directly (no construct bridge) and the layer is
> destructed via `out-μ-valid` (no extract bridge). That rework + `Cata`
> real execution discharges `rec-scheme-semantic` (POC-2, the big piece).
>
> **process-layer reworked to ValidAtWF — BOTH bridges eliminated
> (2026-05-31, commit 27ff80e4):** `process-layer`/`process-layer-prod`/
> `cata-dispatched-new` thread the layer's own `ValidAtWF` (mode-poly
> `mv`); Id-children are `ValidAtWF{μ-type G}` directly; layer destructed
> via `out-μ-valid`. `extract-μLayerValid` + `μValid→μValidAtWF` removed.
> **Cata's validity is now structural**, postulate-free at the bridge
> level. Whole build green (EndToEnd).
>
> **Remaining = the hard semantic core (Phase C-deep + D — A-vs-B
> juncture):** `rec-scheme-semantic` (RecCoreWF + RecSchemePostulates) is
> the documented **trust boundary** for `Fuse`/`Hylo`/`Para`/`Ana`'s
> `ValidAtWF(eval ir x)`. Eliminating it needs **(A)** abstract-machine
> recursive (worklist-loop) execution, or **(B)** direct well-founded
> recursion = **replacing `{-# TERMINATING #-}`** (CataLayer was isolated
> for exactly this). Plus ~50 `SMP.!!` of Cata per-clause bookkeeping
> (`alloc-correct`/`trace-is-ir-to-trace`/`heap-preserved`/Sum-setup
> transports). Large, multi-session; A-vs-B is a real principled choice.
>
> **Cata block extracted (2026-05-31, commit e119ea30):** the ~2500-line
> `{-# TERMINATING #-}` mutual block (`process-layer`/`cata-dispatched-new`
> + private slot-budget helpers) is now `Once.CCC.Machine.IR.CataLayer`;
> `RecTrace` (1290 lines) keeps the reusable infra (`ProcessedLayerResult`,
> trace/budget helpers, the 2 bridges). Isolating the `TERMINATING` block
> makes BOTH the `ValidAtWF`-threading rework and the future
> well-founded-recursion refactor local to `CataLayer`. The
> `process-layer` rework now happens there. Whole build green.

Building `In` exposed that the reverse (`out-μ`, and `Cata`'s
sub-results) is blocked by `μValid`/`μLayerValid` dropping
`LocMatchesMode`/structure (see the "Reverse-Lambek architecture
decision" section). **Decision: `valid-μ-wf`/`valid-ν-wf` carry the
layer's own `ValidAtWF` directly.** Exact migration map (verified by
inspection 2026-05-30; build is green at commit 78d9dbdb before this):

1. **`ClosureWellFormed`** — change the `valid-μ-wf`/`valid-ν-wf`
   constructor premise from `μValid alloc wf x loc s` to
   `ValidAtWF m alloc {⟦F⟧T(μ F)} (coerce-functor⁻¹ F _ (sem-Out wf x))
   loc s` (and ν-dual). Needs `sem-Out`/`coerce-functor⁻¹` in scope and
   `ValidAtWF` to stay positivity-clean with the layer self-reference
   (add `NO_POSITIVITY_CHECK` only if the checker balks — the recursion
   is well-founded on finite μ-values).
2. **`ClosureWellFormed`** — the ~10 `validityWF-*` μ/ν cases (mem-only,
   write-at-frontier, write-at-suc-frontier, alloc-advance,
   frontier-advance, with-bf-transfer, mem-preserved, in-regions-stub)
   become one-line **recursive** calls on the layer validity
   (`validityWF-foo … layerV`), structurally smaller → terminating. This
   *removes* the `MuValidity.μValid-*` dependency from these lemmas.
3. **`LambekValidity`** — `In-valid-bf` collapses to trivial
   (`valid-μ-wf wf x layerValidity`); the `layer→μlayer` kernel and
   `wf-irrel` are **repurposed** for `Cata`'s structural consumers (which
   still induct on `WellFormedF`).
4. **`RecTrace`** (the `Cata` machinery — 2 `valid-μ-wf` sites at 1503
   construct / 3438 match, + 34 `μValid` uses) — rework validity from
   `μValid` to layer-`ValidAtWF`. The substantial part; folds into
   Phase C. Until migrated, the build is red — this phase carries it back
   to green.
5. **`out-μ`** (`run-out-μ`) becomes trivial: unwrap the stored layer
   `ValidAtWF` (replaces the `out-μ-trace-valid` postulate). `In`
   re-simplifies to wrap.

`MuValidity` ends up largely orphaned (its 16 preservation lemmas are
subsumed by `ValidAtWF`'s own); delete or leave unused.
**Gate:** `valid-μ-wf` carries layer `ValidAtWF`; `out-μ-trace-valid`
discharged; `RecTrace`/`SumRecWF`/`ClosureWellFormed` green; `In`/`out-μ`
mode-polymorphic + cross-mode-safe.

## Recursion-as-loop codegen — design decision (2026-05-31)

**Status going in:** the abstract Cata machine (`process-layer` /
`cata-dispatched-new`) is built, mode-polymorphic, and now **fast** — the
well-founded recursion that replaced `{-# TERMINATING #-}` had blown the
foetus typecheck to 26 min; reifying both recursions through capability
parameters (`CataRecBound`/`make-cata-rec`, `ProcLayerCap`/`make-proc-rec`)
took the heavy functions out of the termination SCC → **0:38**, full
build green (commit `bcef7985`; see `[[reify-recursion-for-foetus-perf]]`).
A worked IR-level Cata example (`Once.CCC.Examples.NatCata`, `isEven`
folding μNat→Bool, verified via `sem-cata-compute`) runs in the **model**
(commit `d19959b8`). What's missing is the **runtime loop codegen**: one
finite program that reproduces `process-layer`'s per-input *unrolled*
trace for *any* μ-value.

### How the recursion is realized — chosen: **A1, native recursive subroutine**

Options weighed (heap mode, reusable across Cata/Para/Hylo/Ana):

- **A1 — native recursive subroutine (`call`/`ret`).** Emit a
  per-`(functor, scheme)` routine: read node → `call` itself at each `Id`
  child → assemble the result-layer → apply the combiner → `ret`.
- **A2 — explicit worklist loop (`jmp` + growable heap stack).** A
  descend/combine loop over an explicit work stack.
- **A3 — generic runtime fixpoint primitive.** One routine interpreting a
  reified-functor descriptor + an algebra function-pointer at runtime.

**Performance:** per-node speed A1 ≳ A2 ≫ A3 (A3 pays runtime functor
interpretation + an indirect call per node; A1/A2 are specialized
straight-line). A1's *only* weakness is native-stack depth = data depth
(overflow risk on pathologically deep structures); A2's growable **heap**
stack is the sole thing that removes that ceiling.

**Proof complexity:** A1 ≪ A2 < A3. A1's execution is structural recursion
on the μ-value, mirroring `process-layer` **clause-for-clause**, so
`trace-is-ir-to-trace` is a direct induction — and `call`/`ret`+`Acc`
recursion is *already modeled and proven* for Apply closures
(`Dispatcher.make-rec-wf`/`run-ir-wf`, `RecDispatcherWF`) → reuse, not new
machinery. A2 needs a refinement proof (explicit-stack invariant + loop
variant + reconstruction) against structural recursion. A3 adds
functor-interpreter + closure-correctness on top.

**Initial recommendation was A1**, on the belief that A2 meant
build-from-scratch with harder proofs. **REVISED to A2 (2026-06-01)** after
finding the codebase already contains substantial A2 (worklist)
scaffolding, which inverts the cost comparison:

- `TreeTrace` (`SMCore.agda`) with a **`call-sub`** recursion node +
  **`branch`** (Sum dispatch); `exec-tree-trace` is *real* and dispatches
  on the slot tag (whereas flat `exec-trace`'s `instr-case-on-tag` is
  stubbed to `halt`).
- `worklist-init/push/pop/check` instructions ("for loop-based recursion
  schemes", OCP-0003); `treeToRunnable` lowers `call-sub t →
  worklist-push ∷ … ∷ worklist-pop`, with intended correctness
  `exec-tree-trace t ≡ exec-trace (treeToRunnable wl t)`.
- `cata-tree-μ`/`cata-tree-layer` in RecTrace, with
  `cata-tree-layer-correct` **partially proven** (`wf-K`=tt, `wf-Id`=IH).

A1 has *no* such scaffolding — it would need parallel native-`call`/`ret`
codegen **plus a machine call-stack in the abstract state** (absent
today). So A2 reuses real, partially-proven groundwork and matches "heap
mode uses stack scratch" (the worklist is a slot-based stack).

**Caveat (neither approach is near-done):** the *fixed finite program* is
unbuilt in both. `cata-tree-μ x` still recurses on the input `x` (per-input
tree; `call-sub` is presently a transparent marker), and `treeToRunnable`
of a per-input tree is still per-input. The finish line is **one fixed
worklist-loop body, driven by the worklist holding dynamic nodes**,
processing any input — that leap is the remaining work. Existing
scaffolding = per-input-tree semantic correctness (reusable groundwork).

**Decision: A2.** Reuse `TreeTrace`/`call-sub`/`worklist-*`/`treeToRunnable`
/`cata-tree-*`: finish `cata-tree-layer-correct`, then emit + verify the
fixed worklist loop. (A1's `emit-rec-driver` framing below still applies as
the per-scheme parameterization — node-source/combiner — just realized as a
worklist loop rather than native recursion.)

### Heap mode ≠ heap-only: stack scratch + register results

"Heap mode" is the *persistent* structure on the heap; transients use the
stack — and the abstract machine **already models this hybrid**:

- **Input μ-nodes** → heap pointers (`In`/`out-μ` are representational
  identity, so "destruct" = read through the pointer).
- **Per-node transient scratch** (the layer-of-results under construction;
  values saved across a child's recursive `call`, e.g. the left result of
  an `FL ⊗ FR` node while folding the right) → **stack call-frame scratch**
  (`sub rsp`/`add rsp`, already used for pairs/closures). `process-layer`'s
  trace already does `load-indirect`/`mov` into **stack slots** for the
  layer, alongside heap bump-allocation for persistent values, and its
  obligations already include the stack **slot budgets**
  (`layer-capacity` / `ir-stack-requirement (Cata wfG alg)` in
  `CCC/IR/Stack.agda`) — we **reuse** that accounting, not invent it.
- **Result values** → registers/stack when the result type **`FitsInReg`**
  (Plan 0.26 machinery), heap only for compound results. So scalar folds
  (`isEven : μNat→Bool`, `sum : μList→Int`) allocate **zero heap for
  results** — heap is touched only to *read* the input.

### Reusability across the schemes

The shared abstraction is exactly `process-layer`'s "traverse an F-layer
by structural recursion on the (reified, static) functor, recurse at `Id`
positions, apply a combiner." A single parameterized Agda emitter
`emit-rec-driver(node-source, combiner, direction)` covers all of them;
the reuse is at the *compiler* level (the emitter + a shared correctness
schema), not a single runtime routine:

- **Cata** — node-source `out-μ` (read), combiner `alg`.
- **Para** — node-source `out-μ`, combiner over `(substructure, result)`.
- **Hylo** — node-source `coalg` (generate the layer), combiner `alg` —
  the fused build/fold; no intermediate μ materialized.
- **Ana** — the dual: corecursion building ν top-down, demand-driven
  (productive); same skeleton mirrored, bounded by the consumer.

Build the Cata driver first as the concrete instance, factor the emitter
so Para/Hylo plug in by swapping node-source/combiner (Ana as the dual).

### Phase C — `Cata` real execution + structural proof + tag-dispatch
Per the **A1 decision** above. `process-layer`/`cata-dispatched-new` (the
heap-mode abstract machine, now landed + fast) is the engine; the
`emit-rec-driver` (Cata instance) emits a **native recursive subroutine**:
read node (`out-μ` = pointer read) → structural-on-`F` layer traversal in
stack scratch, `call` the subroutine at each `Id` position → assemble the
result-layer → apply `alg` (inline / dispatch) → place result per
`FitsInReg` (register/stack, else heap) → `ret`. Make `instr-case-on-tag`
**dispatch** (Plan 0.13.1 Phase 1.5 — lift the ~12 SMPrimitives lemmas to
instruction-or-trace shape) for the Sum tag branch.

Concretely: (1) replace `ir-to-trace' (Cata _ _) = []` with the real
recursive trace mirroring `process-layer`; (2) replace
`compile-ir' (Cata _ _) = ud2` with the subroutine emit (`call`/`ret`,
reusing the Apply `call` path); (3) close the ~5 `trace-is-ir-to-trace`
holes by the direct correspondence (induction on the μ-value matching
`process-layer`); (4) **discharge `rec-scheme-semantic` for `Cata`** (no
longer a postulate). Thread the (empty) `evs` accumulator.

**Already done:** model-level Cata correctness (`eval`/`sem-cata`) +
worked example (`Once.CCC.Examples.NatCata`, commit `d19959b8`); the
abstract machine and its termination perf (commit `bcef7985`).
**Gate:** `Cata` over `Nat` (e.g. `isEven`/depth/`sum`) emits a real
subroutine, runs to the correct result, `rec-scheme-semantic` unused for
`Cata`.

### Phase D — `Para`, `Ana`/`Hylo`/`Fuse`
Reuse the Phase-C `emit-rec-driver` by swapping node-source/combiner (no
new recursion machinery): `Para` (paramorphism) — same fold, combiner over
`(substructure, result)`; `Hylo` — node-source `coalg` instead of `out-μ`
(fused build/fold, no intermediate μ materialized); `Ana` — the dual
(productive corecursion to ν, demand-driven / fuel-bounded unfold
returning `(…, evs)`); `Fuse` the μ-anchored fusion. Discharge the
remaining `RecSchemePostulates` / `AnaWF` / `ParaWF` obligations
structurally. **Gate:** all recursion schemes verified generic over `F`;
`rec-scheme-semantic` eliminated.

### Phase E — `IRTraceCorrect` / `compile-correct` bridge for the schemes
Thread the recursion-scheme constructors through `IRTraceCorrect` and
hence `compile-correct` (today it covers only id/fst/snd/terminal/initial
+ the heap pair/sum producers per `project-heap-mode-layer2`). **Gate:**
`compile-correct` holds for an IR containing `In`/`out-μ`/`Cata`.

### Phase F — End-to-end test vehicle
A `List`/`Nat` program: build a structure on the heap, `Cata` it to an
`Int`, `linux.exit` the result (e.g. `length [a,b,c]` → exit 3, or
`sum`). Build → run → exit code, exercising heap node allocation + a real
fold at runtime. If a SigOp-in-algebra program is expressible, add one to
pre-exercise the trace-`evs` channel (otherwise leave the channel proven-
empty for 0.24 to fill). **Gate:** runtime exit code matches; trace-shape
fields in place.

## Correctness statement & TCB

**Discharged here (was postulate/stub, now proven):**
- `rec-scheme-semantic` (`RecSchemePostulates.agda:76`) — replaced by
  structural induction on μ/ν, generic over `F`.
- `LambekValidity`'s per-operation postulates — proven by induction on
  `F`.
- The `[]`-stub abstract traces for `In`/`Cata`/`Para`/`Ana`/`Hylo`/
  `Fuse` — replaced by real node-allocating / node-visiting execution.

**Trusted base unchanged:** assembler + `programToText`, the ISA model,
external SigOp effects (the irreducible TCB per Plan 0.24). The bump
allocator's missing bounds check (`project-heap-mode-layer2` open #3)
remains a Layer-2 limitation, not a proof gap.

## Risk & blast radius (and the POC gates)

Per `feedback_load_bearing_lemma_poc` — POC the load-bearing claims and
separately estimate integration cost before committing the phases:

- **POC-1 (Phase A/B) — forward LANDED (commit a61b51d9).** The node
  layout is *already* formalized: `MuValidity.μLayerValid`/`μValid`
  (0 postulates) describe exactly the heap pair/sum producers' layout,
  and `ValidAtWF` already wraps it via `valid-μ-wf`. `layer→μlayer`
  proves the forward correspondence. **Reverse** (`μlayer→layer`, for
  `out-μ`) is the symmetric induction; see the `μlayer-K` limitation
  below.
- **POC-2 (Phase C, the big one):** `Cata`'s structural execution proof
  generic over `F` (generalize `NatCataProof`). This is where
  `rec-scheme-semantic` actually dies; if the induction doesn't lift from
  `Nat` to generic `F`, the phase plan is wrong.
- **Integration cost:** (a) ~20 `SMP.!!` on heap pair/sum that `In`
  depends on; (b) `instr-case-on-tag` dispatch = the ~12-lemma lift
  (Plan 0.13.1 Phase 1.5); (c) `PairAllocWF`'s construction-vs-runtime
  alloc-alignment blocker (`project-heap-mode-layer2`) if `In` routes
  through it. Budget these explicitly — they are not "fight the proof"
  shortcuts but real machine-definition completions.

Per `feedback_blast_radius_not_a_constraint`: the size is the principled
price of compiling structured recursion for real; the alternative is the
`rec-scheme-semantic` postulate indefinitely.

## Reverse-Lambek architecture decision — carry validity, don't recompute it (2026-05-30)

Building `In` (forward) exposed that `MuValidity.μValid`/`μLayerValid`
are a **"calculate, throw away, recreate"** anti-pattern, and the
**reverse** transfer (`out-μ`, and `Cata`'s sub-result reads) is blocked
by it:

- A producer builds a μ-node with **full `ValidAtWF`** (carrying
  `LocMatchesMode`, the pointer structure, the mode).
- `valid-μ-wf` converts it to `μValid`/`μLayerValid`, which **discards**
  `LocMatchesMode` (and, for `K`-of-compound, the payload structure) —
  `μlayer-inl` carries `readLoc`/`BeforeFrontier` but no
  `LocMatchesMode` (`MuValidity.agda:102`); `valid-inl-wf` *requires* it
  (`ClosureWellFormed.agda:177`).
- `out-μ` must then **recreate** the discarded info — which it can't.
  This hits **every functor including `NatF`** (not just `K`-of-compound),
  via the `LocMatchesMode` gap.

This is wasteful (duplicate proof structures hurt compile performance)
and the source of the block. **Heap-specific `μLayerValid` (require
`AtDynamic`) is rejected**: it breaks the moment a heap-mode IR consumes
a stack-mode IR's μ-result (cross-mode), which the architecture must
support.

**Decision: `valid-μ-wf`/`valid-ν-wf` carry the layer's own `ValidAtWF`
directly** — `valid-μ-wf : ValidAtWF m alloc {⟦F⟧T(μ F)} (sem-Out …) loc
s → ValidAtWF m alloc {μ-type F} x loc s`. The layer's `ValidAtWF`
already carries its mode/`LocMatchesMode`/structure, so:

- `In` = wrap (store it); `out-μ` = unwrap (retrieve it). Both trivial,
  **mode-polymorphic and cross-mode-safe** (validity *is* the layer's,
  at whatever mode it was built).
- No info loss → no recompute → no `μlayer-K`/`LocMatchesMode` gaps,
  including `K`-of-compound (the layer's `ValidAtWF` carries pair chains).
- `μValid`/`μLayerValid` become redundant; `MuValidity`'s ~16
  preservation lemmas are subsumed by `ValidAtWF`'s own
  (`validityWF-mem-only`, frontier-advance, etc.).

The original design chose the separate `μLayerValid` to avoid
"pattern-matching the abstract layer type `⟦F⟧T(μ F)`". That wall is
**avoidable**: `In`/`out-μ` never match it (wrap/unwrap), and structural
consumers (`Cata`) induct on `WellFormedF` — the technique the forward
kernel `layer→μlayer` already proved works (commit a61b51d9).

**Blast radius (accepted, per `feedback_blast_radius_not_a_constraint`):**
`ClosureWellFormed` (`valid-μ-wf` def + ~32 `μValid` refs),
`MuValidity` (largely removed), `RecTrace` (34 `μValid` uses, the `Cata`
machinery — migrated as part of Phase C), `LambekValidity` (`In-valid-bf`
becomes trivial; forward kernel repurposed for `Cata`'s consumers).

## What this plan does **not** do
- Does not add `List`/`Tree`/`Nat` as primitive `Type`s — they stay
  derived `μ F` instances.
- Does not thread actual SigOp events through the schemes (that is Plan
  0.24 Phase D, using the `evs` channel this plan installs).
- Does not add a bump-allocator bounds check, or RiscV64/X86-32 heap
  codegen (X86-64 only, per `project-heap-mode-layer2`).
