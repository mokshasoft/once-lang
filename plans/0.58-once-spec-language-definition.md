# Plan 0.58 — `Once.Spec`: a minimal, IR-free language definition (OCP-0006, north star)

Implements **OCP-0006**. The re-export first cut landed (`Once.Spec.*` exist), but it
enshrined a NON-minimal boundary: the "meaning" re-exports `Realize` + `DenotTrace`,
which drags **`Once.IR`** and the compiler-side `evalᴰ` into the trust surface. This
plan makes the spec **genuinely minimal**: the reference MEANING becomes a denotation
of typing DERIVATIONS, IR-free and elaborator-free.

## North-star target

The reference meaning is
```
⟦_⟧ᵈ : (Γ ⊢ e ∶ A) → ⟦Γ⟧ᴰ → T ⟦A⟧ᴰ        -- recursion on the DERIVATION
```
No `Surface.Expr`, no `realize`, no `evalᴰ`, no `Once.IR` anywhere in the spec surface.

Final trust boundary (`Once.Spec.*`), each importing only spec + shared semantic-domain
vocabulary (never the compiler, never `Once.IR`):

```
Once.Spec.Type      -- the Type + Functor GRAMMAR (inductive defs only)
Once.Spec.Syntax    -- RawExpr — the program you write & the rules judge
Once.Spec.Typing    -- ⊢ᵢ/⊢ᶜ/⊢ᵍ/⊢ᵐ + Typed        (Judgment, whole)
Once.Spec.Meaning   -- Behavior + semantic domain (⟦_⟧ᴰ, T) + ⟦_⟧ᵈ
Once.Spec.Correct   -- CorrectCompiler                (record, proof-free)
```

Demoted to IMPLEMENTATION (checked against the spec, not trusted): `Surface.Syntax`
(`Expr`), `Once.Denotation.Realize` (`realize`/`realize-global`/`realize-morph`),
`Once.Denotation.DenotTrace.evalᴰ`, all of `Once.IR`, the elaborator, the parser, codegen.

## Why the IR leak == the `Surface.Expr`/`realize` machinery (the key insight)

`SourceDenote` touches IR in exactly TWO places:
```
⟦ lift-morphism ir ⟧ˢ = returnT (evalᴰ ir)
⟦ morph-app ir e ⟧ˢ   = ⟦ e ⟧ˢ >>=T λ v → evalᴰ ir v
```
i.e. `Surface.Expr` embeds a pre-built CCC **morphism as an opaque `IR`**, because the
morphism realm `⊢ᵐ` is represented AS IR (`realize-morph : ⊢ᵐ → IR`). Denoting `⊢ᵐ`
**directly** — recursion on `m-id`/`m-fst`/`m-compose`/`m-curry`/`m-cata`/… straight to
a function `⟦A⟧ᴰ → T⟦B⟧ᴰ = ⟦A ⇒ B⟧ᴰ` — removes the IR entirely. The value realm
`⊢ᵍ` (`realize-global : ⊢ᵍ → IR`) goes the same way. Everything else in `SD` is already
IR-free (`⟦_⟧ᴰ`/`inject`/`forget`/`emit-D` are the semantic domain; cata/ana are already
`⟦alg⟧ˢ`-based, not `evalᴰ`).

## Strategy: a bridge, NOT re-proving adequacy

`⟦_⟧ᵈ` is the FUSION of `realize` (deriv→Surface) and `SD` (Surface→meaning) with the IR
intermediate replaced by a direct morphism denotation. So:
- Prove ONE bridge: `⟦ deriv ⟧ᵈ  ≈  SD.⟦ realize deriv ⟧ˢ`. For the non-morphism rules the
  fused denotation computes identically ⇒ mostly `refl`; the real content is the `⊢ᵐ`
  cases (`m-cata`/`m-named`/`m-const`), where the direct morphism semantics must agree
  with `evalᴰ ∘ realize-morph`.
- The existing green apex (which relates `exec bytes ≈ SD∘realize`, incl. the freshly
  proven `main-extract`/`main-realize-agrees`) then LIFTS to the new meaning by composing
  with the bridge. We do NOT re-derive the correctness proof.

## Option B decision (Surface-free) — the judgment refactor is CONTAINED

The typing judgment references `Surface.Expr`: `t-var-local` carries
`{eE : SExpr (debruijn ctx) Ψ A}` from `lookupLocal`. BUT `lookupLocal-go` always
produces `var i` (`Surface.var zero` / `weaken`), never arbitrary surface content. So
the fix is faithful and contained: **`lookupLocal` returns the de-Bruijn INDEX into
`NamedCtx.debruijn` instead of the `var i` SExpr.** Then `t-var-local` carries an index,
`realize` rebuilds `var i` from it (impl), and `⟦ t-var-local ⟧ᵈ dγ = returnT (lookupᴰ … i dγ)`
— Surface-free AND IR-free. `const`/value lift is the EASY part (already IR-free in P1;
the `= realize-global` identity is a bridge lemma, not spec).

### Proof reuse (why this is mostly re-composition)
- **Insulated / untouched:** the whole BACKEND (`IR → machine → targets`, all codegen /
  simulation / arch / arith / flat-machine proofs — 100+ modules; `elaborate` still emits
  identical IR). Also `Once.Adequacy`, the semantic domain, and `SD`/`realize` (kept as impl).
- **Re-composition:** define `⟦_⟧ᵈ` + prove ONE bridge `⟦_⟧ᵈ ≈ SD∘realize`, then COMPOSE with
  the existing `exec ≈ SD∘realize` apex (incl. the just-landed `main-extract`). Not re-derived.
- **Genuinely reworked (contained):** `lookupLocal`→index + `t-var-local`; a mechanical
  index-instead-of-`var i` pass over the 8 files matching `t-var-local` (`Judgment`,
  `Elaborate`, `Soundness`, `Completeness`, 3× `Canon*`, `Realize`) — apex-touching, ends
  with `certified`; and building `⟦_⟧ᵈ` fresh.

## METHOD (non-negotiable): TOP-DOWN, apex-first, no islands

Mirror how `main-extract` was discharged. Do NOT grow `Once.Denotation.Meaning` bottom-up
as an unused island. Instead: **wire the GOAL at the apex first.** State the re-pointed
reference meaning (`⟦_⟧ˢ ← ⟦_⟧ᵈ`) with `⟦_⟧ᵈ` and the bridge as TEMP POSTULATES, re-point
the instance, and get `certified` green — this proves the re-composition architecture and
lets the typechecker PIN the exact shapes of `⟦_⟧ᵈ` and the bridge. THEN discharge downward
(build `⟦_⟧ᵈ`'s realms, prove the bridge), shapes known. Every module stays connected to the
apex; no leaf is built speculatively. (The P1 morphism/value realms already exist — they get
CONNECTED via the bridge, not left standalone.)

### Design decision to settle first: the bridge's equality
`⟦ d ⟧ᵐ` and `evalᴰ (realize-morph d)` are FUNCTIONS `⟦A⟧ᴰ → T⟦B⟧ᴰ`; `m-compose`/`m-pair`
bind the sub-morphism as a `>>=T` function argument, so a raw `≡` bridge needs funext. Match
the apex's existing style instead: state the bridge OBSERVATIONALLY (the `_≈_` up-to-`n`
prefix `Once.Adequacy` already uses), so no funext and it composes directly with the existing
`exec ≈ SD∘realize` chain. (Settle before writing the bridge.)

## Phases

### Discharge sequence for `⟦_⟧ᵈ` (scoped 2026-07-04) — do IN ORDER, each certified-green
The whole spec's IR entanglement is EXACTLY `Surface.Syntax`'s two `Expr` constructors
`lift-morphism`/`morph-app` (the only users of its `Once.IR` import). So:
1. **Extract `Once.Surface.Context`** — move the IR-free block `Surface/Syntax.agda:31–141`
   (`Ctx`/`_,_^_`/`lookup`/`⟦_⟧ᶜ`/`Usage` + ops) into a new IR-free base module;
   `Surface.Syntax` `open import Once.Surface.Context public` (consumers unchanged).
   ✅ DONE (`01c3e318`, certified green — the re-export is transparent).
1b. **Split `DenotTrace`** the same way — the value domain `⟦_⟧ᴰ` (+ `forget`/`inject`/`emit-D`)
   is IR-free (IR is used only from `evalᴰ`, line 126+). Extract an IR-free `⟦_⟧ᴰ` base;
   `DenotTrace` keeps `evalᴰ` and imports the base. Same containable re-export pattern as (1).
   Needed so `⟦_⟧ᶜ`'s env (`⟦ ⟦ debruijn ctx ⟧ᶜ ⟧ᴰ`) is IR-free.
2. **`lookupLocal` → `Fin n`** — the stored `eE` is ALWAYS `var i` (`Classify:208`
   `Surface.var zero` / `weaken`), so return the `Fin n` index; `t-var-local` carries the index,
   `realize` rebuilds `var i`. Re-point `Classify`/`Judgment` onto `Once.Surface.Context` so
   `NamedCtx`/`⊢` become IR-free. ~8 files match `t-var-local`; faithful, mechanical; certified.
3. **Build `⟦_⟧ᶜ`/`⟦_⟧ᵢ`** in `Once.Denotation.Meaning` on the direct `⟦_⟧ᵐ`/`⟦_⟧ᵍ` + the
   IR-free env; `t-var-local` = `returnT (lookupᵈ i dγ)`.
4. **Discharge `⟦_⟧ᵈ`** = `runMainᵈ` over the main `⊢ᶜ` derivation (parallel to `mainRealized`
   but using `⟦_⟧ᶜ`); delete the `⟦_⟧ᵈ` postulate in `Compile.agda`.
5. **Discharge `bridgeᵈ`** — observational; non-`⊢ᵐ` rules refl by fusion, `⊢ᵐ` cases from the
   `⟦_⟧ᵐ ≈ evalᴰ∘realize-morph` bridge.
WARNING: steps 1–2 are uncontainable core refactors — start only with room to finish green.

### P1 — wire the apex goal with postulated `⟦_⟧ᵈ` + bridge → certified [DONE, certified green]
Re-point the instance's reference meaning (`Once.Adequacy.Compile` `⟦_⟧ˢ`) to a postulated
`⟦_⟧ᵈ`, with a postulated observational bridge `⟦_⟧ᵈ ≈ SD∘realize`; compose with the existing
adequacy. `certified` exit 0 with the two temp postulates. This PINS every downstream shape.
(The already-built `⟦_⟧ᵍ`/`⟦_⟧ᵐ` in `Once.Denotation.Meaning` are the seed for `⟦_⟧ᵈ`.)

### P2 — discharge the scaffolds (postulate-free)
`m-cata` via the structural fold (reuse `SD`'s `cata-ev-algᴰ` shape, elaborate-free);
`m-named`/`m-named-resolved` via the definition environment; `m-const` closure. Module has
zero postulates. `timeout` per-module checks.

### P3 — the bridge `⟦_⟧ᵈ ≈ SD ∘ realize`
Structural induction; non-`⊢ᵐ` rules are `refl`/`cong`; `⊢ᵐ` cases prove the direct
morphism semantics = `evalᴰ ∘ realize-morph`. This is the coherence heart.

### P4 — re-point the reference meaning; certified green
Change the `CorrectCompiler` instance's `⟦_⟧ˢ` (and the `runMainˢ`/`source-meaningᴰ` path)
to `⟦_⟧ᵈ`, composing the existing chain with the P3 bridge. `certified` exit 0 against the
NEW meaning. `Once.Adequacy` record UNCHANGED (only the instance's field definition moves).

### P5 — split modules, rebuild `Once.Spec`, canonicalize, delete duplicates
- Split `DenotTrace` → value-domain vocabulary (spec-side) vs `evalᴰ` (impl); `Realize`,
  `Surface.Syntax` become impl; split `Once.Type` grammar vs deciders/printers
  (`_≟T_`/`fits-in-reg?`/`showType` → impl).
- Rebuild `Once.Spec.Meaning` = `Behavior` + semantic domain + `⟦_⟧ᵈ` (no realize/evalᴰ/
  Surface); `Once.Spec.Syntax` = `RawExpr` only.
- **Canonicalize:** rewire implementation importers onto `Once.Spec.*` so it is not an
  island; delete now-dead duplicate access paths.
- `make check-all` + full `cabal test` green; update OCP-0006; delete THIS plan.

## DONE criteria
- Reference meaning is `⟦_⟧ᵈ` (derivation denotation); `Once.Spec.*` imports contain **no
  `Once.IR`**, no `Surface.Expr`, no `realize`, no `evalᴰ`.
- `certified` exit 0; `make check-all` + tests green; zero new postulates end-state.
- `Once.Spec` is canonical (imported by implementation, not an island); duplicates deleted.
- `Once.Adequacy` record byte-identical (only the instance re-points).

## Discipline
- Per module: `timeout N scripts/agda-safe.sh agda MODULE=…`. Apex-touching phases (P4)
  end with `certified` exit 0 before commit. Final compile+extraction+tests batched at the
  end (per the standing plan).
- Scaffold-then-discharge is fine WITHIN a phase; every phase's END state is postulate-free.
- If a phase forces a REAL edit to a source spec module's semantics, that is expected here
  (this IS a semantic refactor) — but keep `Once.Adequacy`'s *statement* untouched; if the
  top-level claim itself would change, STOP and float it.

---

## Progress log (2026-07-04) — construction COMPLETE, bridge foundation in

All committed, every commit `certified`-green (branch `ocp-0006-once-spec`):

- **Steps 1/1b** — IR-free `Once.Surface.Context` + `Once.Denotation.ValueDomain` extracted.
- **Step 2** — `lookupLocal → SVar` (Fin-index witness in `Context`); `t-var-local` carries it;
  impl side rebuilds `var i` via `svar→expr`. **`Classify`/`Judgment` are now IR-free.** ~9-module
  cascade = faithful `SExpr→SVar` type edits.
- **Step 3** — full four-realm denotation in `Once.Denotation.Meaning`: `⟦_⟧ᶜ`/`⟦_⟧ᵢ`/`⟦_⟧ᵍ`/`⟦_⟧ᵐ`,
  IR-free (morphisms via `⟦_⟧ᵐ`, values via `⟦_⟧ᵍ`, locals via `lookupᴰ`, sigops via `emit-D`/`semM`).
- **Step 4** — `Once.Denotation.MainMeaning.meaningᵈ` mirrors `mainRealized` with `⟦_⟧ᶜ`;
  **`Compile.⟦_⟧ᵈ` is now a DEFINITION** (`= meaningᵈ`), not a postulate. `Meaning` is de-islanded.
- **Steps 5a/5b/5c** — all 3 scaffolds discharged, `Meaning` **postulate-free**:
  - `named-sem cn` = `evalᴰ (SigOp (value-info cn))` (emit-D/semM/forget, definitional).
  - `in-value {F}` = `sem-In F ∘ coerce-functor F (μF) ∘ forget` (= `eval (In ·) ∘ forget`).
  - `cata-sem wf dalg` = event-tracking fold; `cata-ev-algᴰ-D` is a direct-algebra mirror of
    SD's `cata-ev-algᴰ` (`evalᴰ alg` → `dalg`), run via `sem-cata`.

### Remaining: `bridgeᵈ` via a FUNEXT-FREE logical relation (user-chosen)
- **DONE** `Once.Adequacy.MeaningRelation`: `RelV`/`RelT` (RelT = equal event traces + related
  values ∀ budget n; RelV at arrows = related inputs ↦ related computations, a Π not a funext `≡`;
  first-order = `≡`). `RelT-return`/`RelT-bind` proven (definitional from `_>>=T_`'s `++`-of-traces).
- **TODO** the fundamental lemma (`MeaningBridge`, new module) — a STRUCTURAL 4-realm mutual
  induction (realize's `⊢ᶜ`/`⊢ᵢ` cases map 1:1 to the `⟦_⟧ᶜ`/`⟦_⟧ᵢ` clauses):
  - `bridge-i (d)(re : RelEnv) : RelT A (⟦ d ⟧ᵢ dγ₁) (SD.⟦ realize-infer d ⟧ˢ dγ₂)`
  - `bridge-c (d)(re) : RelT A (⟦ d ⟧ᶜ dγ₁) (SD.⟦ realize d ⟧ˢ dγ₂)`
  - `bridge-m (d) : RelV (A⇒B) (⟦ d ⟧ᵐ) (evalᴰ (realize-morph d))`
  - `bridge-g (d) : RelV/RelT relating (⟦ d ⟧ᵍ) to (eval/evalᴰ (realize-global d))`
  - most cases = `RelT-bind`/`RelT-return` + IH; `t-var-local` = `RelEnv` lookup; `t-morph-lift`
    bridges to `bridge-m`; `m-cata` reduces to `bridge-m` on the algebra (via `cata-ev-algᴰ-D` =
    `cata-ev-algᴰ` when `dalg = evalᴰ alg`); scaffold cases are `refl` (scaffolds = `evalᴰ` defn'ly).
  - then a selection lemma (`mainMeaningᵈ`/`mainRealized` parallel dispatch) + apply `bridge-c` at
    `main : EffUU`, `tt` → discharge the apex `bridgeᵈ` postulate.
- Then P5 cleanup (rebuild `Once.Spec.Meaning`, canonicalize, delete duplicates).

---

## Bridge discharge — the `forget`-equality obstruction + the way past (2026-07-04)

State: `MeaningBridge` STATES the fundamental lemma (bridge-c/i/m/g) + `RelEnv`
(green, scaffolded bodies). Discharging the env-free realms (`bridge-m`/`bridge-g`)
is structural (`RelT-bind`/`RelT-return` + IH) EXCEPT the sigop cases (`m-named`/
`m-named-resolved`), which surfaced a genuine subtlety.

### The obstruction
`bridge-m (m-named x)` reduces to `RelV (A⇒B) F F` for `F = evalᴰ (SigOp (value-info
(bare x)))` (since `named-sem (bare x) ≡ F` definitionally). `RelV` at arrows is a Π:
`∀ related a b → RelT B (F a) (F b)`. A sigop's denotation is
`F a = λ n → (emit-D si (forget a) , inject (semM si (forget a)))`, so relating `F a`
and `F b` wants `forget a ≡ forget b`. `forget` at ARROW types runs the closure
(`forget {A⇒B} clo = λ va → forget (valueT (clo (inject va)) 0)`), so for related-but-
unequal HIGHER-ORDER `a`,`b` that equality needs **funext**. `m-named`'s domain is
unconstrained (`{A B : Type}` from `lookupImport … ≡ just (A ⇒[…] B)`), so a higher-
order imported function reaches this case in general.

### KEY INSIGHT — the trace is already funext-free (`mkEvent` drops non-`Int` args)
`Once.Denotation.Trace.mkEvent`:
```
mkEvent-name nm (just refl) arg = mk-event nm (just arg)   -- D ≡ Int  → keep arg
mkEvent-name nm nothing     arg = mk-event nm nothing        -- D ≢ Int  → DROP arg
mkEvent si arg = mkEvent-name (name si) (isInt? D) arg
```
So a sigop's EVENT depends on `forget a` only when the domain is `Int` (first-order,
`RelV Int = ≡`, so `forget a ≡ forget b` by `cong` — funext-free). For every non-`Int`
(incl. all higher-order) domain the event is argument-independent. **The apex observable
is the trace (`projTrace`), so the obstruction dissolves where it actually matters.**

### Remaining question — the VALUE component
`RelT` also tracks the value `inject (semM si (forget a))`, and `semM : SigOpInfo A B →
M.⟦A⟧ → M.⟦B⟧` DOES consume the argument. So the value half at a higher-order-domain
sigop still wants `forget a ≡ forget b`. This is bookkeeping for the induction (the apex
only observes the trace), so it should be dischargeable, but isn't yet closed.

### Options to finish (pick on pickup)
1. **`isInt?`-domain case-split (RECOMMENDED — likely fully funext-free).** Mirror
   `mkEvent`: split the sigop bridge case on `isInt? A`. `Int` → `forget a ≡ forget b`
   by `cong` (`RelV Int = ≡`). non-`Int` → event is `refl` (arg-independent); discharge
   the value half either by (a) showing higher-order-domain named `semM` is arg-opaque
   in practice, or (b) weakening `RelT`'s value component to a trace-adequate relation
   (track only what downstream emits observe — which is `Int`-based). Needs a focused
   proof but hits NO funext on the trace.
2. **Localized funext for `rel→forget-eq` arrow case only.** Prove `rel→forget-eq`
   funext-free at first-order/product/sum; funext ONLY at its arrow clause (one lemma).
   Denotation + rest stay funext-free. Smallest diff; contradicts the funext-free goal
   in exactly one proof lemma.
3. **Enforce first-order sigop domains.** Typing-side invariant that named/sigop domains
   are ground; makes `forget`-equality `cong`-only. Larger; may not hold for genuinely
   higher-order imports.

### Where the code is
- `Once.Adequacy.MeaningRelation` — `RelV`/`RelT` + `RelT-return`/`RelT-bind` (done).
- `Once.Adequacy.MeaningBridge` — fundamental-lemma statement + `RelEnv`/`rel-lookup`
  (done); bridge-c/i/m/g scaffolded. Constructor imports for the discharge are in place.
- Env-free realm case shapes (worked out, ready to type): `bridge-m` — id/fst/snd/
  terminal/inl/inr = `RelT-return`(∘proj); compose/pair = `RelT-bind`+IH; case = split
  on the sum + IH; curry = `RelT-return (λ rb → bridge-m df (ra , rb))`; const = `bridge-g`;
  cata = `sem-cata` congruence over `cata-ev-algᴰ-D` (needs `bridge-m alg`); named =
  the sigop case above. `bridge-g` — terminal = `RelT-return tt`; pair/inl/inr = monad-
  law rewrite of `returnT` into `>>=T` then `RelT-bind`+IH; g-In = `in-value` vs
  `evalᴰ (In)` (needs the `In` evalᴰ reduction); g-int = `intLit` evalᴰ reduction.
- Then bridge-c/i (env realms, use `RelEnv`/`rel-lookup` + bridge-m/g), a selection lemma
  (`mainMeaningᵈ`/`mainRealized` parallel dispatch), and apply `bridge-c` at `main:EffUU`,
  `tt` to discharge the apex `bridgeᵈ` (Compile.agda).

### REFINEMENT (2026-07-04) — the boundary is `FitsInReg`, not `Int`
`Once.Type.FitsInReg = {Int, Float}` (one-word, register-passable scalars). `mkEvent`'s
`isInt?` is a current UNDER-approximation (records only `Int`; even `Float` args are
dropped). The principled boundary is `FitsInReg`:
- `FitsInReg` domains are first-order scalars ⇒ `RelV = ≡` ⇒ `forget a ≡ forget b` by
  `cong` — funext-free for BOTH the trace event AND the `semM` value. No value-component
  gap (unlike the `isInt?`-only split, where non-`Int` first-order codomains lingered).
- non-`FitsInReg` domains (compound / higher-order) are NOT register-passable; `IR.SigOp`
  allows them formally (`∀ {A B}`), but the adequacy chain only runs on VALID sigops
  (`valid-primitive-wf` / `ValidPrimitive`, already required upstream), and the register
  machine passes sigop args in registers ⇒ a valid sigop's domain is `FitsInReg`.

**Cleanest funext-free close:** in the `bridge-m` sigop case, extract `FitsInReg A` from
the sigop's validity witness (the derivation already carries what the adequacy needs),
then `forget a ≡ forget b` is `cong` from `RelV A a b = a ≡ b`. No funext, no value gap,
no new typing constraint — it reuses the existing `FitsInReg`/validity machinery. The
open task is to thread the `FitsInReg`-domain fact from the sigop validity to the bridge
(check `valid-primitive-wf` exposes it; if effectful sigops aren't yet `FitsInReg`-gated,
that gate is the one small addition, and it is physically forced anyway).

### Bridge discharge progress (2026-07-04, cont.)
`Once.Adequacy.MeaningBridge`: **bridge-g + bridge-m DISCHARGED** (green). Technique
validated and now standard:
- **Direct `∀ n`, not `RelT-bind`/`RelT-return` at application.** `RelT` is a defined
  function (`∀ n → …`), so Agda can't invert it during unification — `RelT-return`/`bind`
  leave metas at call sites. Instead unfold: `bridge-X … n = <trace> , <value>`.
- Trace: pure side is `[]`; `evalᴰ`/SD side is `projTrace sub n ++ …` → `cong (_++ [])` /
  `cong₂ (λ x y → x ++ (y ++ [])) (proj₁ (rec …)) …`. Value: `proj₂ (rec …)` (projected/
  wrapped as the constructor's `RelV` reduces).
- **Provide implicits that sit inside `evalᴰ`/`SigOp`/`value-info`** (`{A}{B}{a}{b}`,
  `{wfF}`, `{cn}`) — they can't be inverted out of the goal.
- Two hard leaves remain postulated: `sigop-bridge` (funext-free via `FitsInReg`) and
  `cata-bridge` (`sem-cata` congruence over `cata-ev-algᴰ-D`), plus `int-bridge`/`in-bridge`.

**Remaining (the env realms + apex):**
- `bridge-i` (~30 `⊢ᵢ` cases) + `bridge-c` (~13 `⊢ᶜ` cases), MUTUAL (`t-embed`→`bridge-i`,
  `t-annot`/`t-app`→`bridge-c`), using `RelEnv`/`rel-lookup` + `bridge-m`/`bridge-g`.
  Structural via the technique above; env-extending cases (`t-lam`/`t-let`/`t-case`) thread
  `(re , v-rel)`; `t-var-local {eV = svar i}` = `refl , rel-lookup _ i re`; sigop-var cases
  (`t-var-qualified`/`resolved`/`import`/`poly`) reuse the `FitsInReg` sigop argument.
- Then the selection lemma (`mainMeaningᵈ`/`mainRealized` parallel dispatch, bottoming in
  `bridge-c` at the leaf) + apply at `main : EffUU` / `tt` → discharge apex `bridgeᵈ`
  (Compile.agda) → discharge `sigop-bridge`/`cata-bridge`/`int-bridge`/`in-bridge` leaves.

---

# ============================================================
# RESUME POINT (2026-07-04) — read this first next session
# ============================================================

Branch `ocp-0006-once-spec`, every commit `certified`-green. The north-star
CONSTRUCTION is complete and postulate-free; only the `bridgeᵈ` proof remains.

## DONE (all committed, green)
1. **`⟦_⟧ᵈ` fully built + postulate-free + wired into the apex.**
   - IR-free `Once.Surface.Context` (SVar) + `Once.Denotation.ValueDomain`.
   - `lookupLocal → SVar` (Fin index); `Classify`/`Judgment` IR-free.
   - `Once.Denotation.Meaning`: `⟦_⟧ᶜ`/`⟦_⟧ᵢ`/`⟦_⟧ᵍ`/`⟦_⟧ᵐ` (0 postulates —
     `named-sem`/`in-value`/`cata-sem` discharged).
   - `Once.Denotation.MainMeaning.meaningᵈ`; `Compile.⟦_⟧ᵈ` is a DEFINITION.
2. **Bridge foundation** — `Once.Adequacy.MeaningRelation`: `RelV`/`RelT` (funext-free
   observational relation) + `RelT-return`/`RelT-bind`.
3. **Bridge fundamental lemma — 2 of 4 realms DONE** — `Once.Adequacy.MeaningBridge`:
   `RelEnv`/`rel-lookup` proven; **`bridge-g` + `bridge-m` discharged**.

## TODO (the last pieces — in order)
1. **`bridge-i` (~30 `⊢ᵢ` cases) + `bridge-c` (~13 `⊢ᶜ` cases)** in `MeaningBridge`
   (currently `postulate`d). MUTUAL. Use the VALIDATED TECHNIQUE (see the section above:
   direct `∀ n`, `cong`/`cong₂` on traces, `proj₂` on values, provide implicits inside
   `evalᴰ`/`SigOp`). Map each clause against `Meaning.⟦_⟧ᵢ`/`⟦_⟧ᶜ` (LHS) and
   `SD.⟦ realize(-infer) d ⟧ˢ` (RHS) — they mirror 1:1. Env-extending cases thread
   `(re , v-rel)`. `t-var-local {eV = svar i}` = `refl , rel-lookup _ i re`. sigop-var
   cases (`t-var-qualified`/`resolved`/`import`/`poly`) → a `sigop-bridge`-style leaf.
2. **Discharge the 4 leaf postulates**: `sigop-bridge` (funext-free via `FitsInReg` —
   split on `fits-in-reg?`/`isInt?` of the sigop domain; `FitsInReg` ⇒ `RelV = ≡` ⇒
   `forget`-eq by `cong`; non-`FitsInReg` args are dropped from the event by `mkEvent`),
   `cata-bridge` (`sem-cata` congruence over `cata-ev-algᴰ-D`, recursing `bridge-m alg`),
   `int-bridge`/`in-bridge` (`evalᴰ` of `intLit`/`In`).
3. **Selection lemma + apex.** Prove `main-bridge m mt hvm n : runMainˢ (proj₂
   (mainRealized …)) n ≡ runMainᵈ (proj₂ (mainMeaningᵈ …)) n` by parallel induction on
   the `mainMeaningᵈ-go`/`mainRealized-go` dispatch (identical shape), bottoming in
   `bridge-c` at the leaf + `RelT` at `EffUU` applied to `tt`. Then in `Compile.agda`
   replace `postulate bridgeᵈ` with `bridgeᵈ (m,mt,hvm) = main-bridge m mt hvm`.
4. **`certified` exit 0**, then **P5 cleanup**: rebuild `Once.Spec.Meaning` on `⟦_⟧ᵈ`,
   canonicalize `Once.Spec.*`, delete superseded duplicates, `make check-all` + tests,
   update OCP-0006, DELETE this plan.

## Gotchas
- Don't use `RelT-bind`/`RelT-return` at call sites — inference fails (`RelT` is a
  defined `∀ n → …`, non-invertible). Inline `∀ n` instead. (They're fine as lemmas.)
- Per-module check: `timeout 250 formal/scripts/agda-safe.sh agda MODULE=…`
  (MODULE is a FILE PATH `Once/Path/File.agda`, not a dotted module name).
- The funext question is SETTLED: funext-free via `FitsInReg` (see the two analysis
  sections above). No funext, no new typing rule.

---

# ============================================================
# RESUME POINT (2026-07-11) — supersedes the 2026-07-04 one above
# ============================================================

Branch `ocp-0006-once-spec`, every commit `certified`-green. The bridge is now
MOSTLY proven — the apex is wired and load-bearing.

## DONE this session (all committed, `certified` exit 0)
- **`bridge-i` (~20 `⊢ᵢ`) + `bridge-c` (~13 `⊢ᶜ`)** discharged as real DEFINITIONS
  in `Once.Adequacy.MeaningBridge` (mutual structural induction; `t-case` splits
  on the related scrutinee; env-extending cases thread `(re , v-rel)`;
  `t-var-local` via `rel-lookup` re-pointed to `SD.lookupᴰ`). Commit part 6.
- **Apex `bridgeᵈ` DISCHARGED** — new module `Once.Adequacy.MainMeaningBridge`
  (`main-bridge`): parallel induction over the shared `mainRealized`/`mainMeaningᵈ`
  dispatch, bottoming in `bridge-c` at `main : EffUU` (env `∅`, thunk `tt`).
  `Compile.agda`'s `postulate bridgeᵈ` is now `= MMB.main-bridge`. Commit part 7.
  ⇒ the direct meaning `⟦_⟧ᵈ` is tied to the surface meaning by a REAL proof.
- **3 leaf reductions discharged**: `int-bridge` (definitional `refl,refl` — commit
  part 8); `in-app-bridge` + `in-bridge` (commit part 9) via a NEW funext-free tool
  `wfF-layer-eq` (`RelV (⟦F⟧T X) → ≡` by `WellFormedF`-induction, since `wf-K` holds
  only `IsBaseType` ⇒ a functor layer is polynomial/arrow-free; built on
  `base-rel→eq`). `in-bridge` folded into `bridge-g`'s `g-In` case.

## REMAINING — 4 leaf postulates in `Once.Adequacy.MeaningBridge` (the deep content)
Each needs a DESIGN STEP, not mechanical work:
1. **`cata-bridge`** — the stated signature takes `dalg`/`mir` UNRELATED, so it is
   not provable as-is. STRENGTHEN it with the algebra relation (bridge-m already
   has `bridge-m alg : RelV(⟦F⟧T A' ⇒ A')(⟦alg⟧ᵐ)(evalᴰ(realize-morph alg))`,
   passable from the `m-cata` case), then prove the `sem-cata` congruence
   (`sem-cata : WellFormedF F → (⟦F⟧F A→A)→⟦μ⟧F→A`, in `Once.Semantics.Value`)
   over `cata-ev-algᴰ-D` — induction on `⟦μ⟧ F`. Substantial.
2. **`sigop-bridge`** (bridge-m `m-named`/`m-named-resolved`) — needs
   `forget a ≡ forget b` from `RelV A a b` at the sigop DOMAIN `A`. Funext-free ONLY
   at `FitsInReg A`. But `m-named` carries NO `FitsInReg`/validity witness (its A is
   whatever the import table says), so the fact must be THREADED from the adequacy
   validity (`valid-primitive-wf`/`ValidPrimitive`) — the plan's open question.
   DESIGN FORK: add a `FitsInReg`(-derived) hypothesis to `sigop-bridge` and supply
   it at the `m-named` site (possibly a small typing/validity gate — cascade risk;
   POC before committing). Value half also uses `semM`, so `FitsInReg` (not just
   `isInt?`) is the right boundary (see the 2026-07-04 REFINEMENT above).
3. **`sigop-ref-bridge`** (bridge-i `t-var-qualified`/`-resolved`/`-import`) — value
   position (domain `Unit`). Split on whether the RESULT type `A` is an arrow: non-arrow
   ⇒ LHS ≡ RHS definitionally, need `RelV`-reflexivity at that value; arrow ⇒ the
   `value-info`-vs-`arrow-info` closure ABI (same content as `sigop-bridge`).
4. **`poly-ref-bridge`** (bridge-c `t-var-poly-instantiate`) — LHS ≡ RHS definitionally
   (SD's `poly` clause is the un-dispatched `value-info` form); residual is
   `RelV`-reflexivity at the poly's `T`.

A shared `rel→refl`/`FitsInReg⇒forget-eq` helper likely unlocks 2–4; (1) is separate.

---

# ============================================================
# DECISION (2026-07-11) — SigOps are an FFI boundary: CONCRETE types only
# ============================================================

The sigop leaves (2/3/4 above) reduce to `forget a ≡ forget b` + `RelV`-reflexivity
at a sigop's domain/codomain — funext-free ONLY when those types are arrow-free
(first-order). The current spec does NOT enforce this: `SigOpInfo (A B : Type)` takes
arbitrary types and `lookupImport` maps names to arbitrary `Type`. `generic-semM` (the
named-primitive value) is a `postulate` — the spec deliberately leaves a primitive's
returned value abstract over arbitrary types.

Restricting sigop types to first-order is therefore NOT a proof-convenience gate
(which top-down forbids). It is a JUSTIFIED SPEC REFINEMENT: a SigOp **is** an FFI
boundary (reused by the compiler for its own primitives), and an FFI/register-ABI
boundary genuinely only passes CONCRETE values — never a Once closure or a live
recursive value. The current spec is UNDER-constrained (it admits programs the target
cannot faithfully run); adding the condition makes the language definition more honest.

## The decision (settled with the user)
- **Predicate = `IsBaseType`** (`Unit/Void/Int/Float/Str/Buffer`, closed under `×`/`+`):
  concrete scalars + by-value structs. EXCLUDES arrows and `μ`/`ν` (an FFI takes a
  `Buffer`/pointer, not a closure or heap value). Same predicate `WellFormedF` uses for
  functor `K`-positions ⇒ the already-green `base-rel→eq` discharges the bridge leaves.
- **Enforce BY CONSTRUCTION on `SigOpInfo`/`value-info`** — require `IsBaseType A` and
  `IsBaseType B` to build a SigOp, so a bad SigOp value is unrepresentable. Because
  `value-info` is generic and its callers get `A`/`B` from typing derivations, the SAME
  witness must reach the derivation anyway ⇒ this SUBSUMES gating the typing rules:
  - `SigOpInfo`/`value-info` can't be built at non-concrete types (interface honesty);
  - typing rules (`m-named`, `t-var-qualified/resolved/import`, poly) CARRY the witness
    ⇒ no program is well-typed with a bad SigOp;
  - the import boundary PRODUCES the witness from the declared signature.
  One shared `IsBaseType` witness, threaded down.

## Option A chosen (2026-07-11) — witness on the SURFACE `Expr` too (cleanest proofs)
The witness rides on `Surface.Expr.sigOp`/`poly`/`closure` (not just `SigOpInfo`), so
`SD.⟦_⟧ˢ` stays TOTAL and branch-free — NO `with isBaseType?` in the reference meaning
(which would be stuck on abstract types and pollute `faithful`/the bridge with a dead
branch). `realize` + `elaborate` read the SAME proof-irrelevant witness off the surface
node ⇒ `faithful` stays `refl`, bridge leaves reduce cleanly, no impossible cases to carry.

## Predicate (settled 2026-07-11): `IsConcrete` (`Once.Functor.Translate`)
`con-base : IsBaseType A → IsConcrete A` | `con-fun : IsBaseType A → IsConcrete B →
IsConcrete (A ⇒[ k ] B)`. ASYMMETRIC on a SigOp: DOMAIN `IsBaseType` (a base scalar —
a higher-order callback ARG would need funext, out of scope, loses nothing that works
today); RESULT `IsConcrete` (base OR a first-order function pointer — gains first-class
references to first-order functions). Decider `isConcrete?` in `Once.Functor.Decide`.

## Execution (top-down, typechecker-driven — WIP, tree RED mid-cascade)
FOUNDATION ✅ (green individually): `IsConcrete` + `isConcrete?`; `SigOpInfo` fields
`baseA : IsBaseType A` / `conB : IsConcrete B` (by construction); `mk-info`; all of
`Builders` (arith `con-base`; `value-info`/`generic-info`/`arrow-info(-eff)` take the
witnesses); `IntLit`; `Block` (`shape-as-type-base`). `IR.agda` unaffected (carries only).

REMAINING SWEEP (thread the witness; ~11 files) — each site's witness source in parens:
CLEAN HALF ✅ DONE+green (witness through the SPEC + reference meaning):
`Surface/Syntax` (sigOp/closure `IsConcrete A`, poly `IsConcrete T`); `Surface/IR`+
`Surface/Desugar` (legacy `SurfaceIR.SigOp` carries it); `Surface/Elaborate`
(`elaborate` reads it off the node); `TypeCheck/Judgment` (`m-named(-resolved)` carry
`IsBaseType A × IsConcrete B`; `t-var-qualified/resolved/import` + poly carry `IsConcrete
T`; `extractMorphWitness` splits `con-fun`); `Denotation/Realize` (derivation→Expr.sigOp);
`Denotation/SourceDenote` (Expr.sigOp arrow→`con-fun`, non-arrow→conc); `Denotation/
Meaning` (`named-sem`/value-refs from the derivation).

DEEP HALF ⏳ REMAINING (elaborator + correctness proofs):
- `TypeCheck/Elaborate.agda` — `inferElabV-RQualified/RResolved/RVar` + poly sites emit
  BOTH a surface term AND a `t-var-*` derivation, so they must now DECIDE the witness
  (`isConcrete?`/`isBaseType?`) and REJECT non-concrete refs (NEW failure branches).
  `ext-arrow-info`/`ext-resolved-info` (`mk-info'`) take the decided witnesses.
- Adequacy proofs reasoning about elaborate success/failure: `RealizeAgrees` (the
  elaborate-vs-realize masquerade), `Soundness`, `Completeness`, `SourceFaithful`
  (`faithful` stays `refl` — same witness both sides).
9. DISCHARGE the 4 sigop leaves: new helpers `base-rel→refl : IsBaseType A → (v) →
   RelV A v v` and mutual `concrete-rel→refl : IsConcrete B → (v) → RelV B v v` /
   `RelT-refl : IsConcrete B → (t) → RelT B t t` (arrow case: `base-rel→eq` on the
   base domain collapses `RelV A x y` to `x ≡ y`, then `cong` + `RelT-refl`). sigop-bridge:
   trace `refl` (value-info Pure ⇒ `emit-D = []`); value `base-rel→eq baseA rv` →
   `forget a ≡ forget b` → `cong` → `subst` `concrete-rel→refl conB`. Then `certified`.
2. Follow the red up through `value-info`'s 8 callers + the ~6 construction sites +
   `mk-info` pattern/record-update sites; internal sigops (arith: `Int×Int→Int`) supply
   trivial `IsBaseType` constants; the import/named path threads from the typing rule.
3. Add the `IsBaseType` premises to the typing rules + import boundary; thread through
   elaborator / soundness / completeness / realize (the `t-var-*`/`m-named` cascade).
4. Discharge the 4 sigop leaves in `MeaningBridge` via `base-rel→eq` (domain forget-eq)
   + base-codomain `RelV`-reflexivity. Then `certified` exit 0.
CHECKPOINT if the cascade surfaces a legitimate existing SigOp needing a non-base type.

## THEN P5 cleanup (unchanged): rebuild `Once.Spec.Meaning` on `⟦_⟧ᵈ`, canonicalize
`Once.Spec.*`, delete duplicates, `make check-all` + tests, update OCP-0006, DELETE
this plan.

# ============================================================
# RESUME POINT (2026-07-11, cont.) — SigOp migration LANDED; cata leaves remain
# ============================================================

## DONE (committed, `certified` exit 0): the SigOp-concreteness migration
`IsConcrete` threaded end-to-end (elaborator emission w/ honest `NonConcreteSigOpType`
reject; Soundness/Completeness/adequacy/Canon*/RealizeAgrees; de-with of the concreteness
`with` in the auxes so the proofs can drive it). Of the 4 `MeaningBridge` sigop leaves,
**3 discharged**: `sigop-bridge`, `poly-ref-bridge`, `sigop-ref-bridge` (all cases). The
4th, the arrow-typed value ref, was NOT an ABI iso — it exposed a **latent meaning bug**
(`⟦ t-var-* ⟧ᵢ` used a pure `value-info`, dropping the pointee's effect); fixed with
`Meaning.sigOpRefᴰ` dispatching exactly as SD's `sigOp`, collapsing the leaf to `refl`.
So Plan 0.58's actual subject (SigOps) is COMPLETE.

## REMAINING: two `cata` fold-congruence postulates (PRE-EXISTING, not migration-caused)
- `cata-bridge`   (`MeaningBridge`) — `RelT`: spec `cata-sem ⟦alg⟧ᵐ` ↔ IR-eval `evalᴰ(Cata …)`.
- `agree-cata-denotes` (`RealizeAgrees`) — `≡`: SD `cata` ↔ SD `lift-morphism(Cata …)`.
Both are the SAME content: **the fold construct preserves the bridge, given its algebra
does** — a congruence over the recursion (like `m-compose`/`m-pair`, but the one FOLD case,
so structural induction over `μ` instead of a finite combination).

## DEEP ANALYSIS (2026-07-11) — carrier restriction REJECTED (not principled)
Constraining the cata carrier `A` with `IsConcrete` is a SHIM: no FFI boundary (cata is
pure internal compute), and `Val.⟦A⇒B⟧ = Val.⟦A⟧→Val.⟦B⟧` (the machine model represents
higher-order fns), so no representability constraint either. The real obstacle is in the
TOOL, not the language:
- Every other bridge case THREADS the input relation to the output. Cata is the only fold;
  its input `RelV(μF) a b = a ≡ b` carries no relational content, so the output `RelT A`
  must come entirely from the algebra, threaded through the fold.
- But `RelV` is **funext-free** (a Π at arrows: `RelV(A⇒B) f g = ∀ related-inputs → RelT`),
  hence **NOT reflexive** at arrow types. Reconstructing the `⟦A⟧ᴰ`-level relation from the
  fold's `Val`-level carrier needs `inject∘forget = id` at `A` — true first-order, FALSE at
  genuinely-higher-order carriers (a fn OF a fn). Checked: NOT rescued by funext (funext is
  about fn EQUALITY, not `RelV` reflexivity), NOT by a Val-level relation (`RelV`'s inputs
  are related-but-UNEQUAL; their `forget`s differ at higher-order, breaking any pointwise
  reconstruction). It's a structural property of the funext-free `RelV`.

## DECISION (2026-07-11, with user) — OPTION 1: redesign `RelV` (the principled fix)
Don't restrict the language to fit the relation; **fix the relation**. Replace the current
funext-free-but-non-reflexive `RelV` with a proper logical relation that carries a
FUNDAMENTAL LEMMA (reflexivity/parametricity theorem): *every semantically-produced value
is related to itself*. Reflexivity is FALSE for arbitrary higher-order values but TRUE for
REALIZABLE ones (built from combinators/morphisms that preserve the relation) — the
standard logical-relations move. With the fundamental lemma in hand, the cata output
reflexivity is a corollary and `cata-bridge` (plus, we expect, other leaves) FALLS OUT.
This is architectural — it touches `Once.Adequacy.MeaningRelation` and every `bridge-*`
case — so it is its own focused effort. Guiding principle (user): find the abstraction that
makes ALL the proofs fall out; principle over expedience.

## ORDER OF WORK (agreed with user, 2026-07-11)
1. **[NEXT]** Discharge `agree-cata-denotes` — the `≡` formulation needs NO reflexivity:
   given the algebra faithfulness `⟦algE⟧ˢ tt ≡ returnT(evalᴰ m-alg)`, the two `cata-ev-alg`s
   collapse by monad-left-id and `Once.Semantics.Functor.cataS-cong` lifts it through the
   fold. Wrinkle: `extract-morph-eff` recurses into the cata case ⇒ mutual induction with
   the algebra-faithfulness lemma. Tractable, no shim, no carrier constraint. Removes one
   of the two cata postulates.
2. **[THEN]** The `RelV` reflexive-logical-relation redesign (Option 1 above) to discharge
   `cata-bridge`. Expect to establish a `RelV`/`RelT` FUNDAMENTAL LEMMA and re-derive the
   `bridge-*` cases on top of it.

## UPDATE (2026-07-11 cont.) — agree-cata-denotes DONE; cata-bridge → Option (b)
`agree-cata-denotes` DISCHARGED (commit 21689cb2c): `Once.Adequacy.CataFold.cata-fold-eq`
(fold congruence given algebra faithfulness, NO reflexivity) + `RealizeAgrees.faithful-aux`
(algebra faithfulness by structural recursion over `extract-morph-eff-aux` with a GENERAL
result type + `T≡A⇒B` eq). Certified green.

**`cata-bridge` FINDING:** Option 1 (reflexive `RelV`) alone does NOT suffice — `cata-bridge`
is `RelT A' t t`, needing `RelV A' v v` at the fold RESULT, and the `sem-cata` `Val`-forget
blocks that at higher-order carriers. DEEPER: `forget` at an arrow runs the fn at depth 0
and DROPS its trace, so a cata to an EFFECTFUL-arrow carrier folds via `Val` and comes out
PURE — a LATENT BUG, the same class as the `value-info`-vs-`arrow-info` sigOp arrow bug just
fixed. So the principled fix is **Option (b): fold in the TRACE domain** (carry `⟦_⟧ᴰ`/trace,
not `Val.⟦C⟧`, per layer) — this fixes the definition (effects preserved) AND lets the clean
related-input relation thread with no reflexivity. (User chose (b) over (a) same-input `RelV`,
which would merely certify the buggy definition.)

**POC finding for (b) — the load-bearing questions before the cascade:**
- The fold's coercions are `Val`-specific: `coerce-functor⁻¹ : ∀ F X → ⟦ F ⟧F ⟦ X ⟧ →
  ⟦ ⟦ F ⟧T X ⟧` (all `Val`). Folding in `⟦_⟧ᴰ` needs `⟦_⟧ᴰ`-level functor coercions (a
  `WellFormedF`-structural build of `⟦ ⟦ F ⟧T C ⟧ᴰ` from `⟦ F ⟧F ⟦ C ⟧ᴰ`).
- MACHINE COHERENCE (the real load-bearing claim): `evalᴰ (Cata …)` folds must still match
  the x86/RISC-V cata codegen. If the codegen ALSO drops effectful-arrow-carrier effects
  (like the `Val`-fold), then (b) DIVERGES from the machine and the machine bridge breaks;
  if the codegen fires them (closure firing on apply), (b) coheres. RESOLVE THIS FIRST
  (POC per load-bearing-lemma discipline) before touching `cata-sem`/`evalᴰ(Cata)`/`SD.cata`
  + the cata correctness cascade (Optimize/Escape/Fusion/machine).
- Scope of (b): redefine the recursion-scheme value carrier `Val.⟦C⟧ → ⟦C⟧ᴰ` in
  `cata-ev-algᴰ`/`cata-ev-algˢ`/`cata-sem`/`cata-ev-algᴰ-D` + re-verify all cata proofs.

## METHODOLOGY CORRECTION (2026-07-11, user) — do (b) TOP-DOWN, don't gate on codegen
The "machine-coherence gate" above was BOTTOM-UP thinking (treating the existing codegen as
the authority the spec must fit). WRONG — spec is the authority; the implementation bridges
UP to it (cf. [[feedback_top_down_spec]], [[feedback_no_shortcuts_wire_to_top]]). If the
codegen drops effects, the CODEGEN is wrong.

**What the higher proofs dictate about the codegen:** `RelT` demands EQUAL event traces at
every budget ⇒ the meaning of `cata alg` is "fold each layer keeping its `⟦_⟧ᴰ` computation,
trace intact — never collapse to pure `Val`." The `forget`-per-layer is a SHORTCUT,
observationally invisible only at first-order carriers (`⟦A⟧ᴰ ≅ Val.⟦A⟧`); at an arrow
carrier it erases apply-time effects the relation says must be preserved. So the codegen for
a fold-to-a-function must emit a closure whose effects fire on apply (a normal closure
calling convention already does this ⇒ codegen is likely already right; the `Val`-fold
DENOTATION is the outlier).

**ORDERING (top-down, no gate):**
1. Fix the SPEC — `cata-sem` folds in the trace domain (effects preserved); needs a
   `⟦_⟧ᴰ`-level functor coercion (current `coerce-functor⁻¹` is `Val`-only).
2. Make `evalᴰ(Cata)` match the spec (same trace-preserving fold).
3. `cata-bridge` falls out on the clean related-input relation — no reflexivity.
4. Re-verify the machine bridge AGAINST the corrected `evalᴰ(Cata)`; where codegen diverges
   from the now-correct denotation, FIX THE CODEGEN, not the spec.
FIRST concrete move: define the trace-preserving `cata-sem` + its `⟦_⟧ᴰ` functor coercion.

## POC (2026-07-11) — trace-preserving `cata-sem` IS DEFINABLE (no spec-level obstacle)
`coerce-functor⁻¹` is a purely structural reshuffle (`K`→pass-through, `Id`→carrier,
`⊕`/`⊗`→structural). Its `⟦_⟧ᴰ` mirror `coerce-functor⁻¹-D : ⟦ F ⟧F ⟦ C ⟧ᴰ → ⟦ ⟦ F ⟧T C ⟧ᴰ`
differs from the Val one ONLY at `K` positions (`⟦A⟧` vs `⟦A⟧ᴰ`), and `WellFormedF` forces
every `K` to be BASE where `⟦A⟧ᴰ = ⟦A⟧` — so they coincide and the coercion is the SAME
structural definition (needs the `WellFormedF` witness to see `K`=base). `forget`-per-layer
compensated for nothing real. ⇒ (b) is principled AND feasible.

## EXECUTION (top-down, in progress)
1. **[doing]** `coerce-functor⁻¹-D F (wf : WellFormedF F) C : ⟦ F ⟧F ⟦ C ⟧ᴰ → ⟦ ⟦ F ⟧T C ⟧ᴰ`.
2. Redefine `cata-ev-algᴰ` (DenotTrace) / `cata-ev-algˢ` (SourceDenote) / `cata-ev-algᴰ-D`
   + `cata-sem` (Meaning): carry `List × ⟦C⟧ᴰ` (drop the `forget`; final value stays `⟦C⟧ᴰ`,
   no re-`inject`). `evalᴰ alg (coerce⁻¹-D …)` directly (no `inject z`).
3. `cata-bridge` (MeaningBridge) + `agree-cata-denotes` (already via `cata-fold-eq`) fall out
   on the clean related-input relation — no reflexivity.
4. Follow the RED through the cata correctness cascade (SourceFaithful/ResolveFaithful/
   Optimize/Escape/Fusion/machine); fix codegen where it diverges from the corrected
   denotation, NOT the spec.

---

# ═══ SESSION 2026-07-11 (late) — cata-bridge DONE; poly-concreteness bug found & fix IN PROGRESS ═══

## 1. cata-bridge DISCHARGED — bridge is POSTULATE-FREE  (COMMITTED, green)
- Commit `42044c164`. `Once.Adequacy.MeaningBridge` has ZERO postulates; apex `bridgeᵈ`
  fully proven; `Once/Certified.agda` exit 0.
- New `Once.Adequacy.CataBridge.cata-bridge`: `RelV(μ-type F) a b = a≡b` ⇒ BOTH folds run
  over the SAME `forget a`; differ only in the per-layer step (`⟦alg⟧ᵐ z` vs `evalᴰ mir z`),
  bridged by the recursive `bridge-m alg` passed as `algR`. Proof = generic `cataS-rel`
  (`CataRel`, commit `8ed70457b`) at product relation `RelC (t₁,v₁)(t₂,v₂) = (t₁≡t₂) ×
  RelV A' v₁ v₂`, applied to `forget a`, with `algR-full` from a structural `layer-lemma`
  (induction on `WellFormedF`, mirroring translateF/coerce-μ-out/sem-fmap/coerce-functor⁻¹-D)
  lifting `RelSF` → `RelC`. K-case: `rewrite feq` then `base-refl ib _` (reflexivity dodges
  the stuck `sem-fmap (K _)` reduction). Call site MeaningBridge:274.
- `sigop-ref-arrow-bridge` was already gone (prior session). So the whole 0.58 bridge goal
  (postulate-free `⟦_⟧ᵈ ≈ SD∘realize`) was COMPLETE at this commit.

## 2. Extraction + test suite — 13 FAILURES exposed the poly-concreteness bug
- Re-extracted MAlonzo (`make malonzo`), synced `compiler/once.cabal` (+10 new modules:
  CataBridge/CataRel/CataFold/MeaningBridge/MeaningRelation/MainMeaningBridge/MainMeaning/
  Meaning/ValueDomain/Context), `cabal build` green, `once` binary links.
  **NOT COMMITTED** — the extraction reflects the BUGGY elaborator; re-extract after the fix.
- `tests/run-exit-tests.sh`: **37 pass, 13 FAIL(build)** — ALL same-module defs with
  non-concrete types: `layer5-cata-*` (cata `μNat→Int`), `layer4-*` (closures/fn-as-arg).
  Error: `Reference 'toInt' has non-concrete type (μ(...)→Int) (FFI/SigOp references must be
  base types or first-order function pointers)`.

## 3. DIAGNOSIS (settled with user) — an ABSTRACTION bug in the SPEC, not a proof bug
- Same-module def references elaborate to a `poly` node; the migration's Option-A modeled
  a poly ref as an FFI `SigOpInfo` (`value-info`), which BAKES `IsBaseType`/`IsConcrete`
  (`Once/SigOp/Info.agda:141-142`). So EVERY internal def ref inherited the FFI register-ABI
  rule. cata/closures (`μ`/arrow-typed) got rejected.
- The concreteness was LOAD-BEARING for `poly-ref-bridge`'s old `RelT-refl` (the funext-free
  relation is only reflexive at concrete types). That's WHY the migration added it.
- **User ruling: "cata is not about concrete types. Period."** Concreteness is FFI-ONLY.
  This was a SPEC bug (the migration injected an ABI constraint into the language definition):
  (1) `t-var-poly-instantiate` carried an `IsConcrete T` premise ⇒ cata programs were
  literally UNTYPABLE in the spec; (2) `⟦internal-ref⟧ᶜ` was an FFI `value-info` placeholder
  ⇒ the spec claimed "a def ref means emit-an-FFI-event" (semantically wrong).

## 4. THE PRINCIPLED FIX — "a poly reference means its closed body" (mathematically standard)
Standard denotational semantics of a (non-recursive) definition reference. Well-founded
because `t-var-poly-instantiate` types the body in `removePoly x polys` (no self-reference ⇒
acyclic ⇒ topologically-ordered let-block). Body is closed (empty LOCAL ctx) ⇒ meaning is
env-independent (`⟦body⟧ᶜ tt`, discard caller `dγ`).
- `⟦t-var-poly⟧ᶜ = ⟦body⟧ᶜ tt` (Meaning).
- `realize(t-var-poly) = morph-app (elaborate IR.Heap (realize body)) unit` (Realize) —
  KEY: `⟦morph-app ir unit⟧ˢ dγ = evalᴰ ir tt`, ENV-INDEPENDENT BY DEFINITION (reuses the
  existing `morph-app`/`unit` combinators — NO new node, NO whole-grammar weakening lemma).
- `poly-ref-bridge` then FALLS OUT: `faithful (realize body)` + `bridge-c body <RelEnv-∅>`.
  (Per user philosophy "fight the definition not the proof / missing combinator, not a
  proof fight" — chose the env-discarding `morph-app` embedding over grinding a renaming lemma.)

## 5. STATE OF THE POLY FIX — uncommitted WIP, tree RED mid-cascade
GREEN (typechecked individually): `Once/Denotation/Meaning.agda`, `Once/Denotation/Realize.agda`.
Edited (uncommitted):
- `Once/Surface/Syntax.agda` — `poly : (name)(T) → Expr Γ zeroUsage T` (dropped `IsConcrete T`).
- `Once/TypeCheck/Judgment.agda` — `t-var-poly-instantiate` dropped the `IsConcrete T` premise.
- `Once/Denotation/Meaning.agda` — `⟦t-var-poly⟧ᶜ = ⟦bodyD⟧ᶜ tt`. ✔ green.
- `Once/Denotation/Realize.agda` — `realize(t-var-poly) = morph-app (elaborate IR.Heap (realize bodyD)) unit`. ✔ green.
- `Once/Surface/Thinning.agda:241` — `rename … (poly name T)` (dropped conc).
- `Once/Adequacy/Compile.agda` — comment only (cata-bridge is postulate-free).

## 6. REMAINING CASCADE (follow the RED, `agda-safe.sh MODULE=<path>`)
Mechanical "drop `conc` from every poly build/match site" — ~8 modules:
- `Once/Surface/Elaborate.agda:290` — `elaborate (poly name _ conc)` fallback.  ← the WART (below).
- `Once/Denotation/SourceDenote.agda:194` — `⟦ poly name PT conc ⟧ˢ`.               ← WART.
- `Once/Adequacy/SourceFaithful.agda:173` — `faithful (poly name PT conc) = refl`.  ← WART.
- `Once/TypeCheck/Elaborate.agda` — the Phase-1 GATE (`1256/1263-1265` + de-withed aux `2489/2491`)
  `… | just _ | nothing = failure (NonConcreteSigOpType …)` ⇒ REMOVE rejection, emit `poly x T`
  unconditionally; `checkElab-fallback-RVar-poly` (3410/3428) + `applySplice`/`resolvePolyCase`
  (3057-3061, ~3262) drop `conc`.
- `Once/TypeCheck/Completeness.agda:1866,1936` — drop `conc` args.
- `Once/Adequacy/RealizeAgrees.agda:1125` — `faithful-aux (Surface.poly _ _ _)` → `(_ _)`.
- `Once/Adequacy/ResolveFaithful.agda:60-90` — the (already-POSTULATED, trusted) resolver
  faithfulness lemmas mention `Srf.poly … conc`; drop `conc`.

### THE ONE DECISION (fresh tomorrow): dead `poly` SURFACE node totality — POSTULATE-FREE ONLY
**USER RULING (2026-07-11): a trust-boundary marker IS a postulate ⇒ NOT ACCEPTABLE.**
After the fix the `poly` Surface node is DEAD in every successful compile (realize emits
`morph-app` not `poly`; the compile path splices `poly` in `resolveExpr`; a surviving `poly`
⟹ recursive/unbound poly def — which Once disallows — ⟹ malformed program). But
`elaborate`/`⟦_⟧ˢ`/`faithful` are TOTAL over `Expr`, and `Expr` has a `poly` constructor, so
its clause needs a term of `IR ⟦Γ⟧ᶜ T`; for non-concrete `T` (e.g. `Void`) there is NO
postulate-free inhabitant. ⇒ Postulate-free ⟺ the `poly` node must carry a CLOSED TERM OF ITS
OWN TYPE (naturally, its resolved body), so `elaborate (poly name body) = elaborate body ∘
terminal` — the exact mirror of `realize`'s `morph-app` fix. This is IMPLEMENTATION-only (the
poly Surface node), NOT the spec — the semantics (poly ref MEANS its body) is DONE & green.
Two postulate-free shapes:
  (A) [lean toward this] `poly` carries its resolved closed body:
      `poly : (name : String) → Expr ∅ [] T → Expr Γ zeroUsage T`. All three clauses fall out
      postulate-free (`elaborate body ∘ terminal`, `⟦body⟧ˢ tt`, `faithful body`). `resolveExpr`
      SUCCESS already computes the body `eE` — produce a body-carrying `poly` instead of splicing;
      UNRESOLVABLE polys (recursive/unbound = malformed) become clean compile FAILURES (pipeline
      already threads `failure`/`Maybe`). Touches the two-phase design: checkElab's bodyless
      Phase-1 `poly` must not reach elaborate (checkElab resolves inline w/ the Acc recursion
      resolveExpr already has, OR a post-resolve type distinguishes the two). ARCHITECTURE CALL.
  (B) type-level "resolved" guarantee: index a post-resolve `Expr` (or thread `NoPoly e`) so
      `elaborate`/`⟦_⟧ˢ`/`faithful` exclude `poly` by type ⇒ the clause is a genuine absurd
      pattern. Fully postulate-free; larger (elaborate signature + all callers).

## 7. AFTER THE FIX GOES GREEN
1. `make certified` exit 0 (apex — my Meaning/Realize spec edits are apex-touching).
2. `make malonzo` re-extraction + `cabal build` + `tests/run-exit-tests.sh` (expect 50 pass).
   Re-sync `compiler/once.cabal` if module set changed; COMMIT the extraction this time.
3. Commit the poly fix (spec + cascade) as one coherent change.
4. THEN P5 cleanup (rebuild `Once.Spec.Meaning`, canonicalize, delete duplicates, update
   OCP-0006, delete this plan) — see the P5 section far above.

## Gotchas (this session)
- `scripts/agda-safe.sh MODULE=<file-path>` (NOT bare path — "Nothing to be done" if bare).
- `morph-app`'s usage is `zeroUsage +ᵘ (Many *ᵘ Ψ)`; with `Ψ = zeroUsage` (the `unit` arg)
  it reduces to `zeroUsage` (matches the poly rule's conclusion usage).
- HANDOFF.md stays UNTRACKED (never git-add). Extraction (`compiler/src/MAlonzo/*`) currently
  reflects the BUGGY elaborator — do NOT commit it until the poly fix lands.

# ═══════════════════════════════════════════════════════════════════════════
# RESUME POINT (2026-07-12) — SUPERSEDES all above: the TELESCOPE redesign
# ═══════════════════════════════════════════════════════════════════════════

Branch `ocp-0006-once-spec`. Baseline = commit `42044c16` (`certified`-green;
bridge postulate-free). The abandoned "drop-IsConcrete bodyless-poly" WIP was
REVERTED — we replace it wholesale with the redesign below.

## What we decided (with the user) and WHY

The poly-concreteness bug (13 build failures: cata/closure same-module defs
wrongly rejected) is the symptom of a deeper SPEC un-cleanliness the user spotted:
**acyclicity of top-level definitions is enforced only EMERGENTLY** (via
`removePoly x` shrinking the poly ctx mid-derivation), never DECLARED. And the
implementation's `poly` surface node is an "unresolved-reference" placeholder —
a term with no meaning of its own — which is why `elaborate`/`⟦_⟧ˢ`/`faithful`
can't be made total postulate-free once its FFI `IsConcrete` crutch is (correctly)
removed (`cata` is not an FFI boundary — see [[feedback_cata_not_concrete]]).

**Mathematical framing (settled).** Once forbids general/mutual recursion —
recursion is ONLY the structured `cata`/`ana` schemes. So top-level definitions
are a genuine ACYCLIC global environment, and a reference is nothing but an
UNFOLD of its (closed) body. A reference is therefore not a semantic primitive;
the term language the compiler + denotation range over should contain NO
"unresolved reference" node. The Spec ALREADY does this: `t-var-poly-instantiate`
embeds the body's derivation as a premise (`prefix ⊢ᶜ body : T`) and
`⟦ref⟧ = ⟦body⟧` — inline unfold at the level of derivations. The two-phase
`poly`-node/`resolveExpr` is an IMPLEMENTATION-only divergence from the spec.

**The unifying move — a TELESCOPE definition-environment (the DTT global context).**
Replace the flat unordered `PolyCtx` + `removePoly` with an ORDERED environment
built incrementally, each definition typed in the PREFIX before it:
```
DefEnv :  ∅  |  DefEnv ▷ (name , schema , body)      -- dependency order
```
This makes THREE things structural at once (why it's the right target):
1. **Acyclicity is manifest** — an entry sees only its prefix; self/mutual
   cycles are unrepresentable. No `removePoly`. (Answers the spec-honesty gap.)
2. **E1 inline-unfold termination is structural** — a reference recurses on the
   strictly-smaller PREFIX telescope, so `checkElab` gets LEXICOGRAPHIC descent
   `(telescope, rawExpr)` that Agda can see WITHOUT an explicit `Acc` or
   `TERMINATING` pragma (the old flat-`List`+`removePoly` couldn't: `removePoly x
   polys` is not a structural sub-term, hence its `removePoly-decreases`+`Acc`).
3. **Dependent-types alignment** — this IS the DTT global context with
   δ-reduction (a reference = its unfold); the acyclic telescope is exactly the
   well-founded signature dependent typing will need for conversion checking.
See [[feedback_top_down_spec]] / [[feedback_no_shortcuts_wire_to_top]]: the spec
is the authority; we make the implementation faithful to it (E1), not reconciled
by a bridge (E2) — and never by a proof-plumbing type-index dodge.

## Design decisions settled
- **Polymorphism = re-typecheck the raw body at each use-type** (option I —
  template/monomorphization; PRESERVES current behavior). `PolyType` (Type.agda:452)
  is genuinely polymorphic (`PTVar` + `instantiate : PolyType → Type → Maybe Subst`),
  but the spec rule + elaborator already realize instantiation by re-typechecking
  the RAW body at the concrete `T`, NOT by substituting a typed schema. So the
  telescope stores raw bodies; a reference re-typechecks in the prefix. (Option II —
  elaborate once at the schema + type-substitute per use — is the cleaner DTT form
  but needs elaboration-level type-instantiation Once lacks; deferred as a future
  refinement, NOT part of this plan.)
- **`DefEnv` is a genuine inductive telescope** (not `List`), so the prefix is a
  structural sub-term ⇒ lexicographic termination for `checkElab` with no pragma.
- **Topological sort at the module boundary** (`Compile`): a dependency CYCLE
  becomes a manifest COMPILE FAILURE (honest rejection), not an emergent typing
  dead-end. `DefEnv` built incrementally over the sorted defs.
- **The `poly` surface node + `resolveExpr` + `ResolveFaithful` are DELETED.**

## Load-bearing POC — ✅ DONE (2026-07-12): POC-A PASSED
**POC-A (the crux):** an isolated scratch module (`Once/POC/DefEnvTermination.agda`,
now deleted) defined the inductive `DefEnv` telescope + `resolve`/`resolveVar`
where the reference case recurses `resolve Δ body` on the pattern-matched PREFIX
`Δ` (structural sub-term) and normal cases recurse on the smaller `RawExpr`.
**Agda accepts the lexicographic `(telescope, rawExpr)` termination with NO `Acc`
and NO `TERMINATING` pragma** (exit 0, only benign CoverageNoExactSplit warnings).
⇒ `checkElab` can resolve references INLINE with clean structural termination —
the whole reason the old flat-`List`+`removePoly` needed an explicit `Acc` is gone
once the environment is a genuine inductive telescope. The E1 path is clear.
Validated reference shape:
```
resolve Δ (RVar x) = suc (resolveVar Δ x)
resolveVar (Δ ▷ (n , body)) x with n ≟ x
... | yes _ = resolve Δ body     -- HIT: recurse on prefix Δ + body
... | no  _ = resolveVar Δ x       -- miss: smaller Δ
```

## Phases (top-down, spec-first; follow the typechecker's RED down the E1 path)
Apex `certified` stays RED through T0–T4 (an uncontainable core refactor — the
user has authorized the long red window, keep-going, room to finish green).

- **T0 — spec foundation.** New `DefEnv` telescope (new module, e.g.
  `Once.TypeCheck.DefEnv`, or in `Classify`); `NamedCtx` carries it instead of the
  flat `PolyCtx`. Replace `t-var-poly-instantiate` in `Judgment` with the
  telescope reference rule (lookup + `prefix ⊢ᶜ body : T` premise; no `removePoly`,
  no `IsConcrete`).
- **T1 — spec meaning.** `Meaning`: `⟦ref⟧ᶜ = ⟦bodyD⟧ᶜ tt` (closed body ⇒
  env-independent). `Realize`: reference → env-independent unfold of the body
  (`morph-app (elaborate Heap (realize bodyD)) unit`, reusing existing combinators).
- **T2 — delete the `poly` surface node (E1 core).** `Surface.Syntax`: remove
  `poly`. `checkElab` (`TypeCheck/Elaborate`): at a user-def reference, resolve
  INLINE — re-elaborate the body at the use-type via the telescope prefix (the
  POC-A recursion). DELETE `resolveExpr`/`resolvePolyCase`/`applySplice` +
  `ResolveFaithful`. Ripple: `Thinning`, `Desugar`, `Surface/Properties`,
  `SourceDenote` (drop poly clause), `SourceFaithful` (drop poly clause).
- **T3 — module pipeline.** `Compile`: `buildPolyCtx` → topological sort (cycle ⇒
  failure) + incremental `DefEnv` build; thread the telescope where `polys` went.
- **T4 — follow the red through adequacy → apex.** `Soundness`, `Completeness`,
  `RealizeAgrees`, `Canon*`, `FunBundle`, `ModuleComplete`, `MainExtract`,
  `MeaningBridge` (the poly-ref leaf should SIMPLIFY/vanish — a reference now
  recurses structurally via `bridge-c` on the body, no reflexivity leaf). End:
  `certified` exit 0, zero new postulates.
- **T5 — P5 cleanup (unchanged).** Rebuild `Once.Spec.Meaning` on `⟦_⟧ᵈ`,
  canonicalize `Once.Spec.*`, delete duplicates; `make certified` + `make malonzo`
  re-extraction + `cabal build` + `tests/run-exit-tests.sh` (expect the 13
  previously-failing cata/closure tests to PASS); update OCP-0006; DELETE this plan.

## Discipline
- `timeout 250 scripts/agda-safe.sh MODULE=Once/Path/File.agda` (MODULE = file path).
- Scaffold-then-discharge WITHIN a phase is fine; each phase's END is postulate-free.
- `Once.Adequacy`'s top-level STATEMENT stays byte-identical; only the instance +
  the definition-environment machinery move. If the top-level claim itself would
  change, STOP and float it.

# ═══ SESSION 2026-07-12 (cont.) — telescope spec win LANDING (cascade green) ═══

## Decision recap (with user)
E1-full's faithful inline resolution needs a well-founded measure threaded through
the ~60-member (~250-callsite) checkElabV mutual block — impractical now. E1-lite's
PolyFree threading turned out even worse (~500 sites via ⟦_⟧ˢ/faithful/elaborate,
25 modules). ROOT: the concreteness migration baked `IsConcrete` INTO `SigOpInfo`
(load-bearing for the COMPLETE bridge — can't un-bake), so the dead `poly` clause
inherits concreteness and can't go conc-free without one of those big pushes.
⇒ DECOUPLE: land the TELESCOPE spec win now KEEPING `conc` on the poly node (rule +
node unchanged w.r.t. concreteness); defer the conc-removal bug fix to E1-full later
(user: "do E1-full if lite blocks a feature").

## LANDED this session (all GREEN individually)
Telescope rule (manifest acyclicity), `conc` KEPT:
- `Classify`: `lookupPolyPrefix` (returns the prefix = tail) + `lookupPolyPrefix-decreases`
  + `lookupPolyPrefix⇒lookupPoly` (same-entry bridge).
- `Judgment`: `t-var-poly-instantiate` types the body in the `prefix` (was `removePoly x`);
  `IsConcrete T` premise KEPT (deferred).
- `Meaning`: `⟦ ref ⟧ᶜ = ⟦ bodyD ⟧ᶜ tt` (a def ref MEANS its closed body — the real
  semantic fix, was an FFI `value-info` placeholder).
- `Realize`: `realize ref = subst poly-usage-eq (morph-app (elaborate Heap (realize bodyD)) unit)`
  (env-independent unfold, no `poly` node); `poly-usage-eq` top-level (bridge names it).
- `CanonPolyTransport`: `lookupPolyPrefix-canon`(-just), `-mono`, `-PInB` (one commute
  subsumes lookupPoly-canon + removePoly-canon — prefix = canonicalized tail); transport
  case cleaner (no subst).
- `CanonReflectPolyTransport`: reflect case inverts the commute (3-tuple).
- `CanonPreserveMutual`: `app pib (lookupPolyPrefix⇒lookupPoly …)`.
- `Elaborate`: `checkElab-fallback-RVar-poly` body premise retyped to `prefix` (ignored `_`);
  `eqPoly` stays `lookupPoly` (checkElab UNCHANGED — E1-full deferred).
- `Completeness`: both poly callers convert via `lookupPolyPrefix⇒lookupPoly`.
- `MeaningBridge`: poly case now RECURSES `bridge-c bodyD {tt}{tt} tt` (was `RelT-refl`),
  through `SD-subst-usage` (SD ignores the usage `subst`) + `faithful (realize bodyD)`.
  The old `poly-ref-bridge` lemma is now unused (still typechecks; leave/delete later).

## IN PROGRESS
`make certified` apex build running (Elaborate + dependents recompiling). When green:
COMMIT the telescope spec win. Then E1-full (concreteness bug fix) as focused work,
then P5 cleanup. NOTE: the 13 cata/closure test failures REMAIN until E1-full (the
elaborator still rejects non-concrete refs — that gate is untouched here).

# ═══ SESSION 2026-07-12 (cont.) — E1-full START (concreteness bug fix) ═══

## POC-B PASSED — E1-full is NOT a 250-site edit
Scratch `Once/POC/AccThreading.agda` (deleted): a HELPER `g` that re-derives a
FRESH `<-wellFounded n` when calling the Acc-carrying worker `f` — with only the
poly-ref case using the threaded `Acc` — TYPECHECKS (Agda's size-change accepts the
composed cycle). ⇒ E1-full keeps the `Acc` on a SHORT worker chain
(`checkElabV-wf` → `checkElab-RVar-wf` → poly aux) + Acc-free WRAPPERS
(`checkElabV ctx e T = checkElabV-wf ctx (<-wellFounded (length polys)) e T`) so the
~300 existing `checkElabV`/`checkElab` call sites stay UNCHANGED. Only the poly aux
resolves inline (recurse `checkElabV-wf` on the body with `rec (lookupPolyPrefix-decreases …)`).

## E1-full stages (each committed + pushed)
1. Acc worker/wrapper scaffolding on checkElabV/checkElab/checkElab-RVar + the
   bbc-other aux (Acc param, wrappers derive fresh); poly case still emits `poly`
   (behaviour unchanged) — just wires the Acc through. Green + certified.
2. Poly aux resolves INLINE: re-elaborate body at use-type in the prefix via
   `checkElabV-wf … (rec decreases)`; produce the resolved Surface.Expr AND the real
   `t-var-poly-instantiate` derivation ⇒ DISCHARGE `bbc-other-poly-witness` postulate.
3. DELETE the `poly` surface node + `resolveExpr`/`resolvePolyCase`/`applySplice` +
   `ResolveFaithful`; drop the poly clauses from `elaborate`/`⟦_⟧ˢ`/`faithful`/
   `Thinning`/`Desugar`/`Properties`; drop `conc` everywhere it was poly-only. Ripple
   adequacy. `make certified` exit 0.
4. `make malonzo` re-extraction + `cabal build` + `tests/run-exit-tests.sh` (expect
   the 13 cata/closure tests to PASS ⇒ ~50 pass). Commit the extraction.
Then P5 cleanup.

## E1-full STEP 1 DONE (commit 9786ae67, certified green, pushed)
`checkElabV-wf` worker (Acc on `length polys`) + Acc-free `checkElabV` wrapper.
POC-B's fresh-Acc-in-wrapper approach VALIDATED on the real 1600-line mutual block
(foetus accepts it). Behaviour-preserving; the ~300 call sites untouched.

## STEP 2 (NEXT) — resolve poly refs INLINE, discharge the 2 poly postulates
`checkElabV-RVar-bbc-other-aux` → add an `-wf` variant taking the Acc; the
`checkElabV-wf` bbc-other case passes `ac`. Body (worked out):
```
aux-wf ctx (acc rec) x T (failure err , _) with lookupPolyPrefix (polys ctx) x in eqLP
... | nothing = failure err , tt
... | just (schema , body , prefix)
    with checkElabV-wf (ctxWithImportsAndPolys (imports ctx) prefix)
                       (rec (lookupPolyPrefix-decreases x (polys ctx) eqLP)) body T | isConcrete? T
...   | success Ψb bodyE db fb , wBody | just conc =
        success zeroUsage (weakenFromEmpty bodyE) db fb ,      -- INLINED body, no poly node
          t-var-poly-instantiate <clsEq> <¬unit> <localN> <importN> eqLP wBody conc
...   | success … | nothing = failure (NonConcreteSigOpType x T) , tt   -- conc STILL gated (step 3 drops it)
...   | failure e' , _ | _  = failure e' , tt
```
- `weakenFromEmpty : SExpr S∅ [] A → SExpr Γ zeroUsage A` (Thinning:415) splices the
  closed body. Ψb : Usage 0 = [] = zeroUsage{0} by Vec-eta (no subst).
- The 4 premises: `<clsEq>` = the `classifyBareBuiltin x ≡ bbc-other` from checkElabV-wf's
  dispatch (thread it in, or `bbc-other-refl`); `<localN>`/`<importN>` from the infer-FAILURE
  via `inferElabV-RVar-fail-bridge ctx x ¬unit …` (see checkElab-fallback-RVar-poly:3429);
  `<¬unit>` — the poly path already excludes "unit".
- This makes the elaborator produce the INLINED body (not a `poly` node) ⇒ DISCHARGES
  `bbc-other-poly-witness` (real derivation) AND lets `resolveExpr`'s poly splice go away.
- MASQUERADE: elaborate now emits `weakenFromEmpty bodyE`; `realize` emits
  `morph-app (elaborate (realize bodyD)) unit`. `RealizeAgrees.check-agreeV-RVar-poly-todo`
  (postulate, line 91) must be DISCHARGED — the two must agree semantically. Also
  `faithful-aux (poly …) () ` (line 1125) becomes vacuous once the node is gone.

## STEP 3 — drop `conc`, DELETE the poly node, ripple
Remove `IsConcrete` from the rule + the bbc-other gate (⇒ non-concrete cata/closure refs
ACCEPTED = the 13-test bug FIXED); delete the `poly` Surface node + `resolveExpr`/
`resolvePolyCase`/`applySplice` + `ResolveFaithful`; drop poly clauses from `elaborate`/
`⟦_⟧ˢ`/`faithful`/`Thinning`/`Desugar`/`Properties`. Drop the `conc` arg from the rule
consumers (Meaning/Realize/Canon*/MeaningBridge/Completeness — 2nd pass, same sites as
the telescope cascade). `make certified` exit 0.

## STEP 4 — re-extract + tests (expect the 13 to PASS ⇒ ~50 pass); commit extraction.

## CORRECTION (2026-07-12) — POC-B was INSUFFICIENT; step 2 needs FULL threading
Attempted step 2 (inline resolution: `checkElabV-RVar-bbc-other-aux-wf` +
`poly-inline-aux` producing the real `t-var-poly-instantiate` derivation via
`weakenFromEmpty` + extracted premises). The DERIVATION construction works (premises
from `inspectLookupLocal`/`inspectLookupImport` `not-found` views + `x ≟ "unit"` +
`classifyBareBuiltin … in eqCls`; usage `[]` by matching `success Surface.[] …`).
BUT foetus REJECTS it: **[TerminationIssue] on checkElabV-wf / checkPair /
checkElabV-RApp-dispatch / …**. Root: a NESTED poly ref inside a def body is reached
THROUGH a helper (checkPair, RApp-dispatch), which calls the Acc-FREE wrapper
`checkElabV` → re-derives a FRESH Acc → resets the structural chain. A mixed cycle
(poly-recursion → helper → poly-recursion) then decreases NEITHER the Acc (reset) nor
the RawExpr (body grows). POC-B missed this — its "body" had no nested-ref-through-helper.

**FIX = thread the Acc through the ~15 recursive members** (checkElabV-wf + checkPair,
checkCase, checkCompose, checkCurry, checkApply, checkIn, checkCata, +Go variants,
checkElabV-RApp-dispatch, checkPairLit, checkElab-RVar). Mechanical: each gets an Acc
param; subexpr recursion passes the SAME `ac` (polys unchanged under extend, so the Acc
type matches); only the poly recursion passes `rec (lookupPolyPrefix-decreases …)`.
Wrappers stay for the ~300 external call sites. (Reduction-dependent proofs like
`checkElab-fallback-RInt` err only as a CASCADE of the termination failure — they
resolve once termination passes.) STAGE-2 CODE (aux-wf/poly-inline-aux) is correct
modulo termination — recorded above; reverted to the green stage-1 checkpoint.

ALTERNATIVE = β raw-inline pre-pass (resolveRaw : PolyCtx → Acc → RawExpr → RawExpr,
scope-aware) BEFORE checkElab ⇒ checkElab stays structural, poly node deletable, but
needs scope-tracking + a resolveRaw-preserves-typing bridge. Full threading is more
mechanical and keeps the clean inline-in-checkElab architecture (discharges the
postulate); β keeps a postulate. LEAN: full threading.

# ═══════════════════════════════════════════════════════════════════════════
# DECISION (2026-07-12) — OPTION C: references are CONTEXT PROJECTIONS (DTT-aligned)
# ═══════════════════════════════════════════════════════════════════════════

After E1-full's inline resolution hit an all-or-nothing ~25-member Acc-threading wall
TWICE, we stepped back to the math. The concreteness "wall" (a reference of arbitrary
type needs a total value — impossible for Void) is an ARTIFACT of representing a
polymorphic internal reference as a MONOMORPHIC opaque FFI value (the `SigOp`
masquerade on `poly`/`closure`). SigOp's `IsConcrete` is CORRECT spec (FFI boundary);
the bug is poly/closure abusing it. This is standard parametric polymorphism:
- monomorphization (Rust/C++/current Once: re-elaborate per use) = options A/B (inline);
- polymorphic values + type application (Haskell Core/System F, Idris2) = option C.
**C is the dependent-types-aligned one** (Agda/Idris/Coq/Lean: defs live in a context Γ,
a reference is a NAME that δ-reduces to its body ON DEMAND; `⟦x⟧Γ = Γ(x)`). Monomorph.
(A/B) CANNOT align with DTT (can't pre-instantiate when types depend on terms;
instantiations can be unbounded; conversion needs shared references, not copies).
Crucially **C has NO totality wall** (Γ(x) is a real value from a well-formed context)
and **NO termination threading** (a reference is an O(1) projection, not an inlined
recursion). USER DECISION: do C — it's the choice that OCP-9 (dependent types) won't
have to redo. Work TOP-DOWN: C at the spec is the authority; rewrite SD/evalᴰ/adequacy
to conform — do NOT preserve what they currently expect ([[feedback_top_down_spec]]).

## The C design
- **Γ = the definition-context** — the telescope of top-level defs AS MEANINGS (each
  def's semantic value, built incrementally over the prefix). This IS the DTT global
  context / signature; the ordered telescope we already landed (5b4c25ac) is its syntax.
- **Reference node** — `poly`/`closure` stop emitting `SigOp (value-info …)`. A
  reference is a NAME/index into Γ (NO `IsConcrete`, NO body carried). (poly = a
  δ-reducible value ref; closure = a δ-reducible fn ref — likely ONE `ref` node,
  possibly two if the call-vs-inline codegen split needs it.)
- **Meaning carries Γ** — `⟦_⟧ᵈ`/`⟦_⟧ᶜ` (Meaning), `SD.⟦_⟧ˢ` (SourceDenote), `evalᴰ`
  (DenotTrace) become Γ-aware; `⟦ ref x ⟧Γ = Γ(x)` (δ-reduction). Cleanest as an Agda
  MODULE PARAMETER (`module … (Γ : DefContext) where`) so Γ is threaded once per module,
  not per call site. Totality from Γ well-formed (no wall). Refs are lookups (no Acc).
- **Codegen** — `ref x` compiles to a call/load of the def's symbol (internal linkage,
  NOT an FFI SigOp; no concreteness constraint). The machine's linked def-code IS Γ;
  adequacy relates `exec (linked module)` ≈ `SD Γ` / `evalᴰ Γ`.
- **Polymorphism** — Γ(x) is the def's meaning at the reference's use-type. In the
  Agda meaning this is a meta-level type-indexed value (a function of the use-type);
  in codegen it's the monomorphized symbol per instantiation. (Once already
  monomorphizes per use at codegen; C keeps the SHARED reference in the meaning/kernel.)

## Honest scale
Largest structural change in 0.58: Γ threads through the meaning functions (SD/evalᴰ)
and the adequacy relates machine-linking to Γ. But it DISSOLVES both blockers (no
totality wall, no termination threading) and is the only DTT-ready option. Module-
parameterization keeps it "threaded once per module."

## Execution (top-down)
1. Define `DefContext` (Γ) — the semantic def-context (telescope of def-meanings).
2. Add the `ref` node (Surface/IR), drop the `SigOp` masquerade from poly/closure.
3. Module-parameterize the meaning by Γ; `⟦ ref x ⟧ = Γ(x)`. Wire at the spec/apex first.
4. Follow the RED down: SD, evalᴰ, RealizeAgrees, Completeness, bridge, machine adequacy
   — rewrite each to the Γ-carrying meaning (do not preserve old expectations).
5. `make certified` exit 0; re-extract + tests (13 cata/closure PASS); P5 cleanup.

# ═══════════════════════════════════════════════════════════════════════════
# OPTION C LANDED (2026-07-13) — certified green, 50/0/2 exit tests
# ═══════════════════════════════════════════════════════════════════════════

Implemented via the analyzed-and-agreed *telescope* route (NOT explicit Γ module
parameters): the acyclic telescope (5b4c25ac) already IS Γ's syntax, and
`t-var-poly-instantiate`'s embedded `bodyD` premise IS Γ(x) materialized — so we
rode the landed splice machinery instead of threading Γ through every meaning
function. Full mechanism write-up: decision-log **D071 → "Implementation" +
"Implementation, part 2"**. Summary:

1. `SigOpInfo.conB : Linkage B` (`ffi-concrete`/`internal-ref`, write-only field);
   `internal-info` builder; `poly`/`closure` drop `IsConcrete`; checkElab poly
   gate deleted; dead `poly-ref-bridge` deleted.
2. Routing: ground-non-concrete own-module defs → telescope entries (split by
   `isConcrete? (extractGround ty g)` in Parser/Resolve + 3 mirror proofs).
3. NEW `t-var-poly-instantiate-infer` (⊢ᵢ, ground defs infer at declared type,
   generic-codomain trick); check rule restricted to non-ground. This rule was
   the missing piece behind the earlier "deadlock" — applied uses (`toInt three`)
   infer the head then splice.
4. Meaning/Realize/MeaningBridge/Canon transports/RealizeAgrees/Completeness/
   ErrorProofs all extended with the mirrored -ᵢ cases.

Residuals: 2 premise-erased witness postulates + 2 RealizeAgrees agreement
postulates (Phase-2-gap pattern); cross-module non-concrete defs still gated
(known, same-module tests unaffected).

Verification: `make certified` exit 0; MAlonzo re-extracted + cabal build under
cgroup cap clean; `tests/run-exit-tests.sh` **50 pass, 0 fail, 2 skip** — the 13
layer5 cata/closure regressions from the 2026-07-11 concreteness migration are
fixed.

# ═══════════════════════════════════════════════════════════════════════════
# D072 PHASE (2026-07-13) — principal-type ORACLE for sig-less defs (Option B)
# ═══════════════════════════════════════════════════════════════════════════

Full design: decision-log **D072**. One-line: sig-less def types are computed by
an UNTRUSTED unification oracle (Hindley principal types — valid because the
language is first-order with fixed generator schemas), then re-checked by the
UNCHANGED verified bidirectional kernel. Check-after-infer = the trust boundary;
soundness free, completeness = ONE future theorem about the oracle. Flips the
two PENDING tests (infer-id, infer-compose) → 52-test suite; delivers D007.

## Milestones
- **M1 — oracle module** `Once/TypeCheck/Principal.agda`: metavariable
  representation over PolyType (reserved `?n` PTVar names), fuel-bounded
  unification with occurs check, builtin schema table (the 13 poly builtins),
  W-style traversal of RawExpr (RVar/RResolved via showCanonical-keyed lookups
  — canon-invariance by construction), def-boundary generalization.
  Deliverable: `principal : NamedCtx → RawExpr → Maybe (Type ⊎ PolyType)`.
  Standalone green. NO proofs about it in M1 (untrusted by design).
- **M2 — ground wiring**: `inferType` (Compile.agda) falls back to the oracle's
  ground answers when `inferElab` fails, guarded by a `checkElab` validation
  call INSIDE the fallback (so `inferType ≡ inj₂ ty` still implies elaborator
  acceptance at `ty` — keeps AcceptSound's shape). Follow the red top-down;
  certified green. Flips **infer-compose**.
- **M3 — schema routing**: sig-less defs whose principal type is NON-ground
  route to `PolyFunInfo` (Parser `extractFunctions-go` + Resolve `polyDefNames`
  + the 3 mirror proofs CanonExtract/CanonReflectExtract/CanonPolyNames —
  IDENTICAL nested with-structure, the D071 gotcha). Flips **infer-id**.
- **M4 — extraction + tests**: ONE malonzo extraction, cabal under cgroup cap,
  un-PEND the two tests, expect 52/0/0.
- **M5 — obligations ledger**: record the oracle-completeness theorem as the
  open obligation replacing the classifier frontier; decision-log D072
  implementation notes.

## Gotchas
- Oracle sees POST-RESOLVER exprs: RResolved must behave identically to RVar
  (showCanonical-keyed lookups) or the Canon transports stop being definitional.
- Purity: PolyType arrows are pure-only (`_P⇒[_]_` + separate `PEff`); v1 infers
  eff structurally and REJECTS purity-polymorphic leftovers (signature required).
- Agda totality: fuel on unification + traversal; exhaustion = inference failure.
