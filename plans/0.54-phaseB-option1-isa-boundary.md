---
parent: 0.54-arith-codegen-correctness
status: active (ARCHITECTURE REVISED 2026-07-19 — top-down spine; see the last section)
date: 2026-07-17
branch: plan-0.54-phaseB-arith-apex-wire
---

> **READ THIS FIRST.** The body below (Option 1 / Option 2, `asm-trace-correct`
> reduction) is SUPERSEDED by the section **"ARCHITECTURE REVISION (2026-07-19)"**
> at the end. The old framing missed that the flat model is itself WRONG for arith
> (the sentinel gap, above `asm-trace-correct`) and that arith's proofs are orphan
> islands on four different machines. Execute the revised top-down spine.

# Plan 0.54 Phase B / Option 1 — make the arith island load-bearing at the apex

## The gap (precise)

The apex field `asm-trace-correct` (`Adequacy/ArchCorrectness/FlatFromObs.agda:69`,
`Adequacy/Compile.agda:189`) is ONE postulate asserting
`asm-sem asm n ≡ flat-trace (moduleToIR m) n`, where
`asm-sem = run-trace ∘ decode ∘ assemble` and all three are **opaque postulated
fields** of `ArchSemantics` (per-arch: `run-trace-x86-64` etc. are `postulate`s).

For the **arith slice**, this hides two claims about each emitted
`once_arith.block.<digest>` subroutine:
1. **value:** it computes `block-semM e` into the result register (`%rax` / io).
2. **non-interference:** it does not clobber CCC-live state — the flat model
   treats the arith SigOp as an atomic `pure-preserves` (`CCC/SigOp/Helper.agda`),
   so the real subroutine must actually preserve CCC registers/memory.

Claim (1) is Phase A, **already PROVEN**: `block-correct : exec-x86 (compile-abs e)
≡ block-semM e` (`Arith/Backend/Correct.agda`), but the proof is ORPHAN.

## Why full elimination is out of scope (→ option 2, a separate plan)

`assemble`/`decode`/`run-trace` are opaque. No property of the real CPU is
provable without an in-Agda assembler + ISA model. So Option 1 does NOT reach
zero postulates; it **reshapes** the trust: proves everything that is the
*compiler's* responsibility, and reduces the residual to a minimal, explicit,
per-instruction ISA axiom (the honest boundary every asm-emitting verified
compiler keeps — cf. CompCert's assembler/linker trust).

## Structure — ONE generic obligation, discharged per arch (the apex stays generic)

The apex must NOT mention any concrete arch. It already parameterizes over
`ArchSemantics` / `FrameSemantics` / `RegConvention`; the arith correctness is
the same shape: **one generic obligation the apex depends on, each arch
instantiates.** No per-arch wiring in `Adequacy/`.

```
-- Once/Arith/Backend/Adequacy.agda  (the generic interface the apex consumes)
record ArithBackendCorrect (RC : RegConvention) (as : ArchSemantics) : Set where
  open RegConvention RC
  field
    -- ① preserves-CCC: the emit's clobber footprint is CCC-disjoint.
    writes    : XInstr → List Reg
    confined  : ∀ i → All (λ r → owner r ≢ ccc) (writes i)   -- x86-64/riscv64: refl;
                                                             -- x86-32: restore-correctness
    -- ② ISA faithfulness (the honest residual axiom, shared with CCC codegen):
    --    the CPU runs the emitted arith asm as `exec-x86` says, writing ⊆ `writes`.
    isa-faithful : <the per-arch ISA seam>
```

- **Arch-neutral, PROVEN once:** `block-correct : exec-x86 (compile-abs e) ≡
  block-semM e` (`Backend/Correct`, width-generic — done). The compiler LOGIC.
- **Generic discharge (once):** `Backend/Adequacy` derives the arith slice of
  `asm-trace-correct` from `ArithBackendCorrect` + `block-correct` +
  `pure-preserves` — uniformly, for any arch.
- **Per-arch instance:** each `Arith/Backend/<arch>/Confine` supplies `writes` +
  `confined` (field ①); each declares its `isa-faithful` axiom (field ②).
  `X86-64/Confine.agda` is the x86-64 witness for ① — it is NOT apex-wired.
- **Apex:** `FlatFromObs`/`Compile` take `ArithBackendCorrect` as a parameter and
  discharge the arith slice generically. A fourth arch = one new instance, zero
  apex edits.

`confined` is stated over the shared `RegConvention` (`owner`/budget), so field ①
is itself generic; each arch only provides `writes` + the witness.

## The one structural obstacle + fix

`instr-text : XInstr → String` emits strings, so register footprints are not
inspectable ⇒ confinement can't be stated. **Fix (the "shared/structured emit"):**
interpose a concrete physical-instruction AST:
`XInstr → PhysAsm Reg → String`, where `PhysAsm` carries register operands
structurally and `render : PhysAsm → String` is a pure printer. Then:
- `phys-writes : PhysAsm Reg → List Reg` is readable, and
- `confined : ∀ i → All (λ r → owner r ≢ ccc) (phys-writes (lower i))` is
  structural (`arith-reg` image ⊆ arith, rax ∈ io).
- `instr-text i ≡ render (lower i)` keeps the emitted text identical (`refl`),
  so no codegen/test change. This structured layer is also exactly what option 2
  (in-Agda assembler) would build on.

## Three-arch obligations

- **x86-64 / riscv64:** confinement is definitional — `phys-writes ⊆ arith ∪ io`,
  `owner` distinguishes `ccc` by distinct constructors ⇒ `refl`/`()`.
- **x86-32 (k=0):** arith BORROWS ccc registers (`arith-borrows : owner
  (arith-reg x) ≡ ccc`). Confinement is FALSE here; instead prove
  **restore-correctness**: the emit brackets borrowed regs with save…use…restore
  so their net footprint is empty. A bounded induction over the emitted bracket.

## CORRECTION (2026-07-17) — the concrete machine EXISTS; the gap is bounded

Earlier this plan overstated the gap as a months-long "build a machine." Re-audit:
**`X64.run = exec defaultFuel` IS a concrete state machine** (fetch → `execInstr`
→ loop over a concrete `State` = RegFile + `Memory : Word → Maybe Word` + flags +
pc + halted). And `Preserve.agda`'s `write-nonccc-agrees`/`step-of` already run on
its REAL `RegFile` (`X64.Semantics.writeReg`) — so **register-preservation is
already proven over the actual machine**, not a mock.

What's actually missing is bounded:
1. `execInstr syscall = halt`, `call-sym = halt` — `run` STOPS at the first SigOp.
   `run-trace` = a variant that RECORDS the event (from register args) and
   CONTINUES. A bounded instrumentation of the existing loop at TWO instruction
   clauses (Plan 0.44 emit-and-continue), not a new machine. Arith blocks are
   Pure (no syscall/call-sym), so they emit nothing and only transform state.
2. Arith's instructions live in its own `XInstr` (the "spread-out" half). Unify by
   giving `XInstr` a concrete interpretation over the SAME `X64.State`
   (`exec-arith-instr : XInstr → State → State`) — register writes via the real
   `writeReg` (preservation already done), scratch via `writeMem` at `rsp-offset`.
3. Memory-preservation = a disjointness argument mirroring register-ownership:
   arith scratch writes at `< rsp`; CCC memory at `≥ rsp`; `a ≢ addr` ⇒
   `readMem (writeMem …) a ≡ readMem … a` (writeMem is a one-address update).
4. Residual: `assemble`/`decode` (text↔program, GNU-as/decoder) — the honest
   external boundary, kept as an axiom or a concrete encoder later.

So the path is UNIFY (arith onto X64.run) + INSTRUMENT (run-trace) + the
assemble/decode residual — all bounded, reusing X64.run. Not months.

## (superseded) earlier note — apex-wiring "gated on the concrete machine"

Investigating the wiring revealed the confinement proof cannot REDUCE
`asm-trace-correct` on its own:

- `asm-sem = run-trace ∘ decode ∘ assemble`. All three are **postulated
  per-arch** (`run-trace-x86-64`, `decode-x86-64`, `assemble-x86-64`, …).
- `run = X64.run` IS concrete (real ISA step semantics) — but you only reach it
  via `decode (assemble text)`, which is opaque, so you never obtain a concrete
  `Program` to feed `run`.
- `confined` is a property of the emitted TEXT. Connecting it to EXECUTION needs:
  (a) concrete `assemble`/`decode` (text → inspectable `Program`), and
  (b) `run-trace` DERIVED from `run` (the Plan-0.44 emit-and-continue machine;
      today `run-trace` is postulated "until then").
  Both are exactly the in-Agda assembler + CPU-observable infrastructure = the
  **Option 2** foundation.

**So Option 1's apex-wiring is a PREREQUISITE-of-Option-2 situation, not a
follow-on.** What Option 1 delivers standalone: the **proven, generic
confinement obligation + per-arch witnesses** — the exact foundation the
concrete-machine work will consume to split the register-preservation content
out of the monolithic postulate. The trust REDUCTION lands when (a)+(b) exist.

Landed this session (green, committed): the generic `ArithEmitConfined`
obligation + x86-64/riscv64 witnesses. x86-32 (block-level restore) and the
apex-wiring wait on the concrete-machine layer.

## Residual trust after Option 1 (the explicit end-state)

Per arch, exactly:
1. `assemble`/`decode` text↔bytes faithfulness (or folded into 2).
2. `run-trace` executes a straight-line block per the per-instruction ISA model,
   and respects `call`/`ret`.

These replace the whole-program `asm-trace-correct`. The arith register
allocation / spilling / lowering LOGIC (the parts that could harbor compiler
bugs) is then PROVEN, not trusted.

## Sequencing

1. ✅ **x86-64 confinement PoC** — `Arith/Backend/X86-64/Confine.agda`:
   `writes` + `confined : ∀ i → All NotCCC (writes i)` (green, committed).
2. **Generic obligation** — `Arith/Backend/Adequacy.agda`: the
   `ArithBackendCorrect` record (fields ①②) + the arch-neutral discharge of the
   arith slice from it (`block-correct` + `pure-preserves`).
3. **x86-64 instance** — assemble `Confine` (①) + the `isa-faithful` axiom (②)
   into an `ArithBackendCorrect` value.
4. **Apex wiring** — `FlatFromObs`/`Compile` take `ArithBackendCorrect` and
   discharge the arith slice of `asm-trace-correct` GENERICALLY.
5. **riscv64** instance (definitional, mirrors x86-64); **x86-32** instance
   (restore-correctness for field ①).
6. Green: apex + `make certified`; tests stay 266/266 and 55/0/0 ×3.

Note: step 4's decomposition may need a compositionality axiom on
`assemble`/`run-trace` over the subroutine boundary — if so it is an EXPLICIT
part of the residual (field ②'s honest boundary), surfaced, not hidden.

## Guardrails

- Frontend never touched. Work is `Arith/Backend/`, `Target/`, `Adequacy/`.
- No new axioms beyond the explicit ISA-model residual; NO funext.
- `instr-text ≡ render ∘ lower` must be `refl` so emitted asm is byte-identical
  (tests unchanged). Stop and surface if it isn't.

---

# ARCHITECTURE REVISION (2026-07-19) — the four-machine diagnosis + the top-down spine

## Why the old plan kept producing orphan bottom-up work

An investigation pass (three parallel readers over the flat machine, the
concrete↔flat bridge, and the concrete arith machine) found the real problem:
**arith has FOUR machines and every arith proof is an orphan island on a
different one.** The old plan aimed all effort at reducing `asm-trace-correct`
(the concrete↔flat seam) and never noticed the flat model is itself wrong for
arith — a gap that sits ABOVE that seam.

| # | Machine | Arith is… | Proof on it | Wired to apex? |
|---|---------|-----------|-------------|----------------|
| 1 | `FlatState` (`flat-events`, the APEX machine) | a `Pure instr-sigop`, output = **sentinel `0`** | `traces-agree` (pure slice real; cata/rest postulated) | yes — but arith VALUE is a sentinel |
| 2 | `ArithAbsState` (word-level XInstr) | `exec-xprog` | **`block-correct`** (value, 0 postulates) | **no** — zero importers |
| 3 | `X64.State` + `val` (concrete registers) | `exec-arith-block`/`dispatch-arith` | 5 cores → **`dispatch-arith-preserves`** | **no** — orphan; `run-events` uses the *function*, never the *proof* |
| 4 | asm text | `emit-arith-block` string | — | what actually runs |

Decisive facts:
- **`flat-sim` does not exist** — it is a comment-only name. The real
  concrete↔flat bridge is the postulate pair `asm-trace-correct` + `ir-flat-correct`
  over an **undefined** `flat-trace`; `flat-from-obs` even DISCARDS the abstract
  theorem it is handed. No real proof depends on the concrete machine.
- **`val` (concrete per-XInstr semantics) is a postulate** (`val-x86-64`). Machine
  #3 — the one now wired into `run-trace` — rests on admitted semantics.
- **`block-correct` is on machine #2, not #3** — it proves the WORD machine, not
  the concrete `val` machine, so it does not even mention the state `run-trace`
  runs on. `block-correct` and `dispatch-arith-preserves` are both orphan.
- **The sentinel is a real correctness gap, not mere opacity.** Arith is `Pure`,
  so it emits `[]` on BOTH machines ⇒ its TRACE contribution is trivially `[]`.
  Arith's value matters to the observable ONLY when it feeds a downstream
  Emits/Halts SigOp argument — and there the flat machine decodes the sentinel
  `0`, not the true result. That discrepancy is what's swept into the
  `cata-correct` / `obs-correct-rest` postulates. So `block-semM` is needed to
  FIX the flat model, not merely to bridge to concrete.

Root cause: the arith verification was built **bottom-up as independent
islands** — each layer got a bespoke machine and a locally-sound proof, but no
**spine** was built to compose value → registers → asm → apex-trace. Classic
`feedback_prove_in_real_path_not_pocs` at architectural scale.

## Target architecture — one spine, ONE trusted boundary

```
source obs
  ≡  traces-agree           ← RUNG A: arith value = block-semM at the ABSTRACT layer (kill the sentinel)
flat-events (abstract, REAL arith values)
  ≡  concrete↔abstract refinement; arith slice = dispatch-arith-preserves + block-correct(on val)   ← RUNG B
run-events / concrete asm execution
  ≡  ISA / printer / loader trust — SHARED with all non-arith code (the ONE honest external seam)
hardware
```

Four principles:
1. **Kill the sentinel (rung A).** Give the flat arith SigOp a real value from the
   `SigOpInfo.semM` (`block-semM`) it already carries — read the input registers,
   apply `semM`. This unifies machines #1 and #2; arith value correctness now
   lives WHERE THE APEX REASONS, and `block-correct` becomes the load-bearing
   content of `traces-agree` for arith-fed observables.
2. **Define `val`, don't postulate it.** The concrete XInstr semantics must be a
   real function; then re-target `block-correct` (or a companion) to prove the
   `val`-machine arith equals `block-semM`, moving it off orphan `ArithAbsState`.
3. **Define `flat-trace` and decompose `asm-trace-correct` (rung B).** Replace the
   wholesale axiom with a real concrete↔abstract refinement whose ARITH slice is
   discharged by `dispatch-arith-preserves` + the value equality, and whose
   NON-ARITH slice is the ISA/printer/loader trust shared with every instruction.
   This is where the preservation tower stops being orphan.
4. **One trusted boundary** — the same one all non-arith code already relies on.
   No arith-specific trust.

## Top-down execution order (this REPLACES the "Sequencing" section above)

Build DOWN from the apex; make each rung load-bearing before descending.

- **Rung A — flat arith value (the top of the spine).** Target the apex obligation
  `ir-flat-correct`/`traces-agree` directly. The value device `value-realized`
  (`IRObsCorrectFlat.MachineRefinesObsF`) references the real `eval ir x`, but its
  primitive leaf `valid-int-wf`/`valid-float-wf` (`ClosureWellFormed.agda`) was
  LOCATION-ONLY — so a Pure-SigOp sentinel `SV-Lit 0` satisfied it vacuously and no
  arith value was ever pinned. Rung A strengthens that leaf and follows the forced
  ripple DOWN to `pure-sigop-output`. NO concrete machine; do NOT build any new
  machine or preservation lemma in this rung. **Concrete ordered sub-steps (each
  strict top-down, each an apex-path edit; do IN ORDER):**

  - **A1 — strengthen the primitive value leaf (DONE 2026-07-20).** `valid-int-wf`
    gains `readLoc s loc ≡ just (SV-Lit fits-intˢ n)` (the stored value = the
    semantic value). Load-bearing definitional change everything keys off.
  - **A2 — the `prim-sv` bridge + generic premise.** Define `prim-sv : FitsInRegI B
    → ⟦ B ⟧ᴵ → StoredValue FS` (crossing the 0.52-M2 IRTy/Type seam ONCE), so the
    premise reads `readLoc s loc ≡ just (prim-sv fit v)` uniformly. Restate
    `valid-float-wf` to match, and thread the premise through the `valid-primitive-wf`
    wrapper (`ClosureWellFormed.agda:283`). `ClosureWellFormed` green.
  - **A3 — chase the red DOWNWARD through validity-construction sites.** Every place
    that builds a primitive `ValidAtWF`/`valid-primitive-wf` witness now must PROVE
    `readLoc s loc ≡ just (prim-sv fit v)` — i.e. establish the real stored value.
    - **A3.0 `ClosureWellFormed` in-module ripple — DONE 2026-07-20 (green).** The 8
      preservation lemmas thread the premise via their existing transports;
      `LocsInRegions` for primitives strengthened `⊤ → LocInRegions … loc` (a value
      leaf must sit in a preserved region), consumed by the strong lemma.
    - **A3.1 downstream — TIGHT (4 files, ~8 sites; no external `LocsInRegions`
      ripple).** `ValidAtWFHalted` (2 mechanical set-halted transports);
      `SigOp/Helper:205` + `IRObsCorrectFlat:246` are the two FORCING sites (Pure-SigOp
      primitive validity — where the sentinel becomes a type error → drives A4).
  - **A2.5 — BOTH residences (register + spill), via the existing `ResultPlace`
    (design steer 2026-07-20).** A value lives in a register (fast path) or spills to
    a memory slot under register pressure — the arch's register count sets how much
    (x86-32 8 / x86-64 16 / riscv 32). The flat machine is arch-generic, so the value
    obligation is `Place`-parameterized, and `ResultPlace` already IS that split:
    `at-loc` (memory: `Output ≡ SV-Ptr loc`, value at `loc`) and `at-reg` (register:
    value in `Output`). A1's `readLoc` premise is the **spill/memory half**; the
    **register half** is `at-reg` recording `readReg Output ≡ prim-sv fit v` instead
    of its `unit-storedvalue` sentinel (`ClosureWellFormed:372`). The arith result is
    register-resident (`at-reg`) ⇒ NO memory write, NO invariant break. The
    register-vs-spill *count* per arch is **rung B** (concrete machine — where the 5
    Option-2 cores + x86-32 borrow/restore already live). `IRObsCorrectFlat`'s
    `value-realized` (currently memory-only `ValidAtWF`) generalizes to the same
    `Place` split so a register-resident result discharges it.
  - **A4 — the flat Pure-SigOp VALUE MODEL (bigger than one line).** `SigOp/Helper`'s
    `mkPurePrimResult` models a Pure SigOp as `mov-to-output` (copies the INPUT
    pointer to Output; asserts the result at the input loc with the input's value —
    computes nothing). To satisfy the strengthened leaf, the flat model must actually
    COMPUTE and STORE the output: `exec-abstract (instr-sigop si)` writes the real
    `semM` value (not the `SV-Lit 0` sentinel of `pure-sigop-output`, `SMCore:1332`),
    the emitted trace stores it at the output location, and `mkPurePrimResult`
    establishes `readLoc … ≡ just (prim-sv fit (semM si ⟨inputs⟩))`. Reconstruct the
    shaped input (unit/int/pair) via `readLoc` at `regs s Input1/Input2`. `block-semM`
    = this SigOp's `semM`, so arith blocks ride the same clause — NOT arith-special.
  - **A5 — close the apex obligation.** `pure-obs-correct-sigop`'s `value-realized`
    now closes with the real value; the arith-fed Emits/Halts case of
    `obs-correct-rest` becomes dischargeable (the machine event's ℕ arg = obs's).
    Deliverable: `block-correct`/`semM` is load-bearing at the apex; the flat model
    emits real args; the sentinel is gone.
- **Rung 2 — concrete `val`.** Define `exec-arith-instr : XInstr → X64.State →
  X64.State` concretely (kill `val-x86-64`); re-target `block-correct` onto it (or
  prove `val` refines `ArithAbsState`).
- **Rung B — `flat-trace` + `asm-trace-correct` decomposition.** Define
  `flat-trace = take n (flat-events (EF…) …)`; decompose the seam; wire the arith
  slice to `dispatch-arith-preserves` + rung-2 value equality. Non-arith slice
  stays the honest shared ISA/printer/loader axiom.
- **Then** riscv64, then x86-32 — each a per-arch instance, zero apex edits.

Discipline: each rung starts from the apex obligation it discharges and works
DOWN. If a rung needs a lemma that isn't yet forced by an apex obligation, that is
the signal it's drifting bottom-up — stop and re-anchor at the apex.

## Status of in-flight work (2026-07-19)

- `Once/Arith/Backend/RunTraceCore.agda` (generic emit-and-continue + Behavior
  adapter) + the x86-64/riscv64 `RunTrace` thin instances: green, KEEP.
- `Adequacy/CPU/X86-64.agda`: `run-trace-x86-64` derived from the core with named
  postulates `val-x86-64`/`arith-env`/`ev`/`step-budget`. KEEP as scaffold; rung 2
  eliminates `val-x86-64`.
- These are correct plumbing but NOT yet load-bearing — rung A is what makes an
  arith proof (`block-correct`) actually carry weight at the apex.

---

# RUNG B step 2 — `conc-flat-sim` decomposition (2026-07-21)

## Status recap (all committed, apex green at each step)
Rung A is complete except `comp-step` (the general `_∘_` clause — NOT arith).
The arith VALUE is load-bearing: `pure-sigop-value-correct` (both residences),
`ir-flat-correct`, the size bounds, and the entry state are all PROVED.

Rung B step 1 (`83476fd4`): `asm-trace-correct` localised to the per-arch
instances. Step 2 STRUCTURE (`73d518d7`): split into

  asm-trace-correct-<arch> = trans loader-faithful conc-flat-sim
    conc-trace ir := run-trace (compile-trace (ir-to-trace ir)) initialState  -- DEFINED
    <arch>-loader-faithful : asm-sem ≡ conc-trace          -- TRUST (GNU as class)
    <arch>-conc-flat-sim   : conc-trace ≡ flat-trace        -- PROVABLE (postulated)

`loader-faithful` STAYS trust (assembler + loader + printer + decoder). The
target is `conc-flat-sim`.

## What `conc-flat-sim` needs — and what already exists

`conc-flat-sim : conc-trace mir ≡ flat-trace mir`. Both sides start from the SAME
`ir-to-trace mir` (arch-generic AbstractTrace). Difference is the interpreter:
  * flat side — `flat-events` interprets each AbstractInstr over abstract LocState.
  * conc side — `compile-trace` lowers each AbstractInstr to concrete arch Instr,
                then `run-events` steps it over concrete State.
So it factors as a per-instruction simulation with an arch-generic SKELETON.

THE FOUR-MACHINE REALITY (why this is real work despite existing proofs):
  1. FlatState / flat-events   — CCC/abstract (the apex machine). Rung A made it
     COMPUTE `semM`.
  2. ArithAbsState             — arith's OWN toy machine; `block-correct` lives
     HERE (`exec-xinstr : XInstr → ArithAbsState → ArithAbsState` proves XInstr
     execution = arith `semM`). Disconnected from 1 and 3.
  3. X64.State + `val`         — concrete; `dispatch-arith-preserves` + 5 cores
     live HERE, but PRESERVATION-ONLY and parameterised by an ABSTRACT `val`.
     `val-x86-64` is a POSTULATE.
Machines 1 and 2 were separate BY DESIGN (CCC treats an arith block as an opaque
Pure SigOp), so the 1↔3 simulation was never built. NOTHING relates flat-events
to run-events today.

## The reuse path (substantial but SCOPED — reuses the arith proofs)

Decompose `conc-flat-sim` AGAIN by instruction class:
  * ARITH-BLOCK case — PROVABLE from the existing material, once the connective
    tissue is built. This is the branch's goal.
  * NON-ARITH case — keep a NARROW per-instruction ISA-lowering axiom (do not
    re-prove the whole backend). Reduce, don't eliminate.

Concrete ordered sub-steps (each strict top-down from `conc-flat-sim`):

- **B2.1 — the generic `SimCore`.** Arch-generic simulation skeleton: fuel
  induction over `ir-to-trace`, event-agreement (both sides' `SigOpEvent` lists
  match), threading a state-relation `R(LocState, State)`. Parameters: concrete
  State, `execInstr`, `compile-abstract`, `R`, and the per-instruction step
  lemmas. Mirrors `IRObsCorrectFlatness` / the 5 arith cores (write ONCE).
- **B2.2 — define `val`** (kills the `val-x86-64` postulate). `val i s r` =
  read result reg after `exec-xinstr`-style concrete step. This is rung-2 from
  the original spine, scaffolded but never done.
- **B2.3 — bridge `block-correct` onto `X64.State`.** A representation relation
  `ArithAbsState ↔ X64.State` (regs/scratch ↔ concrete regs/mem), transferring
  machine-2's value logic (`exec-xinstr = semM`) to machine 3 (`val` = `semM`).
- **B2.4 — arith-block case of `conc-flat-sim`** = B2.3 value (`= semM`, matching
  flat's rung-A `semM`) + `dispatch-arith-preserves` (CCC state preserved under
  R) + event-triviality (arith Pure ⇒ both sides emit `[]`). Wire into SimCore's
  arith-block case.
- **B2.5 — non-arith ISA axiom.** The narrow per-instruction lowering-faithfulness
  for the non-arith AbstractInstr, as SimCore's non-arith parameter. Named trust.
- **B2.6 — instantiate** SimCore per arch. x86-64 first; riscv64 mechanical
  mirror; x86-32 also needs `BorrowRestoreCore` (borrow/restore of CCC regs).

Discipline: start from `conc-flat-sim` (the apex-consumed obligation) and work
DOWN. `block-correct` / `dispatch-arith-preserves` are RAW MATERIAL — only wire
them in where B2.4 actually consumes them (else island anti-pattern).
