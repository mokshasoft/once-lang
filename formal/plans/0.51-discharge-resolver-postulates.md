# Plan 0.51 (discharge) — prove the three ResolverBridge postulates

status: ready-to-execute (2026-06-27). POC done; NOT blocked by 0.50.

## Goal
Replace the three postulates in `Once.Adequacy.ResolverBridge`
(`resolver-preserves-typing`, `resolver-reflects-typing`, `resolver-preserves-trace`)
with proofs, for the IMPORT-FREE fragment (the import case → residual `*-imports`
postulates, keeping the apex interface general).

## Key facts (verified)
- `resolveImports mm (mkModule ds)` = inline import sigs (per `DImport`) + map
  `canonDecl` over decls. **Import-free** (no `DImport`) ⇒ just `canonDecl` (=
  `canonExpr [] um am` on each `DFunDef` body; `um=[]`,`am=[]` when no imports).
- `canonExpr` only rewrites a FREE NON-BUILTIN `RVar x` → `RResolved (canonical [x])`
  (`canonVar`, Resolve.agda 198-211). Local/bound names and the 18 `isBuiltinName`
  names (id,fst,snd,inl,inr,unit,pair,terminal,initial,curry,apply,compose,arr,
  case,cata,ana,In,Out) stay `RVar`. **isBuiltinName ⊇ all 16 judgment-special
  heads** (verified), so every special rule's head is preserved.
- `bare x ≡ canonical [x]` and `showCanonical (canonical [x]) ≡ x` are DEFINITIONAL
  (Once.CanonicalName 40-53). `t-var-import`/`m-named` premise `lookupImport ctx x`
  and `t-var-resolved`/`m-named-resolved` premise `lookupImport ctx (showCanonical cn)`
  are then the SAME term ⇒ the var map is just `t-var-resolved <same proof>`.
- `ModuleTyped` = inductive `⊢ᶜ` (AcceptSound: "ONLY the judgment, no elaborator").

## Step 1 — foundational lemmas (standalone, commit green first)
New module e.g. `Once.Adequacy.CanonPreserve`.
- `BLA ctx bound` := `∀ x → elemStr x bound ≡ is-just (lookupLocal ctx x)` (or the
  two implications bla-local / bla-import). Needs `lookupLocal`/`extendNamedCtx`
  (Classify.agda 174-235) + `elemStr` (Resolve 167-171).
- `bla-nil`   : `BLA ctx []` when ctx has no locals (initial fun ctx) — verify
  `lookupLocal initialCtx x ≡ nothing`.
- `bla-extend`: `BLA ctx bound → BLA (extendNamedCtx ctx x A) (x ∷ bound)`.
- `classify-canon` : `classifyAppHead f ≡ nothing → classifyAppHead (canonExpr bound [] [] f) ≡ nothing`
  (canonExpr can't CREATE a builtin head; Classify 346-386).
- helper: for free non-builtin x, `canonExpr bound [] [] (RVar x) ≡ RResolved (canonical [x])`,
  and for builtin/bound x, `≡ RVar x` — by `elemStr`/`isBuiltinName` case (use BLA to
  align "x not local" ⇒ elemStr false).

## Step 2 — mutual preservation (ATOMIC ~48 cases; commit when green)
`canon-pres-ᵢ/ᵍ/ᵐ/ᶜ : BLA ctx bound → ctx ⊢_ e ∶ … → ctx ⊢_ (canonExpr bound [] [] e) ∶ …`
Case shapes:
- literals/unit (t-int,t-str,t-unit,g-int): canonExpr is id ⇒ refl / same rule.
- t-unit-var, g-terminal, m-id…m-inr: head is builtin ⇒ kept; lookup-fail premises
  unchanged (ctx fixed) ⇒ re-apply same rule.
- t-var-local: BLA ⇒ elemStr true ⇒ canonExpr keeps RVar x ⇒ same rule.
- t-var-resolved / m-named-resolved: RResolved unchanged ⇒ refl.
- t-var-import / m-named: x not local ⇒ (BLA) elemStr false; case isBuiltinName x:
  true → kept (same rule); false → `RResolved (canonical [x])`, apply t-var-resolved/
  m-named-resolved with the SAME `lookupImport` proof (showCanonical (canonical [x]) = x).
- t-var-qualified: import-free ⇒ am=[] ⇒ canonExpr's RQualified→(lookup nothing) branch;
  but a qualified ref can't be import-free-well-typed (no qualified import in table) ⇒
  rule out via the premise, or handle the am-nothing branch.
- congruence: t-annot,t-pair,t-neg,t-binop×2,t-id/fst/snd/terminal/arr/apply-app,
  g-pair,g-inl,g-inr,g-In,m-compose,m-case,m-pair,m-curry,m-cata,m-arr,m-const,
  t-morph-lift,t-embed — recurse (IH) on sub-derivations, re-apply rule. The *-app
  rules keep the builtin head; m-compose/case keep `RApp(RApp(RVar "compose")…)`.
- t-app/t-effApp: use `classify-canon` for the `classifyAppHead … ≡ nothing` premise;
  IH on f (⊢ᵢ) and x (⊢ᶜ).
- binders t-lam/t-let/t-case: canonExpr extends bound by the binder; use `bla-extend`
  for the IH on the body/branches; note canonExpr (RLam x b)=RLam x (canonExpr (x∷bound) b)
  etc. (Resolve 214-218).
- m-cata: alg checked in `ctxWithImportsAndPolys …` (locals reset) ⇒ BLA [] there;
  canonExpr recurses with the SAME bound (cata alg uses `canonExpr bound` — check it
  resets or not; if alg is closed, bound is irrelevant). Verify against Resolve's RAna/cata clause.

## Step 3 — reflection (reverse direction)
`canon-refl-ᵢ/…` : `ctx ⊢_ (canonExpr … e) ∶ … → ctx ⊢_ e ∶ …`. Mirror Step 2; the
var case maps t-var-resolved (canonical [x]) BACK to t-var-import (need: the RResolved
in the image came from a free RVar — true since canonExpr only produces canonical [x]
from RVar x). Cleanest: induct on e (not the derivation) and invert.

## Step 4 — lift to module + discharge (1) & (2)
- `extractFunctions`/`ModuleTyped`/`AllFunsTyped` (AcceptSound 80-101): mU and mR
  (import-free) share signatures ⇒ same ctx; per-fun bodies differ by canonExpr ⇒
  apply Step 2/3 per `AllFunsTyped` cons. `HasValidMain-decl` (ModuleComplete):
  main's type unchanged (signatures unchanged) ⇒ preserved.
- import-free split: `NoImports mU` (no DImport). `resolver-preserves-typing` etc.
  case on `decidable NoImports`; import-free branch = proven; else residual postulate
  `resolver-preserves-typing-imports`. Provide `resolveImports mm mU ≡ inj₂ mR` from
  the import-free reduction of `resolveDecls` (all non-import ⇒ inj₂ (map canonDecl)).

## Step 5 — trace, discharge (3)
`moduleToIR mod = moduleToIR-aux (compileResolvedModule Heap false mod)`. Need
`compileResolvedModule Heap false mR ≡ compileResolvedModule Heap false mU` (import-free),
then `cong (⟦_⟧IR ∘ moduleToIR-aux)` + per-n refl. compileResolvedModule elaborates
each body via the 3445-line elaborator (`checkElab`/`inferElabV`). Two routes:
  (a) prove `checkElab ctx (canonExpr…e) T ≡ checkElab ctx e T` by elaborator induction
      (heavy; bare=canonical makes the sigOp leaf definitionally equal — Builders.agda
      `value-info` depends only on `showCanonical`); OR
  (b) reuse compile-correctness: if the elaborated SURFACE term is determined by the
      ⊢ᶜ derivation (Step 2 gives corresponding derivations), the IR coincides.
Route (a) is the direct one; scope it after (1)&(2) land. THIS is the heavy third.

## UPDATE 2026-06-27 (execution findings — `Once.Adequacy.CanonPreserve`)

LANDED GREEN (committed): foundational layer of `CanonPreserve` —
  - `llg-just→elem` (lookupLocal-go found ⇒ in `names`, by direct `Ctx` induction —
    THE way past `lookupLocal-go`'s with-opacity; the with drives both lookupLocal-go
    AND elemStr when the binder is applied directly) + `lookup-just→elem`.
  - `canon-RVar-keep`/`-resolve`, `canon-builtin`, `∨-true`, `classify-canon` (~25 cases).
  - `_⊆ᵇ_` + `⊆ᵇ-refl`/`-nil`/`-cons` (cons drives elemStr in-branch, no opacity).
ALSO LANDED (Plan 0.50 Stage 3, separate commit): `domainOfHead`/`composeArgB` get
  `RResolved` clauses (lookup `showCanonical cn`) — additive, Elaborate+MorphComplete green.

KEY REFORMULATION: state preservation with an EXPLICIT `bound : List String` param +
  hypothesis `names(named ctx) ⊆ᵇ bound` (subset, NOT equality). Then:
  - t-var-local: `lookup-just→elem` + the subset ⇒ `elemStr x bound ≡ true` ⇒ keep.
  - t-var-import: just CASE `elemStr x bound ∨ isBuiltinName x` (no hypothesis) —
    true⇒keep⇒same rule; false⇒RResolved⇒`t-var-resolved` with the SAME lookupImport
    proof (`showCanonical (canonical [x]) = x` definitional).
  - binders (t-lam/t-let/t-case): recurse with `bound'=x∷bound`, `⊆ᵇ-cons sub`.
  - m-cata: alg is in a locals-RESET ctx (`ctxWithImportsAndPolys`); apply pres at it
    with the SAME outer `bound` and `⊆ᵇ-nil` (names ∅ = [] ⊆ᵇ bound, vacuous). canonExpr
    threads the outer bound into the cata body, so this matches — the misalignment is GONE.

THE HARD CASE — `m-compose` (and `m-case`): premise `composeMid ctx f g A ≡ just B`
  routes through `domainOfHead`/`composeArgB`, which have literal patterns (`RVar "arr"`,
  `RVar "fst"`…) that are STUCK for an abstract head, and `domainOfHead-canon`/
  `composeArgB-canon` are FALSE as general lemmas over abstract f/g (canonExpr
  RVar→RResolved makes a stuck term vs `nothing`). Resolution: in the m-compose case,
  CASE the `⊢ᵐ f` / `⊢ᵐ g` sub-derivations to expose concrete shapes, THEN domainOfHead/
  composeArgB reduce and invariance holds case-by-case (RVar named: definitional via
  showCanonical; builtins: kept by isBuiltinName; arr-app: recurse). ~80-150 lines for
  m-compose alone. This is the bulk of the remaining mutual work. Everything else
  (literals/vars/builtins/structural/binders/m-cata) is short.

## UPDATE 2026-06-27 (resolver poly-name bug — FIXED, commit `Fix B`)

Discovered + fixed a LATENT BUG the `resolver-preserves-typing` postulate was
hiding: the resolver canonicalized own-module POLYMORPHIC-def references to
`RResolved`, but poly defs are INLINED by `t-var-poly-instantiate` (bare-`RVar`
only) and live in `polys`, not `imports` → resolved poly name = "unbound".
Repro: `tests/poly-bare-ref.once` (a MONO bare ref resolves fine; a POLY one
failed). Fix: `Resolve.polyDefNames` (DTypeSig with non-ground type, mirroring
`extractFunctions`' ground/poly split) is threaded as `canonDecl`'s initial
`bound`, so poly names are kept bare by the SAME dispatch as locals. **No
`canonExpr`/`canonVar` signature change** ⇒ `CanonPreserve` stays green.

IMPACT ON THIS PLAN (Step 2): the `t-var-poly-instantiate` case of the mutual
preservation now has a CLEAN discharge. canonExpr keeps `RVar x` iff
`elemStr x bound ∨ isBuiltinName x ≡ true`; since `canonDecl` seeds `bound :=
polyDefNames`, every poly name is in `bound` ⇒ canonExpr is the IDENTITY on it ⇒
re-apply `t-var-poly-instantiate` with the IH on the inlined body. The needed
hypothesis is `(poly names of ctx) ⊆ᵇ bound`, true by construction at `canonDecl`
(`bound = polyDefNames ds`). Add this alongside the existing `names(named) ⊆ᵇ bound`.

## UPDATE 2026-06-27 (Steps 2+4 LANDED — canon-pres wired into the apex)

- **Step 2 (`Once.Adequacy.CanonPreserveMutual`, GREEN)**: `canon-pres-ᵢ/ᵐ/ᶜ` —
  ~54-case mutual induction proving `canonExpr bound [] []` preserves `⊢ᵢ/ᵐ/ᶜ`,
  given `Names⊆ ctx bound` + `PolyInB ctx bound`. `PolyInB` is a RECORD (rigid
  index inference). ONE deferred hole: `composeMid-canon` (m-compose middle-type).
- **Step 4 (`Once.Adequacy.CanonModule`, GREEN; `ResolverBridge` rewired)**:
  `resolver-preserves-typing` is now a DEFINITION (not a postulate). Spine:
  `resolver-preserves-typing → canon-preserves-typing → module-typed-canon
   → AllFunsTyped-canon → canon-pres-ᶜ`. The apex `Once.Adequacy` typechecks. So
  the Step-2 induction is LOAD-BEARING (not an island). `module-typed-canon-ef`
  takes the `extractFunctions` result EXPLICITLY (top-level aux, no `with`-block).

REMAINING residuals (named, top-down-dictated, all TRUE) to drive 0.51(1) to
postulate-free:
  1. `composeMid-canon` (CanonPreserveMutual) — domainOfHead/composeArgB are
     canonExpr-invariant on well-typed arms; case `⊢ᵐ f`/`⊢ᵐ g`. The m-named arm
     needs `isBuiltinName x ≡ false ⇒ x ∉ {fst,snd,id,terminal}` to unstick the
     literal patterns; the RVar/RResolved cases coincide via showCanonical. HARD.
  2. `module-bridge` (CanonModule) — `extractFunctions ∘ canonDecl` commute
     (funs/sigEffs preserved, bodies canonExpr'd) + POLY-CONTEXT TRANSPORT: `mR`'s
     poly bodies are ALSO canonExpr'd, so `ModuleTyped mR` is at `buildPolyCtx
     polysR ≠ polysU`. The transport is the Step-2 generalization (canon-pres over
     a canonExpr'd polys context; only t-var-poly-instantiate + m-cata read polys).
  3. `polyInB-bridge` — `lookupPoly (buildPolyCtx polysU) x ⇒ x ∈ polyDefNames ds`
     (both = non-ground DTypeSigs); induction on `extractFunctions-go`.
  4. `resolveFunType-canon` — `inferType` is canonExpr-invariant (D007 no-sig);
     elaborator-level (Step-5 flavour).
  5. `has-valid-main-canon` — main's signature unchanged ⇒ validity rides.
  6. `resolve-result` — import-free resolution always succeeds (induction on
     `resolveDecls` over non-`DImport` decls); import case = ModuleMap-complete.
Steps 3 (`resolver-reflects-typing`) + 5 (`resolver-preserves-trace`) untouched.

## UPDATE 2026-06-27 (residual discharge grind — 4 of 6 done)

DISCHARGED (committed green, apex green throughout):
- `composeMid-canon` (`CanonComposeMid`): case `⊢ᵐ` arms; refactored `domainOfHead`
  to a shared `domainOfHead-arrow` helper. ONE atom left: `composeArgB-RVar-resolved`
  (composeArgB's fst/snd/id/terminal LITERAL patterns stuck for abstract x).
- `polyInB-bridge` (`CanonPolyNames`): `poly⊆` induction on `extractFunctions-go`.
- `resolveFunType-canon`: signatured case definitional; residual `inferType-canon`
  (elaborator inferType invariance, D007 no-sig only).
- `resolve-result` (`CanonResolve`): import-free `resolveImports-ni` PROVEN; spine
  cases on `noImports?`; import case → residual `resolver-preserves-typing-imports`.

REMAINING (the hard coupled arc — NOT mechanical):
- `module-bridge` + `has-valid-main-canon`: both gate on **extractFunctions-commute**
  (`extractFunctions (canonModule mU)` relates to `extractFunctions mU`: USER DFunDef
  bodies canonExpr'd, PRIMITIVE DSignature bodies UNCHANGED — `canonDecl` only maps
  DFunDef; ~150-200-line induction like `poly⊆`). FINDING: `AllFunsTyped-canon`'s
  uniform `mapCanonBody` is WRONG for the bridge — it canonExpr's primitive bodies
  too, but `funsR` keeps them `RVar name`. FIX: `mapCanonBody` must skip `isPrim`
  FunInfos, and `AllFunsTyped-canon` must REUSE primitive derivations (no canon-pres).
  module-bridge ALSO needs the **poly-context transport** (`ModuleTyped mR` is at
  `buildPolyCtx polysR`, poly bodies canonExpr'd ≠ `polysU`) — a Step-2 generalization
  over a canonExpr'd polys ctx (only `t-var-poly-instantiate`/`m-cata` read polys).
- `composeArgB-RVar-resolved`: needs `composeArgB` RVar dispatch refactored from
  literal patterns to explicit `≟` (blast radius: Elaborate/Completeness/Judgment).
- `inferType-canon`: elaborator inferElab canonExpr-invariance (Step-5 flavour).
- `resolver-preserves-typing-imports`: the import case (needs Plan-0.50 machinery).

## Notes
- All `≡` leaf collapses are DEFINITIONAL (bare/showCanonical) ⇒ no UIP/subst needed.
- Keep `_⊢R_` (Compile.agda) front-end-anchored — these facts BRIDGE, don't replace it.
- Estimate: Steps 1-4 ~250-400 lines; Step 5 the heavy add. Land 1&2 first, commit,
  then 3.
