# Repository rules

## Git commit messages

- **Do NOT append `Claude-Session:` trailers (or any `claude.ai/code/session_...` links) to commit messages.** Commit messages must contain only the human-meaningful description — no session/tooling trailers of any kind.
- The same applies to PR bodies: no `claude.ai/code/session_...` links.
