# HANDOFF — 2026-07-31

*Session entry point. Supersedes `HANDOFF-2026-07-30.md`, which remains accurate
for the W1a–W1g history and its five negative results.*

--------------------------------------------------------------------------
## 0. START HERE

> ### Two lines, both moved. Pick one.
>
> **Toward DT dHoTT: ✅ OPTION (a) TAKEN.** `SpikeVar` found the kernel does not
> charge the covariance fee — transport is free AND symmetric — because
> **reduction is too small to be a path type**. `SpikeHom` then showed the fee
> IS real once endpoints may differ definitionally. So `Hom` gets its own
> inhabitants. Phase 1 survives intact; the cascade is what W2 was already
> priced at. **Items 1–3 are now CLOSED** (§4.2): the unfolding terminates and
> `Hom` is primitive-and-computing like `El` (item 1); the POINTWISE reading is
> taken (item 2); and naturality at `U` is **REFUTABLE, not merely underivable**
> (`SpikeHomNatU`, item 3) — the labelled-kernel outcome is the outcome.
> **Item 4 is also CLOSED** (owner's decision, 2026-08-01): `base` is DISCRETE
> BY GENERATION — the congruence floor is the ceiling for an opaque type;
> ordered base types are relocated to future declared-hom work (W3-adjacent).
> ✅ **AND THE CASCADE IS PAID** (2026-08-01, three commits `f1ff7661` →
> `d2209f79`): `Hom` is a former of the REAL `RTy`, and the whole tower —
> substitution calculus, confluence (type-level, `Hom` interleaved), SR,
> `Inj`, `Subj`, the LR, `fund`, `wnorm`, `dec-conv-typed` — is re-proved
> over it. `⊢hom-across` types `λx.λy.x` at
> `Hom U ⌜base⌝ (⌜Π⌝ ⌜base⌝ ⌜base⌝)`: an inhabited path between
> definitionally distinct codes, IN the kernel.
>
> ✅ **AND THE ELIMINATOR'S DESIGN IS SETTLED** (same day, `4517ab9d` →
> `a1e58608`): `sym` is FALSE at `U` (`SpikeNoSym`); `refl` computes by
> CODE annotation (`SpikeHomRefl` — the RTy-annotated alternative forces
> mutual `⟶`/`⟶ᵀ`, rejected); W3's floor is in the tree (`NbEPDirDBVar` —
> `Hom` contra/co by endpoint, composition motive `Pos`, `sym`'s motive
> provably not `Pos` and knowably `Neg`).
> ✅ **AND `tr`'s RULES ARE FIXED** (2026-08-01, `SpikeTr`): code-annotated
> motives confirmed — the (B) move a third time — but the forecast rule set
> was **wrong twice and incomplete once**. `tr` is an ELIMINATOR OF ITS
> PATH: unkeyed J and unkeyed taut each have an unjoinable raw critical
> pair (measured); J fires only on `hrefl` at head-stable stuck codes; a
> THIRD rule (pointwise composition at `⌜Π⌝`-ambient `⌜Hom⌝` motives) is
> forced by canonicity; and constant motives are EXCLUDED from the license
> (`PosC` ⊊ `Pos`) — a discard rule is neither safely statable nor needed
> (measured: `const-motive-invisible`, over the real syntax).
>
> ✅ **AND THE ELIMINATOR CASCADE IS PAID, STAGE 1** (2026-08-02):
> `⌜Hom⌝`/`hrefl`/`tr` are constructors of the real syntax, pushed through
> the WHOLE tower — the first cascade with new term formers, the §3.1 flip
> condition paid (`sn-anti`/`subTm-var` per-constructor, `sne-hrefl`).
> `⊢⌜Hom⌝`/`⊢hrefl` are in the base judgment with `sr` AND `fund`
> (`⊩₀Hom` + `homSem₀` landed — `SpikeHomRefl`'s repeal, as priced).
> `⊢tr` is STAGED (`NbEPDirDBTr`: the judgment + full `srᵗ`, incl. the
> confluence-based endpoint extraction the J cases need).  Done-when
> demos are in: `trans` internal at the `⌜Hom⌝` composition motive, the
> J-equation computing it away with typing preserved, directed univalence
> computing a third time, and the `no-sym` `PosC` refutation.
> Two findings, both spike-grade (§4.3 item 4b'):
> ① `⌜Hom⌝` is NOT a uniformly stuck head — `⌜Π⌝`-SPINE `⌜Hom⌝` codes
> (higher paths over function paths) decode to `Π`s, so `hrefl`'s unfold
> is a SPINE-RECURSIVE family and J-at-`⌜Hom⌝` needs spine-stuckness; the
> whole CANONICITY PACKAGE (`hrefl` unfolds incl. `hrefl-Π`, J-Hom,
> `tr-pw`, spine judgments `Pw`/`StkC`) is deferred as ONE unit — higher
> paths were already unscoped, and this tower's LR is SN-based.
> ② ★ the `SpikeHomLR` gate REOPENS at the ELIMINATOR, exactly where the
> gate said to stop and record: `fund`'s `tr` case needs the
> `Hom`-membership to package a TRANSPORT CLOSURE (as `⊩Π` packages `app`).
> 🔴 **NEXT SESSION: `SpikeTrLR` — the transport-closure membership**, then
> `⊢tr` merges into the base judgment; the canonicity package (W2b) after.
>
> **KERNEL LINE — Phase 1 is CLOSED.** W1h landed `fund`; `dec-conv` no longer
> carries a normalization premise. Next is **W3, the variance judgment** —
> scoped at file level in PLAN §3, three modules, risk low.
> ⚠ **Not W2.** The ordering was revised on measurement (§3.2 below).
>
> **LINEARIZATION LINE — the core is now OWNED.** `NbEPLinCore` replaces the
> borrowed object language; `dyn-linear` covers both fixpoints. Next is
> retiring `Rec`/`Dyn` into it (mechanical), then re-pointing `Pass`/`QTT` onto
> a POC-owned source language — whose SHAPE is already decided by D063/D070
> (point-free morphisms), see §4.
>
> ⚠ **Read §1 before touching either.** A standing rule changed this session,
> and it invalidates a whole category of previously-sensible-looking work.

Orientation, if needed:

1. **`ARCHITECTURE.md`** — the map: compiler tower vs. kernel tower, the `Hom`
   seam, the option taken *and rejected* at each layer.
2. `PLAN-dHoTT-kernel.md` §1.2 (standing constraints — **two are new**), §3
   (workstreams), §8.3/§8.4 (the linearization line's re-scoping).
3. §5 below — negative results. Do not re-walk them.

--------------------------------------------------------------------------
## 1. ★ THE RULE THAT CHANGED, and what it invalidates

> **The POC owns its own syntax.** No dependence on `bootstrap/normalizer/**` —
> that is **another POC**, not shared infrastructure — and none on
> `formal/Once/**`. (PLAN §1.2.)

**Why it matters more than it sounds.** Borrowing a peer POC's object language
couples our design decisions to their accidental ones, and then *their limits
get recorded as our constraints*. That is not hypothetical — it happened this
session, twice, and cost real work:

- **W0d was scoped as "port the real IR to the linear core".** That framing is
  bottom-up by construction: it takes the existing target as authoritative and
  measures what the source can be squeezed into. Every wall it hit
  (`no ν`, `no Int`, `List Int is out`) got written down as a limit on the POC.
  None were findings about the right shape. The module that made the error
  (`NbEPLinIR`) is deleted.
- **Then the replacement made the same error one level down**, breaking the
  dependency but keeping the *vocabulary* — `Fix`/`fix`/`unfix`. `Fix` reads as
  a general fixpoint, which Once explicitly rejected (OCP-0003 removed
  `fold`/`unfold` from the IR). Caught by review, not by Agda.

**Corollary, worth internalising:** *"port the real X to Y" is almost always the
wrong scoping for POC work.* The port is downstream compiler work, gated on the
shape being settled.

**Second new standing constraint (PLAN §1.2): NO GENERAL FIXPOINT.** `μ`/`ν` are
the initial algebra and final coalgebra of a polynomial code, eliminated by
`cata` and one observation. No `fold`/`unfold`, no fixpoint combinator, no
`Hylo` as a primitive. Name things `Mu`/`Nu`/`inμ`/`outμ`/`inν` after Once's own
`In`/`out-μ`/`Out`/`in-ν`.

**Where the rule is still violated.** `NbEPLinPass`/`NbEPLinQTT` borrow a
cartesian SOURCE language (`Syntax.CCC.Term`) and `eval` from the normalizer
POC. ⚠ Note this is a PROVENANCE violation, not a shape one: D063/D070 say
point-free is correct, so `Pass`'s source shape was right all along. See §4.

--------------------------------------------------------------------------
## 2. WHAT LANDED

Seven commits, `9f126f4f..f1dd01dd`. All check; no postulates, holes, or pragmas
anywhere.

| commit | what |
|---|---|
| `ed410631` | **W1h `fund`** — the fundamental theorem. Phase 1 closed. |
| `74e781a2` | W1h finding (2) right-sized; the flip condition recorded |
| `06d5de40` | **W0e `SpikeLinNu`** — codata in the linear core |
| `33671f72` | W0d measured (later superseded) + **W2/W3 scoped** |
| `8302235d` | ★ the POC owns its syntax; `NbEPLinIR` deleted; W0d re-scoped |
| `b8efc3bd` | no general fixpoint; `Fix`→`Mu`/`inμ`/`outμ`/`inν` |
| `f1dd01dd` | **`Free` stratified**; `dyn-linear` covers both fixpoints |

### 2.1 Kernel line — `NbEPDirDBFund`, 634 lines

```agda
fund : Γ ⊢ t ∷ A → Var Ξ → Γ ⊩ˢ σ → Σ (⊩₁ (subTy σ A)) (λ R → R ⊩₁∋ subTm σ t)
```

The existential form went through exactly as the 2026-07-30 handoff §4 scoped
it. No syntactic validity, no `⊢ty`-closed-under-conversion, no premise on
`⊢conv` — discharged by `conv₁` + `sem-conv`, because W1b had already made the
relation closed under conversion. Delivered: `⊩ˢ`/`⊩ˢ-ext`, the shape
inversions, mutual `fund-ty`/`fund` over all nine term rules and five type
rules, `⊩ˢ-ren`, `snorm`, `wnorm`, and **`dec-conv-typed`** — conversion of two
well-typed terms decided with no normalization premise. `wnorm` COMPUTES: two
`refl`s pin the normal forms.

Cheaper than scoped: the shape inversions are ~15 lines each, not ~30. As
ELIMINATION rules rather than inversion records, the four wrong constructors
fall to `Π-reduct`/`Σ-reduct` alone — `joinW` is not needed, because the type is
literally `Π F G` rather than merely convertible to one.

### 2.2 Linearization line — `NbEPLinCore`, 776 lines, ZERO IMPORTS

The object language, semantics and payoff theorem, owned. `LF` (functor codes,
object-language-independent), `LTy` (with `νt` and base leaves from the start),
instrumented `⟦_⟧`, `Mu`/`Nu` in one knot, the full `LTm` generator set —
including `lzero`, `lOutμ`, `lOut`/`lAna`/`lInν`, the three PLAN §8's coverage
table listed as gaps — `Lᶜ`, `DupFree`, `Free`, and `dyn-linear` covering both
fixpoints. Base leaves are module parameters: the theory says only that they are
inert.

★ **`⇒` and `ν` turn out to be the same decision.** "A closure reports its own
cost" (W0b) and "an unfolding reports its own cost" (W0e) were two answers to
one question. In an owned semantics they are one clause each of the same idea,
and `Mu`/`Nu` share a single interpretation knot.

--------------------------------------------------------------------------
## 3. THE TWO THINGS THAT WERE ACTUALLY HARD

### 3.1 `SN` under a binder — where W1c's strike was too broad

`sem-lam` takes `SN` of the lambda's BODY; `fund` only ever sees it closed up as
`s[σ,u]`. W1c's "Kripke is not needed" **held for the family** — the target
scope never grows — **but not for this premise**, and a renaming action on `⊩₁`
is not available to supply it:

> The structural transport `ren₁` is **BLOCKED** at `⊩₁Π`: the stored family
> offers instances only at `v = renTm ρ u`, the renamed clause demands one at
> every `v`, and the missing ones cannot be manufactured because **`⊩` is not
> total over `RTy`** (W1e/`SpikeSNK`) — so there is no fallback. The obvious
> weaker ask dies too: for `ρ = vs`, a `v : RTm (Ξ ∙)` may mention `vz`.
>
> ⚠ **ARGUED, not measured. NOT a refutation.** `ren₁` is not shown false, only
> unreachable by the induction. That is deliberately the right strength: the
> only decision it licenses is "do not build it", and it licenses that fully.

**The fix, and it costs the relation nothing:** instantiate the family at
`var x₀` (free by `CR3`); `s[single (var x₀)]` **is a renaming of `s`**; so
anti-renaming for `SN` recovers `SN s`. ~60 lines, mutual over `SNe`/`SN`/
`SNRed`. Hence `fund`'s `Var Ξ` argument, and hence `⊩ˢ-ren` (every renaming
substitution is reducible, by recursion on `⊢ctx`) as the way the induction
starts.

★ **THE FLIP CONDITION — this is what W2 needs.** Anti-renaming scales to new
formers because `sn-anti` is a lemma about RAW SYNTAX: it never mentions `⊩`, so
it is immune to whatever W2/W3 do to the relation, and sensitive only to `_⟶_`.
Cost is ~3 lines per new `RTm` constructor plus one commutation lemma per former
that BINDS and REDUCES. **One condition brings Kripke back: the kernel adopting
η-LONG normal forms** (βη equality, or W5 going NbE-shaped) — reify-at-a-fresh-
variable IS the Kripke step. Not met today: η lives in a separate `_≅η_`,
`⊢conv` uses the β-only `_≅ᵀ_`, and the committed route is reduction-based.

*(Aside for whoever costs βη: `NbEPDirDBEta`'s header defers Σ-η because `RTm`
lacks pair/projection constructors. W1g landed them. That blocker is stale.)*

### 3.2 `Free` does not fit in one recursion — W1e's finding, other line

`Free (A ⇒t B) f = (a : ⟦A⟧) → Free A a → …` puts `Free` **negatively**, and
that hypothesis is exactly what bounds `leval`, so it cannot go. Meanwhile
`Free` at `μt`/`νt` must be real and re-enters — so `FreeMu` occurs negatively
in its own definition. **Same wall as `SpikeSNK`'s "`⊩Π`'s function field puts
`⊩∋` negatively", from the opposite direction.** Same answer: stratify.

What makes it work: `FS G C` is built from `⊤`, base carriers, `Mu`, `Nu`, `⊎`,
`×` and contains **no function space, ever**, so a fixpoint payload's freedom
never needs the `⇒t` clause. Three layers — (1) `FreeMu`/`FreeNu` over the data
fragment only, arrow-free hence strictly positive; (2) `Free` as a plain
function structural in `LTy`; (3) `FreeFS G A` at an arbitrary carrier, bridged
to (1) by four one-line inductions. ⚠ **The carrier in (3) must be EXPLICIT** —
implicit, the `⊕f`/`⊗f` recursive calls leave it an unsolvable metavariable.

★★ **AND THE BORROWED CORE COULD NOT HAVE SHOWN IT.** `NbEPLinDyn` sets
`Free (μ F) x = ⊤`, justified by "a fixpoint holds no functions". True, and
beside the point: with `Kν` among the codes, **data can hold a PRODUCER**.
`box-not-free` is the concrete refutation — inductive data that is not free,
which `⊤` would have called free — and it is **not statable in the borrowed core
at all**, whose functor codes cannot put a `ν` inside a `μ`. Owning the syntax
is what made the counterexample expressible. That is the strongest single
argument for §1's rule.

--------------------------------------------------------------------------
## 4. NEXT — both lines, honestly priced

### Kernel line: **W3, then W2** (PLAN §3, scoped at file level this session)

The ordering was revised on W1g's own measurement: *adding a JUDGMENT does not
move the reduction side; adding a CONSTRUCTOR does.* §4's old "W2 and W3 in
parallel as ONE cascade" priced W3 at W2's rate.

### 4.0 ✅ THE DT dHoTT CHOICE — OPTION (a), TAKEN (`SpikeVar` + `SpikeHom`)

W3 was scoped around the covariance fee. Located in the DEPENDENT kernel, **it is
not charged**. Two three-line lemmas, both checked:

```agda
transport-fwd : t ⟶* t' → Γ ⊢ x ∷ B[t]  → Γ ⊢ x ∷ B[t']
transport-bwd : t ⟶* t' → Γ ⊢ x ∷ B[t'] → Γ ⊢ x ∷ B[t]     -- ★ BOTH WAYS
```

`transport⟶` needs a fee because its `P` is an arbitrary Agda family; in the
kernel the family is a TYPE and substitution is monotone, so it is discharged
structurally for every `B`. And **backwards too** — transport lands via `⊢conv`,
which consumes `_≅ᵀ_`, and `_≅ᵀ_` has `csymᵀ`. The direction is forgotten.

⚠ `no-sym` is untouched: you still cannot invert a reduction. What collapses is
its ACTION on type families — and that is a CONSEQUENCE OF THE DESIGN
(definitional equality IS `core(Hom)`, ARCHITECTURE K3), never previously
costed at the type level.

**So W2's gate is not the `⊩`-clause. It is this.** Adding `Hom A t u` with
reductions as inhabitants buys no directed structure over families; a directed
`J` over it would have symmetric transport as a derivable consequence.

★ **THE CORRECT READING — reduction is too small to be a path type.** An earlier
draft said (a) meant abandoning `core(Hom)`. Wrong. Under `Hom = ⟶*` every
INHABITED `Hom t u` has definitionally equal endpoints, so `B[t] ≅ᵀ B[u]` by the
ordinary congruence every type theory has. The problem is only that such a path
type has **nothing to transport along**.

✅ **(a) TAKEN:** `Hom A t u` is a type former with genuine inhabitants,
including between definitionally DISTINCT terms.

**What survives — nearly everything.** Definitional equality stays β/η
conversion: reduction-based, confluent, DECIDABLE. **Phase 1 untouched.** `Hom`
is added on top with the six-module cascade W2 was already priced at. Only the
READING of `Id = core(Hom)` moves — propositional identity, not definitional.
The kernel had been reading the slogan one level too low.

★ **VALIDATED, not assumed — `SpikeHom`.** (a) is worth taking only if transport
really cannot be recovered from `⊢conv` once endpoints differ:

- `base≇Π` — refuted via Church–Rosser + `base-nf` + `Π-reduct`;
- ★ `fee-is-real` — a family (`El x`) and two closed codes whose instances are
  **not convertible**.

`SpikeVar` §1/§2 and `fee-is-real` are two halves of one statement: **transport
is free precisely where endpoints are definitionally equal, and nowhere else.**

★ **THE TWO `Hom`s MEET AT THE UNIVERSE.** The codes decode to `base` and
`Π base base`, and a path between them is a MAP — the universe is a category
whose hom is the function type, i.e. `NbEPDirV`'s `Homₜ A B = Term A B`. So
`Hom` at `U` IS `Homₜ`, and `NbEPDirV`'s variance results become the semantics
W3 discharges against rather than re-derives.

⚠ **ORDER settled:** under (a), W2 and W3 are MUTUALLY dependent — the fee
cannot be stated until `Hom` exists, and `Hom`'s eliminator cannot be stated
without the motive condition. **They close together.** Cost still says write the
variance judgment first as a self-contained piece; it no longer says it lands
first.

🔴 **THE NEXT QUESTION — now SCOPED, see §4.1.** Answered per former: `U` is
maps (decided), `El` commutes with decoding (decided), `Σ'` is forced, `base` is
a genuine CHOICE, and **`Π` is the crux** — natural transformations, whose
naturality condition is the open research item. §4.1 also exposes the design
question underneath: **primitive vs DERIVED `Hom`**, where derived gets directed
univalence as a COMPUTATION rule and so dodges the very objection ARCHITECTURE
K3 raises against univalence-as-axiom. §4.2 has the order to attack it.

*(Option (b) — keep `Hom = ⟶*`, directed content at `Homₜ` — is not wrong, just
SMALLER: real content W4 needs regardless, but no directed IDENTITY TYPE.)*

--------------------------------------------------------------------------

*(W3's original scoping, retained — accurate on cost and cascade, superseded on
motivation by §4.0.)*

- **W3 — the variance judgment.**

  ★★ **Scoped as a prerequisite for W2.**
  `NbEPDirJ`'s own signature is the argument:

  ```agda
  transport⟶ : (P : Term A B → Set)
             → (∀ {u v} → u ⟶ v → P u → P v)   -- ★ THE COVARIANCE FEE
             → Hom t u → P t → P u
  ```

  > "Directed transport is NOT free (in `Id`-land it falls out of J): **it costs
  > exactly STEP-COVARIANCE of the motive**."

  Today that fee is an AGDA-LEVEL HYPOTHESIS, because `Hom` is a meta relation
  and the motive is an Agda family. W2 internalizes `Hom`, so the motive becomes
  a KERNEL type family and the fee must be stated IN THE KERNEL. That statement
  is a variance judgment. **Without W3, W2's eliminator has a premise the kernel
  cannot express** — which is why ARCHITECTURE K2 already reads "W3 variance as a
  JUDGMENT (Nuyts–Devriese), not a motive side-cond".

  Shape: `_⊢var_` mutual with `_⊢ty_` in W1g's shape, cascading only into
  `Subj`/`SR`/`Fund` — three modules. `NbEPDirV` has the semantic content
  already, including the contravariant `Π`-domain rule. Prior art exists
  (Nuyts–Devriese); it does NOT for W4 ("no prior art anywhere"), so W3 is also
  the last step before the genuinely unexplored part. Done-when includes a
  NEGATIVE control: a motive that is not covariant, refuted.

  ⚠ **Where the kernel actually stands.** It is DEPENDENT but not DIRECTED. All
  the directed content — `J`, `no-sym`, variance, transport — exists, but at the
  META level, over `⟶*` as a `Set`-relation. "DT dHoTT" is precisely the move of
  that content inside, and W3 → W2 is that move.
- **W2 — internalize `Hom`. Six modules, and it carries a GATE.** ★ **Spike the
  `⊩`-clause for `Hom` BEFORE touching `RTy`** (`SpikeHomLR`). Per §3.1's flip
  condition, new formers are cheap only while no clause needs a member of `⊩` at
  a LARGER SCOPE. `J`'s motive binds, which is the already-handled pattern; the
  `Hom` clause itself is the unknown. **If it needs a larger scope, stop and
  record it.** Priced from W1g/W1h: `Σ'` cost eight cross cases per level plus a
  real case that was not copy-paste, and `Hom`'s indices are TERMS, so expect
  worse. Done-when includes re-proving `no-sym` INTERNALLY — if internalizing
  the former makes `sym` derivable, the former is wrong.

### 4.1 🔴 SCOPED — WHAT INHABITS `Hom`, FORMER BY FORMER

The question `SpikeHom` named. Answered per former below, with what is DECIDED,
what is a CHOICE, and what is genuinely UNKNOWN kept apart.

**The organising principle.** In a directed theory every type is a CATEGORY, and
`Hom A x y` is its hom. So "what inhabits `Hom` at `A`" is "what category is
`A`". Read that way each former has a forced or near-forced answer — except one.

| at | `Hom A x y` is | status |
|---|---|---|
| `U` | `El x ⇒ El y` — the universe is the category of types | ✅ decided (`SpikeHom`; `NbEPDirV`'s `Homₜ`) |
| `base` | identities only — discrete BY GENERATION | ✅ decided (item 4, 2026-08-01) |
| `Π A B` | the plain pointwise family — natural only over identities-only domains | ✅ decided (items 2–3) |
| `Σ' A B` | pairs of morphisms, second transported | ✅ forced, once `Π` is settled |
| `El c` | reduce `c` and recurse; NEUTRAL when `c` is | ✅ decided |

**`U` — decided.** `Hom U c d = El c ⇒ El d`. This is directed univalence, and
`SpikeHom`'s `fee-is-real` is its witness: the two codes there decode to `base`
and `Π base base`, and a path between them is a map.

**`base` — a genuine design CHOICE, do not let it default silently.**
- *(i) discrete*: `Hom base t u` inhabited only by identities. The right default
  for an OPAQUE base type — nothing generates non-identity morphisms — and it
  makes `base` a discrete category, where `Hom` coincides with `Id`.
- *(ii) ordered*: base types in Once are not really opaque (`Int`, `Str`,
  `Buffer` have operations). `Hom Int m n = m ≤ n` is a coherent and interesting
  reading — a base type carrying a partial order as its hom.
- Recommendation: **(i) for the kernel**, and record (ii) as available. The POC
  should not commit Once's base types to a categorical structure it has not
  needed yet. But this is the owner's call, not a forced move.

**`Π A B` — THE CRUX, and where the variance judgment is actually consumed.**
For functors `f g : A → B`, the exponential of categories is the FUNCTOR
category, whose morphisms are NATURAL TRANSFORMATIONS. So

    Hom (Π A B) f g  ≈  (a : A) → Hom (B[a]) (f a) (g a)   + NATURALITY

and stating naturality requires transporting along `Hom A a a'`, which requires
**`B` covariant**. ★ That is the real consumer of W3 — not (only) `J`'s motive,
but the FORMATION of `Hom` at `Π`. It also explains `NbEPDirV`'s `_⇒→_`
contravariance: the functor category is contravariant in its domain.

⚠ **CORRECTED BY ITEM 2 (`SpikeHomNat`).** The claim just made — that `Π`'s
formation is "the real consumer of W3" — is **true only if naturality is
carried**. On the plain-family reading `Hom` needs no transport and **no
variance judgment at all**. See §4.2 item 2: the W2/W3 coupling is a
CONSEQUENCE of that choice, not a fact about the kernel.

**`Σ' A B` — forced.** `Hom (Σ' A B) (a,b) (a',b')` = a morphism `p : Hom A a a'`
together with a morphism from `b` transported along `p` to `b'`. Needs the same
covariance of `B`. Nothing new once `Π` is settled.

**`El c` — decided, and it is the structurally important one.** `Hom` must
COMMUTE with decoding: `Hom (El ⌜base⌝) ⟶ᵀ Hom base`,
`Hom (El (⌜Π⌝ c d)) ⟶ᵀ Hom (Π (El c) (El d))`, and so on. When `c` is NEUTRAL
the type is stuck and `Hom (El n) x y` is a genuine neutral type — so the
logical relation needs a clause for it, exactly as `⊩ne` already handles `El n`.

--------------------------------------------------------------------------

### ✅ RESOLVED — primitive vs derived was a FALSE DICHOTOMY (`SpikeHomTy`)

§4.1 posed this as a choice. Writing the clauses out settles it: **it is BOTH,
and the repo already has the pattern.**

Two clauses are STUCK for a structural reason. The kernel's `RTy` is
`base`/`U`/`Π`/`Σ'`/`El` — **no identity type and no empty type** — so
"identities only" is *not expressible*, and `Hom base x y` cannot be derived.
Same for `Hom (El n)` at a neutral code. Therefore:

> **`Hom` is a PRIMITIVE CONSTRUCTOR that COMPUTES — exactly like `El`.**

It unfolds at `U`, `Π`, `Σ'`; it is stuck at `base`, at `El n`, and at a `Hom`
(higher paths). That is the Tarski pattern ARCHITECTURE K2 already names —
"codes + `El` decoding BY REDUCTION" — so there is precedent and no new
machinery is being invented.

★ **And the payoff survives.** The `U` clause — directed univalence — is a
COMPUTATION rule, so ARCHITECTURE K3's objection ("univalence as an AXIOM does
not compute ⇒ canonicity dies ⇒ the checker STALLS") does not apply to it. It is
also, notably, the clause that makes **no recursive call**.

★ **And the derived reading has a payoff worth weighing.** ARCHITECTURE K3
rejects univalence because *"univalence as an AXIOM does not compute
(`transport (ua e) x` sticks ⇒ canonicity dies ⇒ the conversion checker
STALLS)"*. Under the derived reading, **directed univalence is a COMPUTATION
RULE** — `Hom U c d` literally reduces to a function type — so the thing that
kills the axiom version does not arise. That is a strong argument for derived,
and it is the repo's own criterion applied to a case it had not considered.

### ✅ ITEM 1 DISCHARGED — the recursion terminates (`SpikeHomTy`)

`homU : HTy → Tm → Tm → HTy` is accepted with **no `TERMINATING` pragma, no
explicit measure, no sized types**. The measure is the type argument,
structurally: `Π` recurses on `B`, `Σ'` on `A` and `B`, and the other four
clauses do not recurse. The clause that looked riskiest — `U`, which jumps to a
type built from arbitrary terms — simply makes no recursive call.

Terms are a MODULE PARAMETER in that spike, deliberately: no clause inspects a
term, and making them opaque *proves* it rather than asserting it.

⚠ **Item 2 is independent of this.** Any naturality condition at `Π` is stated
USING `Hom` at the codomain — already the same smaller call — so it cannot
enlarge the measure. Item 2 can be settled either way without redoing item 1.

⚠ **DO NOT OVER-QUOTE IT.** This is termination of the UNFOLDING FUNCTION, not
normalization of the extended `_⟶ᵀ_`. The second has to survive `Hom`'s
unfolding INTERLEAVED with term reduction inside `El` — `Hom (El c)` stuck, `c`
reduces to a code, the type reduces, `Hom` unfolds, exposing a fresh
`Hom (El b)`. That chain looks fine (term reduction is normalizing — Phase 1 —
and each unfolding strictly shrinks the type) but it is **not proven**, and it
belongs to item 4's confluence/SR step.

### ✅ ITEM 3 ANSWERED AT `U` — naturality is REFUTABLE there (`SpikeHomNatU`)

`U` was checked first, as scoped, and it decides: **the statement is FALSE at
`U`, not merely out of the eliminator's reach.** Under reading (i) the plain
family is the TYPE, so a non-natural pointwise family — measured concretely on
a two-code universe, where `Hom U` on `{⊥, ⊤}` *is* the walking arrow with
nothing stipulated — is a legitimate inhabitant, and no sound eliminator
derives a false statement. W2 may design `Hom`'s eliminator with no naturality
obligation hanging over it.

The POSITIVE control also went through: over identities-only homs (discrete
`base`), naturality of EVERY pointwise family is derived literally from `J`
plus the functor identity laws — HoTT 2.4.3 survives the directed setting
exactly that far.

★ **The forecast's abstraction was wrong, and the correction matters.**
"Derivable where paths are inductively generated" is not the dividing line —
`SpikeHomNat`'s walking arrow is inductively generated and fails. The line is
**identities-only vs everything else**: a pointwise family can ignore any
non-identity hom in the domain, and a functor pair disagreeing on that hom
sees it. What path induction buys in HoTT is not "the hom is inductive" but
"the hom is generated by refl alone" — and directed homs exist precisely to
escape that.

★★ **Consequence for item 4: the `base` choice is NO LONGER neutral.**
Discrete (i) keeps `base` functorial; ordered (ii) is exactly the walking
arrow, i.e. pointwise-only. Still the owner's call, still cheap in code —
but it now decides which column `base` lands in. Full table in
`SpikeHomNatU` §4. Residue, flagged not hidden: `Π`/`Σ'`-as-domain are
expected pointwise-only by the same argument — ARGUED, not measured.

--------------------------------------------------------------------------

### 4.2 THE ORDER TO ATTACK IT

1. ✅ **Termination of the derived recursion** — DONE (`SpikeHomTy`), and it
   resolved primitive-vs-derived as a bonus. See above.
2. ✅ **RESOLVED — the POINTWISE reading taken** (`SpikeHomNat` + the
   first-principles argument, commit `e62520f7`): congruence forces the
   pointwise family as a floor, directed funext becomes definitional, and
   naturality is NOT carried — it became item 3. The old carried-equation
   decidability worry is moot. *(This marker was stale for one session: the
   commit resolved it but the file still said NEXT.)*
3. ✅ **ANSWERED — naturality at `U` is REFUTABLE, not underivable**
   (`SpikeHomNatU`). The labelled-kernel outcome is triggered: functorial
   over identities-only domains (measured), pointwise-only at `U` (measured),
   at an ordered `base` (measured — `SpikeHomNat` §2 re-read), and expectedly
   at `Π`/`Σ'`-as-domain (argued). See the section above.
4. ✅ **CLOSED — `base` is DISCRETE, by GENERATION, not preference** (owner's
   decision, 2026-08-01; criterion: the principled reading, POC reversibility
   explicitly ruled irrelevant). The principle: `Hom` at a type is generated by
   `refl` + the type's DECLARED hom-generators, closed under congruence — the
   directed mirror of HoTT's "paths are generated by `refl` unless a HIT
   declares more". The kernel's `base` is OPAQUE: no operations, no generators,
   so **the congruence floor is also the ceiling** — identities only.
   Discreteness is a PROPERTY it has (the Riehl–Shulman reading), not a
   stipulation.

   ★ The ordered reading is not lost — it is RELOCATED, and shown to be
   non-free-standing: `Int` with `≤` as hom is a base type declared WITH
   hom-generators (the directed analogue of a HIT), and it is only coherent
   with per-primitive VARIANCE declarations — congruence is forced (item 2's
   floor), so an unrestricted `ap negate` on `0 ≤ 1` would inhabit `-0 ≤ -1`.
   Declared-hom base types are therefore W3-adjacent future work (variance
   extended to primitives), not a reading of opaque `base`.

   `U` remains the deliberate exception where `Hom` is DEFINED rather than
   generated (directed univalence as a computation rule) — which is exactly
   why item 3 found it non-functorial, and why that is a feature of `U`'s
   role, not a defect of the scheme.
5. ✅ **DONE — THE CASCADE IS PAID** (2026-08-01, `f1ff7661`/`ecd08fb7`/
   `d2209f79`). `Hom : RTy → RTm → RTm → RTy` is a constructor of the real
   syntax; unfolds at `U` (directed univalence as a computation rule) and `Π`
   (pointwise); stuck at `base`/neutral-`El`/`Σ'`/`Hom`. All nine tower
   modules re-check `--safe`, no pragmas, ~29s total. See §4.3.

### 4.3 ✅ THE CASCADE, PAID — what it taught, and W2's remaining debt

Three findings from the consolidation itself, none visible from the spikes:

1. ★ **`SpikeHomTy`'s `Σ'` clause is RAW-VALID but TYPING-HOSTILE.** The spike
   elided the second component's transport; promoting the clause as-is puts
   `B` (formed in `Γ ▹ A`) under a binder of type `Hom A (fst p) (fst q)`,
   so `⊢ty`-preservation fails at the reduct. Transport is a TERM former —
   W2's eliminator introduces it — so `Hom` at `Σ'` is STUCK for now,
   **deferred not dropped**, documented at the constructor.
2. ★★ **LEVEL 0 NEEDS NO `Hom` CLAUSE AT ALL.** There is no `⌜Hom⌝` code, so
   no level-0 type ever reduces to a `Hom`; the entire LR extension is
   level-1 only (`StkHd` + `⊩₁Hom` with membership `SN` + `homSem₁`). The
   predicative cut does structural work a second time. This also ANSWERS the
   `SpikeHomLR` gate W2 carried: the `⊩`-clause needs **no larger scope** —
   `homSem₁` recurses through the stored `Π`-family, the already-handled
   pattern.
3. **The `⁺ᵀ` development must decode-first at `El` codes** (one-step-behind,
   `El`'s own pattern) and weaken-AFTER-develop at `Hom-U` — those two
   choices are what keep the type-level triangle renaming-commutation-free.

**Non-vacuity, internal:** `⊢hom-id` (identity at `Hom U ⌜base⌝ ⌜base⌝`, by
conversion alone) and ★ `⊢hom-across` (`λx.λy.x` at
`Hom U ⌜base⌝ (⌜Π⌝ ⌜base⌝ ⌜base⌝)` — `SpikeHom`'s fee-is-real pair as a
KERNEL derivation: an inhabited `Hom` between definitionally distinct codes).

🔶 **W2's REMAINING DEBT — the eliminator. DESIGN NOW SETTLED (2026-08-01,
`4517ab9d`/`f3610f01`), consolidation still owed:**

1. ✅ **`no-sym`'s strength is fixed: `sym` is FALSE at `U`, not merely
   underivable** (`SpikeNoSym` — `Hom U ⌜⊤⌝ ⌜⊥⌝` is empty while the reverse
   is inhabited). The eliminator's variance premise guards a semantic
   boundary; internal `no-sym` is a soundness regression test.
2. ✅ **`refl` computes by CODE annotation** (`SpikeHomRefl`, design (B)
   taken, (A) rejected on recorded cost): `hrefl c t ∷ Hom (El c) t t`,
   unfolding pointwise at `⌜Π⌝` (structural termination, measured), stuck
   canonically at `⌜base⌝`/`⌜Σ⌝`/`⌜Hom⌝`/neutral codes. The RTy-annotated
   alternative forces `_⟶_`/`_⟶ᵀ_` MUTUAL and merges both confluence
   developments — rejected. Price accepted instead: a **`⌜Hom⌝` code**
   (hom-sets of small types are small; predicativity intact — still no code
   for `U`), its decode rule, and ⚠ the REPEAL of §4.3 finding 2: with
   `⌜Hom⌝`, level 0 CAN reach `Hom`, so `⊩₀Hom` + `homSem₀` come due.
   Gap, documented: `refl` at a LARGE `Σ'` `Hom` waits on `Σ'` unfolding.
3. ✅ **W3's FLOOR is IN THE TREE** (`NbEPDirDBVar`, additive-only): the
   polarity judgment `Pos`/`Neg` with occurrence machinery. `Hom` is
   contravariant in its first endpoint, covariant in its second;
   `comp-pos` (the composition motive is `Pos` ⇒ `tr` will derive `trans`);
   ★ `sym-motive-not-pos` (one-line inversion) + `sym-motive-neg` — `sym`'s
   motive is `Neg`, and the checker can tell. `⊢tr`'s spec is in that
   module's header. Open there, honestly labelled: typed (⊢ty-mutual)
   refinement, and the SEMANTIC validation of `Pos` (= `tr`'s `fund` case).
4. 🔶 **(4a) ✅ DONE — the `tr` spike** (`SpikeTr`, 2026-08-01). The
   motive-annotation question closed AS FORECAST — CODE-annotated motives,
   the (B) move a third time: `tr d p e` with `d : RTm (Γ ∙)`, motive
   `El d`; every congruence stays in `_⟶_` (visible by construction — the
   spike's whole reduction relation is term-level), and a frozen motive
   cannot arise (`ξ-trᵈ` is ordinary term congruence, so
   `El ((λy.y) (var vz))` as a motive CODE just β-reduces in place).
   `⌜Hom⌝` does make the composition motive code-expressible, and the
   floor does derive `trans` — both confirmed.

   ★★ **But the RULE SET forecast above was refuted twice and extended
   once — the spike earned its gate:**
     - ★ **`tr` is an eliminator of its PATH; every rule must be keyed on
       the path's canonical form** — the way `app` computes on `lam`. The
       forecast rules key on the motive and leave the path arbitrary, and
       both have UNJOINABLE RAW critical pairs, measured (`Conf` is a raw
       theorem, so ill-typed peaks count): unkeyed taut vs J at
       `p = hrefl …` (`taut-unkeyed-breaks-confluence`), and unkeyed J vs
       `hrefl`'s own `⌜Π⌝`-unfold (`J-unkeyed-breaks-confluence`).
     - **The fixed set is FIVE path-keyed rules**, root-deterministic
       (measured, `det`): `J-base`/`J-Σ`/`J-Hom` — the J-equation only
       where `hrefl` is CANONICAL, i.e. at the head-STABLE stuck codes
       `⌜base⌝`/`⌜Σ⌝`/`⌜Hom⌝` (not at neutral codes, which can still
       become `⌜Π⌝`-headed and reopen the pair one step later);
       `taut-lam` — directed univalence a third time, on a canonical
       path, producing the β-redex rather than contracting it; and
       ★ `pw-Π`, **the rule the forecast missed** — pointwise composition
       at a `⌜Π⌝`-ambient `⌜Hom⌝` motive, FORCED by canonicity:
       `comp-pos` licenses arbitrary ambient codes, and at `⌜Π⌝` the path
       is a lambda, so an unruled `tr` is a stuck non-neutral at a `Π`
       type. Its recursion is structural in the ambient code (measured,
       `pwtrans`, no pragma).
     - ★ **Constant motives: no discard rule AND no license.** A discard
       rule needs an occurrence side-condition (vz-freeness is not a
       pattern); without one, a licensed constant motive is the same
       canonicity hole. Exclusion loses nothing — measured over the REAL
       syntax (`const-motive-invisible`): substituting either endpoint
       into a `vz`-free code yields the SAME term, so the payload already
       has the target type on the nose. Hence **`PosC` ⊊ `Pos`**: `Pos`
       states semantic covariance; `tr`'s premise is covariance WITH a
       computation rule (`{posc-var, posc-Hom}` only; `sym`'s motive code
       refuted by pattern alone). Everything dropped is derivable without
       `tr`: constant ⇒ identity, `Π`-shaped/large ⇒ pointwise per
       instance (the `⊢hom-id` pattern), U-ambient composition (`Pos`
       admits it, no code expresses it) ⇒ plain function composition.
   The revised `⊢tr` spec is in `SpikeTr` §0; `NbEPDirDBVar`'s header
   spec is marked superseded in place.

   ✅ **(4b) DONE, STAGE 1 — THE CONSOLIDATION LANDED** (2026-08-02, one
   session): `⌜Hom⌝`/`hrefl`/`tr` through the whole tower — Pi (syntax +
   the four mutual fusion families + `⌜Hom⌝-cong₃`/`tr-cong₃`) → Var
   (`occTm`/`occ-ren-tm` + `PosC` beside `Pos`, with `occ-ren-eq`/
   `occ-sub`/`subTm-occ` and `posc-ren`/`posc-sub` — `⊢tr` moves under
   renaming AND substitution) → Type (`El-⌜Hom⌝` decode; the path-keyed
   `tr` rules; `⊢⌜Hom⌝`/`⊢hrefl` in the base judgment) → SR (with the
   `wk₁-sub`/`swp-sub` renaming bridges, pre-paid for W2b) → Conf (the
   development's `tr` case tree + triangle, root-orthogonality live) →
   Inj (`pEl-⌜Hom⌝` through `⟹ᵀ`/`⁺ᵀ`/triangle; ⁺ᵀ decodes El-codes
   first, as before) → Subj (`sr` for every new rule reachable in the
   base judgment; `occ-red`/`posc-red` — reduction cannot introduce the
   transported variable; the `Hom`-reduct analyses `homred-inv`/
   `Hom-to-Hom`/`hom-to-Π`) → NbEPDirDBTr (★ NEW, additive: the STAGED
   `_⊢ᵗ_∷_` with `⊢tr` on the SpikeTr spec and FULL `srᵗ` — the J cases
   extract endpoint conversion via confluence, the taut case re-types
   through the `Hom-U` unfold, refuting `Hom-Π` against the tautological
   motive's typing) → LR (`⊩₀Hom` + `homSem₀` per the repeal — level 0
   is SIMPLER, no `U` clause; `sn-cH`; `hrefl` as an inert NEUTRAL —
   `sne-hrefl`/`ne-hrefl` — exactly as long as its unfold family is
   deferred) → Fund (`⊢⌜Hom⌝` via `sem-⌜Hom⌝`/`homSem₀`, `⊢hrefl` via
   `sem-hrefl`/CR3; anti-renaming extended per constructor — the §3.1
   flip condition's actual bill) → Dec.  Also fixed in passing:
   `NbEPDirDBIdJ` had been broken since the `Hom⟶` rename (not one of
   the nine verified modules).

   **Done-when, DISCHARGED** (demos at the end of `NbEPDirDBTr`):
   `⊢trans-tr` — `trans` internally, `tr` at the `⌜Hom⌝` composition
   motive, `PosC` by computation; `trans-tr-J` + `⊢trans-tr-red` — the
   J-equation computes the composite away WITH typing preserved (`srᵗ`);
   `univ-tr` — directed univalence computing a third time (taut then β);
   `no-sym-tr` — the `sym`-motive `tr` fails `PosC` by pattern
   (`SpikeNoSym` holds the semantic half).  `Conf`'s root-orthogonality
   is in the tree as the development's case structure.

   🔶 **(4b′) THE RESIDUE — two findings, both spike-grade, honestly
   parked:**
     - ★ **THE `⌜Π⌝`-SPINE FINDING** (corrects SpikeTr AND SpikeHomRefl):
       `⌜Hom⌝` is NOT a uniformly stuck head.  `⌜Hom⌝ⁿ (⌜Π⌝ …) …` codes
       (higher paths over function-type paths) decode to types that
       unfold pointwise to `Π`s, so `hrefl` there is NOT canonical —
       its unfold is a SPINE-RECURSIVE family (the single `hrefl-Π`
       clause SpikeHomRefl measured is incomplete), and J-at-`⌜Hom⌝`
       needs spine-stuckness, an unbounded-depth key no finite pattern
       expresses.  THE CANONICITY PACKAGE — `hrefl`'s unfold family,
       J-Hom, `tr-pw`, and the spine judgments `Pw`/`StkC` that key them
       — is deferred as ONE unit (W2b).  Coherent: higher paths were
       already unscoped; this tower's LR proves weak normalization +
       decidability, NOT canonicity, so nothing here needs them.  Until
       W2b, `hrefl` is operationally INERT and LR-neutral.
     - ★★ **THE `SpikeHomLR` GATE REOPENED AT THE ELIMINATOR — AND
       `SpikeTrLR` HAS NOW ANSWERED IT** (2026-08-02, same day).  The
       answer is NOT the guessed membership-clause transport closure:
       the JM presentation extends PER-ELIMINATOR instead, and that
       machinery is **LANDED AND CHECKED in the tower**:
         * `NbEPDirDBLR`: the head strategy gains the eliminators'
           scrutinee positions (`snr-hreflᶜ` — `hrefl` is an eliminator
           of its CODE — plus the J rules, taut, and `snr-trᵖ` as head
           steps, discarded material carried as `SN`); the permanently
           stuck `tr` shapes are NEUTRAL, keyed by the reduction-closed
           family `NeV`/`StableCd`/`PathStk` (`sne-tr-stk`/
           `sne-tr-lam`); the strategy is DETERMINISTIC (`snr-det`), so
           `SN` and every membership move FORWARD along it (`sn-whred`,
           `mem-whred₁`) — `exp₁`'s converse, the transfer `fund`'s
           path analysis runs on.  Anti-renaming paid per constructor
           (`NbEPDirDBFund`).
         * `SpikeTrLR`: `hom-shape` (reducts of a `Hom` type are `Hom`-
           or `Π`-headed — refutes the impossible interps) and
           ★ `homSem₀-mem-endpoints` (memberships at `homSem₀`-interps
           are ENDPOINT-BLIND — SN at stuck leaves, pointwise through
           `Π`) — the lemma that carries the payload across the
           J-branches' endpoint switch at composition motives, where
           `PosC` makes every other motive component endpoint-blind
           (`subTm-occ`).
       ✅ **THE MERGE LANDED — STAGE 2 COMPLETE** (2026-08-02, same
       session): `⊢tr` AT THE COMPOSITION MOTIVE is in the BASE
       JUDGMENT (`NbEPDirDBType` — motive shape pinned in the rule,
       `posc-Hom`'s content inlined, the motive's `⊢⌜Hom⌝` premise
       carried COMPONENTWISE so `fund`'s recursion stays structural,
       endpoint premises per the option-A pattern), with:
         * `sr` (`NbEPDirDBSubj`: `gen-tr` records the pinned shape;
           the J cases as before; taut is VACUOUS in the base judgment
           — the rule cannot state it; `ξ-trᵈ` via `hom-step` inversion
           so the recursion stays on the original step);
         * ★★ `fund` (`NbEPDirDBFund`) — THE SEMANTIC VALIDATION OF THE
           VARIANCE FLOOR, delivered: the path analysis by mutual
           induction on `SN p` (and, inside the `hrefl` branch, on the
           code's `SN`): head steps expand (`exp₁` ∘ `mem-whred₁`),
           permanently stuck shapes are `CR3₁` at `sne-tr` with keys
           from the classifier extractors, and the J branches hand the
           payload across the endpoint switch with
           `homSem₀-mem-endpoints` — the motive's components are
           endpoint-blind (`subTm-occ` on the vz-freeness premises), so
           source and target interps are the SAME `homSem₀` up to the
           transported endpoint.  No interp refutations were needed at
           all — the analysis is clause-generic in the path's interp.
         * The shape layer was REWORKED to BOOLEAN classifiers
           (`spine?`/`stablecd?`/`pathstk?`/`trstk?` in `NbEPDirDBLR`),
           which widened coverage (spines over inert heads, canonical
           inert paths), merged the two `tr`-neutral forms into one
           (`sne-tr`), made every refutation definitional, and
           collapsed the anti-renaming bill to one renaming-equality
           per classifier.
       **Done-when, now at FULL strength** (`NbEPDirDBTr` demos):
       `⊢trans-base` — `trans` internally in the BASE judgment;
       `⊢trans-base-red` — the J-equation under `sr`; and
       ★ `trans-wnorm = refl` — `wnorm` (through `fund`) COMPUTES the
       composite along an identity path down to the original path.
       ✅ **STAGE 3 (2026-08-02, next session): THE TAUT MOTIVE MERGED
       TOO — the eliminator is CLOSED.**  The resolution was a fourth
       route none of SpikeTrLR's candidates named: **RE-KEY J ON THE
       MOTIVE** (`tr-J-base`/`tr-J-Σ` fire only at `⌜Hom⌝`-headed
       motives).  Soundness of the narrowing: at a `var` motive a path
       can never be a typed `hrefl` (`Hom U t u` unfolds toward `Π`,
       `Hom (El c) s s` toward a stuck `Hom` — shapes clash under
       confluence), so the un-keyed rule was never typed-exercised;
       keying it makes the taut J-configurations PERMANENTLY STUCK
       (`trstk?` gained the var-motive clause; `_⁺` stays path-major
       with motive-discriminating helpers `trB⁺`/`trS⁺` so every
       congruence row still reduces).  The obstruction dissolved — the
       J-branches that needed `t ≅ u` ceased to exist:
         * `⊢trU` (ambient pinned `U`; a merely-convertible ambient
           routes through `⊢conv` on the path) joins the base judgment:
           transport along a universe path IS application — directed
           univalence in the kernel;
         * `sr`: `gen-tr` returns `TrGen` (= `TrInv ⊎ TrInvU`); the
           taut redex's case is REAL now, and SIMPLER than the staged
           proof (the pinned `U` makes `via-Π` a one-line `U-reduct`
           clash — no `gen-var` renaming dance);
         * `fund`'s `⊢trU` case: `Hom U tI uI` interps only as `⊩₁Π`
           (`hom-shape`, now promoted into `NbEPDirDBSubj`, kills
           base/U/ne/Σ'; `U-reduct` against `StkHd` kills stuck-Hom);
           the path analysis needs NO J-branches at all — neutrals and
           canonicals are `CR3` at the var-motive key, and the one
           computing branch (taut) is discharged by the path's
           Π-membership: the app-closure applied at the payload;
         * the staged judgment `_⊢ᵗ_∷_`/`srᵗ` is RETIRED —
           `NbEPDirDBTr` is now the demos module: `⊢trans-base` /
           `⊢trans-base-red` / ★ `trans-wnorm = refl`, and
           `⊢univ-base` / `⊢univ-red` / ★ `univ-wnorm = refl` (`wnorm`
           computes taut-then-β down to the payload), plus `no-sym-tr`.
       The remaining open item is the CANONICITY PACKAGE (W2b: `hrefl`
       spine unfolds, J-Hom, `tr-pw`, the `Pw`/`StkC` spine judgments).
       ✅ **W2b SPIKED (2026-08-03, `SpikeCanon.agda`) — the rule
       format is SETTLED and the cascade fear was unfounded.**
       Findings:
         1. ★★ The stage-3 Boolean-classifier architecture already IS
            the answer: `Pw`/`StkC` become total Booleans `pw?`/`stkC?`
            (mechanized on the real syntax, with the full stability
            bill paid: red/ren/sub for both keys, plus
            `pwBody-red`/`-ren`/`-sub`), the spine-recursive unfold
            family collapses to ONE rule `hrefl-pw` whose RHS calls the
            total function `pwBody`, and every rule carries only a
            Boolean-equality key — the `sne-tr`/`trstk?` pattern.
            `_⁺` stays a total syntax-directed function; Takahashi
            stays premise-free.
         2. ★★ NO code-level `⌜Hom⌝-Π` (the tempting alternative):
            it would make the pinned motive `⌜Hom⌝ c a (var vz)` a
            root redex whenever `c` is ⌜Π⌝-headed — `ξ-trᵈ` then
            rewrites the motive to a shape no typing rule matches
            (gen-tr/SR break) and the pair with `tr-pw` cannot join.
            The spine collapse lives in the FUNCTION `pwBody`, not in
            the reduction relation.
         3. The rule set, final (keys inlined): `hrefl-pw` (pw? C),
            `tr-J-Hom` (stkC? c₁ — excludes ⌜Π⌝-able codes by
            `stk⊥pw`, and neutrals because substitution could make
            them ⌜Π⌝-able), `tr-pw` (pw? c, literal `var vz` endpoint
            like taut).  RHSs are import-ready definitions
            (`hreflPwRHS`, `trPwRHS`, `pwShift`).
         4. ★ `pw-Hom-decode` (the coherence centerpiece, MECHANIZED):
            `Hom (El C) x y ⟶ᵀ* Π (El (pwDom C)) Body` with
            `Hom (El (pwBody C)) (x·) (y·) ⟶ᵀ* Body` — the JOIN every
            new rule's sr case converts through.
         5. All critical pairs discharged by the keys (spike header
            §Finding 3 has the list).
       The landing bill (measured, spike header): Type (3 rules +
       classifiers), Conf (`_⁺` Boolean rows + parallel rules +
       parallel `pwBody`-compat, template = `pwBody-red`), Subj (sr
       via `pw-Hom-decode` + a `StkAmb` analysis for J-Hom mirroring
       `BaseAmb`/`ΣAmb`), LR (classifier flips: `sne-hrefl` keyed,
       `pathstk?` hrefl-clause narrows to neutral spines, `trstk?`
       lam-clause gains `not ∘ pw?`; three new SNRed heads), Fund
       (`⊢hrefl` splits on `pw?`; `go`/`goh` gain three branches).
       W2b's done-when: closed normal codes of type `U` split as
       `pw? ∨ stkC?` (code canonicity) ⇒ closed paths at decoded types
       are `hrefl`s or lambdas ⇒ closed `tr`s always step.
       🟡 **G1 LANDING IN PROGRESS (2026-08-03, four commits, tree
       MID-CASCADE — Fund is the frontier).**  Landed GREEN:
         * part 1 (`d2d4c05b`): the three rules through Type + SR
           (⟶-sub with the pwShift-sub bridge) + the ENTIRE Takahashi
           development (parallel rules, Boolean `_⁺` rows via
           hr⁺/trH⁺/trHK⁺/trP⁺/trPK⁺, the triangle via the
           hr-tri/trHK-tri/trPK-tri dispatchers).  Confluence of the
           full W2b relation is PROVEN.
         * part 2 (`c9117b41`): subject reduction for all three rules
           (`pw-Hom-decode`/`pw-El-decode` promoted into Subj; StkAmb;
           ctx-conv via identity-substitution; `pw-app` the workhorse;
           ★ sr tr-pw assembles the inner pointwise ⊢tr completely).
         * part 3a (`1044f3ef`): the LR classifier flips (stablecd? →
           dead-code-ness; spine? guards hrefl heads by the NEW `nopw?`;
           trstk? through `trlam?`; keyed sne-hrefl/ne-hrefl/sem-hrefl;
           snr-hrefl-pw/snr-J-Hom/snr-tr-pw; snr-det via snr-nonpw and
           the disjointness lemmas pw⊥dead/stk⊥dead/nopw⊥pw/dead→nopw).
       Fund: anti-renaming is EXTENDED (sne-hrefl key transport;
       snr-anti for all three new heads incl. the full tr-pw equality;
       the ⌜Π⌝/⌜Hom⌝-code snr-trᵖ rows now recurse).  ⚠ Fund does NOT
       compile: the frontier is `fund`'s ⊢hrefl case (line ~732, the
       sem-hrefl key) and, behind it, the go/goh W2b branches.
       ★★ TWO DISCOVERIES made while landing (both spike-grade, record
       per the raw-M3c rule — do NOT grind):
         1. **CSR — the head strategy must descend ⌜Hom⌝ SPINES.**
            `hrefl (⌜Hom⌝ (app (lam ⌜base⌝) x) a b) t` is typed, SN in
            reality, but UNWITNESSABLE in the JM family: the code is
            neither head-reducible (no snr enters ⌜Hom⌝'s spine) nor
            neutral (`nopw?` is false for the transient β-redex, and
            Boolean keys cannot see through reduction).  Fix: replace
            `snr-hreflᶜ`'s premise with a code-spine reduction
            `CSR c c'` (csr-here : SNRed → CSR; csr-hom : CSR c c' →
            CSR (⌜Hom⌝ c a b) (⌜Hom⌝ c' a b)) — still deterministic
            (SNRed-det at the bottom + no SNRed at ⌜Hom⌝ heads), still
            ⊆ ⟶ (ξ-hreflᶜ ∘ ξ-⌜Hom⌝ᶜⁿ), disjoint from hrefl-pw via
            snr-nonpw at the spine bottom.  LR-only change (SNRed is
            not the rewrite system); consumers: sne-whred, snr-det,
            snr-anti (a csr-anti), fund's goh.
         2. **THE WALL — pointwise `hrefl` membership needs semantic
            data the U-clause does not carry.**  At a pw code C, the
            membership of `hrefl C t` at the Π-interp needs, for each
            (v,r), the membership of `hrefl (pwBody C)[v] (app t v)` at
            the body interp — and in particular SN of the SUBSTITUTED
            body code.  When the code has a syntactic derivation
            (⌜Π⌝/⌜Hom⌝-headed under σ), fund-recursion on the CODE'S
            DERIVATION supplies everything (fund dδ at (σ ,ₛ v) envs —
            the ⊢tr case's own pattern, so fund's ⊢tr go/goh pw
            branches are POSSIBLE, just large).  But codes reached
            through the ENVIRONMENT (⊢var: σ x an arbitrary pw code
            whose ⊩ˢ-entry is only (SN, ⊩₀ (El _))) or through
            β-TRANSIENT forms (⊢app-typed codes reducing to ⌜Π⌝;
            reducts' typing exists via sr but is not structurally
            smaller) have NO derivation to recurse on — the membership
            clause `⊩₁U ∋ c = SN c × ⊩₀ (El c)` is
            information-theoretically too weak.  THE FIX is SpikeTrLR
            candidate (ii) resurfaced: a U-MEMBERSHIP PAYLOAD — carry
            the hrefl-closure (∀ t → mem t → mem (hrefl c t) at
            homSem₀) inside the U-clause, built once per code-former
            in fund (derivations in scope ✓), transported along
            exp/conversion like every membership.  This is a RELATION
            CLAUSE CHANGE ⇒ per the gate discipline: **SPIKE FIRST**
            (SpikeUPay: the clause shape, irrel₁'s U-U case, emb/exp
            transport, and the per-former payload constructions).
       ✅ **SpikeUPay PASSED (2026-08-03, same session,
       `SpikeUPay.agda`)** — the payload design is MECHANIZED:
         * `PayT` (the unfolding tree, one existential node per
           semantic Π-layer: next code c′, SN c′, an s-PARAMETRIC
           head-strategy chain `app (hrefl c s) v ⟶snr* hrefl c′
           (app s v)`, and the recursive payload at the body interp)
           is definable by plain recursion over the REAL ⊩₀ — the
           positivity/stratification risk is RETIRED;
         * `pay-⌜Π⌝` composes from exactly `fund dδ`-shaped data (the
           chain is snr-app(snr-hrefl-pw) then snr-β; endpoints by
           `wk-single` on the nose);
         * the payload rides inside the membership tuple and mentions
           only the member — `irrel₁`'s U-U transfer stays the
           IDENTITY; the clause change does not reopen irrel.
       The enriched clause: `⊩₁U p ⊩₁∋ c = SN c × Σ (⊩₀ (El c))
       (λ R → PayT R c)`.
       ✅ **THE PAYLOAD LANDED (2026-08-03, `5e56e24b` + this
       commit).**  In LR: CSR (snr-hreflᶜ over code-spine reduction,
       with csr-det/sn-csr/csr-stk⊥ and the snr-det rows re-dispatched)
       and the FULL PayT machinery (the enriched U-clause, payChain,
       payT-exp/-whred/-irrel/-cast/-code, ★ payHomT mirroring
       homSem₀, sem-⌜Π⌝/⌜Hom⌝ payload-carrying).  In Fund: csr-anti;
       the ⊢⌜Π⌝ case supplies the body payload straight off `fund dδ`'s
       enriched membership (THE ENV WALL IS GONE); ⊢⌜Hom⌝ passes the
       inner payload; ★★ `semHreflPay` — ⊢hrefl's semantic validation:
       non-Π interps via `snHH` (the Spine/CSR raw-SN construction,
       ⌜Π⌝-leaves refuted through the interp-code LINK `lk` by
       confluence), Π-interps by paying out the payload node (the
       closure head-expands along `payChain` into the recursion one
       Π-layer down; the SN part instantiates the node at the fresh
       `x₀` — the sn-body pattern); `codeNorm` (the code-FATE analysis)
       powers goh's new tr-J-Hom/dead branches with memberships
       travelling back along the head star.
       🔴 **THE LAST FRONTIER — `go (sn-lam …)` (fund's ⊢tr case, the
       lam-path branch).**  The old key (`homheaded? = refl`) is gone:
       `trlam?` now keys on `nopw?` of the motive's code, and THREE
       configurations exist: pw (tr-pw fires — the membership needs a
       `semTrPw` mirroring `semHreflPay`, built from the MOTIVE's
       payload node, with the inner path's membership from the path's
       own Π-closure via `mem-whred₁ (snr-β)`), nopw-dead (CR3), and
       TRANSIENT (a β-redex hiding in the motive's code) — the last is
       JM-unwitnessable without a MOTIVE-side spine rule, exactly
       discovery 1 at the tr level: add
       `snr-tr-mot : CSR c c' → SNRed (tr (⌜Hom⌝ c a (var vz)) (lam f) e)
       (tr (⌜Hom⌝ c' a (var vz)) (lam f) e)` (disjoint from snr-trᵖ by
       lam-path shape, from snr-tr-pw by csr-nonpw), tighten `trlam?`'s
       key from `nopw?` to a CSR-dead classifier (`deadmot?`: like
       stablecd? but ⌜base⌝/⌜Σ⌝ true, hrefl FALSE), and give `go`'s
       lam-branch a codeNorm-style motive-fate analysis whose pw-leaf
       runs semTrPw.  All patterns exist; it is one more
       semHreflPay-sized construction.
       **Next:** snr-tr-mot + deadmot? in LR → semTrPw + go's lam
       branch in Fund → sweep (Tr/Dec/Norm/Eta/IdJ/Sig) → G2 (code
       canonicity) → G3 (Σ' Hom, flip stkC? once).
5. **`Σ'` unfolding** — once `tr` exists, revisit the stuck `Σ'` clause with
   the transported second component; `StkHd` loses `sh-Σ`, and `refl` at
   large `Σ'` stops being a gap.

*(Historical: the do-not-start-at-5 rule held through items 1–4. `SpikeVar`,
`SpikeHom`, `SpikeHomTy`, `SpikeHomNat` and `SpikeHomNatU` between them cost
well under two hundred lines of proof and moved this workstream five times —
retiring two proposed tests and one wrong abstraction along the way. With item
4 closed by the owner on 2026-08-01, the gate condition is met: the cascade
now proceeds on a settled shape, paid once.)*

### Linearization line

- **Retire `Rec`/`Dyn` into `NbEPLinCore`** — mechanical; the core is a superset.
- **`Pass`/`QTT` — the source-language question is ALREADY ANSWERED.** ⚠ An
  earlier draft of this handoff warned it was an open shape question. It is not;
  the decision log settles it (PLAN §8.5, checked against `docs/compiler/
  decision-log.md`):

  - **D063** introduced `⊢ᵐ`, the point-free MORPHISM REALM; **D070** moved
    closed lambdas into it by bracket abstraction — *"a closed lambda IS a
    morphism; bracket abstraction is the isomorphism; the `⊢ᶜ`/`⊢ᵐ` distinction
    for lambdas is a syntactic presentation artifact"* — explicitly to "remove
    the only non-point-free thing in the pipeline". So point-free is where Once
    is DELIBERATELY HEADING; `NbEPLinPass`'s source SHAPE was right, only its
    provenance was wrong.
  - ★★ **NOTHING TO OVERRIDE.** "Grade" in the log means **purity**, not
    quantity: every erasure statement traces to D046 (`eff ∘` IS `pure ∘`), and
    quantity was never erased — it lives in `ArrowKind.quantity` and reaches the
    IR. D063's "no usage index" is justified by CLOSEDNESS ("closed ⇒ no usage
    index, like `⊢ᵍ`") — `Ψ` counts CONTEXT variables, and a closed morphism has
    none; silent on how a morphism uses its DOMAIN. And **D066 overrides D065**
    ("Bare Morphisms Are Grade-Free" → "The Morphism Realm Is Grade-Indexed")
    *because a grade-free `⊢ᵐ` was "incomplete and unsound"* for proving
    `morph-complete` — **the index came back because the proofs needed it**, and
    in the shape the POC wants: indexed at the typing level, `realize-morph`
    ignores it, erased at runtime. No decision anywhere discusses erasing
    QUANTITY or enforcing linearity on morphisms — checked across all 66.
    ⇒ The POC adds a SECOND, ORTHOGONAL index on D066's pattern, on an axis no
    decision has closed. D046's rationale invites exactly this: "Keep them
    independent and prove the restrictions at the use sites that need them."
    ⚠ Open and unchecked: whether enforcing linearity REJECTS programs the
    compiler currently accepts. That WOULD be a change to Once.

  ⇒ **Source language: `⊢ᵐ`-shaped point-free combinators with the usage index
  put back.** ⚠ And `NbEPLinQTT` should TRANSFER rather than be redone — D070
  says bracket abstraction IS the isomorphism, so W0c's grading results should
  carry to the point-free form. Confirm that before rebuilding anything.

--------------------------------------------------------------------------
## 5. NEGATIVE RESULTS — do not re-attempt

Carried forward from 2026-07-30 §5 (spine inversion; the unstratified `U`
clause; the Kripke action; syntactic validity; erasure), plus this session:

**(f) `ren₁` — a renaming action on `⊩₁`.** §3.1. Blocked, argued not measured,
not a refutation. Flips only if the kernel goes η-long/NbE-shaped.

**(g) `Free` in one recursion.** §3.2. Measured (`NotStrictlyPositive`).
Stratification is the answer, and it is built.

**(h) "Port the real IR to the linear core" as a POC item.** §1. Not a technical
obstruction — a wrong direction. `NbEPLinIR` deleted.

**(i) `Ty` cannot be extended** — ten of the eleven modules matching its
constructors are in `normalizer/TCB0/`. Still true, but **no longer the
governing reason**: §1's rule decides it regardless, and the "conservative
extension back to `Ty`" that this fact once seemed to require is NOT needed.

### 5.1 Method notes earned this session

- **Breaking a dependency while keeping the names is not breaking the
  dependency.** Agda cannot catch it; only reading can.
- **Match a claim's strength to the decision it licenses.** "UNPROVABLE" was
  written where "the structural transport is blocked" was what had been shown.
  This repo has been burned once by a claim outrunning its evidence (`subTI`,
  actually false while asserted).
- **A spike that passes is not automatically consolidated.** `SpikeLinNu`
  passed, but consolidating it meant first noticing the object language was
  borrowed.

--------------------------------------------------------------------------
## 6. TREE STATE

Everything committed. Working tree clean except two long-standing untracked
files: `NbEPDirDTTChMFDev.agda`, `NbEPDirDTTChMFProbe.agda` — superseded dev
stubs whose findings are prose in `HANDOFF-NbEPDirDTTChMF.md` §4.2″/§4.2‴.
Delete freely.

`check-mf.sh` (untracked local helper, git-excluded) type-checks a module:
`./check-mf.sh NbEPDirDBFund.agda` from `bootstrap/poc/OCP0009`.

Verified this session, all exit 0: `NbEPDirDBFund`, `NbEPLinCore`,
`SpikeLinNu`, `NbEPDirDBLR`, `NbEPDirDBType`, `NbEPDirDBSubj`, `NbEPLinDyn`,
`NbEPLinQTT`.

⚠ `NbEPLinCore` is `--guardedness` as well as `--safe` (guarded corecursion for
`Nu`; **no sized types** — hard ban).
