# OCP-0009 · HANDOFF 2026-08-17 — gap A: the certificate is TYPED; two pieces left

> ⏩ **CURRENT.** Supersedes `HANDOFF-2026-08-16.md`. Gap A's blocker
> (`StepExt`) is CLOSED and the recursive call's certificate is TYPED.
> **PICK UP HERE:** `irrAt` + `gcd-gt-eq`, both drafted verbatim below.

## VERIFIED THIS SESSION

| piece | what it is | cost |
| --- | --- | --- |
| `gcdStepExt` | gcd's `StepExt` — gap A's blocker, undischarged since 2026-08-14 | 5.2s |
| `gcd-unfold` | gcd unfolds INTERNALLY, at variables | 5.4s |
| `certEq` | the recursive call's certificate ≡ `⊢desc-left`'s subject | 27s |
| `⊢certClean` | …and therefore TYPED, by one `⊢desc-left` | 27s |
| `appAux`/`PAIRᵍ`/`prvCast` | the last small interfaces, in `…GcdEqs` | 5s |

## ⭐ WHAT MADE THE CERTIFICATE WORK — six routes failed before this

Ask for a **TERM** equation at a **RAW** context, and move the substitution
by **NATURALITY** (`plusMonoLTm-sub`/`monusLtTm-sub`), never by computation.
Every earlier route asked Agda to compare a TYPE in which the gcd body sat
inside a substitution — that is what it cannot afford, and no tactic fixes
it. ⚠ Pass the naturality lemmas' arguments EXPLICITLY: recovering `x`
from `subTm σ x` needs `subTm` inverted, which is the whole problem.

⚠ `opaque` on `plusMonoLTm` was tried and REVERTED. It made this 5x faster
(5s vs 27s) and `…GcdStep` 3x faster, but needed `unfolding` at a
proliferating set of sites — the numeral demos, `ren-gcdStp`, `goal-shape`,
then more in `…GcdLib`. 27s transparent costs the tree nothing.

## ⭐ UPDATE, LATE 2026-08-17 — equation 3 TYPECHECKS; one obligation left

| piece | state |
| --- | --- |
| clean certificate AT CONSTRUCTION | ✅ `gcd-gt-term` hands back `gtCert a' b'`; typing it is one `⊢desc-left` |
| `gcd-gt-eq` (equation 3) | ✅ **typechecks, 11.5s** — CONDITIONAL on the irrelevance witness |
| `irrAt` | ❌ OOMs; the single remaining obligation |

⚠ SIX EXPERIMENTS ON `irrAt`'s OOM, and the mechanism is STILL not
identified. Two proposed explanations were tested and FALSIFIED — it is not
`irr-ind`'s instantiation (returning it is EXIT 0) and it is not `prvOk`'s
pattern match (`Prv` as a record with eta changed nothing). Module isolation
does not help either: that lever addresses ACCUMULATION, and this is one
definition.

⚠⚠ AND PROFILING CANNOT SETTLE IT: `--profile=all` prints only on
COMPLETION. Tried at 5500M, at 6200M, and at 4000M + 5G swap — OOM every
time, no profile. It is not marginally over the cap.

⭐ THE EXPERIMENT THAT WOULD SETTLE IT, and which none of the six addressed:
shrink the instance until it COMPLETES, profile that, extrapolate. E.g.
`irr-ind` at a trivial `StepExt` (a step ignoring its IH, so the four leaves
collapse), or at a carrier smaller than `PairT`. That separates "`irr-ind` is
inherently large" from "gcd's `stp` makes it large".

## WHAT IS LEFT — two pieces, both drafted

⚠ `irrAt` OOM-killed inside `…GcdEqs`, and again in its own module — but
the second result is INCONCLUSIVE: a concurrent `make agda` was running
(two agda processes OOM-kill each other). **Re-measure on a quiet box
before concluding anything about its size.**

### 1. `irrAt` — in its own module `…GcdRec` (drafted, unverified)

```agda
------------------------------------------------------------------------
-- OCP-0009 — GAP A, EQUATION 3: gcd's RECURSIVE STEP, INTERNALLY.
--
-- ⚠ SPLIT OUT OF `…GcdEqs` FOR COST, 2026-08-17.  `irr-ind` instantiated at
--   gcd's step is a big term — a `natrec` over four leaves carrying the
--   whole `irrT` motive — and adding it to `…GcdEqs` OOM-killed a module
--   that is otherwise 5s.  Alone it has room.  ⭐ Same isolation that took
--   `leaf₃s` from an OOM to 10s and `split2` from an OOM to 4.8s: one big
--   term per module when the term is big enough.
------------------------------------------------------------------------

{-# OPTIONS --safe #-}
module poc.OCP0009.NbEPDirDBExamplesGcdRec where

open import normalizer.Syntax.Types using ( _≡_; refl; trans; cong; sym )
open import poc.OCP0009.NbEPDirDBPi
  using ( Cx; ε; _∙; vz; vs
        ; RTy; El; Hom; Nat; Id
        ; RTm; var; nzero; nsuc; app; pair; fst; snd; ⌜Nat⌝
        ; subTm )
open import poc.OCP0009.NbEPDirDBType
  using ( Ctx; ◇; _▹_; ⌊_⌋; single; _⊢_∷_; ⊢app; _⟶*_; done )
open import poc.OCP0009.NbEPDirDBSubj using ( ⊢-cast )
open import poc.OCP0009.NbEPDirDBLR using ( wk-single )
open import poc.OCP0009.NbEPDirDBLibWk using ( w )
open import poc.OCP0009.NbEPDirDBLibAmrec using ( Prv; prvTm; prvOk )
open import poc.OCP0009.NbEPDirDBLibPair using ( PairT )
open import poc.OCP0009.NbEPDirDBExamplesGcdStepExtA using ( gcdStepExt )
open import poc.OCP0009.NbEPDirDBExamplesGcdEqs using ( module GcdEqAt )

module GcdRecAt (Δ : Ctx) where

  open GcdEqAt Δ public

  ------------------------------------------------------------------------
  -- ★★ THE IRRELEVANCE WITNESS, AT THE TWO BOUNDS.
  --
  -- `irr-ind` proves it as a `Π Nat` — quantified over the SECOND bound —
  -- and one application lands it where the recursive step needs it: the
  -- auxiliary seeded at `x` and run to bound `k` agrees with the auxiliary
  -- seeded at `y` and run to bound `n₂`, at every carrier and both
  -- certificates.  ⭐ THIS is what nothing weaker gives, and what
  -- `StepExt` was ultimately for.
  --
  -- ⚠ The peel is the one `amrec-unfold-Id`'s own `dAt` uses: `irrT-sub`
  --   from the `vs`-tower down to the identity renaming, then `wk-single`
  --   on the first bound.
  ------------------------------------------------------------------------

  irrAt : {x y k n₂ : RTm ⌊ Δ ⌋} (dx : Δ ⊢ x ∷ PairT) (dy : Δ ⊢ y ∷ PairT)
          (dk : Δ ⊢ k ∷ Nat) (dn₂ : Δ ⊢ n₂ ∷ Nat) →
          Δ ⊢ app (prvTm (irr-ind gcdStepExt dx dy dk)) n₂ ∷ irrT idR x y k n₂
  irrAt {x = x} {y = y} {k = k} {n₂ = n₂} dx dy dk dn₂ =
    ⊢-cast (trans (irrT-sub vs idR (λ v → refl) x y (w k) (var vz))
                  (cong (λ u → irrT idR x y u n₂) (wk-single {v = n₂} k)))
           (⊢app (prvOk (irr-ind gcdStepExt dx dy dk)) dn₂)
```

### 2. `gcd-gt-eq` — gap A's equation 3 (drafted, unverified)

```agda

  ------------------------------------------------------------------------
  -- ★★★★★★ GAP A, EQUATION 3 — gcd's RECURSIVE STEP, INTERNALLY, AT
  --         ARBITRARY TERMS.
  --
  --     gcd (suc a , suc b)  ≡  gcd (suc a ∸ suc b , suc b)      (a > b)
  --
  -- ⚠ THE `⟶*`-FORM OF THIS IS UNREACHABLE at variables: the measure is
  --   `a + b`, which recurses on its FIRST argument and is STUCK at an open
  --   term, so no reduction sequence exists.  That is why the equation has
  --   to be an `Id`, and why it needed `StepExt` → `irr-ind` → this.
  --
  -- ★ THE SHAPE, three moves and no more:
  --     1. `gcd-gt-call`  gcd X ≡ ih PAIR CERT      (unfold, then reduce)
  --     2. `ih-app`       ih PAIR CERT ⟶* aux X k PAIR descS
  --     3. `irrElim`      aux X k PAIR descS ≡ aux PAIR (μ PAIR) PAIR refl
  --        …and `amrec-β` reads the right-hand side back as `gcd PAIR`.
  --   `⊢transN` chains 1–2 with 3.  ⭐ Step 3 is where irrelevance is
  --   spent: the two auxiliaries agree though their SEEDS and BOUNDS differ,
  --   which is exactly what `irr-ind` proves and nothing weaker gives.
  ------------------------------------------------------------------------

  gcd-gt-eq : {a' b' d : RTm ⌊ Δ ⌋}
              (da : Δ ⊢ a' ∷ Nat) (db : Δ ⊢ b' ∷ Nat) (dd : Δ ⊢ d ∷ Nat)
              (mh : monusTm (nsuc a') (nsuc b') ⟶* nsuc d) →
              Prv Δ (Id (El ⌜Nat⌝) (app amrecTm (gX a' b'))
                                   (app amrecTm (PAIRᵍ a' b')))
  gcd-gt-eq {a' = a'} {b' = b'} {d = d} da db dd mh =
    prv _ (⊢transN dA dB dC (prvOk s1) (prvOk s2))
    where
      X   = gX a' b'
      Y   = PAIRᵍ a' b'
      K   = gK a' b'
      IHt = gIH a' b'
      CRT = recCert (gcd-gt-term a' b' d IHt mh)

      ⊢X = ⊢gX da db
      ⊢Y = ⊢PAIRᵍ da db
      ⊢K = ⊢plus da (⊢nsuc db)

      dμX = ⊢[] ⊢msr ⊢X
      dμY = ⊢[] ⊢msr ⊢Y

      -- μ X ≤ suc K, by the measure's own reduction
      dP : Δ ⊢ reflTm (subTm (single X) msr)
             ∷ Hom Nat (subTm (single X) msr) (nsuc K)
      dP = ⊢conv (⊢le-refl dμX) (red→≅ᵀ (⟶ᵀ*-Homʳ (μ-ss a' b')))

      -- the recursive call's certificate, TYPED — `…GcdCert` does this
      dQ : Δ ⊢ CRT ∷ Hom Nat (nsuc (subTm (single Y) msr)) (subTm (single X) msr)
      dQ = ⊢conv (GcdCertTy.⊢certClean da db IHt)
                 (csymᵀ (red→≅ᵀ (⟶ᵀ*-Homʳ (μ-pair (nsuc a') (nsuc b')))))

      dC₁ = ⊢descS-at dμY dμX ⊢K dQ dP
      dC₂ = ⊢le-refl dμY

      MID = app (app (auxIH X K) Y)
                (descS-at X X K (reflTm (subTm (single X) msr)) Y CRT)

      s1 : Prv Δ (Id (El ⌜Nat⌝) (app amrecTm X) MID)
      s1 = idToRed done
             (ih-app X X K (reflTm (subTm (single X) msr)) Y CRT)
             (gcd-gt-call da db mh)

      s2 : Prv Δ (Id (El ⌜Nat⌝) MID (app amrecTm Y))
      s2 = idOfRed done (amrec-β Y)
             (prvCast refl
               (irrElim (irrAt ⊢X ⊢Y ⊢K dμY) Y _ _ ⊢Y dC₁ dC₂))

      dA = asN (⊢amrecPt ⊢X)
      dB = asN (appAux ⊢X ⊢K ⊢Y dC₁)
      dC = asN (⊢amrecPt ⊢Y)
```

⚠ `gcd-le-term`'s equation 4 stays UNREACHABLE at variables for the
structural reason `…GcdStep` records: its `⟶*` premise forces both
arguments ground. That needs the propositional route, not this one.
