# HANDOFF — 2026-08-11

*Session entry point. Supersedes `HANDOFF-2026-08-09.md`, whose §0 says
"option C does not scale" — that verdict was about ONE interface and is now
too broad; see §1. `HANDOFF-2026-07-31.md` remains accurate for W1/W2
history. **Read `WF-LIBRARY.md` before touching any combinator** — it holds
the decisions and their use-site evidence; this file holds the state.*

--------------------------------------------------------------------------
## 0. START HERE

**The WF axis now has a LIBRARY, and measure recursion is done in it.**
`NbEPDirDBLibWk` / `LibRec` / `LibNatVal` / `LibAmrec`, four modules, two
green use sites, everything cold-verified.

**What is NOT done: lexicographic recursion.** It has never been built in
the new library, and the question that decides whether it can be — does
branch (S,S) fit? — is still open. Everything except the branch
derivations is in place for answering it (§3).

⚠ **`⊢lexrec` today exists only in the OLD `Γ₅` form (`…ExamplesLexAsm`,
green), and by the argument in `WF-LIBRARY.md` D2 that form is UNUSABLE —
its premise cannot be satisfied.** So there is currently no complete,
callable lexicographic recursor. That is the headline gap.

--------------------------------------------------------------------------
## 1. ⚠ NAMING — two axes, and they were conflated for most of a session

| axis | names | meaning |
|---|---|---|
| *where the data live* | **option B** vs **option C** | B = in a context (`Γ₄`/`Γ₅`) as slots. C = as Agda **parameters** over an abstract `Δ`. |
| *how the data are presented* | **codes-and-functions** vs **families** | only meaningful inside option C. |

⛔ "option B" and the old shorthand "interface B" are unrelated and nearly
opposite. Do not reintroduce single-letter names for the second axis.

**The winner is option C + FAMILIES:**

```agda
-- codes-and-functions (…ExamplesAmrecC)        families (LibAmrec)
cA : RTm, carrier is `El cA`                    A  : RTy ⌊ Δ ⌋
cP : Π (El cA) U    so P x = El (app cP x)      cM : RTm (⌊ Δ ⌋ ∙)   P x IS `El cM`
μ  : Π (El cA) Nat  so μ x = app μ x            m  : RTm (⌊ Δ ⌋ ∙)   μ x IS `m`
```

⚠ The motive stays a **code** family, not an `RTy` family — forced, because
`⊢absurd` is code-indexed and the vacuous branch is ex falso.

**Measured, same function both ways:**

| | codes-and-functions | families |
|---|---|---|
| `div` — conversions / lines | 12 / 113 | **4 / 72** |
| pair carrier — `⊢conv` | ≥7 (step not even built) | **3** |
| pair carrier — `El-⌜Σ⌝` conversions | one per projection | **0** |
| pair carrier — iterations to green | — | **0** |
| "the IH at an arbitrary bound" | ⛔ inexpressible | ✅ `aIHTat` |

--------------------------------------------------------------------------
## 2. WHAT IS GREEN (cold; delete the `.agdai` before timing — see §5)

| module | cold | |
|---|---|---|
| `NbEPDirDBLibWk` | 2.7 s / 0.30 GB | weakening + substitution kit, indexed ladder |
| `NbEPDirDBLibRec` | 1.7 s / 0.30 GB | the IH types (`aIHTat`/`aIHT`) |
| `NbEPDirDBLibNatVal` | 3.1 s / 0.38 GB | a closed `Nat` reaches a numeral |
| `NbEPDirDBLibAmrec` | ~9 s / 0.90 GB | measure recursion, Π form, computation rule |
| `SpikeDivT` | 3.9 s / 0.40 GB | div through the library |
| `SpikePairT` | 3.7 s / 0.45 GB | a PAIR carrier; also its computation tests |
| `SpikeLexT` | 3.0 s / 0.36 GB | lexrec's types, motives, boundary, rec₁'s term |
| `SpikePairC` | 2.9 s / 0.42 GB | the pair carrier under codes-and-functions (⚠ step not built) |

The whole legacy stack (`…ExamplesLexC`, `…LexCZZ/ZS/SZ*`, `…AmrecC`,
`…AmrecT`, option B's `…Lex*`) is untouched and still green. ⚠ The
`…LexCSS*` modules remain deliberately RED — a measured negative result,
never type-checked; do not cite them.

--------------------------------------------------------------------------
## 3. THE GATE — the one open question

**Does branch (S,S) fit under families?** Under codes-and-functions both
halves OOM at the 5.5 GB cap, even with `+RTS -c`. Families removes every
`app` from the types and collapses fitting to one lemma per spine, so the
elaborated terms should be much smaller — but that is a prediction.

### Built (all in `SpikeLexT`, all green)

* the four types — and **`rec1T = aIHT` verbatim**, so `LibRec` already
  supplied half of them;
* `lexMot` / `M0lex` / `M1lex` — ⚠ the μ₁-bound is STILL a parameter, for
  the same reason as `auxMotB`: `renTy vs` does not preserve `var (vs vz)`;
* `ZS.mot-s` — the (0,S) motive boundary;
* `rec2T-ren`, `lStepT-ren` (which reuses `aIHT-ren`), and `lStepT-w^`,
  the indexed ladder;
* `ZS` with its full parameter set, `BCtx`, and **rec₁'s TERM**, verified
  to agree with its type.

### Missing — and it is the entire measurement

`⊢rec1` and `⊢rec2`. **`LexCZS`'s 4.43 GB is almost all derivation cost**,
so nothing built so far bears on the answer. Cheap layers being cheap is
what one would expect either way.

### ⭐ THE IMMEDIATE NEXT STEP

`⊢rec1`'s shape is written into `SpikeLexT` beside the term. It fails at
`dmX` — the substituted `μ₁ x`, whose weakening count around the `subst`
does not line up.

**Probe `dmX` ON ITS OWN before assembling.** The mistake to avoid is the
one I made: I probed the type (which worked), then assembled four guessed
premises at once, so the error carried four unknowns.

⇒ then `⊢rec2` (the real inner-IH application, two descents), then the
(0,S) number, then the (S,S) decision.

⚠ **Port (0,S), not (S,S), for the ratio.** (0,S) exists in both worlds —
4.43 GB under codes-and-functions — so it yields a *ratio* rather than a
bare pass/fail, and it is the cheaper branch.

--------------------------------------------------------------------------
## 4. AFTER THE GATE

1. **D7's ideal shape** (`WF-LIBRARY.md`, marked ◐). The unfolding lemmas
   reach the AUXILIARY's branch; a caller wants their own step:
   `app amrecTm x ⟶* app (app stp x) ⟨ih⟩`. Two βs apart — `aZBr` is
   `lam (lam (app (app (w (w stp)) …) ihZ))`. Cheap at `Δ = ◇`.
2. **The `Γ₄` claim.** "`⊢amrec`'s premise is unsatisfiable" is a
   structural argument, NOT machine-checked. Nothing depends on it — it is
   motivation. Prove it only when `ARCHITECTURE.md` cites it.
3. **Retire the legacy modules** once lexrec is in the library: `AmrecC`,
   `AmrecT`, `LexC`, `LexCMot`, the `LexC*` branches. That also kills the
   inverted dependency (`AmrecC` imports `…ExamplesLexC`).
4. **`#11`/`#9`** — genericity and Ackermann's step — were the original
   reasons for all of this and are now unblocked at the amrec level.

--------------------------------------------------------------------------
## 5. TRAPS

* ⚠ **Timing needs a COLD interface.** A second `check.sh` run on an
  unchanged module reports only load time — measured 2.80 s against 8.73 s
  cold, a 3× understatement. `touch` does NOT invalidate it;
  `find _build -name '*<Module>*.agdai' -delete`.
* ⚠ **RSS varies ±12% run to run.** One sample each; a <15% difference
  between modules is not evidence.
* ⚠ **`w` vs `wᶠ`, and `⊢wk` vs `⊢wkᶠ`.** `wᶠ` inserts a slot BELOW a
  family's own variable. They produce terms that look interchangeable and
  are not — but note **the count is wrong more often than the kind**: both
  bugs in amrec's successor branch were off-by-one, not the wrong lemma.
* ⚠ **P1 (in `LibWk`'s header): eta covers every naturality lemma EXCEPT
  moving a family under a renaming.** Those need a pointwise bridge.
* ⭐ **PROBE, DO NOT GUESS.** Every derivation failure this session — in
  amrec's branches and in lexrec's rec₁ — was a miscounted weakening, never
  a design problem. Put `⊢nzero` where the derivation goes; the
  `UnequalTerms` error prints the goal in full. And probe **one premise at
  a time**.
* ⚠ **`--safe` forbids `postulate`**, so there is no quiet way to skip an
  obligation; skipping costs a module its `--safe`. That is deliberate.
* **Generate branch files, do not type them.** A ~150-line Python emitter
  with `W(n,t)`/`V(n)`/`T(n)`/`WK(n,d)` helpers produced eight modules
  without a de Bruijn slip. ⚠ Assert on every string replacement — two
  silent no-op `replace`s this session left a module claiming to be RED
  while green.
