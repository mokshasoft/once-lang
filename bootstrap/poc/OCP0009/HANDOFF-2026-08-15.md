# OCP-0009 · HANDOFF 2026-08-15 — the route is BUILT; gap A's last mile is the CALLER's

> ⏩ **CURRENT.** Supersedes `HANDOFF-2026-08-14.md`, whose route table
> (pieces 1–10) is now **entirely done** and green.
>
> **PICK UP HERE:** jump to "GCD'S `StepExt`, DESIGNED — start here". It is
> the only thing between the tree and gap A, it is entirely CALLER-side
> (nothing in `LibAmrec` needs to change), and it is designed down to its four
> leaves, its one wrinkle and its three prerequisites. First edit to make:
> prerequisite 1, pulling `⊢G3z`/`⊢G3s`'s certificate out as a named
> sub-derivation — it is small, self-contained, and everything else needs it.
>
> ⚠ That section is a DESIGN, not built code. Everything described under
> "WHAT LANDED" *is* built and checked.

Tree state: **sweep ALL GREEN (82 modules)**, everything committed and pushed.
`check-formers.sh` check 6 lists the same 5 conditional-lemma hits as before,
all pre-existing in `NbEPDirDBLR`/`NbEPDirDBSubj` — no new entries.

| gap | status |
| --- | --- |
| A — defining equations at variables | **library half CLOSED**; the last mile is gcd's `StepExt`, newly scoped below |
| B — a divisibility SPEC | **untouched** |
| C — an end-to-end recursing run | **DONE** (2026-08-14) |

### Where things are, and how to re-verify

Everything new this session is in **`NbEPDirDBLibAmrec.agda`** — no other
module changed. Four commits, on `ocp-0009-inductive-types`:

```
fccdf325  piece 7 — irrT / ⊢irrT
50915539  piece 8 — irr-ind
25a97db0  piece 9 — amrec-unfold-Id
8ed2d557  this handoff
```

```sh
./bootstrap/check.sh poc/OCP0009/NbEPDirDBLibAmrec.agda   # ~61s
bootstrap/poc/OCP0009/sweep.sh                            # 82 modules, all green
bootstrap/poc/OCP0009/check-formers.sh                    # check 6: same 5 pre-existing hits
```

⚠ Never run two `agda` checks at once — they OOM-kill each other. Exit 143 is
contention, exit 42 is a real error.

---

## WHAT LANDED — pieces 7, 8, 9, all in `NbEPDirDBLibAmrec`

The 2026-08-14 route table is complete:

| # | piece | status |
| --- | --- | --- |
| 1–6 | typing interface, `⊢auxAt`, `StepExt`/`Prv`/`idOfRed`, branch IHs, CPS unfolds at a renaming, `aux-irr-z` | done (2026-08-14) |
| 7 | `irrT` / `⊢irrT` — irrelevance as an object-language TYPE | **done** |
| 8 | `irr-ind` — the induction on the bound | **done** |
| 9 | `amrec-unfold-Id` — the internal unfolding | **done** |
| 10 | INDEX irrelevance | **done, by construction** — `irrT` carries two indices `x y` throughout |

The headline statements:

```agda
irr-ind : StepExt Δ A cM m stp → Δ ⊢ x ∷ A → Δ ⊢ y ∷ A → Δ ⊢ n ∷ Nat →
          Prv Δ (Π Nat (irrT vs x y (w n) (var vz)))
-- ⊢ (n₂ : Nat) (a : A) (c₁ : μ a ≤ n) (c₂ : μ a ≤ n₂) → aux x n a c₁ ≡ aux y n₂ a c₂

amrec-unfold-Id : StepExt Δ A cM m stp → Δ ⊢ x ∷ A → Δ ⊢ k ∷ Nat →
                  Δ ⊢ p ∷ Hom Nat (subTm (single x) m) (nsuc k) →
                  Prv Δ (Id (El (subTm (single x) cM))
                            (app amrecTm x) (app (app stp x) (ihS-atP x x k p)))
```

Module cost 5s → 61s. All four dependents re-checked green.

### ⚠⚠ THREE CORRECTIONS TO THE PLAN — and each makes the result stronger

**(1) `jsub` IS NOT ON THIS PATH. Do not re-attempt the `⌜Π⌝`-family
transport.** The route was opened as *transport first, then reduce*: move the
auxiliary's bound off the stuck `μ x` along an `Id`, so `amrec-unfold-s` can
fire. That framing is what forced the certificate to be bound INSIDE the
family and what made the transport's source obligation certificate
irrelevance. But what pieces 6–8 actually produce is **bound** irrelevance as
well — `aux x n₁ a c₁ ≡ aux y n₂ a c₂` at two INDEPENDENT bounds — so the
bound moves directly and `⊢jsub` never enters, with neither its ban on the
family mentioning the proof nor its demand that the family typecheck at an
arbitrary `v`. The 2026-08-14 obstruction analysis was correct *about
`jsub`*; the conclusion "so we need irrelevance" was right and the
intermediate step turned out to be unnecessary.

**(2) The premise is an INEQUALITY, not an identity.** The plan wanted
`Id Nat (μ x) (nsuc k)`; what is needed is `Hom Nat (μ x) (nsuc k)`, because
the auxiliary at ANY bound above the measure computes the same answer. An
identity gives the inequality; the converse is false.

**(3) `StepExt` is needed at a VARIABLE carrier.** Not stated anywhere before
and it is the whole shape of what remains — see below.

### The design decisions worth keeping

- **Index the motive by the AMBIENT RENAMING `θ`, not by a `vs`-tower.**
  Every branch of the induction lands at a different depth; with the tower
  spelled out each depth needs its own peel — that is what `wᶠ¹/²/³-single`
  are, and they ran out at three. With `θ` abstract there is exactly ONE peel
  (`peelθ`) and the branches differ only in which `θ` they pass. This is the
  single change that made piece 8 affordable.
- **The pointwise calculus** (`subren`/`subrenTy`/`renren`/`renrenTy` +
  `extcond`/`extcondR`, top level): *a substitution that meets a renaming is
  another RENAMING, and which one is decided variable-by-variable.* The
  caller supplies a two-case bridge and gets the collapse. Every peel in
  pieces 7–9 is an instance. ⭐ Reach for this before writing another
  tower lemma.
- **`irrT'` takes every slot already at the depth it is used**, so `subTy` and
  `renTy` distribute by `refl` (the `aAuxB'` trick) and all the naturality
  lives in the eight peels of `irrT-sub`/`-ren`.
- **The inner split is a CASE SPLIT, not a recursion** — the second bound
  never descends. That is why two bounds cost ~1.5× one and not twice, as
  risk 10 predicted.
- **`idOfRed` needs a mirror.** `idToRed` (Id between sources → Id between
  reducts). Both directions are needed and they are not interchangeable: a
  proof arrives at whichever end its producer left it.

### ⚠ The one place the route could have stalled — recorded because it will recur

The (S,S) leaf instantiates the IH at the recursive call, and `⊢app` wants
that call's **certificate typed**. The certificate the reduction hands over
is `descS` under SEVEN substitution layers; `subTm` does not invert, so no
amount of subject reduction produces a typing for it, and inversion would
need Π-injectivity plus uniqueness of types. The fix is `descS-peel`: say
what the certificate IS — `ordtr (nsuc μy) μa (nsuc k) q p` — and let
`⊢strong-step` type it from the two hypotheses the pointwise premise already
hands over. Five argument peels, two of them `refl` because the two variables
reach their slots by computation.

⇒ this confirms the 2026-08-15 probe: the "substitution-naturality for every
arithmetic template" item that `gcd-gt-term`'s comment defers is **not** on
this path.

### Non-vacuity, honestly

`amrec-unfold-Id-red` discharges the `Hom Nat (μ x) (nsuc k)` premise from a
measure REDUCTION plus one conversion (`⟶ᵀ*-Homʳ`), so the Id-form subsumes
the `⟶*`-form `amrec-step-s` and the premise is inhabited at exactly the
arguments the library already reduces at.

⚠ **`StepExt` is still undischarged by anything in the tree.** Everything
above is machinery with a real statement; it is *not yet* evidence that any
particular function unfolds internally. That is the last mile.

---

## WHAT IS LEFT — gcd's `StepExt`, and it is NOT one instantiation

The 2026-08-14 handoff said a step that uses its IH once "discharges
`StepExt` by ONE instantiation of the hypothesis". **That is true only per
recursive leaf, and getting to the leaves is the work.** Here is the actual
shape, determined 2026-08-15 by reading `gcdStp`.

### Why it is not free

`StepExt` quantifies over an ARBITRARY carrier `a`, and the induction
consumes it at a **variable** — piece 8's leaves apply it at
`⊢irr-a : … ⊢ var (vs (vs vz)) ∷ renTy (θ₃ θ) A`. That is forced: the motive
`irrT` quantifies `a` internally, because the recursive call's carrier is
bound inside the pointwise hypothesis.

But gcd's step reduces only at a CONSTRUCTOR-HEADED carrier:

```agda
gcdStp  = lam (natrec G1z (natrec G2z (natrec G3z G3s (monusTm …)) (fst …)) (snd (var vz)))
gcd-gt-term : … → RecCall (app (app gcdStp (pair (nsuc a') (nsuc b'))) ih) ih …
gcd-le-term : … → RecCall (app (app gcdStp (pair (nsuc a') (nsuc b'))) ih) ih …
```

At a neutral `a` all three scrutinees — `snd a`, `fst a`, `monusTm _ _` — are
stuck, so `app (app gcdStp a) ih₁` and `app (app gcdStp a) ih₂` are two stuck
neutrals differing only inside. **There is no funext in this kernel** (`ap`
needs a `Hom`, a `flat?` code and a one-variable family — it cannot lift a
POINTWISE equality of two IHs), so they cannot be related by a congruence.

### GCD'S `StepExt`, DESIGNED — start here

⚠ Everything in this section is a DESIGN, derived 2026-08-15 by reading
`gcdStp` and `⊢G3z`/`⊢G3s`. Nothing in it has been built or checked.

**The shape of the step, confirmed:**

```agda
gcdStp  = lam gcdBody
gcdBody = natrec G1z (natrec G2z (natrec G3z G3s (monusTm …)) (fst …)) (snd (var vz))

G1z = lam (fst (var (vs vz)))                       -- IH-FREE (the `lam` binds it, the body ignores it)
G2z = lam (nsuc (var (vs (vs vz))))                 -- IH-FREE
G3z = lam (app (app (var vz) PAIRᶻ) CERTᶻ)          -- IH used ONCE, and neither PAIR nor CERT mentions it
G3s = lam (app (app (var vz) PAIRˢ) CERTˢ)          -- ditto
```

**The technique.** `natrec` proves `P(t)` for a NEUTRAL `t` perfectly well:
the motive is `λ z → P(z)` and the scrutinee is `t`. So abstract each of the
three scrutinees out of the goal and split on it. Three nested `natrec`s, four
leaves:

| leaf | branch | IH used? | proof |
| --- | --- | --- | --- |
| `snd a ⇝ 0` | `G1z` | no | both sides reduce to the SAME term ⇒ `⊢idrefl` after `idToRed` |
| `fst a ⇝ 0` | `G2z` | no | same |
| `monus ⇝ 0` | `G3z` | once | the pointwise hypothesis, at `G3z`'s own `(PAIRᶻ , CERTᶻ)` |
| `monus ⇝ S` | `G3s` | once | ditto |

The two base leaves are genuinely free. The two recursive leaves are ONE
instantiation each, as the old handoff said — but getting the instantiation to
TYPE is the work, and that is where the plan needs one more idea:

### ⚠⚠ THE WRINKLE, and the fix — the split motive must carry an ORDER hypothesis

The pointwise hypothesis demands the recursive call's certificate at

    Θ ⊢ q ∷ Hom Nat (nsuc (μ PAIR)) (μ a)      -- μ a = plusTm (fst a) (snd a)

i.e. **at the measure of the ORIGINAL carrier `a`**. What `⊢G3z` supplies is
`⊢desc-right`, whose target is `plusTm (nsuc k') (nsuc n')` — the measure
written in the SPLIT variables. Those two are not convertible: a `natrec` on
`snd a` hands its successor branch a fresh `n'`, it does **not** hand over the
equation `snd a = nsuc n'`. So the leaf cannot rebuild the certificate, and
the naive three-split plan stalls exactly one step from the end.

⚠ This is the same failure mode that made `amrec-unfold-Id` necessary in the
first place — a case split gives you branches, never an equation about the
scrutinee — so expect it again anywhere a neutral is split under a motive that
mentions it twice.

**THE FIX, and it is cheap: carry the relation in the motive.** Split on `t`
while the motive quantifies an order proof relating the recursion variable
back to `t`:

    M z = Π (Hom Nat z (w t)) (Id (El ⌜Nat⌝) … …)

Instantiate at the scrutinee with `⊢le-refl`; each branch then RECEIVES
`nsuc n' ≤ snd a` (resp. `nsuc k' ≤ fst a`), which is exactly what is missing.
The recursive leaf then rebuilds the certificate as

    ordtr  (⊢desc-right …)                         -- nsuc (μ PAIR) ≤ plusTm (nsuc k') (nsuc n')
           (monotonicity of plusTm in BOTH arguments)  -- … ≤ plusTm (fst a) (snd a)

⚠ **Split 3 does NOT need this** — its motive is constant (the source already
says so: "the branch needs to know only WHETHER `a ∸ b` is zero, never its
value"). Only splits 1 and 2 carry an order hypothesis.

⚠ **A likely missing lemma.** `plusMonoTm` and `plusMonoLTm` exist in the gcd
libs, but the rebuild above needs monotonicity of `plusTm` in *both* arguments
at once. Check before assuming; it may need composing the two one-sided ones.

### Prerequisites and cost

1. ⚠ **Unchanged from 2026-08-14:** `⊢G3z`/`⊢G3s` build their certificate
   INLINE, so it has to be pulled out as a named sub-derivation first. Cheaper
   than `descS-peel` was, because gcd's certificate is already a term the
   derivation names — it just is not exposed.
2. **`renTm ρ gcdStp ≡ gcdStp`.** `StepExt` is context-polymorphic and states
   the goal at `renTm ρ stp`; `gcdStp` is closed but not *definitionally*
   renaming-invariant. One small lemma, by the pointwise calculus.
3. **The three motive abstractions are the cost.** Each split needs
   `subTy (single scrutinee) M ≡ goal`, i.e. the scrutinee un-substituted out
   of a goal that mentions the step's whole case tree twice (once per `ihᵢ`).
   The pointwise calculus (`subren` etc.) is the tool. Budget ~one leaf of
   piece 8 apiece.

### Then, and only then

Eqs 3/4 at full strength (`gcd (suc a, suc b)` at VARIABLES) follow from
`amrec-unfold-Id` + gcd's `StepExt`, with no further theory: `irrElim` already
gives the two-index/two-bound `Id` the right-hand `app gcdTm Y` needs.

⇒ **Gap A is one caller-side construction away.** The library half is done and
its interface is exactly right; nothing in `LibAmrec` needs to change.

---

## Gap B — untouched

No divisibility predicate exists anywhere. `Π PairT (El ⌜Nat⌝)` is equally
consistent with `gcd` computing `min`.

- **B1** — define `d ∣ n` as `Σ k. n ≡ d * k`. `Σ` and `Id` both exist, so
  this route needs no new former.
- **B2** — prove `gcd` meets the spec. Depends on eqs 3/4 at full strength.

---

## Standing lessons (carried forward, plus new)

- **Agda cannot invert `subTm`.** Anything occurring only under a substitution
  must be **named**, never left as `_`. Signature of the failure: an *unsolved
  meta*, never a wrong solution. ⭐ 2026-08-15 corollary: this is also why
  subject reduction cannot type a certificate that only exists as a reduct —
  `descS-peel` is the shape of the fix.
- **Green ≠ meaningful.** For every conditional lemma, ask whether the premise
  is dischargeable *at the arguments it is stated for*, and write the witness.
  `check-formers.sh` check 6's regex is anchored at column 0, so nothing
  inside a `module` — i.e. the whole WF library — is visible to it; for
  anything under `AmT`/`AmTΠ` the vacuity question must be asked by hand.
- **Prefer the pointwise bridge to another tower lemma.** Measured this
  session: the tower form needs a new lemma per depth and the depths are
  unbounded; the pointwise form needs one lemma and a two-case bridge per use.
- **When a plan says "transport, then reduce", check whether the theorem the
  transport's obligation forces you to prove is already strong enough to skip
  the transport.** It was, twice over (bound irrelevance ⇒ no `jsub`; ≤ rather
  than `Id` ⇒ weaker premise).
