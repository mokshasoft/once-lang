# OCP-0009 — HANDOFF 2026-08-27

Supersedes `HANDOFF-2026-08-26.md`, whose work list closed with the spike
arc and handed off to `PLAN-JUDGEMENT.md`. This session took up that plan
and closed **step 0**, plus the motive and per-field halves of **step 2**.

Read `PLAN-JUDGEMENT.md` for the build plan; this file is session state.

Sweep: **ALL GREEN (125 modules)**, trust surface empty across 156 — and
on the warm re-run **nothing is ≥10s at all** (303s total across 121).
Cold, the two that are: `Knot/Wf` 104s with `-c`, `Knot/Ctors` 15–21s.
⚠ Do not quote the sweep's numbers as cold — `Gcd/LeEq` reads 3s there
against 08-26's cold 621s.

---

## What this session did

**Step 0 — `Ctx` is encoded**, ⚠ not as the 8th sort of the knot (which
is how it was built first, and how the plan said to build it) but as its
own 2-row family over a bare depth. **And step 2's two open questions —
the MOTIVE and the PER-FIELD RULE — are both settled.** `KnotD` is back
to **53** rows.

| | | |
|---|---|---|
| 1 | `Ctx` as sort 7 — built, green, adequacy-mapped | (retired same day) |
| 2 | the `◇` weakening method — **compiles, and fabricates** | `Negative/WkEmp` |
| 3 | ⇒ `Ctx` split into its own family | `Examples/Knot/CtxD` |
| 4 | ⇒ the uniform motive is then honest at all 53 sorts | — |
| 5 | the PER-FIELD RULE, at four bracketing rows | `Examples/Knot/WkRows` |
| 6 | `Lib/IWk` — classifier + decider ✅, **typing ⬜** | `Lib/IWk`, `Knot/WkProbe` |

⇒ what is left before `_∋_∷_` is `Lib/IWk`'s TYPING lemma. The rule is
computed rather than enumerated, and validated against the knot; nothing
yet says the tuple it builds inhabits `imethsTy`.

---

## The results worth carrying forward

### 1. ★★★ A GREEN BUILD REFUTED THE DESIGN

`PLAN-JUDGEMENT` step 2 needs object-level weakening over the knot, at
the uniform motive `WkTm` and `WkFin` both use. As sort 7, `◇`'s method
holds `snd ⟨i⟩ ≡ nzero` and would have to prove `nsuc (snd ⟨i⟩) ≡ nzero`
to rebuild itself — but `K (sCtx, 1)` is INHABITED, so a different
constructor closes the goal. **It compiled**, `--safe`, trust surface
empty, computing

    ⋄  ↦  ◇ ▹ Nat            -- a type that was never there

and nothing downstream would have noticed, because there is no
`Ctx`-weakening spec for it to violate.
⇒ `verification-that-covers-less-than-it-claims`, reached from a new
direction: **the hazard was a GREEN build, not a red one**, which is why
it had to be built rather than argued.

### 2. ★★ THE PRINCIPLE IT SETTLED — A STRATUM IS NOT A MEMBER

The seven families in `KnotD` are one mutual recursion in `Spec/Syntax`.
`_▹_` carries an `RTy ⌊ Γ ⌋`, so `Ctx` depends on the syntax and the
syntax never depends back. **One-directional dependency ⇒ stratum, not
member.**

★ And the index had been meaning two things: `KnotD`'s second component
is the AMBIENT SCOPE a term lives in — a parameter — while a context's
depth is its OWN LENGTH, a measure of the datum. Sharing one slot is what
forced `Knot/Map` to grow a third signature shape for `⊢enCtx`. That got
written down as an oddity; it was a symptom.

⇒ ★★★ **AND SPLITTING MADE `wk`'s TYPE TRUE, rather than restricting
it.** With `Ctx` out, `wkK : K (s,d) → K (s, suc d)` is honest at all 53
sorts — closed sorts' depth is degenerate, `ICon` is genuinely scoped,
and no row invents anything.

### 3. ★ THE UNTESTED JOINT WENT THROUGH FIRST TRY

`_▹_`'s `RTy` field has its TYPE in another family, so it is a κ field
carrying a `⌜IMu⌝` code (`icw-imu`) **at an index mentioning a BOUND
field** — three separately-built things (`Scoped.varC`, `DepIx.islamC`,
`DepIx`'s ford naming `var vz`) never before combined. ⚠ Fording could
not do this job: it turns a computed INDEX into a constraint, never a
field's TYPE into a family. Both mechanisms are in `_▹_`, three lines
apart.

And the split is cheaper per row, not dearer:

| | as sort 7 | as `CtxD` |
|---|---|---|
| fords per row | 2 (tag + depth) | **1** (depth) |
| index | `Σ' Nat Nat` | `El ⌜Nat⌝` |
| `_▹_`'s `num-ren`/`num-sub` chains | 14 | **9** |
| `⊢ixP` on the ambient, `fordFst`, `βfst` | throughout | **absent** |

### 4. ⚠ THE COVERAGE GUARD MOVED, AND IT IS NOT WEAKER

`gen-knot.py`'s `verify()` is back to one file and no longer mentions
`Ctx`. `Ctx` is not thereby unchecked: `enCtx`'s clauses check it, because
**Agda's coverage checker checks FUNCTIONS**. A hand-written table with a
hand-written map is guarded already; a GENERATED table with a GENERATED
map is not, which is the entire reason `verify()` exists.
**Controlled**: deleting `enCtx`'s `_▹_` clause gives `Incomplete pattern
matching for enCtx`.

### 5. ★ WHAT A PAIR-INDEXED TRANSPORT COSTS — TWO `jsub`s, FLAT

One per index component, where `WkFin`'s `fsuc` moved along one. ⚠ And
the `wk-single` one expects does NOT appear: the outer transport's family
mentions the inner component, weakened past the transport's own binder
and then substituted — PROPOSITIONAL in general, DEFINITIONAL here,
because that component is `snd` of a VARIABLE and both actions COMPUTE on
variables (`Knot/Build`'s finding (c)). **Measured after a wrong guess**:
replacing either `wk-single` with `refl` type-checks, so both casts came
out.

★ ⇒ **`_∋_∷_`'s THREE-component telescope should cost three `jsub`s,
flat** — the first quantitative thing anyone knows about step 1. ⚠ Its
telescope now mixes two families (`CtxK` and `K`); `DepIx` already builds
one mixing `El ⌜Nat⌝` with an `IMu`, so that is the same mechanism at one
more component.

---

## NEXT

### A. ✅ **THE PER-FIELD RULE IS SETTLED** — `Examples/Knot/WkRows`
###    ⬜ what remains is `Lib/IWk`, i.e. computing it

`PLAN-JUDGEMENT` step 1 (`_∋_∷_`) reads *"needs only the weakening
already in hand"*. ⚠ **TRUE OF THE MECHANISM, FALSE OF THE CODE.**
`Examples/WkTm` weakens a 3-constructor toy; `renTy vs` is an `ielim`
over the **53-row knot** returning a knot element, and it cannot be 53
enumerated methods (`half-generalization-is-worst`, with a SYNTAX
codomain instead of `Nat`).

⚠⚠ **AND `Lib/IFold`'s SHAPE DOES NOT REACH IT.** A fold into a CONSTANT
motive takes the IH at every `iρ`. A weakening does not — and
`cDCon-kap` is the row where that bites, because its TWO `iρ` fields need
OPPOSITE treatments:

> ✅ **FOUR METHODS BUILT, AT ONE MOTIVE, ALL GREEN.**
>
> | row | shape | what the method does | cost |
> |---|---|---|---|
> | `cTy-Nat` | ford-only | pass the tag ford through | **free** — `βfst` takes the shifted constraint to the field it was handed |
> | `cTm-lam` | riding index, under a binder | take the IH, forward off the eliminator's index and backward onto the row's | conversions only |
> | `cDCon-kap` | a PINNED field beside a riding one | ★ `RTy ε` takes the **ORIGINAL FIELD** (its IH is at depth 1, the row wants depth 0); its `DCon` sibling takes the IH | — |
> | `cVar-vs` | depth-Forded | riding case **plus one `congS`** (`βsnd` does the rest) | **one `jsub`** |
>
> ★★ **AND THE TABLE, COUNTED BY SHAPE** (measured off the generator's
> table, not estimated):
>
> | | |
> |---|---|
> | rows | 53 |
> | FORD-ONLY rows | 13 |
> | RIDING recursive fields | 77 |
> | rows with a PINNED-index field | **4** — `cTy-IMu`, `cTm-cIMu`, `cDCon-kap` (`RTy ε` at `pair sTy 0`), `cIDesc-cons` (`ICon (ε ∙)` at `pair sICon 1`) |
> | DEPTH-FORDED rows | **2** — `cVar-vz`, `cVar-vs` |
>
> ⇒ the ORIGINAL-field rule has **four** customers and the transport
> **two**. Everything else is the riding case.
>
> ⚠ AND ONE HELPER `Knot/Terms` COULD NOT SUPPLY: `tyFordFst` was
> written for a CONCRETE index and reuses one derivation as BOTH the
> pair's first component and the `⌜Id⌝`'s right endpoint. In a METHOD
> the index is abstract and those differ (`fst ⟨i⟩` vs the row's literal
> tag). `WkRows.tyFordAt` is the abstract-index twin.

### A″. ⬜ `Lib/IWk` — **STARTED; the classifier is built and validated,
###     the TYPING is not**

Nothing in `WkRows` is generic — each method names its own row. `Lib/IWk`
takes `Lib/ISz`'s step (method computed from the `ICon`, tuple from the
`IDesc`) and needs one predicate, which those four rows pinned down:

    does this field's index expression MENTION THE AMBIENT?
      yes ⇒ the IH · no ⇒ the original field

| | |
|---|---|
| the classification (`IsSucs`/`WkIx`/`WkKa`/`WkCon`/`WkDesc`) | ✅ |
| the METHOD and TUPLE computed from it, term level | ✅ |
| the classification **DECIDED** from a raw description | ✅ |
| `pinned-stable` — the lemma the pinned case cashes out into | ✅ |
| ★ the ESCAPE HATCH — spiked, and it is STRUCTURAL | ✅ |
| ⬜ `⊢iwkMethod` / `⊢iwkMeths` — the TYPING | **not built** |

⚠ **`iwkMeths` produces a TERM and nothing yet says it inhabits
`imethsTy`.** It is not a finished eliminator.

★★ **THE DECIDER IS THE PART THAT MAKES IT GENERIC, and it is validated**
(`Examples/Knot/WkProbe`, 0.6s). Without it a caller would write a
`WkCon` per row and the enumeration would have moved into the CONSUMER —
the measured-worst option. With it, `KnotD`'s rows are classified by a
structural recursion Agda runs:

> ✅ 12 row shapes classify — ford-only, riding, under a binder, at
> `suc (suc ⟨d⟩)`, a `κ ⌜Nat⌝` beside a riding field, cross-sort, and
> **all three pinned shapes** (`cTy-IMu`/`cDCon-kap` at `pair sTy 0`,
> `cIDesc-cons` at the non-zero `pair sICon 1`).
>
> ⚠ **AND THE TWO DEPTH-FORDED ROWS ARE REFUSED, not mis-classified** —
> `decCon vz cVar-vz ≡ nothing`, same for `vs`. Their κ constrains
> `snd ⟨i⟩`, so passing the witness through would be WRONG; the
> classifier declines. A negative control, and it is the one that
> matters.

### ★★★ THE ESCAPE HATCH, SPIKED — and it came out better than either
### option I had sketched

The question was how hand-written rows coexist with computed ones, given
that a hand-written row must carry its method's TYPING — which `WkDesc`
cannot hold and stay decidable data. Both sketched answers (a parallel
per-row typing structure; splicing two tuples) were bookkeeping.

★ **NEITHER IS NEEDED, BECAUSE THE METHOD TUPLE IS RIGHT-NESTED.**
"Classified rows, then given rows" is just *where the nest stops* — so
the hatch is one constructor and one argument:

    wkd-stop : (E : IDesc) → WkDesc E          -- classify no further
    iwkMethsFrom : ℕ → WkDesc E → (tail : RTm Γ) → RTm Γ

⇒ three things fall out, and each is an improvement:

| | |
|---|---|
| `decDesc` becomes **TOTAL** | it classifies as far as it can and stops. No `Maybe`, no `get`, no way for a caller to be blocked. |
| the contract is statable **without naming a row** | `wkdLen`/`wkdRest` give it: the tail must inhabit `imethsTyFrom D I M (j + wkdLen W) (wkdRest W)`. ONE tail, ONE derivation, no parallel structure. |
| a caller with nothing exceptional passes `unit` | the general case degrades to the simple one. |

⚠ **AND IT STILL FAILS LOUDLY.** Stopping early does not drop rows
silently — it makes the TAIL bigger, and the caller has to inhabit it. A
row that stops being classifiable becomes methods someone must write, not
a wrong answer.

✅ **MEASURED ON THE KNOT: `wkdLen (decDesc KnotD) ≡ 51`**, by `refl`,
0.6s. 51 computed rows and a 2-method tail — the same split
`Knot/Ctors` (51 generated) and `Knot/Build` (2 hand-written `Var` rows)
already use, and now for the *same stated reason* rather than by
coincidence.

⚠ **THE COST IS A CONDITION ON THE TABLE**, and it should be written down
before anyone reorders it: the rows a weakening cannot classify must be a
**SUFFIX** of the description. `KnotD` satisfies it — `Var` is rows 52–53
— and the generator already appends exceptional rows last so that `∈ID`
positions do not move. That convention is now **load-bearing**.

⚠ And `wkdLen` is asserted rather than merely type-checked, deliberately:
`decDesc` is total, so "it compiled" says nothing about how far it got.

⬜ **THE THREE OBLIGATIONS THE TYPING STILL OWES** are named in `Lib/IWk`
§7, with what each rests on: `pinned` ✅ done; `rides` needs the
`sucs-nsuc` collapse stated once over an abstract `IsSucs` (the chain is
written out there, and `WkRows` does it by hand at three rows); the two κ
cases are `pinned-stable` again and `WkRows.unFst`.

⚠ **The escape hatch is a design question, not plumbing** — a hand-written
row must carry its method's TYPING, which `WkDesc` cannot hold and stay
decidable data. Design it *with* the typing lemma; deciding it now would
be guessing.

### A′. ✅ **RESOLVED — `Ctx` IS ITS OWN FAMILY**

The fork that stood here (split `Ctx` out / keep one description and
write the junk down) was decided for the split, on the evidence in
results 1–3 above. `Negative/WkEmp` is kept RED as the measurement that
decided it; do not repair it.

⛔ The third option — making `Ctx` depth-degenerate — stays refused: `◇`
would inhabit `K (sCtx, 5)` and the depth would stop measuring the
context.

### B. ⬜ unchanged from 08-26

* the `sz` agreement (`szTm ⌈t⌉ ⟶* ⌜ sz t ⌝`) — bulk, known shape.
* `Confluence`'s `⟹-⁺` `UnreachableClauses` warning.
* check-formers 4–6: a REVIEW list. ⚠ Not affected by the 8th sort —
  those checks are about `RTm`/`RTy` formers in the SN layer; the knot
  table's own tripwire is `gen-knot.py`'s `verify()`.

### ⛔ Do NOT

* Enumerate 55 weakening methods to "get something working". See (A).
* Re-spike `PLAN-JUDGEMENT` §1 — still done, still cited.
* Hand-edit `Knot/{Desc,Wf,Tags,Ctors,Map}`. Regenerate.
* Trust a "needs -c" header without re-measuring — `Knot/Wf`'s was
  re-measured on 08-27 and IS still needed, which is not a licence to
  assume the next one is.

---

## Standing debt

⚠ **A CENSUS NUMBER WAS WRONG, AND CENSUS NUMBERS ARE CLAIMS.**
`Knot/Terms`' header said the ford-only shape covers "32 of the 53 rows".
Counted off the generator's table it is **13**; no reading gives 32
(no-recursive-field 14 · ≤1 recursive field 26 · RTy+RTm rows 41).
Corrected, with the full census by shape recorded in `WkRows`. ⇒ worth a
pass over the other "N of the M" claims in the Knot headers; I checked
only this one.


⚠ **A CENSUS NUMBER WAS WRONG, AND CENSUS NUMBERS ARE CLAIMS.**
`Knot/Terms`' header said the ford-only shape covers "32 of the 53 rows".
Counted off the generator's table it is **13**; no reading gives 32
(no-recursive-field 14 · ≤1 recursive field 26 · RTy+RTm rows 41).
Corrected, with the census recorded in `WkRows`. ⇒ worth a pass over the
other "N of the M" claims in the Knot headers; I checked only this one.

Unchanged from 08-26, except that the knot chain's timings above
supersede that file's. `AmrecIMuDepth` is still 44s (attributed: `maxTm`
inlining two `natrec`s, not the generic fold).

## User-owned decisions still open

* **Is item 7 the syntax half, or the judgement half too?** Step 0 does
  not move this; (A) is what would.
* The WF axis's three-customer judgement (unchanged).
* The comparison write-up, blocked on RAM (unchanged).
