# OCP-0009 — HANDOFF 2026-08-27

Supersedes `HANDOFF-2026-08-26.md`, whose work list closed with the spike
arc and handed off to `PLAN-JUDGEMENT.md`. This session took up that plan
and closed **step 0**, plus the motive and per-field halves of **step 2**.

Read `PLAN-JUDGEMENT.md` for the build plan; this file is session state.

Sweep: **ALL GREEN (125 modules)**, trust surface empty across 156 — and
on the warm re-run **nothing is ≥10s at all** (303s total across 121).
Cold, the two that are: `Knot/Wf` 104s with `-c`, `Knot/Ctors` 15–21s.
⚠ Do not quote the sweep's numbers as cold — `Gcd/LeEq` reads 3s there
against 08-26's cold 621s.

---

## What this session did

**Step 0 — `Ctx` is encoded**, ⚠ not as the 8th sort of the knot (which
is how it was built first, and how the plan said to build it) but as its
own 2-row family over a bare depth. **And step 2's two open questions —
the MOTIVE and the PER-FIELD RULE — are both settled.** `KnotD` is back
to **53** rows.

| | | |
|---|---|---|
| 1 | `Ctx` as sort 7 — built, green, adequacy-mapped | (retired same day) |
| 2 | the `◇` weakening method — **compiles, and fabricates** | `Negative/WkEmp` |
| 3 | ⇒ `Ctx` split into its own family | `Examples/Knot/CtxD` |
| 4 | ⇒ the uniform motive is then honest at all 53 sorts | — |
| 5 | the PER-FIELD RULE, at four bracketing rows | `Examples/Knot/WkRows` |
| 6 | `Lib/IWk` — classifier + decider ✅, **typing ⬜** | `Lib/IWk`, `Knot/WkProbe` |

⇒ what is left before `_∋_∷_` is `Lib/IWk`'s TYPING lemma. The rule is
computed rather than enumerated, and validated against the knot; nothing
yet says the tuple it builds inhabits `imethsTy`.

---

## The results worth carrying forward

### 1. ★★★ A GREEN BUILD REFUTED THE DESIGN

`PLAN-JUDGEMENT` step 2 needs object-level weakening over the knot, at
the uniform motive `WkTm` and `WkFin` both use. As sort 7, `◇`'s method
holds `snd ⟨i⟩ ≡ nzero` and would have to prove `nsuc (snd ⟨i⟩) ≡ nzero`
to rebuild itself — but `K (sCtx, 1)` is INHABITED, so a different
constructor closes the goal. **It compiled**, `--safe`, trust surface
empty, computing

    ⋄  ↦  ◇ ▹ Nat            -- a type that was never there

and nothing downstream would have noticed, because there is no
`Ctx`-weakening spec for it to violate.
⇒ `verification-that-covers-less-than-it-claims`, reached from a new
direction: **the hazard was a GREEN build, not a red one**, which is why
it had to be built rather than argued.

### 2. ★★ THE PRINCIPLE IT SETTLED — A STRATUM IS NOT A MEMBER

The seven families in `KnotD` are one mutual recursion in `Spec/Syntax`.
`_▹_` carries an `RTy ⌊ Γ ⌋`, so `Ctx` depends on the syntax and the
syntax never depends back. **One-directional dependency ⇒ stratum, not
member.**

★ And the index had been meaning two things: `KnotD`'s second component
is the AMBIENT SCOPE a term lives in — a parameter — while a context's
depth is its OWN LENGTH, a measure of the datum. Sharing one slot is what
forced `Knot/Map` to grow a third signature shape for `⊢enCtx`. That got
written down as an oddity; it was a symptom.

⇒ ★★★ **AND SPLITTING MADE `wk`'s TYPE TRUE, rather than restricting
it.** With `Ctx` out, `wkK : K (s,d) → K (s, suc d)` is honest at all 53
sorts — closed sorts' depth is degenerate, `ICon` is genuinely scoped,
and no row invents anything.

### 3. ★ THE UNTESTED JOINT WENT THROUGH FIRST TRY

`_▹_`'s `RTy` field has its TYPE in another family, so it is a κ field
carrying a `⌜IMu⌝` code (`icw-imu`) **at an index mentioning a BOUND
field** — three separately-built things (`Scoped.varC`, `DepIx.islamC`,
`DepIx`'s ford naming `var vz`) never before combined. ⚠ Fording could
not do this job: it turns a computed INDEX into a constraint, never a
field's TYPE into a family. Both mechanisms are in `_▹_`, three lines
apart.

And the split is cheaper per row, not dearer:

| | as sort 7 | as `CtxD` |
|---|---|---|
| fords per row | 2 (tag + depth) | **1** (depth) |
| index | `Σ' Nat Nat` | `El ⌜Nat⌝` |
| `_▹_`'s `num-ren`/`num-sub` chains | 14 | **9** |
| `⊢ixP` on the ambient, `fordFst`, `βfst` | throughout | **absent** |

### 4. ⚠ THE COVERAGE GUARD MOVED, AND IT IS NOT WEAKER

`gen-knot.py`'s `verify()` is back to one file and no longer mentions
`Ctx`. `Ctx` is not thereby unchecked: `enCtx`'s clauses check it, because
**Agda's coverage checker checks FUNCTIONS**. A hand-written table with a
hand-written map is guarded already; a GENERATED table with a GENERATED
map is not, which is the entire reason `verify()` exists.
**Controlled**: deleting `enCtx`'s `_▹_` clause gives `Incomplete pattern
matching for enCtx`.

### 5. ★ WHAT A PAIR-INDEXED TRANSPORT COSTS — TWO `jsub`s, FLAT

One per index component, where `WkFin`'s `fsuc` moved along one. ⚠ And
the `wk-single` one expects does NOT appear: the outer transport's family
mentions the inner component, weakened past the transport's own binder
and then substituted — PROPOSITIONAL in general, DEFINITIONAL here,
because that component is `snd` of a VARIABLE and both actions COMPUTE on
variables (`Knot/Build`'s finding (c)). **Measured after a wrong guess**:
replacing either `wk-single` with `refl` type-checks, so both casts came
out.

★ ⇒ **`_∋_∷_`'s THREE-component telescope should cost three `jsub`s,
flat** — the first quantitative thing anyone knows about step 1. ⚠ Its
telescope now mixes two families (`CtxK` and `K`); `DepIx` already builds
one mixing `El ⌜Nat⌝` with an `IMu`, so that is the same mechanism at one
more component.

---

## NEXT

### A. ✅ **THE PER-FIELD RULE IS SETTLED** — `Examples/Knot/WkRows`
###    ⬜ what remains is `Lib/IWk`, i.e. computing it

`PLAN-JUDGEMENT` step 1 (`_∋_∷_`) reads *"needs only the weakening
already in hand"*. ⚠ **TRUE OF THE MECHANISM, FALSE OF THE CODE.**
`Examples/WkTm` weakens a 3-constructor toy; `renTy vs` is an `ielim`
over the **53-row knot** returning a knot element, and it cannot be 53
enumerated methods (`half-generalization-is-worst`, with a SYNTAX
codomain instead of `Nat`).

⚠⚠ **AND `Lib/IFold`'s SHAPE DOES NOT REACH IT.** A fold into a CONSTANT
motive takes the IH at every `iρ`. A weakening does not — and
`cDCon-kap` is the row where that bites, because its TWO `iρ` fields need
OPPOSITE treatments:

> ✅ **FOUR METHODS BUILT, AT ONE MOTIVE, ALL GREEN.**
>
> | row | shape | what the method does | cost |
> |---|---|---|---|
> | `cTy-Nat` | ford-only | pass the tag ford through | **free** — `βfst` takes the shifted constraint to the field it was handed |
> | `cTm-lam` | riding index, under a binder | take the IH, forward off the eliminator's index and backward onto the row's | conversions only |
> | `cDCon-kap` | a PINNED field beside a riding one | ★ `RTy ε` takes the **ORIGINAL FIELD** (its IH is at depth 1, the row wants depth 0); its `DCon` sibling takes the IH | — |
> | `cVar-vs` | depth-Forded | riding case **plus one `congS`** (`βsnd` does the rest) | **one `jsub`** |
>
> ★★ **AND THE TABLE, COUNTED BY SHAPE** (measured off the generator's
> table, not estimated):
>
> | | |
> |---|---|
> | rows | 53 |
> | FORD-ONLY rows | 13 |
> | RIDING recursive fields | 77 |
> | rows with a PINNED-index field | **4** — `cTy-IMu`, `cTm-cIMu`, `cDCon-kap` (`RTy ε` at `pair sTy 0`), `cIDesc-cons` (`ICon (ε ∙)` at `pair sICon 1`) |
> | DEPTH-FORDED rows | **2** — `cVar-vz`, `cVar-vs` |
>
> ⇒ the ORIGINAL-field rule has **four** customers and the transport
> **two**. Everything else is the riding case.
>
> ⚠ AND ONE HELPER `Knot/Terms` COULD NOT SUPPLY: `tyFordFst` was
> written for a CONCRETE index and reuses one derivation as BOTH the
> pair's first component and the `⌜Id⌝`'s right endpoint. In a METHOD
> the index is abstract and those differ (`fst ⟨i⟩` vs the row's literal
> tag). `WkRows.tyFordAt` is the abstract-index twin.

### A″. ✅ **`Lib/IWk` IS BUILT** — classifier, decider, escape hatch
###     **and the typing**

`WkRows` settled WHAT the per-field rule is; `Lib/IWk` computes it, and
`⊢iwkMethsFrom` says the tuple it builds inhabits `imethsTyFrom`.

| | |
|---|---|
| the classification (`IsSucs`/`WkIx`/`WkKa`/`WkCon`/`WkDesc`) | ✅ |
| the METHOD and TUPLE computed from it | ✅ |
| the classification **DECIDED** from a raw description | ✅ |
| the escape hatch — structural (`wkd-stop` + a tail) | ✅ |
| `⊢iwkPay` / `⊢iwkMethod` / `⊢iwkMethsFrom` — **the typing** | ✅ |

★★★ **AND THE PAYOFF IS THAT THE COMPUTED METHOD *IS* THE HAND-WRITTEN
ONE** (`Knot/WkProbe` §5). Nothing before this said the two agree — the
generic method could have been well typed and a DIFFERENT term:

    iwkMethod tagTy-Nat   clsNat ≡ wkTyNat     by refl
    iwkMethod tagTm-lam   clsLam ≡ wkTmLam     by refl
    iwkMethod tagDCon-kap clsKap ≡ wkDkap      by refl   ← the per-field row

⇒ that is why `WkRows` was kept rather than deleted: it is the **control**
for the library. And §6 types two of them against the real `IConWf`s, so
the generic lemma is exercised at the actual table, not just stated.

### ⚠ THREE THINGS WRITING THE PROOF FOUND, which the term level did not

**1. ★★ THE CLASSIFICATION WAS UNSOUND, twice.** `pinned` originally
required only "does not mention the ambient". That is too weak: the
rebuild changes the ambient **and every `rides` field's slot** (it holds
the IH now, not the original field), so a pinned index avoiding only the
ambient could still move. Same for `rides`' own sort component.
⇒ both strengthened to **closedness**, decided structurally by
`decClosed` — no `Var` enumeration needed, because `occTm x nzero` is
`false` for every `x` definitionally.

⚠ **It happens to be sound on `KnotD`** — every pinned index there is a
closed pair of numerals — which is exactly how a hole like this survives
into a library that then claims to be generic. Coverage after the fix:
still 51.

**2. ⚠ `iwkPay`'s `pinned` CLAUSE DID NOT ADVANCE THE IH TUPLE.**
`iihTy` has one entry per `iρ` regardless of whether the method USES it,
so the clause was reading the next field's IH at this field's position.
**The term level type-checks either way** — only the typing lemma
catches it.

**3. ⚠ TWO OBLIGATIONS `Lib/IFold` NEVER HAD**, both structural:
`IConWf` must be threaded through the recursion (every `⊢pair` wants the
tail's `⊢ty`, and `ipayTy-wf` supplies it only from an `IConWf`), and
`⊢icon` needs BOTH `k ∈ID D` and the payload at `ilookupD D k` — neither
follows from the other. `Split D j E` ("E is D from position j") yields
both, derived on the way past, where `Knot/Tags` enumerates the
memberships at O(n²).

### A‴. ✅ **AND `wkK` EXISTS** — `Examples/Knot/Wk`

    wkK : K i → K (sh i)          i.e.  K (s, d) → K (s, suc d)

**The first FUNCTION OVER SYNTAX the encoded knot has** — everything
before it was a MEASURE (`Knot/Sz`) or a constructor.

| | |
|---|---|
| 51 methods | **computed** by `Lib/IWk` |
| 2 methods | given — the depth-Forded `Var` rows (`WkRows` §5, §7) |
| the tuple | `⊢iwkMethsFrom … spl-nil KnotWf KnotWf …` |
| `wkK` itself | `⊢ielim`, plus one `wk-single` cast on the result index |

⚠ **AND NOTHING IN IT ENUMERATES A ROW.** The classification, the
leftover description, its well-formedness and its position each come from
ONE recursion over `decDesc KnotD` — `wkdRest`, `wfDrop`, `splDrop`.
`Knot/Tags` pays O(n²) to enumerate memberships; `Split` derives each on
the way past.

⚠ Two helpers the assembly needed and did not have: `wfDrop` (peeling
`IDescWfFrom` to the leftover — 51 `idwf-cons` by hand otherwise, i.e.
exactly the enumeration `Lib/IWk` exists to avoid) and `wfStep`, because
`IDescWfFrom` is a datatype and its tail is reached by MATCHING.

### A′. ✅ **RESOLVED — `Ctx` IS ITS OWN FAMILY**

The fork that stood here (split `Ctx` out / keep one description and
write the junk down) was decided for the split, on the evidence in
results 1–3 above. `Negative/WkEmp` is kept RED as the measurement that
decided it; do not repair it.

⛔ The third option — making `Ctx` depth-degenerate — stays refused: `◇`
would inhabit `K (sCtx, 5)` and the depth would stop measuring the
context.

### B. ⬜ unchanged from 08-26

* the `sz` agreement (`szTm ⌈t⌉ ⟶* ⌜ sz t ⌝`) — bulk, known shape.
* `Confluence`'s `⟹-⁺` `UnreachableClauses` warning.
* check-formers 4–6: a REVIEW list. ⚠ Not affected by the 8th sort —
  those checks are about `RTm`/`RTy` formers in the SN layer; the knot
  table's own tripwire is `gen-knot.py`'s `verify()`.

### ⛔ Do NOT

* Enumerate 55 weakening methods to "get something working". See (A).
* Re-spike `PLAN-JUDGEMENT` §1 — still done, still cited.
* Hand-edit `Knot/{Desc,Wf,Tags,Ctors,Map}`. Regenerate.
* Trust a "needs -c" header without re-measuring — `Knot/Wf`'s was
  re-measured on 08-27 and IS still needed, which is not a licence to
  assume the next one is.

---

## Where the KERNEL question landed

⬜⬜ **ASKED: does the escape hatch belong in the kernel?** ⇒ **No**, and
the reason is already recorded there rather than being a gap.

The exceptional rows exist because `Var` is **Forded**, and Fording is a
deliberate kernel decision with a semantic reason: `ICodeWf`'s comment
says `El (⌜Id⌝ c a b)` decodes to `Id (El c) a b` in ONE step and `⊩₀Id`
needs only that chain — *"which is what makes it environment-proof, and
it is why Fording (§3) and not native computed targets is what this
kernel can model."* Native computed target indices would remove the
exception, and were rejected for a stated modelling reason. Not something
to reopen from a library's convenience.

⚠ **AND THE POSITIONAL METHOD TUPLE IS THE OTHER HALF OF THE ANSWER.**
`imethsTy` is a right-nested Σ indexed by position, and `∈ID` is a
position; the whole Metatheory rests on that shape. Changing it so a
library could splice methods anywhere would be the tail wagging the dog.

⇒ the right level for this is `Lib/`, which is where it is.

## ✅✅ STEP 1 — `_∋_∷_` IS A WELL-FORMED INDEXED DESCRIPTION

`Examples/Knot/Lookup`. **The first RELATION over encoded syntax.**

| | |
|---|---|
| the four-component index telescope, over TWO `IMu`s | ✅ |
| `lkHere` + `lkHereWf` — 7 fields, 3 transports | ✅ |
| `lkThere` + `lkThereWf` — 10 fields, 1 recursive, 3 transports | ✅ |
| `LkD` / `LkWf` — the description, well formed | ✅ |
| `⊢lkVz` — **INHABITED**, at `(◇ ▹ Nat) ∋ vz ∷ renTy vs Nat` | ✅ |

★★ **AND THAT CLOSES THE `no-cons-at-zero` HAZARD.** A well-formed
description with no closed inhabitant is indistinguishable from a good
one; `LkWf` alone said nothing. ⇒ `_∋_∷_` is well formed AND inhabited,
so step 3 can be built on it.

★ **AND THE THREE TRANSPORTS EVAPORATE.** At a concrete index each ford
witness IS an `idrefl`, so `WkFin.transport-fires` collapses every `jsub`
in two steps. ⇒ `PLAN-JUDGEMENT` §1's "Fording costs a transport in the
DERIVATION and nothing at runtime" — asserted for the syntax, now
**exercised at a judgement**.

⚠ Four implicits had to be pinned, all the same root cause
(`pin-implicits-on-defined-set-types`): `ipayTy-wf`'s `Θ` (a `Ctx`
reachable only through the non-injective `⌊_⌋`), the ford proofs' types
(left as `_`, Agda tries to INVERT `subTm` and hits the inversion-depth
limit), the substitution environments, and `isingle-Sub⊢`'s `I` (it
appears only under `εwkTy`). ⇒ the telescope is written as
`◇ ▹ εwkTy ILk`, in the form the lemma states it, rather than the
definitionally-equal `◇ ▹ ILk`.

★★ **THE TELESCOPE WAS NOT THE RISK; FORDING WAS.** §3 step 1 flagged the
three-component telescope as "the first place to look". It types first
try, at FOUR components spanning two different `IMu`s. What costs is that
`here` Fords all four components, and **the three later fords must
transport their right-hand sides along the depth ford** — `jsub (⌜IMu⌝ …
⟨-⟩) (symN … p) e`, `WkFin`'s idiom, three times in one row, and the
first time it is paid for a FOREIGN family rather than the row's own
index.

⚠ **AND IT NEEDED VARIABLE-DEPTH SMART CONSTRUCTORS, WHICH DID NOT
EXIST.** `⊢Ctx-extK`/`⊢Var-vzK` are stated at `num n` because their
customer is the adequacy map, whose depths are numerals. A constructor's
telescope binds its depth as an `iκ ⌜Nat⌝` field — a VARIABLE. ⇒ added
`⊢Ctx-extKv` and `⊢Var-vzKv`, and ★ **both are SHORTER than their numeral
siblings**: renaming and substitution COMPUTE on a variable, so every
`num-ren`/`num-sub` chain collapses to `refl`. `Knot/Build`'s route (c),
confirmed. The two forms are siblings; neither subsumes the other.

### ⚠⚠ AND IT OOM'd — diagnosed, not guessed

As one nested term the row does **not fit**: `-A64m` and `-A64m -c` both
OOM (143), on a box with 4.2 GB free against a 5.5 GB cap. Worked in the
order the memories prescribe:

| cause | verdict |
|---|---|
| contention (`never-run-two-agda-checks-at-once`) | ruled out — no concurrent `agda` |
| GC choice (`agda-oom-is-a-gc-choice`) | `-c` tried FIRST, did **not** help |
| elaborated term size | ✅ **this one** |

⇒ every code, telescope and field-proof got a NAME (`κ₀…κ₆`, `Θ0…Θ6`,
`C₁…C₆`, `W₁…W₆`), so the bodies elaborate behind a `Def`. **Result: 4s
at the DEFAULT collector**, no `-c` marker needed. Profile of the split
module: 238 MB peak, and its own `Typing` is 26ms — the rest is
deserialising the import closure.

⚠ **A REAL DEPENDENCY SMELL, FOUND AND FIXED, BUT NOT A PERF WIN.**
`Knot/Wk` imported `Knot/WkProbe` — a real module depending on an
ASSERTIONS module — for one 3-line lemma. `⊢shIPair` now lives in
`Knot/Wk` and the probe imports it. ⚠ Measured: identical residency
either way, so this is architecture, not memory.

⬜⬜ **AND THE OFF-BY-ONE CLASS IS WORTH REMOVING — NOW WITH A MEASURED
COST.** Nearly every iteration on both rows was the term level's
`var (vs^k vz)` disagreeing with the derivation level's
`⊢var (there^k here)` — two encodings of one index, kept in sync by hand.

⚠ **AND ONE OF THEM WAS IN THE `ICon` ITSELF, WHERE NOTHING CATCHES IT.**
`λ₈` referred to `x` at `vs⁶` when `x` is field 2 of 8, i.e. `vs⁵` — it
named `Γ`. The ICon still type-checked, because ANY in-scope variable is
well-scoped there; only the `IConWf` rejected it, and it did so by
demanding a slot that matched the WRONG term, which reads as a Wf bug
rather than an ICon bug. ⇒ the row's `IConWf` is the only thing standing
between a mis-indexed description and a green build, exactly as
`Knot/Map` is for the syntax.

★ The rule that removes it, and it is one line: **at `k` fields bound,
field `j` sits at `vs^(k-1-j) vz` and the ambient at `vs^k vz`.** Both
rows are consistent with it; every error was a departure from it. `Lib/IWk` has no such bugs because it walks the
`ICon` generically and never names an index. ⇒ generating a judgement
row's `IConWf` the same way is the fix; `FUTURE.md` has it, together with
the de Bruijn LEVELS question it raises for Once itself.

## ⬜ PARKED — decide later, all four in one place

Recorded 2026-08-28 at the user's request, so the choice is made from a
list rather than from memory.

| | what | why it is parked |
|---|---|---|
| **B** | the `sz` agreement — `szTm ⌈t⌉ ⟶* ⌜ sz t ⌝`, 53 chains of the `ScopedSize.sizeApp` shape | bulk, known shape, no design risk. ⚠ **But it is `sz`'s ADEQUACY**, the exact counterpart of `Knot/Map` for the syntax — step 4's `prog` is typed against the OBJECT-level `sz`, so without this its meaning is asserted. ⇒ price it on 2–3 rows BEFORE the step-3 bulk: a systematic error in `Lib/ISz` found after 166 judgement rows is the expensive version. |
| **C1** | a CENSUS pass over the Knot headers | one wrong "N of the M" was found and fixed (`Terms.agda`: 32 → **13**); the others are unchecked. Cheap, and census numbers are claims. |
| **C2** | `Confluence`'s `⟹-⁺` `UnreachableClauses` warning; check-formers 4–6 (4 vacuous rows, 3 promissory notes, 5 conditional lemmas with no consumer) | long-standing, independent, a REVIEW list rather than a build. |
| **D** | ★ **is item 7 the syntax half, or the judgement half too?** | user-owned. `ARCHITECTURE.md` turns on it. This session moved evidence toward "reachable" — `Ctx` encoded, weakening built — without answering it. |

⇒ **the path to the payoff is 1 → 2 → 3 → 4**, because step 4 IS the
dogfooding target (see `PLAN-JUDGEMENT` step 4). None of B/C1/C2 is on
that path; B is the one that should nonetheless be *sampled* before the
step-3 bulk, for the reason in its row.

### ⚠⚠ AND STEP 3 SHOULD BE **GENERATED**, NOT HAND-WRITTEN

Measured on step 1: **two rows** — `here` (7 fields) and `there` (10) —
cost a long session, and most of it was de Bruijn bookkeeping that the
one-line rule (field `j` at `vs^(k-1-j) vz`, ambient at `vs^k vz`) makes
mechanical. Step 3 is **~166 rows**.

⇒ at that rate step 3 is not a "keep going" item; it is a GENERATOR job.
And the tree already knows this: `tools/gen-knot.py` exists precisely
because 53 rows of de Bruijn bookkeeping "is not work, it is a
transcription error waiting to happen". The judgement layer is three
times that and has MORE bookkeeping per row (four Forded components, and
three transports each).

★ So the next move on the critical path is **extend `gen-knot.py` to emit
judgement rows**, using `Examples/Knot/Lookup` as the hand-written
reference the generator must reproduce — exactly the role
`Knot/Terms`/`Knot/Build` play for the syntax table. The `IConWf`
generation is also what `FUTURE.md`'s off-by-one fix asks for, so the two
land together.

## Standing debt

⚠ **A CENSUS NUMBER WAS WRONG, AND CENSUS NUMBERS ARE CLAIMS.**
`Knot/Terms`' header said the ford-only shape covers "32 of the 53 rows".
Counted off the generator's table it is **13**; no reading gives 32
(no-recursive-field 14 · ≤1 recursive field 26 · RTy+RTm rows 41).
Corrected, with the full census by shape recorded in `WkRows`. ⇒ worth a
pass over the other "N of the M" claims in the Knot headers; I checked
only this one.


⚠ **A CENSUS NUMBER WAS WRONG, AND CENSUS NUMBERS ARE CLAIMS.**
`Knot/Terms`' header said the ford-only shape covers "32 of the 53 rows".
Counted off the generator's table it is **13**; no reading gives 32
(no-recursive-field 14 · ≤1 recursive field 26 · RTy+RTm rows 41).
Corrected, with the census recorded in `WkRows`. ⇒ worth a pass over the
other "N of the M" claims in the Knot headers; I checked only this one.

Unchanged from 08-26, except that the knot chain's timings above
supersede that file's. `AmrecIMuDepth` is still 44s (attributed: `maxTm`
inlining two `natrec`s, not the generic fold).

## User-owned decisions still open

* **Is item 7 the syntax half, or the judgement half too?** Step 0 does
  not move this; (A) is what would.
* The WF axis's three-customer judgement (unchanged).
* The comparison write-up, blocked on RAM (unchanged).
