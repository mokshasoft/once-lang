------------------------------------------------------------------------
-- ⚠⚠ GENERATED BY `tools/gen-knot.py` — DO NOT EDIT BY HAND. ⚠⚠
--
-- Regenerate with:  python3 tools/gen-knot.py
--
-- 53 constructors over 7 sorts, one description, index `Σ' Nat Nat`.
-- The table, the encoding decisions and the two exceptions (`Var`'s
-- depth-Fording) are documented in the generator's header — read that,
-- not this file, to understand the encoding.
------------------------------------------------------------------------


{-# OPTIONS --safe #-}
module DirectedHoTT.Examples.Knot.Desc where
open import DirectedHoTT.Spec.Syntax
  using ( Cx; ε; _∙; vz; vs
        ; RTy; RTm; var; pair; fst; snd; nzero; nsuc; ⌜Nat⌝; ⌜Id⌝
        ; IMu; ICon; IDesc; iι; iρ; iκ; inil; _◂_ )
open import DirectedHoTT.Examples.Knot.Sorts
  using ( IPair; sTy; sTm; sDesc; sDCon; sIDesc; sICon; sVar )

-- base : RTy Γ
cTy-base : ICon (ε ∙)
cTy-base =
  iκ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sTy)
   iι

-- U : RTy Γ
cTy-U : ICon (ε ∙)
cTy-U =
  iκ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sTy)
   iι

-- Π : RTy Γ → RTy (Γ ∙) → RTy Γ
cTy-Pi : ICon (ε ∙)
cTy-Pi =
  iρ (pair sTy (snd (var vz)))
   (iρ (pair sTy (nsuc (snd (var (vs vz)))))
    (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTy)
     iι))

-- Σ' : RTy Γ → RTy (Γ ∙) → RTy Γ
cTy-Sg : ICon (ε ∙)
cTy-Sg =
  iρ (pair sTy (snd (var vz)))
   (iρ (pair sTy (nsuc (snd (var (vs vz)))))
    (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTy)
     iι))

-- El : RTm Γ → RTy Γ
cTy-El : ICon (ε ∙)
cTy-El =
  iρ (pair sTm (snd (var vz)))
   (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sTy)
    iι)

-- Hom : RTy Γ → RTm Γ → RTm Γ → RTy Γ
cTy-Hom : ICon (ε ∙)
cTy-Hom =
  iρ (pair sTy (snd (var vz)))
   (iρ (pair sTm (snd (var (vs vz))))
    (iρ (pair sTm (snd (var (vs (vs vz)))))
     (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTy)
      iι)))

-- Unit : RTy Γ
cTy-Unit : ICon (ε ∙)
cTy-Unit =
  iκ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sTy)
   iι

-- Nat : RTy Γ
cTy-Nat : ICon (ε ∙)
cTy-Nat =
  iκ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sTy)
   iι

-- Id : RTy Γ → RTm Γ → RTm Γ → RTy Γ
cTy-Id : ICon (ε ∙)
cTy-Id =
  iρ (pair sTy (snd (var vz)))
   (iρ (pair sTm (snd (var (vs vz))))
    (iρ (pair sTm (snd (var (vs (vs vz)))))
     (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTy)
      iι)))

-- Mu : Desc → RTy Γ
cTy-Mu : ICon (ε ∙)
cTy-Mu =
  iρ (pair sDesc (snd (var vz)))
   (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sTy)
    iι)

-- IMu : IDesc → RTy ε → RTm Γ → RTy Γ
cTy-IMu : ICon (ε ∙)
cTy-IMu =
  iρ (pair sIDesc (snd (var vz)))
   (iρ (pair sTy nzero)
    (iρ (pair sTm (snd (var (vs (vs vz)))))
     (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTy)
      iι)))

-- var : Var Γ → RTm Γ
cTm-var : ICon (ε ∙)
cTm-var =
  iρ (pair sVar (snd (var vz)))
   (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sTm)
    iι)

-- lam : RTm (Γ ∙) → RTm Γ
cTm-lam : ICon (ε ∙)
cTm-lam =
  iρ (pair sTm (nsuc (snd (var vz))))
   (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sTm)
    iι)

-- app : RTm Γ → RTm Γ → RTm Γ
cTm-app : ICon (ε ∙)
cTm-app =
  iρ (pair sTm (snd (var vz)))
   (iρ (pair sTm (snd (var (vs vz))))
    (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm)
     iι))

-- pair : RTm Γ → RTm Γ → RTm Γ
cTm-pair : ICon (ε ∙)
cTm-pair =
  iρ (pair sTm (snd (var vz)))
   (iρ (pair sTm (snd (var (vs vz))))
    (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm)
     iι))

-- absurd : RTm Γ → RTm Γ → RTm Γ
cTm-absurd : ICon (ε ∙)
cTm-absurd =
  iρ (pair sTm (snd (var vz)))
   (iρ (pair sTm (snd (var (vs vz))))
    (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm)
     iι))

-- ordtr : (5 × RTm Γ) → RTm Γ
cTm-ordtr : ICon (ε ∙)
cTm-ordtr =
  iρ (pair sTm (snd (var vz)))
   (iρ (pair sTm (snd (var (vs vz))))
    (iρ (pair sTm (snd (var (vs (vs vz)))))
     (iρ (pair sTm (snd (var (vs (vs (vs vz))))))
      (iρ (pair sTm (snd (var (vs (vs (vs (vs vz)))))))
       (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs (vs (vs vz))))))) sTm)
        iι)))))

-- fst : RTm Γ → RTm Γ
cTm-fst : ICon (ε ∙)
cTm-fst =
  iρ (pair sTm (snd (var vz)))
   (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sTm)
    iι)

-- snd : RTm Γ → RTm Γ
cTm-snd : ICon (ε ∙)
cTm-snd =
  iρ (pair sTm (snd (var vz)))
   (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sTm)
    iι)

-- ⌜base⌝ : RTm Γ
cTm-cbase : ICon (ε ∙)
cTm-cbase =
  iκ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sTm)
   iι

-- ⌜Π⌝ : RTm Γ → RTm (Γ ∙) → RTm Γ
cTm-cPi : ICon (ε ∙)
cTm-cPi =
  iρ (pair sTm (snd (var vz)))
   (iρ (pair sTm (nsuc (snd (var (vs vz)))))
    (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm)
     iι))

-- ⌜Σ⌝ : RTm Γ → RTm (Γ ∙) → RTm Γ
cTm-cSg : ICon (ε ∙)
cTm-cSg =
  iρ (pair sTm (snd (var vz)))
   (iρ (pair sTm (nsuc (snd (var (vs vz)))))
    (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm)
     iι))

-- ⌜Hom⌝ : (3 × RTm Γ) → RTm Γ
cTm-cHom : ICon (ε ∙)
cTm-cHom =
  iρ (pair sTm (snd (var vz)))
   (iρ (pair sTm (snd (var (vs vz))))
    (iρ (pair sTm (snd (var (vs (vs vz)))))
     (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm)
      iι)))

-- hrefl : RTm Γ → RTm Γ → RTm Γ
cTm-hrefl : ICon (ε ∙)
cTm-hrefl =
  iρ (pair sTm (snd (var vz)))
   (iρ (pair sTm (snd (var (vs vz))))
    (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm)
     iι))

-- tr : RTm (Γ ∙) → RTm Γ → RTm Γ → RTm Γ
cTm-tr : ICon (ε ∙)
cTm-tr =
  iρ (pair sTm (nsuc (snd (var vz))))
   (iρ (pair sTm (snd (var (vs vz))))
    (iρ (pair sTm (snd (var (vs (vs vz)))))
     (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm)
      iι)))

-- ap : RTm Γ → RTm (Γ ∙) → RTm Γ → RTm Γ
cTm-ap : ICon (ε ∙)
cTm-ap =
  iρ (pair sTm (snd (var vz)))
   (iρ (pair sTm (nsuc (snd (var (vs vz)))))
    (iρ (pair sTm (snd (var (vs (vs vz)))))
     (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm)
      iι)))

-- ⌜Id⌝ : (3 × RTm Γ) → RTm Γ
cTm-cId : ICon (ε ∙)
cTm-cId =
  iρ (pair sTm (snd (var vz)))
   (iρ (pair sTm (snd (var (vs vz))))
    (iρ (pair sTm (snd (var (vs (vs vz)))))
     (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm)
      iι)))

-- idrefl : RTm Γ → RTm Γ → RTm Γ
cTm-idrefl : ICon (ε ∙)
cTm-idrefl =
  iρ (pair sTm (snd (var vz)))
   (iρ (pair sTm (snd (var (vs vz))))
    (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm)
     iι))

-- jsub : RTm (Γ ∙) → RTm Γ → RTm Γ → RTm Γ
cTm-jsub : ICon (ε ∙)
cTm-jsub =
  iρ (pair sTm (nsuc (snd (var vz))))
   (iρ (pair sTm (snd (var (vs vz))))
    (iρ (pair sTm (snd (var (vs (vs vz)))))
     (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm)
      iι)))

-- unit : RTm Γ
cTm-unit : ICon (ε ∙)
cTm-unit =
  iκ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sTm)
   iι

-- nzero : RTm Γ
cTm-nzero : ICon (ε ∙)
cTm-nzero =
  iκ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sTm)
   iι

-- nsuc : RTm Γ → RTm Γ
cTm-nsuc : ICon (ε ∙)
cTm-nsuc =
  iρ (pair sTm (snd (var vz)))
   (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sTm)
    iι)

-- natrec : RTm Γ → RTm ((Γ ∙) ∙) → RTm Γ → RTm Γ
cTm-natrec : ICon (ε ∙)
cTm-natrec =
  iρ (pair sTm (snd (var vz)))
   (iρ (pair sTm (nsuc (nsuc (snd (var (vs vz))))))
    (iρ (pair sTm (snd (var (vs (vs vz)))))
     (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm)
      iι)))

-- con : ℕ → RTm Γ → RTm Γ
cTm-con : ICon (ε ∙)
cTm-con =
  iκ ⌜Nat⌝
   (iρ (pair sTm (snd (var (vs vz))))
    (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm)
     iι))

-- elim : Desc → RTm Γ → RTm Γ → RTm Γ
cTm-elim : ICon (ε ∙)
cTm-elim =
  iρ (pair sDesc (snd (var vz)))
   (iρ (pair sTm (snd (var (vs vz))))
    (iρ (pair sTm (snd (var (vs (vs vz)))))
     (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm)
      iι)))

-- icon : ℕ → RTm Γ → RTm Γ
cTm-icon : ICon (ε ∙)
cTm-icon =
  iκ ⌜Nat⌝
   (iρ (pair sTm (snd (var (vs vz))))
    (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm)
     iι))

-- ielim : IDesc → RTm Γ → RTm Γ → RTm Γ → RTm Γ
cTm-ielim : ICon (ε ∙)
cTm-ielim =
  iρ (pair sIDesc (snd (var vz)))
   (iρ (pair sTm (snd (var (vs vz))))
    (iρ (pair sTm (snd (var (vs (vs vz)))))
     (iρ (pair sTm (snd (var (vs (vs (vs vz))))))
      (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs (vs vz)))))) sTm)
       iι))))

-- ⌜Nat⌝ : RTm Γ
cTm-cNat : ICon (ε ∙)
cTm-cNat =
  iκ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sTm)
   iι

-- ⌜Mu⌝ : Desc → RTm Γ
cTm-cMu : ICon (ε ∙)
cTm-cMu =
  iρ (pair sDesc (snd (var vz)))
   (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sTm)
    iι)

-- ⌜IMu⌝ : IDesc → RTy ε → RTm Γ → RTm Γ
cTm-cIMu : ICon (ε ∙)
cTm-cIMu =
  iρ (pair sIDesc (snd (var vz)))
   (iρ (pair sTy nzero)
    (iρ (pair sTm (snd (var (vs (vs vz)))))
     (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm)
      iι)))

-- ⌜Unit⌝ : RTm Γ
cTm-cUnit : ICon (ε ∙)
cTm-cUnit =
  iκ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sTm)
   iι

-- dnil : Desc
cDesc-nil : ICon (ε ∙)
cDesc-nil =
  iκ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sDesc)
   iι

-- _◃_ : DCon → Desc → Desc
cDesc-cons : ICon (ε ∙)
cDesc-cons =
  iρ (pair sDCon (snd (var vz)))
   (iρ (pair sDesc (snd (var (vs vz))))
    (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sDesc)
     iι))

-- dι : DCon
cDCon-i : ICon (ε ∙)
cDCon-i =
  iκ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sDCon)
   iι

-- dρ : DCon → DCon
cDCon-rho : ICon (ε ∙)
cDCon-rho =
  iρ (pair sDCon (snd (var vz)))
   (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sDCon)
    iι)

-- dκ : RTy ε → DCon → DCon
cDCon-kap : ICon (ε ∙)
cDCon-kap =
  iρ (pair sTy nzero)
   (iρ (pair sDCon (snd (var (vs vz))))
    (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sDCon)
     iι))

-- inil : IDesc
cIDesc-nil : ICon (ε ∙)
cIDesc-nil =
  iκ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sIDesc)
   iι

-- _◂_ : ICon (ε ∙) → IDesc → IDesc
cIDesc-cons : ICon (ε ∙)
cIDesc-cons =
  iρ (pair sICon (nsuc (nzero)))
   (iρ (pair sIDesc (snd (var (vs vz))))
    (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sIDesc)
     iι))

-- iι : ICon Δ
cICon-i : ICon (ε ∙)
cICon-i =
  iκ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sICon)
   iι

-- iρ : RTm Δ → ICon (Δ ∙) → ICon Δ
cICon-rho : ICon (ε ∙)
cICon-rho =
  iρ (pair sTm (snd (var vz)))
   (iρ (pair sICon (nsuc (snd (var (vs vz)))))
    (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sICon)
     iι))

-- iκ : RTm Δ → ICon (Δ ∙) → ICon Δ
cICon-kap : ICon (ε ∙)
cICon-kap =
  iρ (pair sTm (snd (var vz)))
   (iρ (pair sICon (nsuc (snd (var (vs vz)))))
    (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sICon)
     iι))

-- vz : Var (Γ ∙)
cVar-vz : ICon (ε ∙)
cVar-vz =
  iκ ⌜Nat⌝
   (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sVar)
    (iκ (⌜Id⌝ ⌜Nat⌝ (snd (var (vs (vs vz)))) (nsuc (var (vs vz))))
     iι))

-- vs : Var Γ → Var (Γ ∙)
cVar-vs : ICon (ε ∙)
cVar-vs =
  iκ ⌜Nat⌝
   (iρ (pair sVar (var vz))
    (iκ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sVar)
     (iκ (⌜Id⌝ ⌜Nat⌝ (snd (var (vs (vs (vs vz))))) (nsuc (var (vs (vs vz)))))
      iι)))

-- ★ the description: 53 constructors, in table order.
KnotD : IDesc
KnotD =
  cTy-base ◂ cTy-U ◂ cTy-Pi ◂ cTy-Sg ◂ cTy-El ◂ cTy-Hom ◂ cTy-Unit ◂ 
  cTy-Nat ◂ cTy-Id ◂ cTy-Mu ◂ cTy-IMu ◂ cTm-var ◂ cTm-lam ◂ cTm-app ◂ 
  cTm-pair ◂ cTm-absurd ◂ cTm-ordtr ◂ cTm-fst ◂ cTm-snd ◂ cTm-cbase ◂ 
  cTm-cPi ◂ cTm-cSg ◂ cTm-cHom ◂ cTm-hrefl ◂ cTm-tr ◂ cTm-ap ◂ cTm-cId ◂ 
  cTm-idrefl ◂ cTm-jsub ◂ cTm-unit ◂ cTm-nzero ◂ cTm-nsuc ◂ cTm-natrec ◂ 
  cTm-con ◂ cTm-elim ◂ cTm-icon ◂ cTm-ielim ◂ cTm-cNat ◂ cTm-cMu ◂ 
  cTm-cIMu ◂ cTm-cUnit ◂ cDesc-nil ◂ cDesc-cons ◂ cDCon-i ◂ cDCon-rho ◂ 
  cDCon-kap ◂ cIDesc-nil ◂ cIDesc-cons ◂ cICon-i ◂ cICon-rho ◂ cICon-kap 
  ◂ cVar-vz ◂ cVar-vs ◂ inil

-- `K (sort, depth)` — the whole knot as ONE family.
K : {Γ : Cx} → RTm Γ → RTy Γ
K i = IMu KnotD IPair i
