------------------------------------------------------------------------
-- ⚠⚠ GENERATED by tools/gen-knot.py — DO NOT EDIT BY HAND. ⚠⚠
--
-- The kernel's syntax as ONE family fibred by sort (D075), index
-- `(sort, depth)`, rows parsed out of `Spec/Syntax.agda`.  The encoding is
-- documented in the generator's header.
------------------------------------------------------------------------

{-# OPTIONS --safe #-}
module DirectedHoTT.Examples.Knot.Desc where
open import Agda.Builtin.Nat using ( zero; suc ) renaming ( Nat to ℕ )
open import DirectedHoTT.Spec.Syntax hiding ( Fin )
open import DirectedHoTT.Spec.Typing hiding ( _×_; _,,_ )
open import DirectedHoTT.Lib.Sugar using ( tag; lt-z; lt-s )
open import DirectedHoTT.Lib.Tel
open import DirectedHoTT.Lib.Sorted
open import DirectedHoTT.Lib.TelAt
open import DirectedHoTT.Lib.FinFam using ( FinD; ⊢FinD; ⊢isuc )

------------------------------------------------------------------------
-- 0. THE INDEX: (sort, depth).
------------------------------------------------------------------------

KI : {Γ : Cx} → RTm Γ
KI = SortI ⌜Nat⌝ 2

⊢J : {Γ : Ctx} → (Γ ▹ El (⌜Fin⌝ 2)) ⊢ ⌜Nat⌝ ∷ U
⊢J = ⊢⌜Nat⌝

⊢KI : {Γ : Ctx} → Γ ⊢ KI ∷ U
⊢KI = ⊢SortI ⊢J

-- the depth of an index
⊢dep : {Γ : Ctx} {i : RTm ⌊ Γ ⌋} → Γ ⊢ i ∷ El KI → Γ ⊢ snd i ∷ El ⌜Nat⌝
⊢dep d = ⊢snd (unSortI d)

------------------------------------------------------------------------
-- 1. THE ROWS — one telescope per constructor, over the index `i`.
------------------------------------------------------------------------

-- base  (no fields)
T-kbase : {Γ : Cx} → Tel (Γ ∙)
T-kbase = tι

-- U  (no fields)
T-kU : {Γ : Cx} → Tel (Γ ∙)
T-kU = tι

-- Π  (Ty@+0, Ty@+1)
T-kPi : {Γ : Cx} → Tel (Γ ∙)
T-kPi = tρ (pair (tag 0) (snd (var vz))) (tρ (pair (tag 0) (nsuc (snd (var vz)))) (tι))

-- Σ'  (Ty@+0, Ty@+1)
T-kSg : {Γ : Cx} → Tel (Γ ∙)
T-kSg = tρ (pair (tag 0) (snd (var vz))) (tρ (pair (tag 0) (nsuc (snd (var vz)))) (tι))

-- El  (Tm@+0)
T-kEl : {Γ : Cx} → Tel (Γ ∙)
T-kEl = tρ (pair (tag 1) (snd (var vz))) (tι)

-- Hom  (Ty@+0, Tm@+0, Tm@+0)
T-kHom : {Γ : Cx} → Tel (Γ ∙)
T-kHom = tρ (pair (tag 0) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι)))

-- Unit  (no fields)
T-kUnit : {Γ : Cx} → Tel (Γ ∙)
T-kUnit = tι

-- Nat  (no fields)
T-kNat : {Γ : Cx} → Tel (Γ ∙)
T-kNat = tι

-- Id  (Ty@+0, Tm@+0, Tm@+0)
T-kId : {Γ : Cx} → Tel (Γ ∙)
T-kId = tρ (pair (tag 0) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι)))

-- IMu  (Tm@+0, Tm@+0, Tm@+0)
T-kIMu : {Γ : Cx} → Tel (Γ ∙)
T-kIMu = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι)))

-- Desc  (Tm@+0)
T-kDesc : {Γ : Cx} → Tel (Γ ∙)
T-kDesc = tρ (pair (tag 1) (snd (var vz))) (tι)

-- DIh  (Tm@+0, Ty@+2, Tm@+0, Tm@+0)
T-kDIh : {Γ : Cx} → Tel (Γ ∙)
T-kDIh = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 0) (nsuc (nsuc (snd (var vz))))) (tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι))))

-- Fin  (nat)
T-kFin : {Γ : Cx} → Tel (Γ ∙)
T-kFin = tσ ⌜Nat⌝ (tι)

-- var  (var)
T-kvar : {Γ : Cx} → Tel (Γ ∙)
T-kvar = tσ (⌜IMu⌝ ⌜Nat⌝ FinD (snd (var vz))) (tι)

-- lam  (Tm@+1)
T-klam : {Γ : Cx} → Tel (Γ ∙)
T-klam = tρ (pair (tag 1) (nsuc (snd (var vz)))) (tι)

-- app  (Tm@+0, Tm@+0)
T-kapp : {Γ : Cx} → Tel (Γ ∙)
T-kapp = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι))

-- pair  (Tm@+0, Tm@+0)
T-kpair : {Γ : Cx} → Tel (Γ ∙)
T-kpair = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι))

-- absurd  (Tm@+0, Tm@+0)
T-kabsurd : {Γ : Cx} → Tel (Γ ∙)
T-kabsurd = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι))

-- ordtr  (Tm@+0, Tm@+0, Tm@+0, Tm@+0, Tm@+0)
T-kordtr : {Γ : Cx} → Tel (Γ ∙)
T-kordtr = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι)))))

-- fst  (Tm@+0)
T-kfst : {Γ : Cx} → Tel (Γ ∙)
T-kfst = tρ (pair (tag 1) (snd (var vz))) (tι)

-- snd  (Tm@+0)
T-ksnd : {Γ : Cx} → Tel (Γ ∙)
T-ksnd = tρ (pair (tag 1) (snd (var vz))) (tι)

-- ⌜base⌝  (no fields)
T-kcbase : {Γ : Cx} → Tel (Γ ∙)
T-kcbase = tι

-- ⌜Π⌝  (Tm@+0, Tm@+1)
T-kcPi : {Γ : Cx} → Tel (Γ ∙)
T-kcPi = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (nsuc (snd (var vz)))) (tι))

-- ⌜Σ⌝  (Tm@+0, Tm@+1)
T-kcSg : {Γ : Cx} → Tel (Γ ∙)
T-kcSg = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (nsuc (snd (var vz)))) (tι))

-- ⌜Hom⌝  (Tm@+0, Tm@+0, Tm@+0)
T-kcHom : {Γ : Cx} → Tel (Γ ∙)
T-kcHom = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι)))

-- hrefl  (Tm@+0, Tm@+0)
T-khrefl : {Γ : Cx} → Tel (Γ ∙)
T-khrefl = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι))

-- tr  (Tm@+1, Tm@+0, Tm@+0)
T-ktr : {Γ : Cx} → Tel (Γ ∙)
T-ktr = tρ (pair (tag 1) (nsuc (snd (var vz)))) (tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι)))

-- ap  (Tm@+0, Tm@+1, Tm@+0)
T-kap : {Γ : Cx} → Tel (Γ ∙)
T-kap = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (nsuc (snd (var vz)))) (tρ (pair (tag 1) (snd (var vz))) (tι)))

-- ⌜Id⌝  (Tm@+0, Tm@+0, Tm@+0)
T-kcId : {Γ : Cx} → Tel (Γ ∙)
T-kcId = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι)))

-- idrefl  (Tm@+0, Tm@+0)
T-kidrefl : {Γ : Cx} → Tel (Γ ∙)
T-kidrefl = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι))

-- jsub  (Tm@+1, Tm@+0, Tm@+0)
T-kjsub : {Γ : Cx} → Tel (Γ ∙)
T-kjsub = tρ (pair (tag 1) (nsuc (snd (var vz)))) (tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι)))

-- unit  (no fields)
T-kunit : {Γ : Cx} → Tel (Γ ∙)
T-kunit = tι

-- nzero  (no fields)
T-knzero : {Γ : Cx} → Tel (Γ ∙)
T-knzero = tι

-- nsuc  (Tm@+0)
T-knsuc : {Γ : Cx} → Tel (Γ ∙)
T-knsuc = tρ (pair (tag 1) (snd (var vz))) (tι)

-- natrec  (Tm@+0, Tm@+2, Tm@+0)
T-knatrec : {Γ : Cx} → Tel (Γ ∙)
T-knatrec = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (nsuc (nsuc (snd (var vz))))) (tρ (pair (tag 1) (snd (var vz))) (tι)))

-- con  (Tm@+0)
T-kcon : {Γ : Cx} → Tel (Γ ∙)
T-kcon = tρ (pair (tag 1) (snd (var vz))) (tι)

-- ielim  (Tm@+0, Tm@+0, Tm@+0, Tm@+0)
T-kielim : {Γ : Cx} → Tel (Γ ∙)
T-kielim = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι))))

-- dι  (no fields)
T-kdI : {Γ : Cx} → Tel (Γ ∙)
T-kdI = tι

-- dσ  (Tm@+0, Tm@+0)
T-kdS : {Γ : Cx} → Tel (Γ ∙)
T-kdS = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι))

-- dρ  (Tm@+0, Tm@+0)
T-kdR : {Γ : Cx} → Tel (Γ ∙)
T-kdR = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι))

-- dpay  (Tm@+0, Tm@+0, Tm@+0)
T-kdpay : {Γ : Cx} → Tel (Γ ∙)
T-kdpay = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι)))

-- dih  (Tm@+0, Tm@+0, Tm@+0, Tm@+0)
T-kdih : {Γ : Cx} → Tel (Γ ∙)
T-kdih = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι))))

-- fzero  (no fields)
T-kfzero : {Γ : Cx} → Tel (Γ ∙)
T-kfzero = tι

-- fsuc  (Tm@+0)
T-kfsuc : {Γ : Cx} → Tel (Γ ∙)
T-kfsuc = tρ (pair (tag 1) (snd (var vz))) (tι)

-- fcase  (Tm@+0, Tm@+0, Tm@+1)
T-kfcase : {Γ : Cx} → Tel (Γ ∙)
T-kfcase = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (nsuc (snd (var vz)))) (tι)))

-- fcase0  (Tm@+0)
T-kfcase0 : {Γ : Cx} → Tel (Γ ∙)
T-kfcase0 = tρ (pair (tag 1) (snd (var vz))) (tι)

-- psplit  (Tm@+2, Tm@+0)
T-kpsplit : {Γ : Cx} → Tel (Γ ∙)
T-kpsplit = tρ (pair (tag 1) (nsuc (nsuc (snd (var vz))))) (tρ (pair (tag 1) (snd (var vz))) (tι))

-- ⌜Nat⌝  (no fields)
T-kcNat : {Γ : Cx} → Tel (Γ ∙)
T-kcNat = tι

-- ⌜IMu⌝  (Tm@+0, Tm@+0, Tm@+0)
T-kcIMu : {Γ : Cx} → Tel (Γ ∙)
T-kcIMu = tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tρ (pair (tag 1) (snd (var vz))) (tι)))

-- ⌜Fin⌝  (nat)
T-kcFin : {Γ : Cx} → Tel (Γ ∙)
T-kcFin = tσ ⌜Nat⌝ (tι)

-- ⌜Unit⌝  (no fields)
T-kcUnit : {Γ : Cx} → Tel (Γ ∙)
T-kcUnit = tι

TyTs : {Γ : Cx} → Tels (Γ ∙) 13
TyTs = T-kbase ∷ᵗ T-kU ∷ᵗ T-kPi ∷ᵗ T-kSg ∷ᵗ T-kEl ∷ᵗ T-kHom ∷ᵗ T-kUnit ∷ᵗ T-kNat ∷ᵗ T-kId ∷ᵗ T-kIMu ∷ᵗ T-kDesc ∷ᵗ T-kDIh ∷ᵗ T-kFin ∷ᵗ []ᵗ

TmTs : {Γ : Cx} → Tels (Γ ∙) 38
TmTs = T-kvar ∷ᵗ T-klam ∷ᵗ T-kapp ∷ᵗ T-kpair ∷ᵗ T-kabsurd ∷ᵗ T-kordtr ∷ᵗ T-kfst ∷ᵗ T-ksnd ∷ᵗ T-kcbase ∷ᵗ T-kcPi ∷ᵗ T-kcSg ∷ᵗ T-kcHom ∷ᵗ T-khrefl ∷ᵗ T-ktr ∷ᵗ T-kap ∷ᵗ T-kcId ∷ᵗ T-kidrefl ∷ᵗ T-kjsub ∷ᵗ T-kunit ∷ᵗ T-knzero ∷ᵗ T-knsuc ∷ᵗ T-knatrec ∷ᵗ T-kcon ∷ᵗ T-kielim ∷ᵗ T-kdI ∷ᵗ T-kdS ∷ᵗ T-kdR ∷ᵗ T-kdpay ∷ᵗ T-kdih ∷ᵗ T-kfzero ∷ᵗ T-kfsuc ∷ᵗ T-kfcase ∷ᵗ T-kfcase0 ∷ᵗ T-kpsplit ∷ᵗ T-kcNat ∷ᵗ T-kcIMu ∷ᵗ T-kcFin ∷ᵗ T-kcUnit ∷ᵗ []ᵗ

KTss : {Γ : Cx} → STels (Γ ∙) 2
KTss = TyTs ∷ˢᵗ TmTs ∷ˢᵗ []ˢᵗ

-- ★ THE FAMILY, and the syntax at a sort and depth
KD : {Γ : Cx} → RTm Γ
KD = Dₛₜ KTss

K : {Γ : Cx} → ℕ → RTm Γ → RTy Γ
K s d = IMu KI KD (pair (tag s) d)

------------------------------------------------------------------------
-- 2. WELL-FORMEDNESS.
------------------------------------------------------------------------

module _ {Γ : Ctx} where

  OK-kbase : TelOK (Γ ▹ El KI) KI T-kbase
  OK-kbase = ok-ι

  OK-kU : TelOK (Γ ▹ El KI) KI T-kU
  OK-kU = ok-ι

  OK-kPi : TelOK (Γ ▹ El KI) KI T-kPi
  OK-kPi = ok-ρ (⊢ixₛ ⊢J lt-z (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J lt-z (⊢isuc (⊢dep (⊢var (here))))) (ok-ι))

  OK-kSg : TelOK (Γ ▹ El KI) KI T-kSg
  OK-kSg = ok-ρ (⊢ixₛ ⊢J lt-z (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J lt-z (⊢isuc (⊢dep (⊢var (here))))) (ok-ι))

  OK-kEl : TelOK (Γ ▹ El KI) KI T-kEl
  OK-kEl = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)

  OK-kHom : TelOK (Γ ▹ El KI) KI T-kHom
  OK-kHom = ok-ρ (⊢ixₛ ⊢J lt-z (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)))

  OK-kUnit : TelOK (Γ ▹ El KI) KI T-kUnit
  OK-kUnit = ok-ι

  OK-kNat : TelOK (Γ ▹ El KI) KI T-kNat
  OK-kNat = ok-ι

  OK-kId : TelOK (Γ ▹ El KI) KI T-kId
  OK-kId = ok-ρ (⊢ixₛ ⊢J lt-z (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)))

  OK-kIMu : TelOK (Γ ▹ El KI) KI T-kIMu
  OK-kIMu = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)))

  OK-kDesc : TelOK (Γ ▹ El KI) KI T-kDesc
  OK-kDesc = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)

  OK-kDIh : TelOK (Γ ▹ El KI) KI T-kDIh
  OK-kDIh = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J lt-z (⊢isuc (⊢isuc (⊢dep (⊢var (here)))))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι))))

  OK-kFin : TelOK (Γ ▹ El KI) KI T-kFin
  OK-kFin = ok-σ ⊢⌜Nat⌝ (ok-ι)

  OK-kvar : TelOK (Γ ▹ El KI) KI T-kvar
  OK-kvar = ok-σ (⊢⌜IMu⌝ ⊢⌜Nat⌝ ⊢FinD (⊢dep (⊢var (here)))) (ok-ι)

  OK-klam : TelOK (Γ ▹ El KI) KI T-klam
  OK-klam = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢isuc (⊢dep (⊢var (here))))) (ok-ι)

  OK-kapp : TelOK (Γ ▹ El KI) KI T-kapp
  OK-kapp = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι))

  OK-kpair : TelOK (Γ ▹ El KI) KI T-kpair
  OK-kpair = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι))

  OK-kabsurd : TelOK (Γ ▹ El KI) KI T-kabsurd
  OK-kabsurd = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι))

  OK-kordtr : TelOK (Γ ▹ El KI) KI T-kordtr
  OK-kordtr = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)))))

  OK-kfst : TelOK (Γ ▹ El KI) KI T-kfst
  OK-kfst = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)

  OK-ksnd : TelOK (Γ ▹ El KI) KI T-ksnd
  OK-ksnd = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)

  OK-kcbase : TelOK (Γ ▹ El KI) KI T-kcbase
  OK-kcbase = ok-ι

  OK-kcPi : TelOK (Γ ▹ El KI) KI T-kcPi
  OK-kcPi = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢isuc (⊢dep (⊢var (here))))) (ok-ι))

  OK-kcSg : TelOK (Γ ▹ El KI) KI T-kcSg
  OK-kcSg = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢isuc (⊢dep (⊢var (here))))) (ok-ι))

  OK-kcHom : TelOK (Γ ▹ El KI) KI T-kcHom
  OK-kcHom = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)))

  OK-khrefl : TelOK (Γ ▹ El KI) KI T-khrefl
  OK-khrefl = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι))

  OK-ktr : TelOK (Γ ▹ El KI) KI T-ktr
  OK-ktr = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢isuc (⊢dep (⊢var (here))))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)))

  OK-kap : TelOK (Γ ▹ El KI) KI T-kap
  OK-kap = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢isuc (⊢dep (⊢var (here))))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)))

  OK-kcId : TelOK (Γ ▹ El KI) KI T-kcId
  OK-kcId = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)))

  OK-kidrefl : TelOK (Γ ▹ El KI) KI T-kidrefl
  OK-kidrefl = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι))

  OK-kjsub : TelOK (Γ ▹ El KI) KI T-kjsub
  OK-kjsub = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢isuc (⊢dep (⊢var (here))))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)))

  OK-kunit : TelOK (Γ ▹ El KI) KI T-kunit
  OK-kunit = ok-ι

  OK-knzero : TelOK (Γ ▹ El KI) KI T-knzero
  OK-knzero = ok-ι

  OK-knsuc : TelOK (Γ ▹ El KI) KI T-knsuc
  OK-knsuc = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)

  OK-knatrec : TelOK (Γ ▹ El KI) KI T-knatrec
  OK-knatrec = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢isuc (⊢isuc (⊢dep (⊢var (here)))))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)))

  OK-kcon : TelOK (Γ ▹ El KI) KI T-kcon
  OK-kcon = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)

  OK-kielim : TelOK (Γ ▹ El KI) KI T-kielim
  OK-kielim = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι))))

  OK-kdI : TelOK (Γ ▹ El KI) KI T-kdI
  OK-kdI = ok-ι

  OK-kdS : TelOK (Γ ▹ El KI) KI T-kdS
  OK-kdS = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι))

  OK-kdR : TelOK (Γ ▹ El KI) KI T-kdR
  OK-kdR = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι))

  OK-kdpay : TelOK (Γ ▹ El KI) KI T-kdpay
  OK-kdpay = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)))

  OK-kdih : TelOK (Γ ▹ El KI) KI T-kdih
  OK-kdih = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι))))

  OK-kfzero : TelOK (Γ ▹ El KI) KI T-kfzero
  OK-kfzero = ok-ι

  OK-kfsuc : TelOK (Γ ▹ El KI) KI T-kfsuc
  OK-kfsuc = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)

  OK-kfcase : TelOK (Γ ▹ El KI) KI T-kfcase
  OK-kfcase = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢isuc (⊢dep (⊢var (here))))) (ok-ι)))

  OK-kfcase0 : TelOK (Γ ▹ El KI) KI T-kfcase0
  OK-kfcase0 = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)

  OK-kpsplit : TelOK (Γ ▹ El KI) KI T-kpsplit
  OK-kpsplit = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢isuc (⊢isuc (⊢dep (⊢var (here)))))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι))

  OK-kcNat : TelOK (Γ ▹ El KI) KI T-kcNat
  OK-kcNat = ok-ι

  OK-kcIMu : TelOK (Γ ▹ El KI) KI T-kcIMu
  OK-kcIMu = ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ρ (⊢ixₛ ⊢J (lt-s lt-z) (⊢dep (⊢var (here)))) (ok-ι)))

  OK-kcFin : TelOK (Γ ▹ El KI) KI T-kcFin
  OK-kcFin = ok-σ ⊢⌜Nat⌝ (ok-ι)

  OK-kcUnit : TelOK (Γ ▹ El KI) KI T-kcUnit
  OK-kcUnit = ok-ι

KOK : {Γ : Ctx} → AllSOK Γ KI KTss
KOK = (OK-kbase ∷ᵒ OK-kU ∷ᵒ OK-kPi ∷ᵒ OK-kSg ∷ᵒ OK-kEl ∷ᵒ OK-kHom ∷ᵒ OK-kUnit ∷ᵒ OK-kNat ∷ᵒ OK-kId ∷ᵒ OK-kIMu ∷ᵒ OK-kDesc ∷ᵒ OK-kDIh ∷ᵒ OK-kFin ∷ᵒ []ᵒ)
   ∷ˢᵒ (OK-kvar ∷ᵒ OK-klam ∷ᵒ OK-kapp ∷ᵒ OK-kpair ∷ᵒ OK-kabsurd ∷ᵒ OK-kordtr ∷ᵒ OK-kfst ∷ᵒ OK-ksnd ∷ᵒ OK-kcbase ∷ᵒ OK-kcPi ∷ᵒ OK-kcSg ∷ᵒ OK-kcHom ∷ᵒ OK-khrefl ∷ᵒ OK-ktr ∷ᵒ OK-kap ∷ᵒ OK-kcId ∷ᵒ OK-kidrefl ∷ᵒ OK-kjsub ∷ᵒ OK-kunit ∷ᵒ OK-knzero ∷ᵒ OK-knsuc ∷ᵒ OK-knatrec ∷ᵒ OK-kcon ∷ᵒ OK-kielim ∷ᵒ OK-kdI ∷ᵒ OK-kdS ∷ᵒ OK-kdR ∷ᵒ OK-kdpay ∷ᵒ OK-kdih ∷ᵒ OK-kfzero ∷ᵒ OK-kfsuc ∷ᵒ OK-kfcase ∷ᵒ OK-kfcase0 ∷ᵒ OK-kpsplit ∷ᵒ OK-kcNat ∷ᵒ OK-kcIMu ∷ᵒ OK-kcFin ∷ᵒ OK-kcUnit ∷ᵒ []ᵒ)
   ∷ˢᵒ []ˢᵒ

⊢KD : {Γ : Ctx} → Γ ⊢ KD ∷ DescF KI
⊢KD = ⊢Dₛₜ ⊢J KOK
