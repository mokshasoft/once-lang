------------------------------------------------------------------------
-- ⚠⚠ GENERATED BY `tools/gen-knot.py` — DO NOT EDIT BY HAND. ⚠⚠
--
-- Regenerate with:  python3 tools/gen-knot.py
--
-- 53 constructors over 7 sorts, one description, index `Σ' Nat Nat`.
-- The table, the encoding decisions and the two exceptions (`Var`'s
-- depth-Fording) are documented in the generator's header — read that,
-- not this file, to understand the encoding.
------------------------------------------------------------------------
-- ⚠ NO SPECIAL COLLECTOR — and the phrase `sweep.sh` greps for is
--   deliberately not written here.  Measured cold, this shape: 140s at
--   the default `-A64m`, 167s with `-c`, which is 19% SLOWER and buys
--   nothing.  An earlier shape did need it; PINNING the
--   tuple's types (the named `KnotTᵢ` tails below) removed the memory
--   pressure, and the marker outlived the problem.  ⇒ a "needs -c" note
--   is a claim about a SHAPE, not about a module: re-measure it when the
--   shape changes.
--
-- ★★★ `sz` OVER THE KNOT, BY `ielim`.
--
--     szTm i t = ielim KnotD i szMeths t          -- ∷ Nat
--
-- ★ THE MOTIVE IS CONSTANT (`Nat`), so `iinst i t Nat = Nat` and the IH
--   tuple is a nest of `Σ' Nat` — one entry per RECURSIVE field, since
--   `iihTy` skips κ fields outright.  A method returns `suc` of the sum of
--   its children's sizes; a leaf returns `suc 0`.
--
-- ★★ AND EVERY PAYLOAD TYPE HERE IS FREE.  `imethTy`'s first binder IS the
--   index (§9.1), so the ambient is `var vz` — a VARIABLE — and renaming and
--   substitution COMPUTE on it.  The transports the adequacy map needed
--   (`Knot/Build`) do not arise at all.
--
-- ⚠ NO KERNEL CHANGE AND NO METATHEORY OBLIGATION.  `⊢ielim` and
--   `ι-ielim` are existing kernel rules and subject reduction for the
--   latter is proved (`Metatheory/SubjectReduction.sr`, via `iihs-ty`).
--   This module is a CLIENT of them.

{-# OPTIONS --safe #-}
module DirectedHoTT.Examples.Knot.SzM where
open import Agda.Builtin.Nat using ( zero; suc ) renaming ( Nat to ℕ )
open import DirectedHoTT.Spec.Syntax
  using ( Cx; ε; _∙; vz; vs
        ; RTy; RTm; El; Unit; Nat; Σ'; IMu
        ; var; lam; pair; fst; snd; unit; nzero; nsuc; ⌜Nat⌝; ⌜Id⌝
        ; ielim; ICon; IDesc; isingle; inil; _◂_ )
open import DirectedHoTT.Spec.Typing
  using ( Ctx; ◇; _▹_; ⌊_⌋
        ; _⊢_∷_; _⊢ty_; ⊢var; here; there
        ; ⊢lam; ⊢pair; ⊢fst; ⊢snd; ⊢unit; ⊢nzero; ⊢nsuc; ⊢⌜Nat⌝; ⊢⌜Id⌝; ⊢ielim
        ; ty-El; ty-Unit; ty-Nat; ty-Σ; ty-Π; ty-IMu
        ; imethTy; imethsTy; imethsTyFrom )
open import DirectedHoTT.Lib.Nat using ( plusTm; ⊢plus )
open import DirectedHoTT.Examples.Knot.Sorts
  using ( IPair; sTy; sTm; sDesc; sDCon; sIDesc; sICon; sVar
        ; ⊢sTy; ⊢sTm; ⊢sDesc; ⊢sDCon; ⊢sIDesc; ⊢sICon; ⊢sVar
        ; toI; fromI; ⊢ixP )
open import normalizer.Syntax.Types using ( _≡_; sym )
open import DirectedHoTT.Metatheory.SubjectReduction
  using ( isingle-Sub⊢; ⊢-cast )
open import DirectedHoTT.Lib.Wk using ( wk-singleTy )
open import DirectedHoTT.Examples.Knot.Build using ( tyCast )
open import DirectedHoTT.Lib.IPay using ( ipayTy-wf )
open import DirectedHoTT.Examples.Knot.Desc
open import DirectedHoTT.Examples.Knot.Tags
open import DirectedHoTT.Examples.Knot.Wf


-- the IH tuple's type, by ARITY: one `Nat` per RECURSIVE field
-- (`iihTy` skips κ fields outright).
szIH0 : {Γ : Ctx} → Γ ⊢ty Unit
szIH0 = ty-Unit
szIH1 : {Γ : Ctx} → Γ ⊢ty Σ' Nat (Unit)
szIH1 = ty-Σ ty-Nat (ty-Unit)
szIH2 : {Γ : Ctx} → Γ ⊢ty Σ' Nat (Σ' Nat (Unit))
szIH2 = ty-Σ ty-Nat (ty-Σ ty-Nat (ty-Unit))
szIH3 : {Γ : Ctx} → Γ ⊢ty Σ' Nat (Σ' Nat (Σ' Nat (Unit)))
szIH3 = ty-Σ ty-Nat (ty-Σ ty-Nat (ty-Σ ty-Nat (ty-Unit)))
szIH4 : {Γ : Ctx} → Γ ⊢ty Σ' Nat (Σ' Nat (Σ' Nat (Σ' Nat (Unit))))
szIH4 = ty-Σ ty-Nat (ty-Σ ty-Nat (ty-Σ ty-Nat (ty-Σ ty-Nat (ty-Unit))))
szIH5 : {Γ : Ctx} → Γ ⊢ty Σ' Nat (Σ' Nat (Σ' Nat (Σ' Nat (Σ' Nat (Unit)))))
szIH5 = ty-Σ ty-Nat (ty-Σ ty-Nat (ty-Σ ty-Nat (ty-Σ ty-Nat (ty-Σ ty-Nat (ty-Unit)))))

-- base : RTy Γ
szΠTy-base : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTy-base cTy-base Nat
szΠTy-base = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-base
                     KnotWf cTy-baseWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH0 ty-Nat))
szMTy-base : {Γ : Cx} → RTm Γ
szMTy-base = lam (lam (lam (nsuc (nzero))))
⊢szMTy-base : {Γ : Ctx} → Γ ⊢ szMTy-base ∷ imethTy KnotD IPair tagTy-base cTy-base Nat
⊢szMTy-base =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-base
                     KnotWf cTy-baseWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH0 (⊢nsuc (⊢nzero))))

-- U : RTy Γ
szΠTy-U : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTy-U cTy-U Nat
szΠTy-U = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-U
                     KnotWf cTy-UWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH0 ty-Nat))
szMTy-U : {Γ : Cx} → RTm Γ
szMTy-U = lam (lam (lam (nsuc (nzero))))
⊢szMTy-U : {Γ : Ctx} → Γ ⊢ szMTy-U ∷ imethTy KnotD IPair tagTy-U cTy-U Nat
⊢szMTy-U =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-U
                     KnotWf cTy-UWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH0 (⊢nsuc (⊢nzero))))

-- Π : RTy Γ → RTy (Γ ∙) → RTy Γ
szΠTy-Pi : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTy-Pi cTy-Pi Nat
szΠTy-Pi = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-Pi
                     KnotWf cTy-PiWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH2 ty-Nat))
szMTy-Pi : {Γ : Cx} → RTm Γ
szMTy-Pi = lam (lam (lam (nsuc (plusTm (fst (var vz)) (fst (snd (var vz)))))))
⊢szMTy-Pi : {Γ : Ctx} → Γ ⊢ szMTy-Pi ∷ imethTy KnotD IPair tagTy-Pi cTy-Pi Nat
⊢szMTy-Pi =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-Pi
                     KnotWf cTy-PiWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH2 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢fst (⊢snd (⊢var here)))))))

-- Σ' : RTy Γ → RTy (Γ ∙) → RTy Γ
szΠTy-Sg : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTy-Sg cTy-Sg Nat
szΠTy-Sg = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-Sg
                     KnotWf cTy-SgWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH2 ty-Nat))
szMTy-Sg : {Γ : Cx} → RTm Γ
szMTy-Sg = lam (lam (lam (nsuc (plusTm (fst (var vz)) (fst (snd (var vz)))))))
⊢szMTy-Sg : {Γ : Ctx} → Γ ⊢ szMTy-Sg ∷ imethTy KnotD IPair tagTy-Sg cTy-Sg Nat
⊢szMTy-Sg =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-Sg
                     KnotWf cTy-SgWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH2 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢fst (⊢snd (⊢var here)))))))

-- El : RTm Γ → RTy Γ
szΠTy-El : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTy-El cTy-El Nat
szΠTy-El = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-El
                     KnotWf cTy-ElWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH1 ty-Nat))
szMTy-El : {Γ : Cx} → RTm Γ
szMTy-El = lam (lam (lam (nsuc (fst (var vz)))))
⊢szMTy-El : {Γ : Ctx} → Γ ⊢ szMTy-El ∷ imethTy KnotD IPair tagTy-El cTy-El Nat
⊢szMTy-El =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-El
                     KnotWf cTy-ElWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH1 (⊢nsuc (⊢fst (⊢var here)))))

-- Hom : RTy Γ → RTm Γ → RTm Γ → RTy Γ
szΠTy-Hom : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTy-Hom cTy-Hom Nat
szΠTy-Hom = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-Hom
                     KnotWf cTy-HomWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH3 ty-Nat))
szMTy-Hom : {Γ : Cx} → RTm Γ
szMTy-Hom = lam (lam (lam (nsuc (plusTm (fst (var vz)) (plusTm (fst (snd (var vz))) (fst (snd (snd (var vz)))))))))
⊢szMTy-Hom : {Γ : Ctx} → Γ ⊢ szMTy-Hom ∷ imethTy KnotD IPair tagTy-Hom cTy-Hom Nat
⊢szMTy-Hom =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-Hom
                     KnotWf cTy-HomWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH3 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢plus (⊢fst (⊢snd (⊢var here))) (⊢fst (⊢snd (⊢snd (⊢var here)))))))))

-- Unit : RTy Γ
szΠTy-Unit : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTy-Unit cTy-Unit Nat
szΠTy-Unit = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-Unit
                     KnotWf cTy-UnitWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH0 ty-Nat))
szMTy-Unit : {Γ : Cx} → RTm Γ
szMTy-Unit = lam (lam (lam (nsuc (nzero))))
⊢szMTy-Unit : {Γ : Ctx} → Γ ⊢ szMTy-Unit ∷ imethTy KnotD IPair tagTy-Unit cTy-Unit Nat
⊢szMTy-Unit =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-Unit
                     KnotWf cTy-UnitWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH0 (⊢nsuc (⊢nzero))))

-- Nat : RTy Γ
szΠTy-Nat : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTy-Nat cTy-Nat Nat
szΠTy-Nat = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-Nat
                     KnotWf cTy-NatWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH0 ty-Nat))
szMTy-Nat : {Γ : Cx} → RTm Γ
szMTy-Nat = lam (lam (lam (nsuc (nzero))))
⊢szMTy-Nat : {Γ : Ctx} → Γ ⊢ szMTy-Nat ∷ imethTy KnotD IPair tagTy-Nat cTy-Nat Nat
⊢szMTy-Nat =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-Nat
                     KnotWf cTy-NatWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH0 (⊢nsuc (⊢nzero))))

-- Id : RTy Γ → RTm Γ → RTm Γ → RTy Γ
szΠTy-Id : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTy-Id cTy-Id Nat
szΠTy-Id = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-Id
                     KnotWf cTy-IdWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH3 ty-Nat))
szMTy-Id : {Γ : Cx} → RTm Γ
szMTy-Id = lam (lam (lam (nsuc (plusTm (fst (var vz)) (plusTm (fst (snd (var vz))) (fst (snd (snd (var vz)))))))))
⊢szMTy-Id : {Γ : Ctx} → Γ ⊢ szMTy-Id ∷ imethTy KnotD IPair tagTy-Id cTy-Id Nat
⊢szMTy-Id =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-Id
                     KnotWf cTy-IdWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH3 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢plus (⊢fst (⊢snd (⊢var here))) (⊢fst (⊢snd (⊢snd (⊢var here)))))))))

-- Mu : Desc → RTy Γ
szΠTy-Mu : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTy-Mu cTy-Mu Nat
szΠTy-Mu = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-Mu
                     KnotWf cTy-MuWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH1 ty-Nat))
szMTy-Mu : {Γ : Cx} → RTm Γ
szMTy-Mu = lam (lam (lam (nsuc (fst (var vz)))))
⊢szMTy-Mu : {Γ : Ctx} → Γ ⊢ szMTy-Mu ∷ imethTy KnotD IPair tagTy-Mu cTy-Mu Nat
⊢szMTy-Mu =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-Mu
                     KnotWf cTy-MuWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH1 (⊢nsuc (⊢fst (⊢var here)))))

-- IMu : IDesc → RTy ε → RTm Γ → RTy Γ
szΠTy-IMu : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTy-IMu cTy-IMu Nat
szΠTy-IMu = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-IMu
                     KnotWf cTy-IMuWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH3 ty-Nat))
szMTy-IMu : {Γ : Cx} → RTm Γ
szMTy-IMu = lam (lam (lam (nsuc (plusTm (fst (var vz)) (plusTm (fst (snd (var vz))) (fst (snd (snd (var vz)))))))))
⊢szMTy-IMu : {Γ : Ctx} → Γ ⊢ szMTy-IMu ∷ imethTy KnotD IPair tagTy-IMu cTy-IMu Nat
⊢szMTy-IMu =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTy-IMu
                     KnotWf cTy-IMuWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH3 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢plus (⊢fst (⊢snd (⊢var here))) (⊢fst (⊢snd (⊢snd (⊢var here)))))))))

-- var : Var Γ → RTm Γ
szΠTm-var : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-var cTm-var Nat
szΠTm-var = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-var
                     KnotWf cTm-varWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH1 ty-Nat))
szMTm-var : {Γ : Cx} → RTm Γ
szMTm-var = lam (lam (lam (nsuc (fst (var vz)))))
⊢szMTm-var : {Γ : Ctx} → Γ ⊢ szMTm-var ∷ imethTy KnotD IPair tagTm-var cTm-var Nat
⊢szMTm-var =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-var
                     KnotWf cTm-varWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH1 (⊢nsuc (⊢fst (⊢var here)))))

-- lam : RTm (Γ ∙) → RTm Γ
szΠTm-lam : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-lam cTm-lam Nat
szΠTm-lam = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-lam
                     KnotWf cTm-lamWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH1 ty-Nat))
szMTm-lam : {Γ : Cx} → RTm Γ
szMTm-lam = lam (lam (lam (nsuc (fst (var vz)))))
⊢szMTm-lam : {Γ : Ctx} → Γ ⊢ szMTm-lam ∷ imethTy KnotD IPair tagTm-lam cTm-lam Nat
⊢szMTm-lam =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-lam
                     KnotWf cTm-lamWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH1 (⊢nsuc (⊢fst (⊢var here)))))

-- app : RTm Γ → RTm Γ → RTm Γ
szΠTm-app : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-app cTm-app Nat
szΠTm-app = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-app
                     KnotWf cTm-appWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH2 ty-Nat))
szMTm-app : {Γ : Cx} → RTm Γ
szMTm-app = lam (lam (lam (nsuc (plusTm (fst (var vz)) (fst (snd (var vz)))))))
⊢szMTm-app : {Γ : Ctx} → Γ ⊢ szMTm-app ∷ imethTy KnotD IPair tagTm-app cTm-app Nat
⊢szMTm-app =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-app
                     KnotWf cTm-appWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH2 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢fst (⊢snd (⊢var here)))))))

-- pair : RTm Γ → RTm Γ → RTm Γ
szΠTm-pair : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-pair cTm-pair Nat
szΠTm-pair = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-pair
                     KnotWf cTm-pairWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH2 ty-Nat))
szMTm-pair : {Γ : Cx} → RTm Γ
szMTm-pair = lam (lam (lam (nsuc (plusTm (fst (var vz)) (fst (snd (var vz)))))))
⊢szMTm-pair : {Γ : Ctx} → Γ ⊢ szMTm-pair ∷ imethTy KnotD IPair tagTm-pair cTm-pair Nat
⊢szMTm-pair =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-pair
                     KnotWf cTm-pairWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH2 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢fst (⊢snd (⊢var here)))))))

-- absurd : RTm Γ → RTm Γ → RTm Γ
szΠTm-absurd : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-absurd cTm-absurd Nat
szΠTm-absurd = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-absurd
                     KnotWf cTm-absurdWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH2 ty-Nat))
szMTm-absurd : {Γ : Cx} → RTm Γ
szMTm-absurd = lam (lam (lam (nsuc (plusTm (fst (var vz)) (fst (snd (var vz)))))))
⊢szMTm-absurd : {Γ : Ctx} → Γ ⊢ szMTm-absurd ∷ imethTy KnotD IPair tagTm-absurd cTm-absurd Nat
⊢szMTm-absurd =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-absurd
                     KnotWf cTm-absurdWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH2 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢fst (⊢snd (⊢var here)))))))

-- ordtr : (5 × RTm Γ) → RTm Γ
szΠTm-ordtr : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-ordtr cTm-ordtr Nat
szΠTm-ordtr = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-ordtr
                     KnotWf cTm-ordtrWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH5 ty-Nat))
szMTm-ordtr : {Γ : Cx} → RTm Γ
szMTm-ordtr = lam (lam (lam (nsuc (plusTm (fst (var vz)) (plusTm (fst (snd (var vz))) (plusTm (fst (snd (snd (var vz)))) (plusTm (fst (snd (snd (snd (var vz))))) (fst (snd (snd (snd (snd (var vz)))))))))))))
⊢szMTm-ordtr : {Γ : Ctx} → Γ ⊢ szMTm-ordtr ∷ imethTy KnotD IPair tagTm-ordtr cTm-ordtr Nat
⊢szMTm-ordtr =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-ordtr
                     KnotWf cTm-ordtrWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH5 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢plus (⊢fst (⊢snd (⊢var here))) (⊢plus (⊢fst (⊢snd (⊢snd (⊢var here)))) (⊢plus (⊢fst (⊢snd (⊢snd (⊢snd (⊢var here))))) (⊢fst (⊢snd (⊢snd (⊢snd (⊢snd (⊢var here)))))))))))))

-- fst : RTm Γ → RTm Γ
szΠTm-fst : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-fst cTm-fst Nat
szΠTm-fst = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-fst
                     KnotWf cTm-fstWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH1 ty-Nat))
szMTm-fst : {Γ : Cx} → RTm Γ
szMTm-fst = lam (lam (lam (nsuc (fst (var vz)))))
⊢szMTm-fst : {Γ : Ctx} → Γ ⊢ szMTm-fst ∷ imethTy KnotD IPair tagTm-fst cTm-fst Nat
⊢szMTm-fst =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-fst
                     KnotWf cTm-fstWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH1 (⊢nsuc (⊢fst (⊢var here)))))

-- snd : RTm Γ → RTm Γ
szΠTm-snd : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-snd cTm-snd Nat
szΠTm-snd = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-snd
                     KnotWf cTm-sndWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH1 ty-Nat))
szMTm-snd : {Γ : Cx} → RTm Γ
szMTm-snd = lam (lam (lam (nsuc (fst (var vz)))))
⊢szMTm-snd : {Γ : Ctx} → Γ ⊢ szMTm-snd ∷ imethTy KnotD IPair tagTm-snd cTm-snd Nat
⊢szMTm-snd =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-snd
                     KnotWf cTm-sndWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH1 (⊢nsuc (⊢fst (⊢var here)))))

-- ⌜base⌝ : RTm Γ
szΠTm-cbase : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-cbase cTm-cbase Nat
szΠTm-cbase = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-cbase
                     KnotWf cTm-cbaseWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH0 ty-Nat))
szMTm-cbase : {Γ : Cx} → RTm Γ
szMTm-cbase = lam (lam (lam (nsuc (nzero))))
⊢szMTm-cbase : {Γ : Ctx} → Γ ⊢ szMTm-cbase ∷ imethTy KnotD IPair tagTm-cbase cTm-cbase Nat
⊢szMTm-cbase =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-cbase
                     KnotWf cTm-cbaseWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH0 (⊢nsuc (⊢nzero))))

-- ⌜Π⌝ : RTm Γ → RTm (Γ ∙) → RTm Γ
szΠTm-cPi : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-cPi cTm-cPi Nat
szΠTm-cPi = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-cPi
                     KnotWf cTm-cPiWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH2 ty-Nat))
szMTm-cPi : {Γ : Cx} → RTm Γ
szMTm-cPi = lam (lam (lam (nsuc (plusTm (fst (var vz)) (fst (snd (var vz)))))))
⊢szMTm-cPi : {Γ : Ctx} → Γ ⊢ szMTm-cPi ∷ imethTy KnotD IPair tagTm-cPi cTm-cPi Nat
⊢szMTm-cPi =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-cPi
                     KnotWf cTm-cPiWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH2 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢fst (⊢snd (⊢var here)))))))

-- ⌜Σ⌝ : RTm Γ → RTm (Γ ∙) → RTm Γ
szΠTm-cSg : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-cSg cTm-cSg Nat
szΠTm-cSg = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-cSg
                     KnotWf cTm-cSgWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH2 ty-Nat))
szMTm-cSg : {Γ : Cx} → RTm Γ
szMTm-cSg = lam (lam (lam (nsuc (plusTm (fst (var vz)) (fst (snd (var vz)))))))
⊢szMTm-cSg : {Γ : Ctx} → Γ ⊢ szMTm-cSg ∷ imethTy KnotD IPair tagTm-cSg cTm-cSg Nat
⊢szMTm-cSg =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-cSg
                     KnotWf cTm-cSgWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH2 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢fst (⊢snd (⊢var here)))))))

-- ⌜Hom⌝ : (3 × RTm Γ) → RTm Γ
szΠTm-cHom : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-cHom cTm-cHom Nat
szΠTm-cHom = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-cHom
                     KnotWf cTm-cHomWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH3 ty-Nat))
szMTm-cHom : {Γ : Cx} → RTm Γ
szMTm-cHom = lam (lam (lam (nsuc (plusTm (fst (var vz)) (plusTm (fst (snd (var vz))) (fst (snd (snd (var vz)))))))))
⊢szMTm-cHom : {Γ : Ctx} → Γ ⊢ szMTm-cHom ∷ imethTy KnotD IPair tagTm-cHom cTm-cHom Nat
⊢szMTm-cHom =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-cHom
                     KnotWf cTm-cHomWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH3 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢plus (⊢fst (⊢snd (⊢var here))) (⊢fst (⊢snd (⊢snd (⊢var here)))))))))

-- hrefl : RTm Γ → RTm Γ → RTm Γ
szΠTm-hrefl : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-hrefl cTm-hrefl Nat
szΠTm-hrefl = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-hrefl
                     KnotWf cTm-hreflWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH2 ty-Nat))
szMTm-hrefl : {Γ : Cx} → RTm Γ
szMTm-hrefl = lam (lam (lam (nsuc (plusTm (fst (var vz)) (fst (snd (var vz)))))))
⊢szMTm-hrefl : {Γ : Ctx} → Γ ⊢ szMTm-hrefl ∷ imethTy KnotD IPair tagTm-hrefl cTm-hrefl Nat
⊢szMTm-hrefl =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-hrefl
                     KnotWf cTm-hreflWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH2 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢fst (⊢snd (⊢var here)))))))

-- tr : RTm (Γ ∙) → RTm Γ → RTm Γ → RTm Γ
szΠTm-tr : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-tr cTm-tr Nat
szΠTm-tr = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-tr
                     KnotWf cTm-trWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH3 ty-Nat))
szMTm-tr : {Γ : Cx} → RTm Γ
szMTm-tr = lam (lam (lam (nsuc (plusTm (fst (var vz)) (plusTm (fst (snd (var vz))) (fst (snd (snd (var vz)))))))))
⊢szMTm-tr : {Γ : Ctx} → Γ ⊢ szMTm-tr ∷ imethTy KnotD IPair tagTm-tr cTm-tr Nat
⊢szMTm-tr =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-tr
                     KnotWf cTm-trWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH3 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢plus (⊢fst (⊢snd (⊢var here))) (⊢fst (⊢snd (⊢snd (⊢var here)))))))))

-- ap : RTm Γ → RTm (Γ ∙) → RTm Γ → RTm Γ
szΠTm-ap : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-ap cTm-ap Nat
szΠTm-ap = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-ap
                     KnotWf cTm-apWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH3 ty-Nat))
szMTm-ap : {Γ : Cx} → RTm Γ
szMTm-ap = lam (lam (lam (nsuc (plusTm (fst (var vz)) (plusTm (fst (snd (var vz))) (fst (snd (snd (var vz)))))))))
⊢szMTm-ap : {Γ : Ctx} → Γ ⊢ szMTm-ap ∷ imethTy KnotD IPair tagTm-ap cTm-ap Nat
⊢szMTm-ap =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-ap
                     KnotWf cTm-apWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH3 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢plus (⊢fst (⊢snd (⊢var here))) (⊢fst (⊢snd (⊢snd (⊢var here)))))))))

-- ⌜Id⌝ : (3 × RTm Γ) → RTm Γ
szΠTm-cId : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-cId cTm-cId Nat
szΠTm-cId = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-cId
                     KnotWf cTm-cIdWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH3 ty-Nat))
szMTm-cId : {Γ : Cx} → RTm Γ
szMTm-cId = lam (lam (lam (nsuc (plusTm (fst (var vz)) (plusTm (fst (snd (var vz))) (fst (snd (snd (var vz)))))))))
⊢szMTm-cId : {Γ : Ctx} → Γ ⊢ szMTm-cId ∷ imethTy KnotD IPair tagTm-cId cTm-cId Nat
⊢szMTm-cId =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-cId
                     KnotWf cTm-cIdWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH3 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢plus (⊢fst (⊢snd (⊢var here))) (⊢fst (⊢snd (⊢snd (⊢var here)))))))))

-- idrefl : RTm Γ → RTm Γ → RTm Γ
szΠTm-idrefl : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-idrefl cTm-idrefl Nat
szΠTm-idrefl = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-idrefl
                     KnotWf cTm-idreflWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH2 ty-Nat))
szMTm-idrefl : {Γ : Cx} → RTm Γ
szMTm-idrefl = lam (lam (lam (nsuc (plusTm (fst (var vz)) (fst (snd (var vz)))))))
⊢szMTm-idrefl : {Γ : Ctx} → Γ ⊢ szMTm-idrefl ∷ imethTy KnotD IPair tagTm-idrefl cTm-idrefl Nat
⊢szMTm-idrefl =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-idrefl
                     KnotWf cTm-idreflWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH2 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢fst (⊢snd (⊢var here)))))))

-- jsub : RTm (Γ ∙) → RTm Γ → RTm Γ → RTm Γ
szΠTm-jsub : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-jsub cTm-jsub Nat
szΠTm-jsub = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-jsub
                     KnotWf cTm-jsubWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH3 ty-Nat))
szMTm-jsub : {Γ : Cx} → RTm Γ
szMTm-jsub = lam (lam (lam (nsuc (plusTm (fst (var vz)) (plusTm (fst (snd (var vz))) (fst (snd (snd (var vz)))))))))
⊢szMTm-jsub : {Γ : Ctx} → Γ ⊢ szMTm-jsub ∷ imethTy KnotD IPair tagTm-jsub cTm-jsub Nat
⊢szMTm-jsub =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-jsub
                     KnotWf cTm-jsubWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH3 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢plus (⊢fst (⊢snd (⊢var here))) (⊢fst (⊢snd (⊢snd (⊢var here)))))))))

-- unit : RTm Γ
szΠTm-unit : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-unit cTm-unit Nat
szΠTm-unit = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-unit
                     KnotWf cTm-unitWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH0 ty-Nat))
szMTm-unit : {Γ : Cx} → RTm Γ
szMTm-unit = lam (lam (lam (nsuc (nzero))))
⊢szMTm-unit : {Γ : Ctx} → Γ ⊢ szMTm-unit ∷ imethTy KnotD IPair tagTm-unit cTm-unit Nat
⊢szMTm-unit =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-unit
                     KnotWf cTm-unitWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH0 (⊢nsuc (⊢nzero))))

-- nzero : RTm Γ
szΠTm-nzero : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-nzero cTm-nzero Nat
szΠTm-nzero = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-nzero
                     KnotWf cTm-nzeroWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH0 ty-Nat))
szMTm-nzero : {Γ : Cx} → RTm Γ
szMTm-nzero = lam (lam (lam (nsuc (nzero))))
⊢szMTm-nzero : {Γ : Ctx} → Γ ⊢ szMTm-nzero ∷ imethTy KnotD IPair tagTm-nzero cTm-nzero Nat
⊢szMTm-nzero =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-nzero
                     KnotWf cTm-nzeroWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH0 (⊢nsuc (⊢nzero))))

-- nsuc : RTm Γ → RTm Γ
szΠTm-nsuc : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-nsuc cTm-nsuc Nat
szΠTm-nsuc = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-nsuc
                     KnotWf cTm-nsucWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH1 ty-Nat))
szMTm-nsuc : {Γ : Cx} → RTm Γ
szMTm-nsuc = lam (lam (lam (nsuc (fst (var vz)))))
⊢szMTm-nsuc : {Γ : Ctx} → Γ ⊢ szMTm-nsuc ∷ imethTy KnotD IPair tagTm-nsuc cTm-nsuc Nat
⊢szMTm-nsuc =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-nsuc
                     KnotWf cTm-nsucWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH1 (⊢nsuc (⊢fst (⊢var here)))))

-- natrec : RTm Γ → RTm ((Γ ∙) ∙) → RTm Γ → RTm Γ
szΠTm-natrec : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-natrec cTm-natrec Nat
szΠTm-natrec = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-natrec
                     KnotWf cTm-natrecWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH3 ty-Nat))
szMTm-natrec : {Γ : Cx} → RTm Γ
szMTm-natrec = lam (lam (lam (nsuc (plusTm (fst (var vz)) (plusTm (fst (snd (var vz))) (fst (snd (snd (var vz)))))))))
⊢szMTm-natrec : {Γ : Ctx} → Γ ⊢ szMTm-natrec ∷ imethTy KnotD IPair tagTm-natrec cTm-natrec Nat
⊢szMTm-natrec =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-natrec
                     KnotWf cTm-natrecWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH3 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢plus (⊢fst (⊢snd (⊢var here))) (⊢fst (⊢snd (⊢snd (⊢var here)))))))))

-- con : ℕ → RTm Γ → RTm Γ
szΠTm-con : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-con cTm-con Nat
szΠTm-con = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-con
                     KnotWf cTm-conWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH1 ty-Nat))
szMTm-con : {Γ : Cx} → RTm Γ
szMTm-con = lam (lam (lam (nsuc (fst (var vz)))))
⊢szMTm-con : {Γ : Ctx} → Γ ⊢ szMTm-con ∷ imethTy KnotD IPair tagTm-con cTm-con Nat
⊢szMTm-con =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-con
                     KnotWf cTm-conWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH1 (⊢nsuc (⊢fst (⊢var here)))))

-- elim : Desc → RTm Γ → RTm Γ → RTm Γ
szΠTm-elim : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-elim cTm-elim Nat
szΠTm-elim = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-elim
                     KnotWf cTm-elimWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH3 ty-Nat))
szMTm-elim : {Γ : Cx} → RTm Γ
szMTm-elim = lam (lam (lam (nsuc (plusTm (fst (var vz)) (plusTm (fst (snd (var vz))) (fst (snd (snd (var vz)))))))))
⊢szMTm-elim : {Γ : Ctx} → Γ ⊢ szMTm-elim ∷ imethTy KnotD IPair tagTm-elim cTm-elim Nat
⊢szMTm-elim =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-elim
                     KnotWf cTm-elimWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH3 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢plus (⊢fst (⊢snd (⊢var here))) (⊢fst (⊢snd (⊢snd (⊢var here)))))))))

-- icon : ℕ → RTm Γ → RTm Γ
szΠTm-icon : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-icon cTm-icon Nat
szΠTm-icon = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-icon
                     KnotWf cTm-iconWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH1 ty-Nat))
szMTm-icon : {Γ : Cx} → RTm Γ
szMTm-icon = lam (lam (lam (nsuc (fst (var vz)))))
⊢szMTm-icon : {Γ : Ctx} → Γ ⊢ szMTm-icon ∷ imethTy KnotD IPair tagTm-icon cTm-icon Nat
⊢szMTm-icon =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-icon
                     KnotWf cTm-iconWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH1 (⊢nsuc (⊢fst (⊢var here)))))

-- ielim : IDesc → RTm Γ → RTm Γ → RTm Γ → RTm Γ
szΠTm-ielim : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-ielim cTm-ielim Nat
szΠTm-ielim = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-ielim
                     KnotWf cTm-ielimWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH4 ty-Nat))
szMTm-ielim : {Γ : Cx} → RTm Γ
szMTm-ielim = lam (lam (lam (nsuc (plusTm (fst (var vz)) (plusTm (fst (snd (var vz))) (plusTm (fst (snd (snd (var vz)))) (fst (snd (snd (snd (var vz)))))))))))
⊢szMTm-ielim : {Γ : Ctx} → Γ ⊢ szMTm-ielim ∷ imethTy KnotD IPair tagTm-ielim cTm-ielim Nat
⊢szMTm-ielim =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-ielim
                     KnotWf cTm-ielimWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH4 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢plus (⊢fst (⊢snd (⊢var here))) (⊢plus (⊢fst (⊢snd (⊢snd (⊢var here)))) (⊢fst (⊢snd (⊢snd (⊢snd (⊢var here)))))))))))

-- ⌜Nat⌝ : RTm Γ
szΠTm-cNat : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-cNat cTm-cNat Nat
szΠTm-cNat = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-cNat
                     KnotWf cTm-cNatWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH0 ty-Nat))
szMTm-cNat : {Γ : Cx} → RTm Γ
szMTm-cNat = lam (lam (lam (nsuc (nzero))))
⊢szMTm-cNat : {Γ : Ctx} → Γ ⊢ szMTm-cNat ∷ imethTy KnotD IPair tagTm-cNat cTm-cNat Nat
⊢szMTm-cNat =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-cNat
                     KnotWf cTm-cNatWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH0 (⊢nsuc (⊢nzero))))

-- ⌜Mu⌝ : Desc → RTm Γ
szΠTm-cMu : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-cMu cTm-cMu Nat
szΠTm-cMu = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-cMu
                     KnotWf cTm-cMuWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH1 ty-Nat))
szMTm-cMu : {Γ : Cx} → RTm Γ
szMTm-cMu = lam (lam (lam (nsuc (fst (var vz)))))
⊢szMTm-cMu : {Γ : Ctx} → Γ ⊢ szMTm-cMu ∷ imethTy KnotD IPair tagTm-cMu cTm-cMu Nat
⊢szMTm-cMu =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-cMu
                     KnotWf cTm-cMuWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH1 (⊢nsuc (⊢fst (⊢var here)))))

-- ⌜IMu⌝ : IDesc → RTy ε → RTm Γ → RTm Γ
szΠTm-cIMu : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-cIMu cTm-cIMu Nat
szΠTm-cIMu = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-cIMu
                     KnotWf cTm-cIMuWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH3 ty-Nat))
szMTm-cIMu : {Γ : Cx} → RTm Γ
szMTm-cIMu = lam (lam (lam (nsuc (plusTm (fst (var vz)) (plusTm (fst (snd (var vz))) (fst (snd (snd (var vz)))))))))
⊢szMTm-cIMu : {Γ : Ctx} → Γ ⊢ szMTm-cIMu ∷ imethTy KnotD IPair tagTm-cIMu cTm-cIMu Nat
⊢szMTm-cIMu =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-cIMu
                     KnotWf cTm-cIMuWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH3 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢plus (⊢fst (⊢snd (⊢var here))) (⊢fst (⊢snd (⊢snd (⊢var here)))))))))

-- ⌜Unit⌝ : RTm Γ
szΠTm-cUnit : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagTm-cUnit cTm-cUnit Nat
szΠTm-cUnit = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-cUnit
                     KnotWf cTm-cUnitWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH0 ty-Nat))
szMTm-cUnit : {Γ : Cx} → RTm Γ
szMTm-cUnit = lam (lam (lam (nsuc (nzero))))
⊢szMTm-cUnit : {Γ : Ctx} → Γ ⊢ szMTm-cUnit ∷ imethTy KnotD IPair tagTm-cUnit cTm-cUnit Nat
⊢szMTm-cUnit =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cTm-cUnit
                     KnotWf cTm-cUnitWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH0 (⊢nsuc (⊢nzero))))

-- dnil : Desc
szΠDesc-nil : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagDesc-nil cDesc-nil Nat
szΠDesc-nil = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cDesc-nil
                     KnotWf cDesc-nilWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH0 ty-Nat))
szMDesc-nil : {Γ : Cx} → RTm Γ
szMDesc-nil = lam (lam (lam (nsuc (nzero))))
⊢szMDesc-nil : {Γ : Ctx} → Γ ⊢ szMDesc-nil ∷ imethTy KnotD IPair tagDesc-nil cDesc-nil Nat
⊢szMDesc-nil =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cDesc-nil
                     KnotWf cDesc-nilWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH0 (⊢nsuc (⊢nzero))))

-- _◃_ : DCon → Desc → Desc
szΠDesc-cons : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagDesc-cons cDesc-cons Nat
szΠDesc-cons = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cDesc-cons
                     KnotWf cDesc-consWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH2 ty-Nat))
szMDesc-cons : {Γ : Cx} → RTm Γ
szMDesc-cons = lam (lam (lam (nsuc (plusTm (fst (var vz)) (fst (snd (var vz)))))))
⊢szMDesc-cons : {Γ : Ctx} → Γ ⊢ szMDesc-cons ∷ imethTy KnotD IPair tagDesc-cons cDesc-cons Nat
⊢szMDesc-cons =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cDesc-cons
                     KnotWf cDesc-consWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH2 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢fst (⊢snd (⊢var here)))))))

-- dι : DCon
szΠDCon-i : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagDCon-i cDCon-i Nat
szΠDCon-i = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cDCon-i
                     KnotWf cDCon-iWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH0 ty-Nat))
szMDCon-i : {Γ : Cx} → RTm Γ
szMDCon-i = lam (lam (lam (nsuc (nzero))))
⊢szMDCon-i : {Γ : Ctx} → Γ ⊢ szMDCon-i ∷ imethTy KnotD IPair tagDCon-i cDCon-i Nat
⊢szMDCon-i =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cDCon-i
                     KnotWf cDCon-iWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH0 (⊢nsuc (⊢nzero))))

-- dρ : DCon → DCon
szΠDCon-rho : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagDCon-rho cDCon-rho Nat
szΠDCon-rho = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cDCon-rho
                     KnotWf cDCon-rhoWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH1 ty-Nat))
szMDCon-rho : {Γ : Cx} → RTm Γ
szMDCon-rho = lam (lam (lam (nsuc (fst (var vz)))))
⊢szMDCon-rho : {Γ : Ctx} → Γ ⊢ szMDCon-rho ∷ imethTy KnotD IPair tagDCon-rho cDCon-rho Nat
⊢szMDCon-rho =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cDCon-rho
                     KnotWf cDCon-rhoWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH1 (⊢nsuc (⊢fst (⊢var here)))))

-- dκ : RTy ε → DCon → DCon
szΠDCon-kap : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagDCon-kap cDCon-kap Nat
szΠDCon-kap = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cDCon-kap
                     KnotWf cDCon-kapWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH2 ty-Nat))
szMDCon-kap : {Γ : Cx} → RTm Γ
szMDCon-kap = lam (lam (lam (nsuc (plusTm (fst (var vz)) (fst (snd (var vz)))))))
⊢szMDCon-kap : {Γ : Ctx} → Γ ⊢ szMDCon-kap ∷ imethTy KnotD IPair tagDCon-kap cDCon-kap Nat
⊢szMDCon-kap =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cDCon-kap
                     KnotWf cDCon-kapWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH2 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢fst (⊢snd (⊢var here)))))))

-- inil : IDesc
szΠIDesc-nil : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagIDesc-nil cIDesc-nil Nat
szΠIDesc-nil = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cIDesc-nil
                     KnotWf cIDesc-nilWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH0 ty-Nat))
szMIDesc-nil : {Γ : Cx} → RTm Γ
szMIDesc-nil = lam (lam (lam (nsuc (nzero))))
⊢szMIDesc-nil : {Γ : Ctx} → Γ ⊢ szMIDesc-nil ∷ imethTy KnotD IPair tagIDesc-nil cIDesc-nil Nat
⊢szMIDesc-nil =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cIDesc-nil
                     KnotWf cIDesc-nilWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH0 (⊢nsuc (⊢nzero))))

-- _◂_ : ICon (ε ∙) → IDesc → IDesc
szΠIDesc-cons : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagIDesc-cons cIDesc-cons Nat
szΠIDesc-cons = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cIDesc-cons
                     KnotWf cIDesc-consWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH2 ty-Nat))
szMIDesc-cons : {Γ : Cx} → RTm Γ
szMIDesc-cons = lam (lam (lam (nsuc (plusTm (fst (var vz)) (fst (snd (var vz)))))))
⊢szMIDesc-cons : {Γ : Ctx} → Γ ⊢ szMIDesc-cons ∷ imethTy KnotD IPair tagIDesc-cons cIDesc-cons Nat
⊢szMIDesc-cons =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cIDesc-cons
                     KnotWf cIDesc-consWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH2 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢fst (⊢snd (⊢var here)))))))

-- iι : ICon Δ
szΠICon-i : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagICon-i cICon-i Nat
szΠICon-i = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cICon-i
                     KnotWf cICon-iWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH0 ty-Nat))
szMICon-i : {Γ : Cx} → RTm Γ
szMICon-i = lam (lam (lam (nsuc (nzero))))
⊢szMICon-i : {Γ : Ctx} → Γ ⊢ szMICon-i ∷ imethTy KnotD IPair tagICon-i cICon-i Nat
⊢szMICon-i =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cICon-i
                     KnotWf cICon-iWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH0 (⊢nsuc (⊢nzero))))

-- iρ : RTm Δ → ICon (Δ ∙) → ICon Δ
szΠICon-rho : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagICon-rho cICon-rho Nat
szΠICon-rho = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cICon-rho
                     KnotWf cICon-rhoWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH2 ty-Nat))
szMICon-rho : {Γ : Cx} → RTm Γ
szMICon-rho = lam (lam (lam (nsuc (plusTm (fst (var vz)) (fst (snd (var vz)))))))
⊢szMICon-rho : {Γ : Ctx} → Γ ⊢ szMICon-rho ∷ imethTy KnotD IPair tagICon-rho cICon-rho Nat
⊢szMICon-rho =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cICon-rho
                     KnotWf cICon-rhoWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH2 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢fst (⊢snd (⊢var here)))))))

-- iκ : RTm Δ → ICon (Δ ∙) → ICon Δ
szΠICon-kap : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagICon-kap cICon-kap Nat
szΠICon-kap = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cICon-kap
                     KnotWf cICon-kapWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH2 ty-Nat))
szMICon-kap : {Γ : Cx} → RTm Γ
szMICon-kap = lam (lam (lam (nsuc (plusTm (fst (var vz)) (fst (snd (var vz)))))))
⊢szMICon-kap : {Γ : Ctx} → Γ ⊢ szMICon-kap ∷ imethTy KnotD IPair tagICon-kap cICon-kap Nat
⊢szMICon-kap =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cICon-kap
                     KnotWf cICon-kapWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH2 (⊢nsuc (⊢plus (⊢fst (⊢var here)) (⊢fst (⊢snd (⊢var here)))))))

-- vz : Var (Γ ∙)
szΠVar-vz : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagVar-vz cVar-vz Nat
szΠVar-vz = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cVar-vz
                     KnotWf cVar-vzWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH0 ty-Nat))
szMVar-vz : {Γ : Cx} → RTm Γ
szMVar-vz = lam (lam (lam (nsuc (nzero))))
⊢szMVar-vz : {Γ : Ctx} → Γ ⊢ szMVar-vz ∷ imethTy KnotD IPair tagVar-vz cVar-vz Nat
⊢szMVar-vz =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cVar-vz
                     KnotWf cVar-vzWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH0 (⊢nsuc (⊢nzero))))

-- vs : Var Γ → Var (Γ ∙)
szΠVar-vs : {Γ : Ctx} → Γ ⊢ty imethTy KnotD IPair tagVar-vs cVar-vs Nat
szΠVar-vs = ty-Π (ty-Σ ty-Nat ty-Nat)
           (ty-Π (ipayTy-wf KnotD IPair (isingle (var vz)) cVar-vs
                     KnotWf cVar-vsWf (isingle-Sub⊢ (⊢var here)))
                 (ty-Π szIH1 ty-Nat))
szMVar-vs : {Γ : Cx} → RTm Γ
szMVar-vs = lam (lam (lam (nsuc (fst (var vz)))))
⊢szMVar-vs : {Γ : Ctx} → Γ ⊢ szMVar-vs ∷ imethTy KnotD IPair tagVar-vs cVar-vs Nat
⊢szMVar-vs =
  ⊢lam (ty-Σ ty-Nat ty-Nat)
    (⊢lam (ipayTy-wf KnotD IPair (isingle (var vz)) cVar-vs
                     KnotWf cVar-vsWf (isingle-Sub⊢ (⊢var here)))
      (⊢lam szIH1 (⊢nsuc (⊢fst (⊢var here)))))

