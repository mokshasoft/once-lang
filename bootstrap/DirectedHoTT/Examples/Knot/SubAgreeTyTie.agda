--- GENERATED by tools/gen-knot.py — do not edit.
------------------------------------------------------------------------
-- OCP-0009 · EXAMPLES — ★★★ `sub-agree`, TIED: ALL 30 `RTm` ROWS.
--
--     sub-agree : Represents σ s → (t : RTm Γ) →
--                 subAtK sTm ⌈Γ⌉ ⌈Δ⌉ s ⌈t⌉ ⟶* ⌈ subTm σ t ⌉
--
-- ★★★ STEP 3 OF `PLAN-RENAMING.md`, COMPLETE — both halves.
--   `Knot/SubAgreeRows` supplies the 25 same-sort rows (generated),
--   `Knot/SubAgreeX` the four cross-sort ones and the given `var` row,
--   `Knot/SubClosed` the closed-sort identities they lean on.
--
-- ⚠ AND THE SUBSTITUTION HALF IS BUILT ON THE RENAMING HALF, not parallel
--   to it: a binder row needs `Knot/SubExt.extS-Represents`, whose `vs`
--   case composes with `wkTmK-agree`, which is `ren-agree` at `vs`.
------------------------------------------------------------------------

{-# OPTIONS --safe #-}
module DirectedHoTT.Examples.Knot.SubAgreeTyTie where
open import Agda.Builtin.Nat using ( zero; suc ) renaming ( Nat to ℕ )
open import DirectedHoTT.Spec.Syntax
  using ( Cx; RTm; RTy; Var; Sub; subTm; subTy; Desc; IDesc
        ; El
        ; Hom
        ; IMu
        ; Id
        ; Mu
        ; Nat
        ; U
        ; Unit
        ; base
        ; Π
        ; Σ'
        )
open import DirectedHoTT.Spec.Typing using ( _⟶*_ )
open import DirectedHoTT.Examples.Knot.Map using ( enTm; enTy )
open import DirectedHoTT.Examples.Knot.Sorts using ( num; len; sTm; sTy )
open import DirectedHoTT.Examples.Knot.SubApp using ( subAtK )
open import DirectedHoTT.Examples.Knot.SubAgree using ( Represents )
open import DirectedHoTT.Examples.Knot.SubAgreeTyRows
  using ( row-base; row-U; row-Pi; row-Sg; row-El; row-Hom; row-Unit; row-Nat; row-Id; row-Mu; row-IMu )
-- ★ THE LAYER BELOW — the SUBSTITUTION half's `Tm` tie.
open import DirectedHoTT.Examples.Knot.SubAgreeTie using ( sub-agree )



sub-agree-ty : {Γ Δ Θ : Cx} {σ : Sub Γ Δ} {s : RTm Θ} →
            Represents σ s → (t : RTy Γ) →
            subAtK sTy (num (len Γ)) (num (len Δ)) s (enTy {Γ} {Θ} t)
            ⟶* enTy {Δ} {Θ} (subTy σ t)
sub-agree-ty h base = row-base h 
sub-agree-ty h U = row-U h 
sub-agree-ty h (Π y0 y1) = row-Pi h y0 y1 (λ h' → sub-agree-ty h' y0) (λ h' → sub-agree-ty h' y1)
sub-agree-ty h (Σ' y0 y1) = row-Sg h y0 y1 (λ h' → sub-agree-ty h' y0) (λ h' → sub-agree-ty h' y1)
sub-agree-ty h (El y0) = row-El h y0 (λ h' → sub-agree h' y0)
sub-agree-ty h (Hom y0 y1 y2) = row-Hom h y0 y1 y2 (λ h' → sub-agree-ty h' y0) (λ h' → sub-agree h' y1) (λ h' → sub-agree h' y2)
sub-agree-ty h Unit = row-Unit h 
sub-agree-ty h Nat = row-Nat h 
sub-agree-ty h (Id y0 y1 y2) = row-Id h y0 y1 y2 (λ h' → sub-agree-ty h' y0) (λ h' → sub-agree h' y1) (λ h' → sub-agree h' y2)
sub-agree-ty h (Mu y0) = row-Mu h y0
sub-agree-ty h (IMu y0 y1 y2) = row-IMu h y0 y1 y2 (λ h' → sub-agree h' y2)
