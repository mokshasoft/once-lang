--- GENERATED by tools/gen-knot.py — do not edit.
------------------------------------------------------------------------
-- OCP-0009 · EXAMPLES — ★ THE `_⟶_` ROW SPIKE.
--
-- ONE reduction rule, generated end to end: the `ICon` and its
-- `IConWf`.  ⚠ It is here to settle the INTERFACE before 73 rules are
-- transcribed, which is what `JUDGEMENT-ATTEMPTS.md` §2 is about.
--
--     βfst : fst (pair a b) ⟶ a
--
-- ★ WHAT IT EXERCISES THAT `_∋_∷_` DID NOT: index components built from
--   the KNOT'S OWN constructors.  Those carry no term-level depth, so
--   their typing lemma needs the row's depth INJECTED — and not
--   uniformly, since a constructor under a binder sits deeper.
------------------------------------------------------------------------

{-# OPTIONS --safe #-}
module DirectedHoTT.Examples.Knot.RedRows where
open import normalizer.Syntax.Types using ( _≡_; refl )
open import Agda.Builtin.Nat using ( zero; suc ) renaming ( Nat to ℕ )
open import DirectedHoTT.Spec.Syntax
  using ( Cx; ε; _∙; RTy; RTm; var; vz; vs; pair; fst; snd; nsuc; El; IMu; Σ'; Nat
        ; ⌜Nat⌝; ⌜Id⌝; ⌜IMu⌝; jsub; ICon; IDesc; iι; iρ; iκ; εwkTy )
open import DirectedHoTT.Spec.Typing
  using ( Ctx; ◇; _▹_; ⌊_⌋; _⊢_∷_; _⊢ty_
        ; IConWf; iwf-ι; iwf-κ; iwf-ρ; ICodeWf; icw-clo; icw-ford; icw-imu
        ; ⊢var; here; there; ⊢fst; ⊢snd; ⊢nsuc; ⊢num
        ; ⊢⌜Nat⌝; ⊢⌜Id⌝; ⊢⌜IMu⌝; ⊢jsub
        ; ⊢pair; ty-Σ; ty-Nat; ty-IMu
        ; ξ-pairˡ; ξ-pairʳ; ξ-nsuc; βfst; βsnd )
open import DirectedHoTT.Examples.Knot.Sorts
  using ( IPair; sTm; toI; fromI; ⊢ixP; ⊢sTm; num )
open import DirectedHoTT.Examples.Knot.Desc using ( KnotD )
open import DirectedHoTT.Examples.Knot.Wf using ( KnotWf )
open import DirectedHoTT.Examples.Knot.Ctors using ( Tm-fstK; Tm-pairK )
open import DirectedHoTT.Examples.Knot.CtorsV using ( ⊢Tm-fstKv; ⊢Tm-pairKv )
open import DirectedHoTT.Examples.Knot.JudgeLib using ( toMu; fromMu; fordAs; muFwd )
open import DirectedHoTT.Lib.ArithComm using ( symN; ⊢symN )
open import DirectedHoTT.Metatheory.SubjectReduction using ( ⊢wk )

-- ★ the judgement's index: a depth and two terms at it.
IRed : RTy ε
IRed = Σ' Nat (Σ' (IMu KnotD IPair (pair sTm (var vz)))
                  (IMu KnotD IPair (pair sTm (var (vs vz)))))


Ρ0 : Ctx
Ρ0 = ◇ ▹ εwkTy IRed

ψ0 : RTm ⌊ Ρ0 ⌋
ψ0 = ⌜Nat⌝

Ρ1 : Ctx
Ρ1 = Ρ0 ▹ El ψ0

ψ1 : RTm ⌊ Ρ1 ⌋
ψ1 = ⌜IMu⌝ KnotD IPair (pair sTm (var vz))

Ρ2 : Ctx
Ρ2 = Ρ1 ▹ El ψ1

ψ2 : RTm ⌊ Ρ2 ⌋
ψ2 = ⌜IMu⌝ KnotD IPair (pair sTm (var (vs vz)))

Ρ3 : Ctx
Ρ3 = Ρ2 ▹ El ψ2

ψ3 : RTm ⌊ Ρ3 ⌋
ψ3 = ⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) (var (vs (vs vz)))

Ρ4 : Ctx
Ρ4 = Ρ3 ▹ El ψ3

ψ4 : RTm ⌊ Ρ4 ⌋
ψ4 = ⌜Id⌝ (⌜IMu⌝ KnotD IPair (pair sTm (fst (var (vs (vs (vs (vs vz)))))))) (fst (snd (var (vs (vs (vs (vs vz))))))) (jsub (⌜IMu⌝ KnotD IPair (pair sTm (var vz))) (symN (fst (var (vs (vs (vs (vs vz)))))) (var vz)) (Tm-fstK (Tm-pairK (var (vs (vs vz))) (var (vs vz)))))

Ρ5 : Ctx
Ρ5 = Ρ4 ▹ El ψ4

ψ5 : RTm ⌊ Ρ5 ⌋
ψ5 = ⌜Id⌝ (⌜IMu⌝ KnotD IPair (pair sTm (fst (var (vs (vs (vs (vs (vs vz))))))))) (snd (snd (var (vs (vs (vs (vs (vs vz)))))))) (jsub (⌜IMu⌝ KnotD IPair (pair sTm (var vz))) (symN (fst (var (vs (vs (vs (vs (vs vz))))))) (var (vs vz))) (var (vs (vs (vs vz)))))

Ρ6 : Ctx
Ρ6 = Ρ5 ▹ El ψ5

redBfstG : ICon (ε ∙)
redBfstG = iκ ψ0 (iκ ψ1 (iκ ψ2 (iκ ψ3 (iκ ψ4 (iκ ψ5 (iι))))))

W_Ρ5 : (D : IDesc) → IConWf D IRed Ρ5 (iκ ψ5 (iι))
W_Ρ5 D =
  iwf-κ ψ5 (icw-ford _ _ _)
    (⊢⌜Id⌝ (⊢⌜IMu⌝ KnotWf (⊢ixP ⊢sTm (⊢fst (⊢var (there (there (there (there (there here)))))))))
           (toMu (⊢snd (⊢snd (⊢var (there (there (there (there (there here)))))))))
           (⊢jsub (⊢⌜IMu⌝ KnotWf (⊢ixP ⊢sTm (fromI (⊢var here))))
                  (toI (fromI (⊢var (there (there (there (there here)))))))
                  (toI (⊢fst (⊢var (there (there (there (there (there here))))))))
                  (⊢symN (⊢fst (⊢var (there (there (there (there (there here))))))) (fromI (⊢var (there (there (there (there here))))))
                         (fordAs (⊢var (there here))))
                  (toMu (fromMu (⊢var (there (there (there here))))))))
    (iwf-ι)

W_Ρ4 : (D : IDesc) → IConWf D IRed Ρ4 (iκ ψ4 (iκ ψ5 (iι)))
W_Ρ4 D =
  iwf-κ ψ4 (icw-ford _ _ _)
    (⊢⌜Id⌝ (⊢⌜IMu⌝ KnotWf (⊢ixP ⊢sTm (⊢fst (⊢var (there (there (there (there here))))))))
           (toMu (⊢fst (⊢snd (⊢var (there (there (there (there here))))))))
           (⊢jsub (⊢⌜IMu⌝ KnotWf (⊢ixP ⊢sTm (fromI (⊢var here))))
                  (toI (fromI (⊢var (there (there (there here))))))
                  (toI (⊢fst (⊢var (there (there (there (there here)))))))
                  (⊢symN (⊢fst (⊢var (there (there (there (there here)))))) (fromI (⊢var (there (there (there here)))))
                         (fordAs (⊢var here)))
                  (toMu (⊢Tm-fstKv (fromI (⊢var (there (there (there here))))) (⊢Tm-pairKv (fromI (⊢var (there (there (there here))))) (fromMu (⊢var (there (there here)))) (fromMu (⊢var (there here))))))))
    (W_Ρ5 D)

W_Ρ3 : (D : IDesc) → IConWf D IRed Ρ3 (iκ ψ3 (iκ ψ4 (iκ ψ5 (iι))))
W_Ρ3 D =
  iwf-κ ψ3 (icw-ford _ _ _)
    (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (there (there (there here)))))) (toI (fromI (⊢var (there (there here))))))
    (W_Ρ4 D)

W_Ρ2 : (D : IDesc) → IConWf D IRed Ρ2 (iκ ψ2 (iκ ψ3 (iκ ψ4 (iκ ψ5 (iι)))))
W_Ρ2 D =
  iwf-κ ψ2 (icw-imu (pair sTm (var (vs vz))) KnotWf)
    (⊢⌜IMu⌝ KnotWf (⊢ixP ⊢sTm (fromI (⊢var (there here)))))
    (W_Ρ3 D)

W_Ρ1 : (D : IDesc) → IConWf D IRed Ρ1 (iκ ψ1 (iκ ψ2 (iκ ψ3 (iκ ψ4 (iκ ψ5 (iι))))))
W_Ρ1 D =
  iwf-κ ψ1 (icw-imu (pair sTm (var vz)) KnotWf)
    (⊢⌜IMu⌝ KnotWf (⊢ixP ⊢sTm (fromI (⊢var here))))
    (W_Ρ2 D)

W_Ρ0 : (D : IDesc) → IConWf D IRed Ρ0 (iκ ψ0 (iκ ψ1 (iκ ψ2 (iκ ψ3 (iκ ψ4 (iκ ψ5 (iι)))))))
W_Ρ0 D =
  iwf-κ ψ0 (icw-clo ⌜Nat⌝ ⊢⌜Nat⌝) ⊢⌜Nat⌝
    (W_Ρ1 D)

redBfstWfG : (D : IDesc) → IConWf D IRed Ρ0 redBfstG
redBfstWfG = W_Ρ0

