------------------------------------------------------------------------
-- OCP-0009 · KNOT — ★★★ `pwK`'s ADEQUACY, ALL 30 `RTm` ROWS.
--
-- ⚠ GENERATED by `tools/gen-knot.py`.  Do not hand-edit.
--
--     agree : ielim KnotD i pwMethsK ⌈t⌉ ⟶* ⌈ b2n (pw? t) ⌉
--
-- ★ 30 ROWS, NOT 53: this eliminates an encoded `RTm`, so the other 23
--   rows of the method tuple are unreachable by any `enTm`.
--
-- ★★ THE CHEAPEST ADEQUACY SHAPE IN THE TREE, and the reason is that the
--   two costs that made `Knot/IhITyAgree` expensive are BOTH ABSENT:
--
--     the motive is CONSTANT `Nat`, so `iinst i t Nat` IS `Nat` and no
--       row carries a cast — there is nothing for a `natⁿ` lift to do;
--     every constant method body is CLOSED under its three binders
--       (`nzero`, `nsuc nzero`), and the folding one reads the INNERMOST
--       binder (`fst (var vz)`), so the weakening tower is ZERO RUNGS.
--
--   ⇒ a default row is three βs and stop.  No fold (`occSum-red`), no
--   `⟶*-cast`, no naturality.  Contrast `Knot/OccAgree`, whose bulk is
--   the `maxℕ`/`_∨_` reassociation its constant motive still needs
--   because `occ` FOLDS its IH tuple; these read one slot or none.
--
-- ⚠ THE SELECTION IS SEGMENTED, and that is the only real content.  The
--   tuple is a sequence of constant runs with explicit overrides between
--   them (5 segments here), because the overrides sit in the MIDDLE
--   of the 53 rows and no `cdTake` prefix reaches them.  A row is reached
--   through whichever segment holds it: `methsFrom-past` CROSSES a run
--   (a `⟶*` step, composed with `»`), `sel-there` crosses an explicit
--   slot (a congruence, which WRAPS its continuation).
------------------------------------------------------------------------

{-# OPTIONS --safe #-}
module DirectedHoTT.Examples.Knot.PwAgree where

open import Agda.Builtin.Nat using ( zero; suc ) renaming ( Nat to ℕ )
open import DirectedHoTT.Spec.Syntax
  using ( Cx; ε; _∙; RTm; RTy; Var; var; vz; vs; lam; app; pair; unit; fst; snd
        ; nzero; nsuc; icon; ielim; iihs; isingle; sel; ilookupD
        ; absurd
        ; ap
        ; app
        ; con
        ; elim
        ; fst
        ; hrefl
        ; icon
        ; idrefl
        ; ielim
        ; jsub
        ; lam
        ; natrec
        ; nsuc
        ; nzero
        ; ordtr
        ; pair
        ; snd
        ; tr
        ; unit
        ; var
        ; ⌜Hom⌝
        ; ⌜IMu⌝
        ; ⌜Id⌝
        ; ⌜Mu⌝
        ; ⌜Nat⌝
        ; ⌜Unit⌝
        ; ⌜base⌝
        ; ⌜Π⌝
        ; ⌜Σ⌝
        )
open import DirectedHoTT.Spec.Typing using ( _⟶*_; step; done; β; βfst; βsnd )
open import DirectedHoTT.Spec.Variance using ( 𝔹; true; false; pw? )
open import DirectedHoTT.Lib.BoolNum using ( b2n )
open import DirectedHoTT.Lib.NatNum using ( num )
open import DirectedHoTT.Lib.RedChain using ( _»_ )
open import DirectedHoTT.Lib.Eval using ( evSpine; chainOf )
open import DirectedHoTT.Metatheory.RedCong using ( ⟶*-ielimᵗ; ⟶*-appˡ )
open import DirectedHoTT.Lib.IMeths
  using ( cdTake; methsFrom-sel; methsFrom-past; inCD; tt; sel-here; sel-there )
open import DirectedHoTT.Lib.IHeadRed using ( ihead-red )
open import DirectedHoTT.Examples.Knot.Desc using ( KnotD )
open import DirectedHoTT.Examples.Knot.Tags
open import DirectedHoTT.Examples.Knot.Map using ( enTm )
open import DirectedHoTT.Examples.Knot.Pw
  using ( pwK; pwMethsK; pwZero; pwOne; pwHom; D23 )

------------------------------------------------------------------------
-- ★ THE HEAD STEP, at this program's tuple.  `Knot/SzAgree.head-red`'s
--   shape, but taking the SELECTION rather than a membership: a
--   segmented tuple has no single `sel` lemma to hide behind an `∈ID`.
------------------------------------------------------------------------
head-red : {Γ : Cx} (k : ℕ) {mth : RTm Γ} → sel k (pwMethsK {Γ}) ⟶* mth →
           (i p : RTm Γ) {u : RTm Γ} →
           app (app (app mth i) p)
               (iihs KnotD pwMethsK (isingle i) (ilookupD KnotD k) p) ⟶* u →
           ielim KnotD i pwMethsK (icon k p) ⟶* u
head-red k sp i p h = ihead-red KnotD pwMethsK k i p sp h

------------------------------------------------------------------------
-- ★★★ THE 30 ROWS.
--
-- ⚠ THE INDEX `i` IS A PARAMETER, not rebuilt: `iihs` hands each child
--   `subTm (isingle i) (pair s (snd (var vz)))`, so a row that PINNED
--   its index could never match a child.  `Knot/SzAgree.agree i t` and
--   `Knot/OccAgree.agree-tm` do the same.
------------------------------------------------------------------------
agree : {Γ Θ : Cx} (i : RTm Θ) (t : RTm Γ) →
        ielim KnotD i pwMethsK (enTm {Γ} {Θ} t) ⟶* num (b2n (pw? t))

agree i (var y0) =
  head-red tagTm-var
    (methsFrom-sel (cdTake 20 KnotD) {m = pwZero} 11 (inCD (cdTake 20 KnotD) 11 tt)) i _
    (chainOf (evSpine 3 _))
agree i (lam y0) =
  head-red tagTm-lam
    (methsFrom-sel (cdTake 20 KnotD) {m = pwZero} 12 (inCD (cdTake 20 KnotD) 12 tt)) i _
    (chainOf (evSpine 3 _))
agree i (app y0 y1) =
  head-red tagTm-app
    (methsFrom-sel (cdTake 20 KnotD) {m = pwZero} 13 (inCD (cdTake 20 KnotD) 13 tt)) i _
    (chainOf (evSpine 3 _))
agree i (pair y0 y1) =
  head-red tagTm-pair
    (methsFrom-sel (cdTake 20 KnotD) {m = pwZero} 14 (inCD (cdTake 20 KnotD) 14 tt)) i _
    (chainOf (evSpine 3 _))
agree i (absurd y0 y1) =
  head-red tagTm-absurd
    (methsFrom-sel (cdTake 20 KnotD) {m = pwZero} 15 (inCD (cdTake 20 KnotD) 15 tt)) i _
    (chainOf (evSpine 3 _))
agree i (ordtr y0 y1 y2 y3 y4) =
  head-red tagTm-ordtr
    (methsFrom-sel (cdTake 20 KnotD) {m = pwZero} 16 (inCD (cdTake 20 KnotD) 16 tt)) i _
    (chainOf (evSpine 3 _))
agree i (fst y0) =
  head-red tagTm-fst
    (methsFrom-sel (cdTake 20 KnotD) {m = pwZero} 17 (inCD (cdTake 20 KnotD) 17 tt)) i _
    (chainOf (evSpine 3 _))
agree i (snd y0) =
  head-red tagTm-snd
    (methsFrom-sel (cdTake 20 KnotD) {m = pwZero} 18 (inCD (cdTake 20 KnotD) 18 tt)) i _
    (chainOf (evSpine 3 _))
agree i ⌜base⌝ =
  head-red tagTm-cbase
    (methsFrom-sel (cdTake 20 KnotD) {m = pwZero} 19 (inCD (cdTake 20 KnotD) 19 tt)) i _
    (chainOf (evSpine 3 _))
agree i (⌜Π⌝ y0 y1) =
  head-red tagTm-cPi
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 0
     » sel-here _ _) i _
    (chainOf (evSpine 3 _))
agree i (⌜Σ⌝ y0 y1) =
  head-red tagTm-cSg
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 1
     » sel-there 0 _ _ (sel-here _ _)) i _
    (chainOf (evSpine 3 _))
agree i (⌜Hom⌝ y0 y1 y2) =
  head-red tagTm-cHom
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 2
     » sel-there 1 _ _ (sel-there 0 _ _ (sel-here _ _))) i _
    (chainOf (evSpine 3 _))
  » step (βfst _ _) done
  » ⟶*-ielimᵗ (step (βfst _ _) done)
  » agree _ y0
agree i (hrefl y0 y1) =
  head-red tagTm-hrefl
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 3
     » sel-there 2 _ _ (sel-there 1 _ _ (sel-there 0 _ _ (methsFrom-sel (cdTake 30 D23) {m = pwZero} 0 (inCD (cdTake 30 D23) 0 tt))))) i _
    (chainOf (evSpine 3 _))
agree i (tr y0 y1 y2) =
  head-red tagTm-tr
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 4
     » sel-there 3 _ _ (sel-there 2 _ _ (sel-there 1 _ _ (methsFrom-sel (cdTake 30 D23) {m = pwZero} 1 (inCD (cdTake 30 D23) 1 tt))))) i _
    (chainOf (evSpine 3 _))
agree i (ap y0 y1 y2) =
  head-red tagTm-ap
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 5
     » sel-there 4 _ _ (sel-there 3 _ _ (sel-there 2 _ _ (methsFrom-sel (cdTake 30 D23) {m = pwZero} 2 (inCD (cdTake 30 D23) 2 tt))))) i _
    (chainOf (evSpine 3 _))
agree i (⌜Id⌝ y0 y1 y2) =
  head-red tagTm-cId
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 6
     » sel-there 5 _ _ (sel-there 4 _ _ (sel-there 3 _ _ (methsFrom-sel (cdTake 30 D23) {m = pwZero} 3 (inCD (cdTake 30 D23) 3 tt))))) i _
    (chainOf (evSpine 3 _))
agree i (idrefl y0 y1) =
  head-red tagTm-idrefl
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 7
     » sel-there 6 _ _ (sel-there 5 _ _ (sel-there 4 _ _ (methsFrom-sel (cdTake 30 D23) {m = pwZero} 4 (inCD (cdTake 30 D23) 4 tt))))) i _
    (chainOf (evSpine 3 _))
agree i (jsub y0 y1 y2) =
  head-red tagTm-jsub
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 8
     » sel-there 7 _ _ (sel-there 6 _ _ (sel-there 5 _ _ (methsFrom-sel (cdTake 30 D23) {m = pwZero} 5 (inCD (cdTake 30 D23) 5 tt))))) i _
    (chainOf (evSpine 3 _))
agree i unit =
  head-red tagTm-unit
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 9
     » sel-there 8 _ _ (sel-there 7 _ _ (sel-there 6 _ _ (methsFrom-sel (cdTake 30 D23) {m = pwZero} 6 (inCD (cdTake 30 D23) 6 tt))))) i _
    (chainOf (evSpine 3 _))
agree i nzero =
  head-red tagTm-nzero
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 10
     » sel-there 9 _ _ (sel-there 8 _ _ (sel-there 7 _ _ (methsFrom-sel (cdTake 30 D23) {m = pwZero} 7 (inCD (cdTake 30 D23) 7 tt))))) i _
    (chainOf (evSpine 3 _))
agree i (nsuc y0) =
  head-red tagTm-nsuc
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 11
     » sel-there 10 _ _ (sel-there 9 _ _ (sel-there 8 _ _ (methsFrom-sel (cdTake 30 D23) {m = pwZero} 8 (inCD (cdTake 30 D23) 8 tt))))) i _
    (chainOf (evSpine 3 _))
agree i (natrec y0 y1 y2) =
  head-red tagTm-natrec
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 12
     » sel-there 11 _ _ (sel-there 10 _ _ (sel-there 9 _ _ (methsFrom-sel (cdTake 30 D23) {m = pwZero} 9 (inCD (cdTake 30 D23) 9 tt))))) i _
    (chainOf (evSpine 3 _))
agree i (con y0 y1) =
  head-red tagTm-con
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 13
     » sel-there 12 _ _ (sel-there 11 _ _ (sel-there 10 _ _ (methsFrom-sel (cdTake 30 D23) {m = pwZero} 10 (inCD (cdTake 30 D23) 10 tt))))) i _
    (chainOf (evSpine 3 _))
agree i (elim y0 y1 y2) =
  head-red tagTm-elim
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 14
     » sel-there 13 _ _ (sel-there 12 _ _ (sel-there 11 _ _ (methsFrom-sel (cdTake 30 D23) {m = pwZero} 11 (inCD (cdTake 30 D23) 11 tt))))) i _
    (chainOf (evSpine 3 _))
agree i (icon y0 y1) =
  head-red tagTm-icon
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 15
     » sel-there 14 _ _ (sel-there 13 _ _ (sel-there 12 _ _ (methsFrom-sel (cdTake 30 D23) {m = pwZero} 12 (inCD (cdTake 30 D23) 12 tt))))) i _
    (chainOf (evSpine 3 _))
agree i (ielim y0 y1 y2 y3) =
  head-red tagTm-ielim
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 16
     » sel-there 15 _ _ (sel-there 14 _ _ (sel-there 13 _ _ (methsFrom-sel (cdTake 30 D23) {m = pwZero} 13 (inCD (cdTake 30 D23) 13 tt))))) i _
    (chainOf (evSpine 3 _))
agree i ⌜Nat⌝ =
  head-red tagTm-cNat
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 17
     » sel-there 16 _ _ (sel-there 15 _ _ (sel-there 14 _ _ (methsFrom-sel (cdTake 30 D23) {m = pwZero} 14 (inCD (cdTake 30 D23) 14 tt))))) i _
    (chainOf (evSpine 3 _))
agree i (⌜Mu⌝ y0) =
  head-red tagTm-cMu
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 18
     » sel-there 17 _ _ (sel-there 16 _ _ (sel-there 15 _ _ (methsFrom-sel (cdTake 30 D23) {m = pwZero} 15 (inCD (cdTake 30 D23) 15 tt))))) i _
    (chainOf (evSpine 3 _))
agree i (⌜IMu⌝ y0 y1 y2) =
  head-red tagTm-cIMu
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 19
     » sel-there 18 _ _ (sel-there 17 _ _ (sel-there 16 _ _ (methsFrom-sel (cdTake 30 D23) {m = pwZero} 16 (inCD (cdTake 30 D23) 16 tt))))) i _
    (chainOf (evSpine 3 _))
agree i ⌜Unit⌝ =
  head-red tagTm-cUnit
    (methsFrom-past (cdTake 20 KnotD) {m = pwZero} 20
     » sel-there 19 _ _ (sel-there 18 _ _ (sel-there 17 _ _ (methsFrom-sel (cdTake 30 D23) {m = pwZero} 17 (inCD (cdTake 30 D23) 17 tt))))) i _
    (chainOf (evSpine 3 _))

------------------------------------------------------------------------
-- ★★★ AT THE LEDGER'S NAME.  `pwK i t = ielim KnotD i pwMethsK t`,
--   so the tie is definitional — but a ledger entry reading "discharged"
--   about a name no theorem contains is the shape of claim this
--   development keeps catching itself in.
------------------------------------------------------------------------
pwK-agree : {Γ Θ : Cx} (i : RTm Θ) (t : RTm Γ) →
            pwK i (enTm {Γ} {Θ} t) ⟶* num (b2n (pw? t))
pwK-agree i t = agree i t
