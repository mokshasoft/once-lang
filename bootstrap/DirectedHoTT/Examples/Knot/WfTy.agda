------------------------------------------------------------------------
-- ⚠⚠ GENERATED BY `tools/gen-knot.py` — DO NOT EDIT BY HAND. ⚠⚠
--
-- Regenerate with:  python3 tools/gen-knot.py
--
-- 53 constructors over 7 sorts, one description, index `Σ' Nat Nat`.
-- The table, the encoding decisions and the two exceptions (`Var`'s
-- depth-Fording) are documented in the generator's header — read that,
-- not this file, to understand the encoding.
--
-- ⛔ `Ctx` IS DELIBERATELY NOT HERE.  `Examples/Knot/CtxD`, and the
--    generator's header says why.
------------------------------------------------------------------------

-- ⚠⚠ THIS MODULE NEEDS THE **COMPACTING COLLECTOR**.
--
--   `tools/sweep.sh` greps this header for the phrase above and
--   switches collectors on its own (`needs_c`), which is why the
--   words are spelled out rather than described.
--
-- ★ THE COST IS TELESCOPE DEPTH, and A-math added a slot.
--   Measured 2026-08-27: 53 rows in one module, -A64m OOM at 80s,
--   -A64m -c 99s.  Re-measured 2026-09-25 under A-math (the root
--   telescope binds the abstract family, one slot deeper; ~1.7×
--   per slot, `agda-cost-is-context-depth`): the 53 rows alone
--   142s, rows + `KnotWf` in one module over the 5.5 GB cap at
--   158s, the assembly alone 1s.  ⇒ the rows live in two halves
--   (`WfTy`: types/descriptions/variables, `WfTm`: terms) and `Wf`
--   assembles and re-exports them.

{-# OPTIONS --safe #-}
module DirectedHoTT.Examples.Knot.WfTy where
open import DirectedHoTT.Lib.Lkp using ( ∋lkp; vsⁿ )
open import DirectedHoTT.Spec.Syntax
  using ( Cx; ε; _∙; vz; vs
        ; RTm; var; pair; fst; snd; nzero; nsuc; ⌜Nat⌝; ⌜Id⌝
        ; ICon; IDesc; iι; iρ; iκ; inil; _◂_ )
open import DirectedHoTT.Spec.Typing
  using ( Ctx; ◇; _▹_
        ; ⊢var; here; there; ⊢fst; ⊢snd; ⊢nzero; ⊢nsuc; ⊢⌜Nat⌝; ⊢⌜Id⌝
        ; IConWf; iwf-ι; iwf-ρ; iwf-κ
        ; ICodeWf; icw-clo; icw-ford
        ; IDescWf; idwf-nil; idwf-cons; _,,_; Θ₀; ρ₀; x₀ )
open import DirectedHoTT.Examples.Knot.Sorts
  using ( IPair; ⊢IPair; sTy; sTm; sDesc; sDCon; sIDesc; sICon; sVar
        ; ⊢sTy; ⊢sTm; ⊢sDesc; ⊢sDCon; ⊢sIDesc; ⊢sICon; ⊢sVar
        ; toI; fromI; ⊢ixP )
open import DirectedHoTT.Examples.Knot.Desc
  using ( KnotD
        ; cTy-base
        ; cTy-U
        ; cTy-Pi
        ; cTy-Sg
        ; cTy-El
        ; cTy-Hom
        ; cTy-Unit
        ; cTy-Nat
        ; cTy-Id
        ; cTy-Mu
        ; cTy-IMu
        ; cTm-var
        ; cTm-lam
        ; cTm-app
        ; cTm-pair
        ; cTm-absurd
        ; cTm-ordtr
        ; cTm-fst
        ; cTm-snd
        ; cTm-cbase
        ; cTm-cPi
        ; cTm-cSg
        ; cTm-cHom
        ; cTm-hrefl
        ; cTm-tr
        ; cTm-ap
        ; cTm-cId
        ; cTm-idrefl
        ; cTm-jsub
        ; cTm-unit
        ; cTm-nzero
        ; cTm-nsuc
        ; cTm-natrec
        ; cTm-con
        ; cTm-elim
        ; cTm-icon
        ; cTm-ielim
        ; cTm-cNat
        ; cTm-cMu
        ; cTm-cIMu
        ; cTm-cUnit
        ; cDesc-nil
        ; cDesc-cons
        ; cDCon-i
        ; cDCon-rho
        ; cDCon-kap
        ; cIDesc-nil
        ; cIDesc-cons
        ; cICon-i
        ; cICon-rho
        ; cICon-kap
        ; cVar-vz
        ; cVar-vs )

cTy-baseWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTy-base
cTy-baseWf =
  iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sTy) (icw-ford ⌜Nat⌝ (fst (var vz)) sTy) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var here))) (toI ⊢sTy))
   iwf-ι

cTy-UWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTy-U
cTy-UWf =
  iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sTy) (icw-ford ⌜Nat⌝ (fst (var vz)) sTy) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var here))) (toI ⊢sTy))
   iwf-ι

cTy-PiWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTy-Pi
cTy-PiWf =
  iwf-ρ (pair sTy (snd (var vz))) (⊢ixP ⊢sTy (⊢snd (⊢var here)))
   (iwf-ρ (pair sTy (nsuc (snd (var (vs vz))))) (⊢ixP ⊢sTy (⊢nsuc (⊢snd (⊢var (there here)))))
    (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTy) (icw-ford ⌜Nat⌝ (fst (var (vs (vs vz)))) sTy) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 2 vz))))) (toI ⊢sTy))
     iwf-ι))

cTy-SgWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTy-Sg
cTy-SgWf =
  iwf-ρ (pair sTy (snd (var vz))) (⊢ixP ⊢sTy (⊢snd (⊢var here)))
   (iwf-ρ (pair sTy (nsuc (snd (var (vs vz))))) (⊢ixP ⊢sTy (⊢nsuc (⊢snd (⊢var (there here)))))
    (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTy) (icw-ford ⌜Nat⌝ (fst (var (vs (vs vz)))) sTy) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 2 vz))))) (toI ⊢sTy))
     iwf-ι))

cTy-ElWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTy-El
cTy-ElWf =
  iwf-ρ (pair sTm (snd (var vz))) (⊢ixP ⊢sTm (⊢snd (⊢var here)))
   (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sTy) (icw-ford ⌜Nat⌝ (fst (var (vs vz))) sTy) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (there here)))) (toI ⊢sTy))
    iwf-ι)

cTy-HomWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTy-Hom
cTy-HomWf =
  iwf-ρ (pair sTy (snd (var vz))) (⊢ixP ⊢sTy (⊢snd (⊢var here)))
   (iwf-ρ (pair sTm (snd (var (vs vz)))) (⊢ixP ⊢sTm (⊢snd (⊢var (there here))))
    (iwf-ρ (pair sTm (snd (var (vs (vs vz))))) (⊢ixP ⊢sTm (⊢snd (⊢var (∋lkp _ (vsⁿ 2 vz)))))
     (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTy) (icw-ford ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTy) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 3 vz))))) (toI ⊢sTy))
      iwf-ι)))

cTy-UnitWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTy-Unit
cTy-UnitWf =
  iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sTy) (icw-ford ⌜Nat⌝ (fst (var vz)) sTy) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var here))) (toI ⊢sTy))
   iwf-ι

cTy-NatWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTy-Nat
cTy-NatWf =
  iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sTy) (icw-ford ⌜Nat⌝ (fst (var vz)) sTy) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var here))) (toI ⊢sTy))
   iwf-ι

cTy-IdWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTy-Id
cTy-IdWf =
  iwf-ρ (pair sTy (snd (var vz))) (⊢ixP ⊢sTy (⊢snd (⊢var here)))
   (iwf-ρ (pair sTm (snd (var (vs vz)))) (⊢ixP ⊢sTm (⊢snd (⊢var (there here))))
    (iwf-ρ (pair sTm (snd (var (vs (vs vz))))) (⊢ixP ⊢sTm (⊢snd (⊢var (∋lkp _ (vsⁿ 2 vz)))))
     (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTy) (icw-ford ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTy) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 3 vz))))) (toI ⊢sTy))
      iwf-ι)))

cTy-MuWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTy-Mu
cTy-MuWf =
  iwf-ρ (pair sDesc (snd (var vz))) (⊢ixP ⊢sDesc (⊢snd (⊢var here)))
   (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sTy) (icw-ford ⌜Nat⌝ (fst (var (vs vz))) sTy) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (there here)))) (toI ⊢sTy))
    iwf-ι)

cTy-IMuWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTy-IMu
cTy-IMuWf =
  iwf-ρ (pair sIDesc (snd (var vz))) (⊢ixP ⊢sIDesc (⊢snd (⊢var here)))
   (iwf-ρ (pair sTy nzero) (⊢ixP ⊢sTy ⊢nzero)
    (iwf-ρ (pair sTm (snd (var (vs (vs vz))))) (⊢ixP ⊢sTm (⊢snd (⊢var (∋lkp _ (vsⁿ 2 vz)))))
     (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTy) (icw-ford ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTy) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 3 vz))))) (toI ⊢sTy))
      iwf-ι)))

cDesc-nilWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cDesc-nil
cDesc-nilWf =
  iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sDesc) (icw-ford ⌜Nat⌝ (fst (var vz)) sDesc) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var here))) (toI ⊢sDesc))
   iwf-ι

cDesc-consWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cDesc-cons
cDesc-consWf =
  iwf-ρ (pair sDCon (snd (var vz))) (⊢ixP ⊢sDCon (⊢snd (⊢var here)))
   (iwf-ρ (pair sDesc (snd (var (vs vz)))) (⊢ixP ⊢sDesc (⊢snd (⊢var (there here))))
    (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sDesc) (icw-ford ⌜Nat⌝ (fst (var (vs (vs vz)))) sDesc) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 2 vz))))) (toI ⊢sDesc))
     iwf-ι))

cDCon-iWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cDCon-i
cDCon-iWf =
  iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sDCon) (icw-ford ⌜Nat⌝ (fst (var vz)) sDCon) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var here))) (toI ⊢sDCon))
   iwf-ι

cDCon-rhoWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cDCon-rho
cDCon-rhoWf =
  iwf-ρ (pair sDCon (snd (var vz))) (⊢ixP ⊢sDCon (⊢snd (⊢var here)))
   (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sDCon) (icw-ford ⌜Nat⌝ (fst (var (vs vz))) sDCon) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (there here)))) (toI ⊢sDCon))
    iwf-ι)

cDCon-kapWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cDCon-kap
cDCon-kapWf =
  iwf-ρ (pair sTy nzero) (⊢ixP ⊢sTy ⊢nzero)
   (iwf-ρ (pair sDCon (snd (var (vs vz)))) (⊢ixP ⊢sDCon (⊢snd (⊢var (there here))))
    (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sDCon) (icw-ford ⌜Nat⌝ (fst (var (vs (vs vz)))) sDCon) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 2 vz))))) (toI ⊢sDCon))
     iwf-ι))

cIDesc-nilWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cIDesc-nil
cIDesc-nilWf =
  iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sIDesc) (icw-ford ⌜Nat⌝ (fst (var vz)) sIDesc) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var here))) (toI ⊢sIDesc))
   iwf-ι

cIDesc-consWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cIDesc-cons
cIDesc-consWf =
  iwf-ρ (pair sICon (nsuc (nzero))) (⊢ixP ⊢sICon (⊢nsuc (⊢nzero)))
   (iwf-ρ (pair sIDesc (snd (var (vs vz)))) (⊢ixP ⊢sIDesc (⊢snd (⊢var (there here))))
    (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sIDesc) (icw-ford ⌜Nat⌝ (fst (var (vs (vs vz)))) sIDesc) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 2 vz))))) (toI ⊢sIDesc))
     iwf-ι))

cICon-iWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cICon-i
cICon-iWf =
  iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sICon) (icw-ford ⌜Nat⌝ (fst (var vz)) sICon) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var here))) (toI ⊢sICon))
   iwf-ι

cICon-rhoWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cICon-rho
cICon-rhoWf =
  iwf-ρ (pair sTm (snd (var vz))) (⊢ixP ⊢sTm (⊢snd (⊢var here)))
   (iwf-ρ (pair sICon (nsuc (snd (var (vs vz))))) (⊢ixP ⊢sICon (⊢nsuc (⊢snd (⊢var (there here)))))
    (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sICon) (icw-ford ⌜Nat⌝ (fst (var (vs (vs vz)))) sICon) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 2 vz))))) (toI ⊢sICon))
     iwf-ι))

cICon-kapWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cICon-kap
cICon-kapWf =
  iwf-ρ (pair sTm (snd (var vz))) (⊢ixP ⊢sTm (⊢snd (⊢var here)))
   (iwf-ρ (pair sICon (nsuc (snd (var (vs vz))))) (⊢ixP ⊢sICon (⊢nsuc (⊢snd (⊢var (there here)))))
    (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sICon) (icw-ford ⌜Nat⌝ (fst (var (vs (vs vz)))) sICon) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 2 vz))))) (toI ⊢sICon))
     iwf-ι))

cVar-vzWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cVar-vz
cVar-vzWf =
  iwf-κ ⌜Nat⌝ (icw-clo ⌜Nat⌝ ⊢⌜Nat⌝) ⊢⌜Nat⌝
   (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sVar) (icw-ford ⌜Nat⌝ (fst (var (vs vz))) sVar) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (there here)))) (toI ⊢sVar))
    (iwf-κ (⌜Id⌝ ⌜Nat⌝ (snd (var (vs (vs vz)))) (nsuc (var (vs vz)))) (icw-ford ⌜Nat⌝ (snd (var (vs (vs vz)))) (nsuc (var (vs vz)))) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢snd (⊢var (∋lkp _ (vsⁿ 2 vz))))) (toI (⊢nsuc (fromI (⊢var (there here))))))
     iwf-ι))

cVar-vsWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cVar-vs
cVar-vsWf =
  iwf-κ ⌜Nat⌝ (icw-clo ⌜Nat⌝ ⊢⌜Nat⌝) ⊢⌜Nat⌝
   (iwf-ρ (pair sVar (var vz)) (⊢ixP ⊢sVar (fromI (⊢var here)))
    (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sVar) (icw-ford ⌜Nat⌝ (fst (var (vs (vs vz)))) sVar) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 2 vz))))) (toI ⊢sVar))
     (iwf-κ (⌜Id⌝ ⌜Nat⌝ (snd (var (vs (vs (vs vz))))) (nsuc (var (vs (vs vz))))) (icw-ford ⌜Nat⌝ (snd (var (vs (vs (vs vz))))) (nsuc (var (vs (vs vz))))) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢snd (⊢var (∋lkp _ (vsⁿ 3 vz))))) (toI (⊢nsuc (fromI (⊢var (∋lkp _ (vsⁿ 2 vz)))))))
      iwf-ι)))

