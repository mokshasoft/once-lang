------------------------------------------------------------------------
-- ⚠⚠ GENERATED BY `tools/gen-knot.py` — DO NOT EDIT BY HAND. ⚠⚠
--
-- Regenerate with:  python3 tools/gen-knot.py
--
-- 53 constructors over 7 sorts, one description, index `Σ' Nat Nat`.
-- The table, the encoding decisions and the two exceptions (`Var`'s
-- depth-Fording) are documented in the generator's header — read that,
-- not this file, to understand the encoding.
--
-- ⛔ `Ctx` IS DELIBERATELY NOT HERE.  `Examples/Knot/CtxD`, and the
--    generator's header says why.
------------------------------------------------------------------------

-- ⚠⚠ THIS MODULE NEEDS THE **COMPACTING COLLECTOR**.
--
--   `tools/sweep.sh` greps this header for the phrase above and
--   switches collectors on its own (`needs_c`), which is why the
--   words are spelled out rather than described.
--
-- ★ THE COST IS TELESCOPE DEPTH, and A-math added a slot.
--   Measured 2026-08-27: 53 rows in one module, -A64m OOM at 80s,
--   -A64m -c 99s.  Re-measured 2026-09-25 under A-math (the root
--   telescope binds the abstract family, one slot deeper; ~1.7×
--   per slot, `agda-cost-is-context-depth`): the 53 rows alone
--   142s, rows + `KnotWf` in one module over the 5.5 GB cap at
--   158s, the assembly alone 1s.  ⇒ the rows live in two halves
--   (`WfTy`: types/descriptions/variables, `WfTm`: terms) and `Wf`
--   assembles and re-exports them.

{-# OPTIONS --safe #-}
module DirectedHoTT.Examples.Knot.WfTm where
open import DirectedHoTT.Lib.Lkp using ( ∋lkp; vsⁿ )
open import DirectedHoTT.Spec.Syntax
  using ( Cx; ε; _∙; vz; vs
        ; RTm; var; pair; fst; snd; nzero; nsuc; ⌜Nat⌝; ⌜Id⌝
        ; ICon; IDesc; iι; iρ; iκ; inil; _◂_ )
open import DirectedHoTT.Spec.Typing
  using ( Ctx; ◇; _▹_
        ; ⊢var; here; there; ⊢fst; ⊢snd; ⊢nzero; ⊢nsuc; ⊢⌜Nat⌝; ⊢⌜Id⌝
        ; IConWf; iwf-ι; iwf-ρ; iwf-κ
        ; ICodeWf; icw-clo; icw-ford
        ; IDescWf; idwf-nil; idwf-cons; _,,_; Θ₀; ρ₀; x₀ )
open import DirectedHoTT.Examples.Knot.Sorts
  using ( IPair; ⊢IPair; sTy; sTm; sDesc; sDCon; sIDesc; sICon; sVar
        ; ⊢sTy; ⊢sTm; ⊢sDesc; ⊢sDCon; ⊢sIDesc; ⊢sICon; ⊢sVar
        ; toI; fromI; ⊢ixP )
open import DirectedHoTT.Examples.Knot.Desc
  using ( KnotD
        ; cTy-base
        ; cTy-U
        ; cTy-Pi
        ; cTy-Sg
        ; cTy-El
        ; cTy-Hom
        ; cTy-Unit
        ; cTy-Nat
        ; cTy-Id
        ; cTy-Mu
        ; cTy-IMu
        ; cTm-var
        ; cTm-lam
        ; cTm-app
        ; cTm-pair
        ; cTm-absurd
        ; cTm-ordtr
        ; cTm-fst
        ; cTm-snd
        ; cTm-cbase
        ; cTm-cPi
        ; cTm-cSg
        ; cTm-cHom
        ; cTm-hrefl
        ; cTm-tr
        ; cTm-ap
        ; cTm-cId
        ; cTm-idrefl
        ; cTm-jsub
        ; cTm-unit
        ; cTm-nzero
        ; cTm-nsuc
        ; cTm-natrec
        ; cTm-con
        ; cTm-elim
        ; cTm-icon
        ; cTm-ielim
        ; cTm-cNat
        ; cTm-cMu
        ; cTm-cIMu
        ; cTm-cUnit
        ; cDesc-nil
        ; cDesc-cons
        ; cDCon-i
        ; cDCon-rho
        ; cDCon-kap
        ; cIDesc-nil
        ; cIDesc-cons
        ; cICon-i
        ; cICon-rho
        ; cICon-kap
        ; cVar-vz
        ; cVar-vs )

cTm-varWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-var
cTm-varWf =
  iwf-ρ (pair sVar (snd (var vz))) (⊢ixP ⊢sVar (⊢snd (⊢var here)))
   (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs vz))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (there here)))) (toI ⊢sTm))
    iwf-ι)

cTm-lamWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-lam
cTm-lamWf =
  iwf-ρ (pair sTm (nsuc (snd (var vz)))) (⊢ixP ⊢sTm (⊢nsuc (⊢snd (⊢var here))))
   (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs vz))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (there here)))) (toI ⊢sTm))
    iwf-ι)

cTm-appWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-app
cTm-appWf =
  iwf-ρ (pair sTm (snd (var vz))) (⊢ixP ⊢sTm (⊢snd (⊢var here)))
   (iwf-ρ (pair sTm (snd (var (vs vz)))) (⊢ixP ⊢sTm (⊢snd (⊢var (there here))))
    (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 2 vz))))) (toI ⊢sTm))
     iwf-ι))

cTm-pairWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-pair
cTm-pairWf =
  iwf-ρ (pair sTm (snd (var vz))) (⊢ixP ⊢sTm (⊢snd (⊢var here)))
   (iwf-ρ (pair sTm (snd (var (vs vz)))) (⊢ixP ⊢sTm (⊢snd (⊢var (there here))))
    (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 2 vz))))) (toI ⊢sTm))
     iwf-ι))

cTm-absurdWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-absurd
cTm-absurdWf =
  iwf-ρ (pair sTm (snd (var vz))) (⊢ixP ⊢sTm (⊢snd (⊢var here)))
   (iwf-ρ (pair sTm (snd (var (vs vz)))) (⊢ixP ⊢sTm (⊢snd (⊢var (there here))))
    (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 2 vz))))) (toI ⊢sTm))
     iwf-ι))

cTm-ordtrWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-ordtr
cTm-ordtrWf =
  iwf-ρ (pair sTm (snd (var vz))) (⊢ixP ⊢sTm (⊢snd (⊢var here)))
   (iwf-ρ (pair sTm (snd (var (vs vz)))) (⊢ixP ⊢sTm (⊢snd (⊢var (there here))))
    (iwf-ρ (pair sTm (snd (var (vs (vs vz))))) (⊢ixP ⊢sTm (⊢snd (⊢var (∋lkp _ (vsⁿ 2 vz)))))
     (iwf-ρ (pair sTm (snd (var (vs (vs (vs vz)))))) (⊢ixP ⊢sTm (⊢snd (⊢var (∋lkp _ (vsⁿ 3 vz)))))
      (iwf-ρ (pair sTm (snd (var (vs (vs (vs (vs vz))))))) (⊢ixP ⊢sTm (⊢snd (⊢var (∋lkp _ (vsⁿ 4 vz)))))
       (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs (vs (vs vz))))))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs (vs (vs (vs (vs vz))))))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 5 vz))))) (toI ⊢sTm))
        iwf-ι)))))

cTm-fstWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-fst
cTm-fstWf =
  iwf-ρ (pair sTm (snd (var vz))) (⊢ixP ⊢sTm (⊢snd (⊢var here)))
   (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs vz))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (there here)))) (toI ⊢sTm))
    iwf-ι)

cTm-sndWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-snd
cTm-sndWf =
  iwf-ρ (pair sTm (snd (var vz))) (⊢ixP ⊢sTm (⊢snd (⊢var here)))
   (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs vz))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (there here)))) (toI ⊢sTm))
    iwf-ι)

cTm-cbaseWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-cbase
cTm-cbaseWf =
  iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sTm) (icw-ford ⌜Nat⌝ (fst (var vz)) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var here))) (toI ⊢sTm))
   iwf-ι

cTm-cPiWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-cPi
cTm-cPiWf =
  iwf-ρ (pair sTm (snd (var vz))) (⊢ixP ⊢sTm (⊢snd (⊢var here)))
   (iwf-ρ (pair sTm (nsuc (snd (var (vs vz))))) (⊢ixP ⊢sTm (⊢nsuc (⊢snd (⊢var (there here)))))
    (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 2 vz))))) (toI ⊢sTm))
     iwf-ι))

cTm-cSgWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-cSg
cTm-cSgWf =
  iwf-ρ (pair sTm (snd (var vz))) (⊢ixP ⊢sTm (⊢snd (⊢var here)))
   (iwf-ρ (pair sTm (nsuc (snd (var (vs vz))))) (⊢ixP ⊢sTm (⊢nsuc (⊢snd (⊢var (there here)))))
    (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 2 vz))))) (toI ⊢sTm))
     iwf-ι))

cTm-cHomWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-cHom
cTm-cHomWf =
  iwf-ρ (pair sTm (snd (var vz))) (⊢ixP ⊢sTm (⊢snd (⊢var here)))
   (iwf-ρ (pair sTm (snd (var (vs vz)))) (⊢ixP ⊢sTm (⊢snd (⊢var (there here))))
    (iwf-ρ (pair sTm (snd (var (vs (vs vz))))) (⊢ixP ⊢sTm (⊢snd (⊢var (∋lkp _ (vsⁿ 2 vz)))))
     (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 3 vz))))) (toI ⊢sTm))
      iwf-ι)))

cTm-hreflWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-hrefl
cTm-hreflWf =
  iwf-ρ (pair sTm (snd (var vz))) (⊢ixP ⊢sTm (⊢snd (⊢var here)))
   (iwf-ρ (pair sTm (snd (var (vs vz)))) (⊢ixP ⊢sTm (⊢snd (⊢var (there here))))
    (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 2 vz))))) (toI ⊢sTm))
     iwf-ι))

cTm-trWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-tr
cTm-trWf =
  iwf-ρ (pair sTm (nsuc (snd (var vz)))) (⊢ixP ⊢sTm (⊢nsuc (⊢snd (⊢var here))))
   (iwf-ρ (pair sTm (snd (var (vs vz)))) (⊢ixP ⊢sTm (⊢snd (⊢var (there here))))
    (iwf-ρ (pair sTm (snd (var (vs (vs vz))))) (⊢ixP ⊢sTm (⊢snd (⊢var (∋lkp _ (vsⁿ 2 vz)))))
     (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 3 vz))))) (toI ⊢sTm))
      iwf-ι)))

cTm-apWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-ap
cTm-apWf =
  iwf-ρ (pair sTm (snd (var vz))) (⊢ixP ⊢sTm (⊢snd (⊢var here)))
   (iwf-ρ (pair sTm (nsuc (snd (var (vs vz))))) (⊢ixP ⊢sTm (⊢nsuc (⊢snd (⊢var (there here)))))
    (iwf-ρ (pair sTm (snd (var (vs (vs vz))))) (⊢ixP ⊢sTm (⊢snd (⊢var (∋lkp _ (vsⁿ 2 vz)))))
     (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 3 vz))))) (toI ⊢sTm))
      iwf-ι)))

cTm-cIdWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-cId
cTm-cIdWf =
  iwf-ρ (pair sTm (snd (var vz))) (⊢ixP ⊢sTm (⊢snd (⊢var here)))
   (iwf-ρ (pair sTm (snd (var (vs vz)))) (⊢ixP ⊢sTm (⊢snd (⊢var (there here))))
    (iwf-ρ (pair sTm (snd (var (vs (vs vz))))) (⊢ixP ⊢sTm (⊢snd (⊢var (∋lkp _ (vsⁿ 2 vz)))))
     (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 3 vz))))) (toI ⊢sTm))
      iwf-ι)))

cTm-idreflWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-idrefl
cTm-idreflWf =
  iwf-ρ (pair sTm (snd (var vz))) (⊢ixP ⊢sTm (⊢snd (⊢var here)))
   (iwf-ρ (pair sTm (snd (var (vs vz)))) (⊢ixP ⊢sTm (⊢snd (⊢var (there here))))
    (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 2 vz))))) (toI ⊢sTm))
     iwf-ι))

cTm-jsubWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-jsub
cTm-jsubWf =
  iwf-ρ (pair sTm (nsuc (snd (var vz)))) (⊢ixP ⊢sTm (⊢nsuc (⊢snd (⊢var here))))
   (iwf-ρ (pair sTm (snd (var (vs vz)))) (⊢ixP ⊢sTm (⊢snd (⊢var (there here))))
    (iwf-ρ (pair sTm (snd (var (vs (vs vz))))) (⊢ixP ⊢sTm (⊢snd (⊢var (∋lkp _ (vsⁿ 2 vz)))))
     (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 3 vz))))) (toI ⊢sTm))
      iwf-ι)))

cTm-unitWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-unit
cTm-unitWf =
  iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sTm) (icw-ford ⌜Nat⌝ (fst (var vz)) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var here))) (toI ⊢sTm))
   iwf-ι

cTm-nzeroWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-nzero
cTm-nzeroWf =
  iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sTm) (icw-ford ⌜Nat⌝ (fst (var vz)) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var here))) (toI ⊢sTm))
   iwf-ι

cTm-nsucWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-nsuc
cTm-nsucWf =
  iwf-ρ (pair sTm (snd (var vz))) (⊢ixP ⊢sTm (⊢snd (⊢var here)))
   (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs vz))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (there here)))) (toI ⊢sTm))
    iwf-ι)

cTm-natrecWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-natrec
cTm-natrecWf =
  iwf-ρ (pair sTm (snd (var vz))) (⊢ixP ⊢sTm (⊢snd (⊢var here)))
   (iwf-ρ (pair sTm (nsuc (nsuc (snd (var (vs vz)))))) (⊢ixP ⊢sTm (⊢nsuc (⊢nsuc (⊢snd (⊢var (there here))))))
    (iwf-ρ (pair sTm (snd (var (vs (vs vz))))) (⊢ixP ⊢sTm (⊢snd (⊢var (∋lkp _ (vsⁿ 2 vz)))))
     (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 3 vz))))) (toI ⊢sTm))
      iwf-ι)))

cTm-conWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-con
cTm-conWf =
  iwf-κ ⌜Nat⌝ (icw-clo ⌜Nat⌝ ⊢⌜Nat⌝) ⊢⌜Nat⌝
   (iwf-ρ (pair sTm (snd (var (vs vz)))) (⊢ixP ⊢sTm (⊢snd (⊢var (there here))))
    (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 2 vz))))) (toI ⊢sTm))
     iwf-ι))

cTm-elimWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-elim
cTm-elimWf =
  iwf-ρ (pair sDesc (snd (var vz))) (⊢ixP ⊢sDesc (⊢snd (⊢var here)))
   (iwf-ρ (pair sTm (snd (var (vs vz)))) (⊢ixP ⊢sTm (⊢snd (⊢var (there here))))
    (iwf-ρ (pair sTm (snd (var (vs (vs vz))))) (⊢ixP ⊢sTm (⊢snd (⊢var (∋lkp _ (vsⁿ 2 vz)))))
     (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 3 vz))))) (toI ⊢sTm))
      iwf-ι)))

cTm-iconWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-icon
cTm-iconWf =
  iwf-κ ⌜Nat⌝ (icw-clo ⌜Nat⌝ ⊢⌜Nat⌝) ⊢⌜Nat⌝
   (iwf-ρ (pair sTm (snd (var (vs vz)))) (⊢ixP ⊢sTm (⊢snd (⊢var (there here))))
    (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs (vs vz)))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 2 vz))))) (toI ⊢sTm))
     iwf-ι))

cTm-ielimWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-ielim
cTm-ielimWf =
  iwf-ρ (pair sIDesc (snd (var vz))) (⊢ixP ⊢sIDesc (⊢snd (⊢var here)))
   (iwf-ρ (pair sTm (snd (var (vs vz)))) (⊢ixP ⊢sTm (⊢snd (⊢var (there here))))
    (iwf-ρ (pair sTm (snd (var (vs (vs vz))))) (⊢ixP ⊢sTm (⊢snd (⊢var (∋lkp _ (vsⁿ 2 vz)))))
     (iwf-ρ (pair sTm (snd (var (vs (vs (vs vz)))))) (⊢ixP ⊢sTm (⊢snd (⊢var (∋lkp _ (vsⁿ 3 vz)))))
      (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs (vs vz)))))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs (vs (vs (vs vz)))))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 4 vz))))) (toI ⊢sTm))
       iwf-ι))))

cTm-cNatWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-cNat
cTm-cNatWf =
  iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sTm) (icw-ford ⌜Nat⌝ (fst (var vz)) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var here))) (toI ⊢sTm))
   iwf-ι

cTm-cMuWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-cMu
cTm-cMuWf =
  iwf-ρ (pair sDesc (snd (var vz))) (⊢ixP ⊢sDesc (⊢snd (⊢var here)))
   (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs vz))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs vz))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (there here)))) (toI ⊢sTm))
    iwf-ι)

cTm-cIMuWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-cIMu
cTm-cIMuWf =
  iwf-ρ (pair sIDesc (snd (var vz))) (⊢ixP ⊢sIDesc (⊢snd (⊢var here)))
   (iwf-ρ (pair sTy nzero) (⊢ixP ⊢sTy ⊢nzero)
    (iwf-ρ (pair sTm (snd (var (vs (vs vz))))) (⊢ixP ⊢sTm (⊢snd (⊢var (∋lkp _ (vsⁿ 2 vz)))))
     (iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm) (icw-ford ⌜Nat⌝ (fst (var (vs (vs (vs vz))))) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var (∋lkp _ (vsⁿ 3 vz))))) (toI ⊢sTm))
      iwf-ι)))

cTm-cUnitWf : IConWf IPair (Θ₀ IPair) ρ₀ x₀ cTm-cUnit
cTm-cUnitWf =
  iwf-κ (⌜Id⌝ ⌜Nat⌝ (fst (var vz)) sTm) (icw-ford ⌜Nat⌝ (fst (var vz)) sTm) (⊢⌜Id⌝ ⊢⌜Nat⌝ (toI (⊢fst (⊢var here))) (toI ⊢sTm))
   iwf-ι

